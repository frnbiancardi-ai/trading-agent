---
context: phase
phase: 07-ml-classifier
task: pre-planning (Wave 0 spike pending)
total_tasks: 5
status: paused_pre_planning
last_updated: 2026-05-10T21:51:13.849Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These were discovered during Phase 7 research (commit `2938374`) and must be acknowledged before resuming planning.

- [ ] CONSTRAINT: **Dataset schema gap** — Phase 5 `baseline_decisions/part-0.parquet` has 1,076 rows × 6 legacy indicator columns (`sma_20, sma_50, ema_50, rsi_14, atr_14, last_close`). Missing 24+ ExtendedIndicators + `profile, regime, run_id, decision_ts_utc` required by ML-01 / CONTEXT.md D-02. Wave 0 dataset remediation must run before any ML training plan can produce a classifier with the designed feature set.
- [ ] CONSTRAINT: **sklearn 1.8 removed `CalibratedClassifierCV(cv='prefit')`** — CONTEXT.md lines 316-319 skeleton code raises `InvalidParameterError` at runtime. Manual `LogisticRegression` (Platt) and `IsotonicRegression` on raw `predict_proba` must replace it. Researcher's Pattern 2 in 07-RESEARCH.md gives the correct shape.
- [ ] CONSTRAINT: **Latency target locked at p95 < 10ms (NOT p99)** — do not re-prompt user. Researcher benchmarked WSL2 Docker p50=4ms / p95=9ms / p99=13ms. Forcing p99<10ms shrinks the model unacceptably.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Plan over a broken dataset | Spawning gsd-planner without resolving Wave 0 produces plans that reference fields the dataset does not contain. Plans pass plan-checker (it can't grep parquet schemas) but fail at execute-phase when the training script crashes on KeyError. | blocking | Wave 0 spike must pick a remediation path (A/B/C) and validate it empirically BEFORE planner spawns. The plan-checker cannot validate dataset schema; only an executed script can. |
| Use `CalibratedClassifierCV(cv='prefit')` from CONTEXT.md skeleton | The skeleton was written assuming sklearn ≤1.7. sklearn 1.8.0 (installed) raises `InvalidParameterError`. | blocking | Planner must reference Pattern 2 in 07-RESEARCH.md (manual LogisticRegression / IsotonicRegression). Cite the pattern in the plan's `read_first` for any task touching calibration. |
| Isotonic on tiny val sets | With 1,076 rows total, fold-1 val ≈ 20 rows. Isotonic regression on <50 samples is unstable; Brier-winner selection picks isotonic and ships a noisy calibrator. | advisory | If staying with current dataset size, force Platt for fold val<50; apply Brier-winner only from fold ≥3. If Option A re-run yields 10k+ rows this is moot. |
| Schedule Phase 7 work without acknowledging Phase 5 dataset deviation | STATE.md notes "drafts_rows = []" deferred. Same engine gap that suppressed FORMING/NONE drafts is the root cause of the indicator gap. | advisory | Wave 0 plan must touch `backtest/baseline/dataset_writer.py` (Option A) or write a backfill that recomputes `compute_all_extended()` (Option B). Either way, Phase 5 schema D-02 is not "done" — Phase 7 inherits the closure. |

<current_state>
`/gsd-plan-phase 7` was invoked. Init JSON loaded; CONTEXT.md was already present from a prior `/gsd-discuss-phase 7` run. The researcher (`gsd-phase-researcher`, sonnet) ran to completion and committed 07-RESEARCH.md (1002 lines) in `2938374`. The user was prompted with the Wave 0 remediation question and chose **"Pause planning — spike Wave 0 first"** rather than letting the planner pick A/B/C. The latency target was answered as **p95 < 10ms**. The planner, pattern-mapper, and plan-checker were never spawned. No PLAN.md files exist for Phase 7.
</current_state>

<completed_work>

Completed Tasks:
- 07-CONTEXT.md — done, committed `2e313d3` (`/gsd-discuss-phase 7`, 4 areas, ~16 questions, 20 D-decisions + 7 Claude discretion areas)
- 07-RESEARCH.md — done, committed `2938374` (gsd-phase-researcher, 6 patterns, 7 pitfalls, validation architecture, schema gap analysis)
</completed_work>

<remaining_work>

- **Wave 0 spike** — empirically validate dataset remediation path. User must run `/gsd-spike` first.
- 07-PATTERNS.md — gsd-pattern-mapper (skipped because planner was never spawned)
- PLAN.md files — gsd-planner (Wave 0 dataset remediation + Wave 1 training pipeline + Wave 2 inference + Wave 3 integration)
- Plan checker + revision loop (max 3 iterations)
- Requirements coverage gate, decision coverage gate, post-planning gap analysis
- STATE.md update + ROADMAP wave annotations + commit
</remaining_work>

<decisions_made>

- **Pause planning before planner spawn** — User chose to spike Wave 0 dataset remediation rather than have the planner pick A (re-run baseline ~3h18m), B (backfill script), or C (reduced feature set). Reason: committing plan structure to the wrong path would force replanning.
- **Latency target: p95 < 10ms over 1000-sample benchmark** — locked. Do not re-ask. p99=13ms in WSL2 accepted as OS scheduling jitter; chasing p99 would force smaller model.
- **Researcher's three options preserved verbatim in 07-RESEARCH.md §Critical Finding: Dataset Schema Gap** — spike must read this section before designing the experiment.
</decisions_made>

<blockers>
- **Dataset gap** (technical, blocking): 1,076 rows × 6 indicator cols vs designed 10k–50k × 30+. Workaround: Wave 0 spike picks A/B/C.
- **sklearn 1.8 API breakage** (technical, advisory): CalibratedClassifierCV(cv='prefit') removed. Workaround: manual calibration pattern in 07-RESEARCH.md.
- **No human action pending other than the spike** — once the spike concludes with a chosen Wave 0 path, planning can resume autonomously.
</blockers>

## Required Reading (in order)
1. `.planning/phases/07-ml-classifier/07-CONTEXT.md` — locked D-decisions D-01..D-20, deferred ideas (Phase 8/9), specific skeleton code (NB: skeleton calibration code uses removed sklearn API).
2. `.planning/phases/07-ml-classifier/07-RESEARCH.md` §Critical Finding: Dataset Schema Gap (lines 498-542) — Wave 0 remediation options A/B/C with verified row counts and field-by-field gap table.
3. `.planning/phases/07-ml-classifier/07-RESEARCH.md` §Pattern 2: Manual Platt + Isotonic Calibration (sklearn 1.8 compatible) — replacement for the broken CalibratedClassifierCV(cv='prefit') skeleton.
4. `.planning/phases/05-baseline-backtest/05-CONTEXT.md` D-02 — the full schema specification that the dataset SHOULD match (and currently does not).
5. `.planning/STATE.md` — confirms Phase 5 baseline produced 1,076 trades vs 10k expected (Rule 4 deviation), drafts_rows deferred.

## Critical Anti-Patterns (do NOT repeat these)
- **ANTI-PATTERN: Spawning gsd-planner before Wave 0 is settled** → structural mitigation: workflow paused at step 8 (planner spawn). Resuming via `/gsd-plan-phase 7 --skip-research` only after spike concludes.
- **ANTI-PATTERN: Trusting CONTEXT.md skeleton code as runnable** → skeleton at lines 316-319 uses `CalibratedClassifierCV(method=..., cv="prefit")`. Removed in sklearn 1.8. Always cross-reference research findings before lifting skeleton code into a plan.

## Infrastructure State
- Branch: `feature/update-pythono-pure-strategy` (no switch — plan-phase invariant respected)
- Python deps verified by researcher: `lightgbm 4.6.0`, `scikit-learn 1.8.0`, `joblib 1.5.3`, `pyarrow` available
- No background processes
- No worktree active for Phase 7
- `data/training/baseline_decisions/part-0.parquet` exists (1,076 rows, 6 indicator cols) — INSUFFICIENT for ML-01 as designed

## Pre-Execution Critique Required
- N/A — pausing pre-planning, not pre-execution

<context>
The plan-phase orchestrator was running cleanly when the researcher's findings forced a fork. The CONTEXT.md skeleton looks correct on paper but rests on two assumptions that don't hold: (1) Phase 5 produced the D-02 schema (it didn't), and (2) sklearn ≤1.7 (it's 1.8). The user's "spike first" choice is the right call — both holes are deep enough that planning over them produces unexecutable plans. The mental model when resuming: Wave 0 is no longer "scaffolding"; it's "fix the dataset I inherited from Phase 5". That changes plan ordering — Wave 0 might span multiple PLAN.md files (writer modification + re-run OR backfill script + verification + augmented parquet) before any LightGBM code is written.
</context>

<next_action>
Start with: `/gsd-spike "Wave 0 dataset remediation for Phase 7 — test Option B backfill (recompute compute_all_extended at entry_time-1 bar on existing 1,076 rows) for no-leakage soundness before committing to Option A baseline re-run (~3h18m)"`

Once the spike concludes with a chosen Wave 0 path, resume with: `/gsd-plan-phase 7 --skip-research` (RESEARCH.md is committed and current; do NOT re-research unless explicitly needed).
</next_action>
