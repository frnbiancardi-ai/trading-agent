---
phase: 08-mcp-tools-part-2
plan: 07
type: execute
wave: 5
depends_on: ["08-02", "08-03", "08-04", "08-05", "08-06"]
files_modified:
  - .planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
tdd: false
requirements: [MCP-04, MCP-05, MCP-06, MCP-R4, INT-02]
tags: [phase-gate, verification, sc-closure, phase-8]

must_haves:
  truths:
    - "SC#1 closed: train_ml_filter + predict_trade_quality + get_ml_calibration registrati in MCP list_tools (verificato via asyncio.run(list_tools()))"
    - "SC#2 closed: evaluate_trade_proposal response include ml_score + calibrated_prob fields without breaking existing consumer (Phase 6 R3 backward-compat suite green)"
    - "SC#3 closed: post-ML backtest report committed a .planning/research/ml-on-{date}.md con per-slice delta (27 row side-by-side)"
    - "SC#4 closed: verdict block YAML macchina-leggibile parsato → status enum (NON_TRIVIAL_IMPROVEMENT | NEGATIVE_RESULT_DOCUMENTED) — entrambi acceptable signal (D-08-D5)"
    - "5 requirement IDs aggiornati nel REQUIREMENTS.md traceability table: MCP-04 ✓ MCP-05 ✓ MCP-06 ✓ MCP-R4 ✓ INT-02 ✓"
    - "ROADMAP.md Phase 8 marker: ✓ COMPLETE 7/7 plans"
    - "STATE.md aggiornato con sezione Phase 8 closure + summary cumulativo Plan 08-01..06"
    - "Test suite globale post-08-07: regression zero vs pre-Phase-8 (Phase 4/5/6/7 tutte green)"
  artifacts:
    - path: ".planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md"
      provides: "Phase 8 verification report con 4 SC truths verbatim + 5 requirements closure + 14 decisioni D-08-XX coverage table"
      contains: "## Phase 8 Verification"
      min_lines: 200
    - path: ".planning/REQUIREMENTS.md"
      provides: "5 requirement IDs Phase 8 transitioned a ✓ Complete"
      contains: "MCP-04 | Phase 8 | ✓ Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 entry status updated"
      contains: "Phase 8: MCP Tools (part 2) ✓ COMPLETE"
  key_links:
    - from: ".planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md"
      to: ".planning/research/ml-on-{date}.md verdict block"
      via: "parsed YAML verdict status enum"
      pattern: "status: (NON_TRIVIAL_IMPROVEMENT|NEGATIVE_RESULT_DOCUMENTED)"
    - from: ".planning/REQUIREMENTS.md MCP-04..06+R4+INT-02 rows"
      to: ".planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md"
      via: "✓ Complete (Plan 08-XX, 2026-05-XX)"
      pattern: "✓ Complete \\(Plan 08-"
---

<objective>
Wave 5 phase-gate: chiudere Phase 8 verificando end-to-end le 4 ROADMAP Success Criteria, validare i 5 requirement IDs (MCP-04/05/06/R4/INT-02), e aggiornare i 3 file di tracking (REQUIREMENTS.md / ROADMAP.md / STATE.md).

**Verifica deterministica** (no soggettiva):
- SC#1: 3 ML tool registrati via asyncio.run(list_tools()) → grep su 3 nomi
- SC#2: pytest suite Phase 6 proposal + nuovo test Phase 8 evaluate_proposal extension → ALL green
- SC#3: file `.planning/research/ml-on-{date}.md` exists con 6 sezioni Markdown + verdict YAML block
- SC#4: verdict YAML status enum parsed → `NON_TRIVIAL_IMPROVEMENT` OR `NEGATIVE_RESULT_DOCUMENTED` (entrambi success per D-08-D5)

14 decisioni D-08-XX coverage validata: ogni decisione tracciata almeno in un Plan 08-XX SUMMARY.

Output: `08-VERIFICATION.md` (~250 LOC) + 3 tracking file updates + commit atomico.
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-mcp-tools-part-2/08-CONTEXT.md
@.planning/phases/08-mcp-tools-part-2/08-01-SUMMARY.md
@.planning/phases/08-mcp-tools-part-2/08-02-SUMMARY.md
@.planning/phases/08-mcp-tools-part-2/08-03-SUMMARY.md
@.planning/phases/08-mcp-tools-part-2/08-04-SUMMARY.md
@.planning/phases/08-mcp-tools-part-2/08-05-SUMMARY.md
@.planning/phases/08-mcp-tools-part-2/08-06-SUMMARY.md
@.planning/phases/05-baseline-backtest/05-VERIFICATION.md
@CLAUDE.md

<interfaces>
ROADMAP Phase 8 success criteria (verbatim):
1. `train_ml_filter`, `predict_trade_quality`, `get_ml_calibration` registered in MCP and pass integration tests.
2. `evaluate_trade_proposal` response includes `ml_score` and `calibrated_prob` fields without breaking existing consumers.
3. Post-ML backtest report committed to `.planning/research/ml-on-{date}.md` showing per-slice delta vs baseline (Sharpe, hit rate, expectancy, drawdown).
4. ML-on backtest demonstrates non-trivial improvement vs baseline OR documented analysis of why not (negative result is acceptable signal, not failure).

Phase 5 verification format reference: `.planning/phases/05-baseline-backtest/05-VERIFICATION.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eseguire suite test globale + 4 SC verification scripts</name>
  <read_first>
    - Tutti i 6 SUMMARY Plan 08-01..06
    - .planning/research/baseline-2026-05-12.md (baseline reference esistente)
    - .planning/research/ml-on-YYYY-MM-DD.md (output Plan 08-06 PC secondario — atteso post-checkpoint)
  </read_first>
  <files>tests/ (read-only execution)</files>
  <action>
**Step A — Suite test globale execution** (salvare output a `/tmp/phase-8-*.log`):

```bash
cd /workspaces/trading-agent
pytest tests/test_mcp_handlers_ml.py -v -m "not integration" 2>&1 | tee /tmp/phase-8-ml.log
pytest tests/test_mcp_evaluate_proposal_ml_extension.py -v 2>&1 | tee /tmp/phase-8-eval-ext.log
pytest tests/test_ml_on_report_writer.py tests/test_run_ml_on_backtest.py -v -m "not integration" 2>&1 | tee /tmp/phase-8-report.log
pytest tests/test_mcp_handlers_backtest.py tests/test_mcp_handlers_proposal.py tests/test_mcp_handlers_market.py -v 2>&1 | tee /tmp/phase-6-regression.log
pytest tests/test_mcp_tools_v2.py -v 2>&1 | tee /tmp/legacy-mcp-regression.log
pytest tests/test_strategy_purity.py tests/test_strategy_regression.py -v 2>&1 | tee /tmp/phase-4-regression.log
pytest --collect-only 2>&1 | tail -5 | tee /tmp/global-count.log
```

**Step B — SC#1 verification deterministica**:

```bash
python -c "
import mcp_tools.server
import asyncio
tools = asyncio.run(mcp_tools.server.list_tools())
names = {t.name for t in tools}
required = {'train_ml_filter', 'predict_trade_quality', 'get_ml_calibration'}
sc1 = required.issubset(names)
print(f'SC#1: {sc1} (registered={sorted(required & names)})')" | tee /tmp/sc1.log
```
Atteso exit 0, `SC#1: True (registered=['get_ml_calibration', 'predict_trade_quality', 'train_ml_filter'])`.

**Step C — SC#2 verification deterministica**:

```bash
python -c "
import inspect
from mcp_tools.handlers.proposal import handle_evaluate_trade_proposal
sig = inspect.signature(handle_evaluate_trade_proposal)
has_kwarg = 'ml_filter_singleton' in sig.parameters
print(f'SC#2 kwarg: {has_kwarg}')" | tee /tmp/sc2.log
```
Atteso `SC#2 kwarg: True`.

Verifica suite green:

```bash
pytest tests/test_mcp_evaluate_proposal_ml_extension.py tests/test_mcp_handlers_proposal.py -v --tb=no -q 2>&1 | tail -10 | tee /tmp/sc2-suite.log
```
Atteso: 6 passed evaluate_proposal_ml_extension + Phase 6 proposal suite green.

**Step D — SC#3 verification deterministica**:

```bash
LATEST=$(ls -t .planning/research/ml-on-*.md 2>/dev/null | head -1)
if [ -z "$LATEST" ]; then
  echo "SC#3: BLOCKED (no ml-on report — Plan 08-06 checkpoint not completed)"
  exit 1
fi
echo "Report: $LATEST" | tee /tmp/sc3.log
SECTIONS=$(grep -c '^## ' "$LATEST")
echo "Sections H2: $SECTIONS" | tee -a /tmp/sc3.log
# Atteso: SECTIONS >= 4 (Aggregate Verdict, Per-slice Breakdown, Rejection Rate, Degraded Slices, Verdict machine-readable)
test "$SECTIONS" -ge 4 || { echo "SC#3 FAIL: too few sections"; exit 1; }
echo "SC#3: True ($SECTIONS sections)" | tee -a /tmp/sc3.log
```

**Step E — SC#4 verification deterministica (parse verdict YAML)**:

```bash
python <<'EOF' | tee /tmp/sc4.log
import re
import sys
from pathlib import Path

candidates = sorted(Path(".planning/research").glob("ml-on-*.md"), reverse=True)
if not candidates:
    print("SC#4: BLOCKED (no ml-on report)")
    sys.exit(1)
latest = candidates[0]
content = latest.read_text(encoding="utf-8")
m = re.search(r"## Verdict \(machine-readable\)\s*```yaml\s*(.*?)```", content, re.DOTALL)
if not m:
    print("SC#4: BLOCKED (verdict yaml block missing)")
    sys.exit(2)
status_match = re.search(r"^status:\s*(\S+)", m.group(1), re.MULTILINE)
if not status_match:
    print("SC#4: BLOCKED (status missing)")
    sys.exit(3)
status = status_match.group(1)
ok = status in ("NON_TRIVIAL_IMPROVEMENT", "NEGATIVE_RESULT_DOCUMENTED")
print(f"SC#4: {ok} status={status} report={latest}")
sys.exit(0 if ok else 4)
EOF
```
Atteso exit 0 + `SC#4: True status=<enum> report=...`.

**Step F** — Tutti i log di Step B/C/D/E salvati su `/tmp/sc*.log` per inclusione verbatim in `08-VERIFICATION.md` (Task 2).

Se ANY di SC#1/2/3/4 ritorna BLOCKED → STOP, segnalare al utente la dipendenza mancante (es. Plan 08-06 checkpoint non completato dal PC secondario).
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent &amp;&amp; pytest tests/test_mcp_handlers_ml.py tests/test_mcp_evaluate_proposal_ml_extension.py tests/test_ml_on_report_writer.py tests/test_run_ml_on_backtest.py tests/test_mcp_handlers_backtest.py tests/test_mcp_handlers_proposal.py tests/test_mcp_tools_v2.py -v -m "not integration" --tb=no -q 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    <!-- ===== B3 revision iter 1 — Cumulative test count math (test_mcp_handlers_ml.py) =====
    Wave 0 (Plan 08-01 Task 4):
      + 1 test_ml_tools_registered_in_list_tools                 (green)
      + 1 test_train_ml_filter_tool_schema_data_source_enum      (green)
      + 1 test_predict_trade_quality_tool_schema_required_fields (green)
      + 1 test_get_ml_calibration_tool_schema_summary_only_*     (green)
      + 1 test_predict_trade_quality_stub_raises_not_implemented (xfail strict)
      + 1 test_get_ml_calibration_stub_raises_not_implemented    (xfail strict)
      + 1 test_train_ml_filter_stub_raises_not_implemented       (xfail strict)
      = 4 green + 3 xfail = 7 test definiti
    Wave 1 (Plan 08-02 Task 2 — include B4 revision iter 1):
      - 1 xfail (predict) rinominato/flippato a test_predict_trade_quality_disabled_returns_null
      + 5 NUOVI test_predict_trade_quality_* (singleton_none, enabled_pass_gate,
          enabled_fail_gate, predict_exception_returns_envelope, threshold_fallback_unknown_profile)
      + 1 NUOVO test_predict_branch_A_eq_branch_B_features_match (B4 revision iter 1 parity)
      = 4 (wave0 schema) + 7 predict green + 2 xfail residui (calibration + train) = 11 green + 2 xfail = 13 test
    Wave 2 (Plan 08-03 Task 2):
      - 1 xfail (calibration) flippato + rinominato a test_get_ml_calibration_default_per_fold
      + 4 NUOVI test_get_ml_calibration_* (summary_only_strips_folds, metadata_missing,
          corrupt_json, fold_n_alias_compatibility)
      = 4 + 7 + 5 calibration green + 1 xfail (train) = 16 green + 1 xfail = 17 test
    Wave 3 (Plan 08-04 Task 3 — include B5 revision iter 1):
      - 1 xfail (train) flippato + rinominato a test_train_ml_filter_returns_job_id
      + 4 NUOVI unit test_train_ml_filter_* (validation_failed_on_unknown_data_source,
          validation_failed_on_missing_parquet, concurrency_cap, sha256_audit_propagated)
      + 1 NUOVO test_train_ml_filter_writes_sha256_to_real_metadata (B5 revision iter 1 real-metadata audit)
      + 1 NUOVO @integration test_train_ml_filter_smoke_round_trip
      = 4 + 7 + 5 + 6 unit + 1 integration = 22 unit + 1 integration
    Totale finale post-Wave-3: 22 PASSED unit + 1 integration (skipif gated, 0 xfail residui).
    NB1: il checker iter 1 suggeriva "21 unit + 1 integration" doppio-contando il rename Wave 3.
    NB2: post B4 parity test (Plan 08-02) e B5 real-metadata test (Plan 08-04), il conteggio
    e' incrementato di 2 → totale 22 unit (NON 20 come pre-revision).
    ===== END math block ===== -->
    - File log `/tmp/sc1.log` `/tmp/sc2.log` `/tmp/sc3.log` `/tmp/sc4.log` esistono con verdict line `SC#N: True ...`
    - Suite test globale post-Phase-8: zero new FAIL vs pre-Phase-8 baseline (delta tracking via `pytest --collect-only` count)
    - test_mcp_handlers_ml.py: 22 unit PASSED + 1 integration (skipif gated, 0 XFAIL residui post-Wave-3 — include B4 parity + B5 real-metadata)
    - test_mcp_evaluate_proposal_ml_extension.py: 6 passed
    - test_ml_on_report_writer.py + test_run_ml_on_backtest.py: >= 11 passed
    - tests/test_baseline_runner_output_dir.py (B2 revision iter 1 Task 0 Plan 08-06): >= 2 passed
    - Phase 6 regression: identico delta vs Plan 06-04 SUMMARY
    - Phase 4 regression: identico delta vs Plan 04-08 SUMMARY
  </acceptance_criteria>
  <done>
    Suite test eseguita, 4 SC verifications PASS deterministicamente, output salvato per Task 2. Zero regressione Phase 4/5/6/7.
  </done>
</task>

<task type="auto">
  <name>Task 2: Comporre .planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md</name>
  <read_first>
    - .planning/phases/05-baseline-backtest/05-VERIFICATION.md (template format)
    - /tmp/sc1.log /tmp/sc2.log /tmp/sc3.log /tmp/sc4.log (output Task 1)
    - Tutti i 6 SUMMARY Plan 08-01..06
    - .planning/phases/08-mcp-tools-part-2/08-CONTEXT.md (14 decisioni D-08-XX)
  </read_first>
  <files>.planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md</files>
  <action>
Creare NUOVO file con la seguente struttura. Includere verbatim output Task 1 SC verification logs.

```markdown
# Phase 8: MCP Tools (part 2) — Verification

**Verified:** 2026-05-XX  (data execution Plan 08-07)
**Phase status:** ✓ COMPLETE 7/7 plans

## SC Verification (ROADMAP Phase 8)

### SC#1 — 3 ML tool registrati in MCP

**Verdict**: ✓ PASS

```
<verbatim content da /tmp/sc1.log>
```

Suite test backing: `tests/test_mcp_handlers_ml.py::test_ml_tools_registered_in_list_tools` (PASSED).

### SC#2 — evaluate_trade_proposal include ml_score + calibrated_prob senza rompere consumer esistenti

**Verdict**: ✓ PASS

```
<verbatim /tmp/sc2.log + /tmp/sc2-suite.log>
```

Suite test backing: `tests/test_mcp_evaluate_proposal_ml_extension.py` (6 PASSED), Phase 6 `tests/test_mcp_handlers_proposal.py` (zero regressione).

### SC#3 — Post-ML backtest report committed a .planning/research/ml-on-{date}.md

**Verdict**: ✓ PASS

```
<verbatim /tmp/sc3.log>
```

Report path: `<latest ml-on-{date}.md>`
Sezioni presenti: Aggregate Verdict, Per-slice Breakdown 27 rows, Rejection Rate Per Profile, Degraded Slices Analysis, Verdict machine-readable.

### SC#4 — Verdict closure automated (D-08-D5)

**Verdict**: ✓ PASS

```
<verbatim /tmp/sc4.log>
```

Status final: `<NON_TRIVIAL_IMPROVEMENT | NEGATIVE_RESULT_DOCUMENTED>`.

NB D-08-D5: entrambi enum acceptable. NEGATIVE_RESULT_DOCUMENTED implica Phase 9 calibration retune (Degraded Slices Analysis automatica nel report fornisce input).

## Requirement Coverage

| ID | Plan | Status | Note |
|----|------|--------|------|
| MCP-04 | 08-04 | ✓ Complete | handle_train_ml_filter GREEN + JobQueue async + 3 layer security (enum + schema + sha256 + path-traversal) |
| MCP-05 | 08-02 | ✓ Complete | handle_predict_trade_quality GREEN + 3 branche (disabled, success, exception) + riuso build_feature_vector Phase 7 |
| MCP-06 | 08-03 | ✓ Complete | handle_get_ml_calibration GREEN + D-08-C1 default per-fold + D-08-C2 summary_only opt-out |
| MCP-R4 | 08-05 | ✓ Complete | evaluate_trade_proposal response include ml_score + calibrated_prob + ml_threshold + ml_filter_active (4 fields additive backward-compat) |
| INT-02 | 08-06 | ✓ Complete | scripts/run_ml_on_backtest.py + ml_on_report_writer.py + PC secondario re-run + verdict YAML closure |

## Decisioni Coverage (D-08-A1..D5)

| Decision ID | Topic | Plan | Verifica |
|-------------|-------|------|----------|
| D-08-A1 | Async via JobQueue submit | 08-04 | _train_ml_filter_worker top-level picklable + job_queue.submit |
| D-08-A2 | Cap concorrenza condiviso MCP_MAX_CONCURRENT_RUNS=1 | 08-04 | test_train_ml_filter_concurrency_cap valida cap shared |
| D-08-A3 | No cancel fire-and-forget | 08-04 | NO _cancel_ml_training tool, training va in fondo |
| D-08-B1 | Data source baseline parquet hardcoded | 08-04 | enum=['baseline'], MCP_TRAINING_DATA_PATH default |
| D-08-B2 | enum extensible Phase 9/11 | 08-01 | TRAIN_ML_FILTER_TOOL.inputSchema enum=['baseline'] |
| D-08-B3 | Strict-fail + sha256 check pre-training | 08-04 | _validate_parquet_schema_v2 returns (ok, msg, sha) |
| D-08-C1 | Default audit-first per-fold | 08-03 | summary_only=false → folds list |
| D-08-C2 | Opt-out leggero summary_only | 08-03 | summary_only=true → no folds key |
| D-08-C3 | 9 fields enumerati per fold | 08-03 | _fold_to_payload returns 9 fields |
| D-08-D1 | Output dir baseline_ml_on separato | 08-06 | BASELINE_OUTPUT_DIR_OVERRIDE=data/training/baseline_ml_on |
| D-08-D2 | PC secondario notturno coordinamento | 08-06 | Task 4 checkpoint human-action |
| D-08-D3 | scripts/run_ml_on_backtest.py standalone | 08-06 | CLI 5 flag analog run_baseline_05_09.py |
| D-08-D4 | Report shape Opt 3 + degraded slices | 08-06 | write_ml_on_report 6 sezioni |
| D-08-D5 | SC#4 verdict YAML automated | 08-06 | _compute_verdict 5 criteria → status enum |

## Cross-phase Carry-forward Validation

- **Phase 7 dependency**: ml/inference.py + ml/feature_extraction.py + ml/train.py riusati senza duplicate logic
- **Phase 6 carry-forward**: JobQueue Plan 06-03 riusato (cap shared), ErrorCodes envelope Plan 06-01 esteso (VALIDATION_FAILED/NOT_FOUND/INTERNAL_ERROR additivi), handler pattern Plan 06-02 followed
- **Phase 5 carry-forward**: _SCHEMA_V2_REQUIRED_KEYS Plan 05-09 importato + file_sha256 determinism.py riusato, run_baseline.py analog wrapper
- **Project memory carry-forward**: priority "training ML data integrity" rispettato (3-layer safety + audit trail + no cancel + cap-shared)

## Test Suite Stats Post-Phase-8

| File | Test count | Status |
|------|-----------|--------|
| tests/test_mcp_handlers_ml.py | 22 | 22 PASSED + 1 integration skipif (post B4 parity + B5 real-metadata) |
| tests/test_mcp_evaluate_proposal_ml_extension.py | 6 | 6 PASSED |
| tests/test_ml_on_report_writer.py | 7+ | PASSED |
| tests/test_run_ml_on_backtest.py | 4 | PASSED + 1 integration skipif |
| Phase 6 regression suite | unchanged | zero new FAIL |
| Phase 5 regression suite | unchanged | zero new FAIL |
| Phase 4 regression suite | unchanged | zero new FAIL |
| Legacy MCP tools v2 | unchanged | zero new FAIL |

## Security Gates Active (ASVS L1)

| Threat | Plan | Mitigation |
|--------|------|-----------|
| T-8-04-04 path traversal MCP_TRAINING_DATA_PATH | 08-04 | path.resolve().is_relative_to(Path('data/training').resolve()) |
| T-8-04-01 enum injection data_source | 08-04 | hard enum check pre-submit |
| T-8-04-02 race su parquet input | 08-04 | JobQueue cap=1 shared |
| T-8-04-03 spoofing parquet post-validation | 08-04 | sha256 propagato a metadata.json output |
| T-8-06-02 ml-on overscrive baseline_decisions | 08-06 | BASELINE_OUTPUT_DIR_OVERRIDE=baseline_ml_on |

Threats deferred (Phase 9+):
- T-8-04-08 atomic bundle write (Phase 7 dump_bundle non atomic — hardening future)
- T-8-06-01 pickle deserialization signature (joblib trust → Phase 9 signature verification)

## Deviation Summary

<lista deviation Rule 1/2/3 cumulative Plan 08-01..06 dai SUMMARY>

## Final Status

Phase 8 ✓ COMPLETE 7/7 plans, 4/4 SC closed, 5/5 requirement IDs closed, 14/14 decisioni D-08-XX coverage validated.

Next: RESUME-PLAN.md STEP 15 = `/gsd-discuss-phase 9` (Failure Analysis + Drift).
```

NB: i placeholder `<verbatim ...>` vanno sostituiti con il contenuto reale dei file `/tmp/sc*.log` letto via Read tool durante esecuzione.

NB: `<lista deviation...>` da popolare leggendo ogni SUMMARY Plan 08-XX e raccogliendo le sezioni "Deviation" + classificandole.
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent &amp;&amp; test -f .planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md &amp;&amp; wc -l .planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md &amp;&amp; grep -c '## SC\|## Requirement Coverage\|## Decisioni Coverage\|## Test Suite Stats\|## Security Gates' .planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File 08-VERIFICATION.md esiste, >=200 LOC
    - Contiene 4 SC sections con verdict "✓ PASS"
    - Tabella Requirement Coverage con 5 righe ✓ Complete
    - Tabella Decisioni Coverage con 14 righe (D-08-A1..D5)
    - Sezione Security Gates Active con almeno 5 mitigation
    - grep su sezioni principali >= 5 match
  </acceptance_criteria>
  <done>
    08-VERIFICATION.md scritto con 4 SC verbatim verdict + 5 requirement + 14 decisioni + security gates + test stats + deviation summary.
  </done>
</task>

<task type="auto">
  <name>Task 3: Aggiornare REQUIREMENTS.md + ROADMAP.md + STATE.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (linee 212-214 + 230 + 232 — rows MCP-04/05/06 + R4 + INT-02)
    - .planning/ROADMAP.md (Phase 8 section line 224-237 + Phase Summary table line 18)
    - .planning/STATE.md (Active Work line 50-52 + Milestone Status table line 41)
    - .planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md (appena scritto)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
**Step A — REQUIREMENTS.md**:

Editare le 5 rows traceability table:

Da:
```
| MCP-04 | Phase 8 | Pending |
| MCP-05 | Phase 8 | Pending |
| MCP-06 | Phase 8 | Pending |
| MCP-R4 | Phase 8 | Pending |
| INT-02 | Phase 8 | Pending |
```

A:
```
| MCP-04 | Phase 8 | ✓ Complete (Plan 08-04, 2026-05-XX) |
| MCP-05 | Phase 8 | ✓ Complete (Plan 08-02, 2026-05-XX) |
| MCP-06 | Phase 8 | ✓ Complete (Plan 08-03, 2026-05-XX) |
| MCP-R4 | Phase 8 | ✓ Complete (Plan 08-05, 2026-05-XX) |
| INT-02 | Phase 8 | ✓ Complete (Plan 08-06, 2026-05-XX) |
```

Editare anche le 5 checkbox righe ML Layer / MCP Tools (new) / MCP Tools (refactor existing) / Integration:
- `- [ ] **MCP-04**: ...` → `- [x] **MCP-04**: ... <!-- complete 2026-05-XX (Plan 08-04) -->`
- analog per MCP-05/06/R4/INT-02.

**Step B — ROADMAP.md**:

Editare Phase 8 section header:

Da:
```
### Phase 8: MCP Tools (part 2)
```

A:
```
### Phase 8: MCP Tools (part 2) ✓ COMPLETE 2026-05-XX
```

Aggiornare Phase Summary table riga 18:
Da:
```
| 8 | MCP Tools (part 2) | Expose ML training, inference, and calibration introspection through MCP; integrate ML score into `evaluate_trade_proposal` response; produce the post-ML backtest report. | MCP-04..06, MCP-R4, INT-02 | 4 |
```
A:
```
| 8 | MCP Tools (part 2) ✓ COMPLETE 2026-05-XX (7/7 plans) | Expose ML training, inference, and calibration introspection through MCP; integrate ML score into `evaluate_trade_proposal` response; produce the post-ML backtest report. | MCP-04..06, MCP-R4, INT-02 | 4 |
```

Aggiungere sotto Phase 8 section:
```
**Plans:** 7 plans (Wave 0-5) — plan-check + plan-execute COMPLETE 2026-05-XX
Plans:
- [x] 08-01-PLAN.md — Wave 0 scaffolding: 3 Tool schemas + stub handler + ErrorCodes + config env
- [x] 08-02-PLAN.md — Wave 1: handle_predict_trade_quality GREEN (MCP-05)
- [x] 08-03-PLAN.md — Wave 2: handle_get_ml_calibration GREEN (MCP-06)
- [x] 08-04-PLAN.md — Wave 3: handle_train_ml_filter GREEN + worker top-level + 3 security layers (MCP-04)
- [x] 08-05-PLAN.md — Wave 1 (parallel): evaluate_trade_proposal extension (MCP-R4)
- [x] 08-06-PLAN.md — Wave 4: scripts/run_ml_on_backtest.py + ml_on_report_writer.py + PC secondario re-run (INT-02)
- [x] 08-07-PLAN.md — Wave 5 phase gate: SC#1..4 closure + REQUIREMENTS/ROADMAP/STATE update
```

**Step C — STATE.md**:

Aggiungere PRIMA della Active Work entry corrente (Phase 8 CONTEXT) una nuova entry Phase 8 COMPLETE:

```
**Phase 8 — COMPLETE 2026-05-XX (7/7 plans).** End-to-end MCP-04/05/06 + MCP-R4 + INT-02 closure. 7 commit atomici Plan 08-01..07. Wave 0 scaffolding (3 Tool schema + stub handler + ErrorCodes additivi VALIDATION_FAILED/NOT_FOUND/INTERNAL_ERROR + 3 env var config ML_MODEL_PATH/MCP_TRAINING_DATA_PATH/MCP_ML_THRESHOLD_MARGIN_PCT). Wave 1 (parallel) handle_predict_trade_quality + evaluate_trade_proposal extension (4 additive fields ml_score/calibrated_prob/ml_threshold/ml_filter_active, backward-compat absolute). Wave 2 handle_get_ml_calibration (D-08-C1 default per-fold + D-08-C2 summary_only). Wave 3 handle_train_ml_filter (async JobQueue Plan 06-03 cap=1 shared D-08-A2, 3-layer safety: enum lock + schema-v2 + sha256 + path-traversal mitigation T-8-04-04). Wave 4 INT-02 deliverable (scripts/run_ml_on_backtest.py + backtest/baseline/ml_on_report_writer.py 6 sezioni D-08-D4 + verdict YAML automated D-08-D5; PC secondario notturno ~14000s wall-clock). Wave 5 phase gate (08-VERIFICATION.md). 4/4 SC ROADMAP closed: SC#1 (3 tool registered), SC#2 (backward-compat preserved), SC#3 (.planning/research/ml-on-{date}.md committed), SC#4 (verdict status=<NON_TRIVIAL_IMPROVEMENT|NEGATIVE_RESULT_DOCUMENTED>). 14/14 decisioni D-08-A1..D5 coverage validated. Suite globale: zero regressione Phase 4/5/6/7 (35+ new tests Phase 8 green, 0 new FAIL). REQUIREMENTS.md MCP-04/05/06/R4 + INT-02 transitioned a ✓ Complete. Phase 9 (Failure Analysis + Drift) sbloccata. RESUME-PLAN.md STEP 14-15 → complete.
```

Aggiornare Milestone Status table (linea 41):
Da:
```
| 8 | MCP Tools (part 2) | 🟡 context-gathered | 0/N | 0% (CONTEXT.md ✓ 2026-05-12 ...) |
```
A:
```
| 8 | MCP Tools (part 2) | ✓ complete | 7/7 | 100% (Plan 08-01..07 SUMMARY ✓ 2026-05-XX; 4/4 SC closed; 5/5 requirement IDs ✓; 14/14 D-08-XX validated; ML pipeline esposta in MCP + INT-02 report committed) |
```

Aggiornare header progress percent: `completed_phases: 7` + `percent: 64`.

Aggiornare `Current focus` (linea 24): da "/gsd-execute-phase 7 + /gsd-plan-phase 8" a "Phase 8 ✓ COMPLETE; next: /gsd-discuss-phase 9".

**Step D — Commit atomico**:

```bash
cd /workspaces/trading-agent
git add .planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md \
        .planning/REQUIREMENTS.md \
        .planning/ROADMAP.md \
        .planning/STATE.md
git commit -m "docs(phase-8): plan 08-07 phase gate — SC#1..4 closure + 5 requirement IDs ✓ Complete

- 08-VERIFICATION.md: 4 SC verdict verbatim + 5 req + 14 decisioni + security gates
- REQUIREMENTS.md: MCP-04/05/06/R4 + INT-02 transitioned to ✓ Complete
- ROADMAP.md: Phase 8 marker ✓ COMPLETE 7/7 plans + Plans checklist [x]
- STATE.md: Active Work + Milestone Status + Current focus refreshed

Phase 8 ✓ COMPLETE: ML pipeline esposta in MCP (3 tool + 1 refactor) + INT-02 backtest report committed. Zero regressione Phase 4/5/6/7. Next: /gsd-discuss-phase 9 (Failure Analysis + Drift)."
```
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent &amp;&amp; grep -c 'MCP-04 | Phase 8 | ✓ Complete\|MCP-05 | Phase 8 | ✓ Complete\|MCP-06 | Phase 8 | ✓ Complete\|MCP-R4 | Phase 8 | ✓ Complete\|INT-02 | Phase 8 | ✓ Complete' .planning/REQUIREMENTS.md &amp;&amp; grep -c 'Phase 8: MCP Tools (part 2) ✓ COMPLETE' .planning/ROADMAP.md &amp;&amp; grep -c '✓ complete | 7/7 | 100%' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md: 5 row traceability table aggiornate (grep >= 5)
    - REQUIREMENTS.md: 5 checkbox `- [x] **MCP-04..06**, **MCP-R4**, **INT-02**` (grep >= 5)
    - ROADMAP.md: Phase 8 header marker `✓ COMPLETE` (grep == 1+)
    - ROADMAP.md: Phase Summary table row aggiornata
    - ROADMAP.md: 7 plan checklist `[x]` (grep `\[x\] 08-`)
    - STATE.md: Milestone Status `✓ complete | 7/7 | 100%` per Phase 8
    - STATE.md: nuova Active Work entry Phase 8 COMPLETE
    - Commit atomico creato con i 4 file
  </acceptance_criteria>
  <done>
    3 tracking file aggiornati con Phase 8 closure. Commit atomico creato. Suite test post-update: zero regressione vs pre-commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verifica finale phase gate + human sign-off</name>
  <what-built>
    Phase 8 deliverable completo:
    - 7 PLAN.md + 7 SUMMARY.md
    - 5 file codice creati (handlers/ml.py, ml_on_report_writer.py, run_ml_on_backtest.py, 2 test file)
    - 4 file codice modificati (server.py, proposal.py, errors.py, config.py, .env.example, __init__.py)
    - 1 VERIFICATION.md
    - 3 tracking file aggiornati (REQUIREMENTS, ROADMAP, STATE)
    - 1 commit phase-gate
    - PC secondario run completato (Plan 08-06 Task 4)
    - 5 requirement IDs ✓ Complete
    - 4 SC ROADMAP closed
    - 14 decisioni D-08-XX coverage validated
  </what-built>
  <how-to-verify>
1. Apri `.planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md` e leggi 4 SC verdict — confirm tutti `✓ PASS`.
2. Apri `.planning/research/ml-on-{date}.md` e leggi verdict block YAML — confirm status enum aspettato.
3. Run:
   ```bash
   git log --oneline .planning/phases/08-mcp-tools-part-2/ | head -10
   ```
   confirm 7+ commit atomici Plan 08-01..07.
4. Run smoke MCP server import:
   ```bash
   python -c "import mcp_tools.server; import asyncio; tools = asyncio.run(mcp_tools.server.list_tools()); ml_tools = [t.name for t in tools if 'ml' in t.name or 'predict' in t.name]; print(f'ML tools: {ml_tools}')"
   ```
   atteso: `['get_ml_calibration', 'predict_trade_quality', 'train_ml_filter']` (3 tool).
5. Review REQUIREMENTS.md table Phase 8 rows ✓ Complete.
6. Review ROADMAP.md Phase 8 marker ✓ COMPLETE 7/7.
7. Verify zero regression: `pytest tests/ -v --tb=no -q -m "not integration" 2>&1 | tail -5` confronto vs pre-Phase-8 baseline test count.
  </how-to-verify>
  <resume-signal>Type "phase-8-approved" per chiudere Phase 8 e proseguire a /gsd-discuss-phase 9. Type "phase-8-issue: <detail>" se trovi problema bloccante.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 08-07 verification logs → 08-VERIFICATION.md content | Logs sotto controllo developer (eseguiti localmente) |
| REQUIREMENTS.md / ROADMAP.md / STATE.md → git commit | Markdown file solo, no eseguibile |
| Commit atomico → repo state finalization | Standard git commit, no secret exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-07-01 | Repudiation | 08-VERIFICATION.md contiene claim "SC#X passed" senza evidence verificabile | mitigate | Task 1 obbliga inclusione verbatim del log output (sc1.log..sc4.log) — non solo claim |
| T-8-07-02 | Information disclosure | STATE.md Active Work entry contiene dettagli implementation interna | accept | Repo è privato + Active Work intent design contiene snapshot completo dello stato del progetto |
| T-8-07-03 | Tampering | Commit phase-gate include un .md ma file codice non modificati Plan 08-XX → consistency check fallisce | mitigate | Task 3 step D commit limitato a 4 file specifici (08-VERIFICATION + 3 tracking) — file codice già committati nei Plan 08-01..06 |
| T-8-07-04 | Repudiation | Plan 08-06 checkpoint Task 4 NON completato (no ml-on report) ma Plan 08-07 procede comunque → SC#3/SC#4 falsi-verdi | mitigate | Task 1 step D/E: `if [ -z "$LATEST" ]; then echo "BLOCKED"; exit 1; fi` — script blocca Plan 08-07 se report missing |
</threat_model>

<verification>
- `cat .planning/phases/08-mcp-tools-part-2/08-VERIFICATION.md` → contiene 4 SC verdict ✓ PASS verbatim
- `git log --oneline | head -5` → ultimo commit "docs(phase-8): plan 08-07 phase gate"
- `grep -c '✓ Complete (Plan 08-' .planning/REQUIREMENTS.md` >= 5
- `grep -c 'Phase 8: MCP Tools (part 2) ✓ COMPLETE' .planning/ROADMAP.md` >= 1
- `pytest tests/ -m "not integration" --tb=no -q 2>&1 | tail -3` → zero new FAIL vs pre-Phase-8
- Phase 9 unblocked: `/gsd-discuss-phase 9` chiamabile
</verification>

<success_criteria>
- 4 SC ROADMAP closed deterministicamente (verbatim log evidence)
- 5 requirement IDs ✓ Complete (REQUIREMENTS.md traceability)
- 14 D-08-XX decisioni coverage validated (table in VERIFICATION)
- 3 tracking file aggiornati (REQUIREMENTS + ROADMAP + STATE)
- Suite test post-Phase-8: zero new FAIL
- Phase 9 sbloccata (RESUME-PLAN.md STEP next: discuss-phase 9)
- Commit atomico phase-gate created
</success_criteria>

<output>
After completion, create `.planning/phases/08-mcp-tools-part-2/08-07-SUMMARY.md` con sezioni: Files Created/Modified (08-VERIFICATION.md + 3 tracking), Test Results (verbatim verdict logs), 4 SC closure status, 5 Requirement IDs status, Phase 9 unblock confirmation, Total Phase 8 cumulative stats (LOC delta, commit count, deviation summary across 7 plans), RESUME-PLAN.md STEP next update suggestion.
</output>
