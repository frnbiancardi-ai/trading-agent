---
phase: 08-mcp-tools-part-2
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - mcp_tools/handlers/ml.py
  - mcp_tools/server.py
  - mcp_tools/__init__.py
  - mcp/errors.py
  - config.py
  - .env.example
  - tests/test_mcp_handlers_ml.py
autonomous: true
tdd: false
requirements: [MCP-04, MCP-05, MCP-06]
tags: [mcp, ml, scaffolding, phase-8]

must_haves:
  truths:
    - "ml.py exists with 3 Tool schema entries (TRAIN_ML_FILTER_TOOL, PREDICT_TRADE_QUALITY_TOOL, GET_ML_CALIBRATION_TOOL) + 3 stub handler raising NotImplementedError"
    - "ErrorCodes enum extended with VALIDATION_FAILED, NOT_FOUND, INTERNAL_ERROR string constants"
    - "config.py exposes ML_MODEL_PATH, MCP_TRAINING_DATA_PATH, MCP_ML_THRESHOLD_MARGIN_PCT"
    - "server.py list_tools() returns 3 new ML tool entries (run python -c print of list_tools length increment by 3)"
    - "server.py dispatch routes train_ml_filter|predict_trade_quality|get_ml_calibration to stub handlers"
    - "tests/test_mcp_handlers_ml.py contains 3 xfail tests targeting stub raise NotImplementedError, suite green"
  artifacts:
    - path: "mcp_tools/handlers/ml.py"
      provides: "3 Tool schemas + 3 stub handler signatures + module docstring"
      exports: ["TRAIN_ML_FILTER_TOOL", "PREDICT_TRADE_QUALITY_TOOL", "GET_ML_CALIBRATION_TOOL", "handle_train_ml_filter", "handle_predict_trade_quality", "handle_get_ml_calibration"]
      min_lines: 150
    - path: "mcp/errors.py"
      provides: "ErrorCodes enum with VALIDATION_FAILED, NOT_FOUND, INTERNAL_ERROR"
      contains: "VALIDATION_FAILED"
    - path: "config.py"
      provides: "ML_MODEL_PATH, MCP_TRAINING_DATA_PATH, MCP_ML_THRESHOLD_MARGIN_PCT additive block"
      contains: "MCP_TRAINING_DATA_PATH"
    - path: ".env.example"
      provides: "Documented Phase 8 ML env vars (commented defaults)"
      contains: "MCP_TRAINING_DATA_PATH"
    - path: "tests/test_mcp_handlers_ml.py"
      provides: "3 xfail tests + pytest.importorskip gate"
      contains: "pytest.importorskip"
  key_links:
    - from: "mcp_tools/server.py::list_tools"
      to: "mcp_tools/handlers/ml.py::{TRAIN_ML_FILTER_TOOL, PREDICT_TRADE_QUALITY_TOOL, GET_ML_CALIBRATION_TOOL}"
      via: "from mcp_tools.handlers.ml import ..."
      pattern: "TRAIN_ML_FILTER_TOOL"
    - from: "mcp_tools/server.py::call_tool dispatch"
      to: "mcp_tools/handlers/ml.py::handle_{train_ml_filter,predict_trade_quality,get_ml_calibration}"
      via: "elif name in ('train_ml_filter', 'predict_trade_quality', 'get_ml_calibration')"
      pattern: "handle_train_ml_filter"
---

<objective>
Wave 0 Phase 8 scaffolding: registra surface MCP per 3 tool ML (`train_ml_filter`, `predict_trade_quality`, `get_ml_calibration`) come stub NotImplementedError più 3 ErrorCodes additivi e 3 nuove env var config. Zero logica di business — solo wiring + schema JSON + dispatch.

Purpose: sblocca Wave 1-3 (handler GREEN) PARALLELI con Phase 7 execute. Plan 08-01 NON dipende da `ml/inference.py` reale: gli handler sono stub che raise. Test xfail-marker garantisce zero falsa-verde mentre Phase 7 execute non è completo.

Output: `mcp_tools/handlers/ml.py` con 3 Tool + 3 stub handler picklable-by-signature; `mcp_tools/server.py` con dispatch bucket condizionale + 3 entry in `list_tools`; `mcp/errors.py` con 3 nuovi ErrorCodes; `config.py` + `.env.example` con 3 env var; `tests/test_mcp_handlers_ml.py` con 3 xfail test.
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-mcp-tools-part-2/08-CONTEXT.md
@.planning/phases/08-mcp-tools-part-2/08-PATTERNS.md
@.planning/phases/06-mcp-tools-part-1/06-03-PLAN.md
@mcp_tools/server.py
@mcp_tools/handlers/backtest.py
@mcp_tools/handlers/proposal.py
@mcp/errors.py
@config.py
@CLAUDE.md

<interfaces>
<!-- ErrorCodes attuali (mcp/errors.py:18-43) — extension target -->
```python
class ErrorCodes:
    RUN_IN_PROGRESS = "run_in_progress"
    UNKNOWN_RUN_ID = "unknown_run_id"
    NO_ACTIVE_RUN = "no_active_run"
    HISTORICAL_DATA_UNAVAILABLE = "historical_data_unavailable"
    AS_OF_TS_OUT_OF_RANGE = "as_of_ts_out_of_range"
    AS_OF_TS_WARMUP_INSUFFICIENT = "as_of_ts_warmup_insufficient"
    STOPS_LEVEL_VIOLATION = "stops_level_violation"
    PARTIAL_EXCEEDS_VOLUME = "partial_exceeds_volume"
    POSITION_NOT_FOUND = "position_not_found"
    BROKER_REJECTED = "broker_rejected"
    CONFLICT_TRAIL_AND_MANUAL_SL = "conflict: trail_and_manual_sl"
    CONFLICT_BE_AND_MANUAL_SL = "conflict: be_and_manual_sl"
    MT5_NOT_READY = "mt5_not_ready"
    DECISION_NOT_FOUND = "decision_not_found"

def envelope(error_code: str, message: str, **context) -> dict:
    base = {"ok": False, "error": error_code, "message": message}
    base.update(context)
    return base
```

<!-- Tool schema esistente analog (backtest.py:56-136) -->
```python
from mcp.types import Tool

RUN_BACKTEST_TOOL = Tool(
    name="run_backtest",
    description="...",
    inputSchema={
        "type": "object",
        "properties": {...},
        "required": [...],
    },
)
```

<!-- server.py extension points già identificati (verbatim line ranges) -->
- Linea 28-71: import block (aggiungere import ml dopo riga 70)
- Linea 156-293: list_tools (aggiungere 3 Tool dopo riga 292)
- Linea 298-381: call_tool dispatch (aggiungere bucket elif dopo riga 375)
- Linea 75-78: singletons (aggiungere `ml_filter_singleton: Any | None = None`)
- Linea 101-128: _bootstrap_state (aggiungere MLFilter.load condizionale post-JobQueue)

<!-- config.py extension point (linea 100-103) -->
```python
# Phase 6 MCP (D-C1, D-D1, D-A4)
MCP_DEFAULT_BARS: int = int(os.getenv("MCP_DEFAULT_BARS", "200"))
MCP_MAX_CONCURRENT_RUNS: int = int(os.getenv("MCP_MAX_CONCURRENT_RUNS", "1"))
TRAIL_TICK_TIMEFRAME: str = os.getenv("TRAIL_TICK_TIMEFRAME", "M15")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estendere ErrorCodes e Config con env var Phase 8 ML</name>
  <read_first>
    - mcp/errors.py (full file, 54 LOC — extension target ErrorCodes class)
    - config.py:100-108 (Phase 6 MCP block — analog pattern per Phase 8 ML block)
    - .planning/phases/08-mcp-tools-part-2/08-PATTERNS.md §config.py lines 670-700 (literal block da scrivere)
    - CLAUDE.md (italiano per commenti/log, .env via config zero magic numbers)
  </read_first>
  <files>mcp/errors.py, config.py, .env.example</files>
  <action>
**Step A — mcp/errors.py:** aggiungere 3 nuovi constant nella class `ErrorCodes` (dopo riga 43 `DECISION_NOT_FOUND`):
```python
# Phase 8 ML (D-08-B3, D-08-C, D-08-A1) — generic error codes
VALIDATION_FAILED = "validation_failed"
NOT_FOUND = "not_found"
INTERNAL_ERROR = "internal_error"
```
Aggiornare il modulo docstring (linea 7-10) aggiungendo riga:
```
- mcp/handlers/ml.py (validation_failed, not_found, internal_error)
```

**Step B — config.py:** aggiungere blocco DOPO riga 107 (`TRAIL_FAVORABLE_ONLY` line) e PRIMA del blocco Scheduler (riga 109):
```python
# Phase 8 MCP ML (D-08-A1/B1/D3)
# ML_MODEL_PATH: bundle joblib Phase 7 Plan 07-05 D-15 path convention.
# Symlink models/classifier_v1_latest.pkl -> classifier_v1_{date}.pkl per
# swap rolling senza restart MCP server (caricamento singleton a bootstrap).
ML_MODEL_PATH: Path = Path(os.getenv("ML_MODEL_PATH", "models/classifier_v1_latest.pkl"))
# MCP_TRAINING_DATA_PATH: parquet baseline (D-08-B1 hardcoded data source enum-only).
MCP_TRAINING_DATA_PATH: Path = Path(os.getenv(
    "MCP_TRAINING_DATA_PATH", "data/training/baseline_decisions/part-0.parquet",
))
# MCP_ML_THRESHOLD_MARGIN_PCT: margine entro cui un trade rejected dal ML
# gate e' considerato "marginale" nella Degraded Slices Analysis (D-08-D4).
MCP_ML_THRESHOLD_MARGIN_PCT: float = float(os.getenv(
    "MCP_ML_THRESHOLD_MARGIN_PCT", "0.05",
))
```

PRE-CONDIZIONE import: `from pathlib import Path` deve essere presente in cima a config.py. Se manca (grep), aggiungere come prima istruzione `from pathlib import Path` dopo le import esistenti `import os`.

D-08 Security mitigation: NON validare path-traversal in questa task (env var sotto controllo developer); il Plan 08-04 implementerà `path.resolve().is_relative_to()` per `MCP_TRAINING_DATA_PATH` dentro `_validate_parquet_schema_v2` (T-8-04-04 mitigation).

**Step C — .env.example:** trovare blocco `# Phase 6 MCP` (grep `MCP_DEFAULT_BARS`), aggiungere DOPO `TRAIL_FAVORABLE_ONLY` (o equivalente):
```bash
# Phase 8 MCP ML (D-08-A1/B1/D3)
# ML_MODEL_PATH=models/classifier_v1_latest.pkl
# MCP_TRAINING_DATA_PATH=data/training/baseline_decisions/part-0.parquet
# MCP_ML_THRESHOLD_MARGIN_PCT=0.05
```
Le righe vanno commentate (#) — sono default Phase 8, override opt-in.
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent &amp;&amp; python -c "from mcp.errors import ErrorCodes; assert ErrorCodes.VALIDATION_FAILED == 'validation_failed'; assert ErrorCodes.NOT_FOUND == 'not_found'; assert ErrorCodes.INTERNAL_ERROR == 'internal_error'; from config import Config; c = Config(); assert str(c.ML_MODEL_PATH).endswith('classifier_v1_latest.pkl'); assert str(c.MCP_TRAINING_DATA_PATH).endswith('part-0.parquet'); assert abs(c.MCP_ML_THRESHOLD_MARGIN_PCT - 0.05) &lt; 1e-9; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'VALIDATION_FAILED\|NOT_FOUND\|INTERNAL_ERROR' mcp/errors.py` >= 3
    - `grep -c 'ML_MODEL_PATH\|MCP_TRAINING_DATA_PATH\|MCP_ML_THRESHOLD_MARGIN_PCT' config.py` >= 3
    - `grep -c '^# MCP_TRAINING_DATA_PATH\|^# ML_MODEL_PATH\|^# MCP_ML_THRESHOLD_MARGIN_PCT' .env.example` >= 3
    - Comando automated PASS senza eccezioni
  </acceptance_criteria>
  <done>
    ErrorCodes enum ha 3 nuovi constant string. Config carica 3 nuove env var con default safe. .env.example documenta 3 commented defaults. Import smoke su Config() + ErrorCodes verde.
  </done>
</task>

<task type="auto">
  <name>Task 2: Creare mcp_tools/handlers/ml.py con 3 Tool schema + 3 stub handler</name>
  <read_first>
    - mcp_tools/handlers/backtest.py:1-145 (analog imports + Tool registration shape + worker decl comment)
    - mcp_tools/handlers/backtest.py:309-380 (analog handler signatures: handle_run_backtest, handle_get_backtest_metrics, handle_cancel_backtest)
    - .planning/phases/08-mcp-tools-part-2/08-PATTERNS.md §mcp_tools/handlers/ml.py §1-§3 (verbatim docstring + Tool schemas)
    - .planning/phases/08-mcp-tools-part-2/08-CONTEXT.md §Specifics lines 220-251 (predict_trade_quality input/response shape)
    - mcp/errors.py (ErrorCodes + envelope già esteso da Task 1)
  </read_first>
  <files>mcp_tools/handlers/ml.py</files>
  <action>
Creare NUOVO file `mcp_tools/handlers/ml.py` con la seguente struttura. Usare italiano per docstring/commenti (CLAUDE.md). 3 Tool schema D-08-A1/A2/B2/C1/C2 + 3 stub handler che raise `NotImplementedError("Wave N green plan 08-NN")` — il numero specifico per ogni handler.

```python
"""ML handlers per Phase 8 (MCP-04/05/06).

Tool inclusi:
- train_ml_filter (MCP-04) — D-08-A1 async submit via JobQueue Phase 6
  (cap=1 condiviso MCP_MAX_CONCURRENT_RUNS — D-08-A2).
- predict_trade_quality (MCP-05) — D-12 wrapper read-only MLFilter.predict
  (NON un gate; risk_engine resta unico gate decisione CLAUDE.md).
- get_ml_calibration (MCP-06) — D-08-C1 default per-fold breakdown;
  D-08-C2 opt-out summary_only.

Worker `_train_ml_filter_worker`:
- TOP-LEVEL (picklable per ProcessPoolExecutor cap=1 condiviso con backtest).
- NON importa MetaTrader5 (Phase 5 D-15 carry-forward).
- NON usa stdout (Pitfall 8 MCP JSON-RPC framing).
- Lazy imports interni di ml.train.train_classifier (picklability).
- Pre-validation parquet schema-v2 + sha256 (D-08-B3) PRIMA del submit.

Wave 0 (Plan 08-01): scaffolding stub NotImplementedError + Tool schemas.
Wave 1 (Plan 08-02): handle_predict_trade_quality GREEN.
Wave 2 (Plan 08-03): handle_get_ml_calibration GREEN.
Wave 3 (Plan 08-04): handle_train_ml_filter GREEN + _train_ml_filter_worker.
"""
from __future__ import annotations

from typing import Any

from mcp.types import Tool

from mcp_tools.errors import ErrorCodes, envelope

# ─── Tool schemas (D-08-A1/A2/B2/C1/C2) ─────────────────────────────────

TRAIN_ML_FILTER_TOOL = Tool(
    name="train_ml_filter",
    description=(
        "Avvia training async del classificatore LightGBM su parquet baseline. "
        "Ritorna job_id immediato (non blocca); pollare get_backtest_metrics(run_id) "
        "per status/result. Max 1 job concorrente (cap MCP_MAX_CONCURRENT_RUNS "
        "condiviso con backtest). Schema-v2 validation + sha256 check pre-training "
        "(D-08-B3 strict-fail)."
    ),
    inputSchema={
        "type": "object",
        "properties": {
            "data_source": {
                "type": "string",
                "enum": ["baseline"],
                "default": "baseline",
                "description": (
                    "D-08-B1: solo 'baseline' parquet supportato in Phase 8. "
                    "D-08-B2 enum extensible Phase 9/11 (logs/trades.db quando "
                    "schema bridge maturo)."
                ),
            },
        },
        "required": [],
    },
)

PREDICT_TRADE_QUALITY_TOOL = Tool(
    name="predict_trade_quality",
    description=(
        "Wrapper read-only di MLFilter.predict(). Ritorna ml_score (raw LightGBM) "
        "+ calibrated_prob + threshold_for_profile + would_pass_gate. NON e' un "
        "gate decisione (risk_engine resta unico gate). Se ENABLE_ML_FILTER=false "
        "OR singleton non caricato: tutti i campi null, ok:true, warning='ml_filter_disabled'."
    ),
    inputSchema={
        "type": "object",
        "properties": {
            "symbol":      {"type": "string", "enum": ["EURUSD", "GBPUSD", "USDJPY"]},
            "timeframe":   {"type": "string", "enum": ["M15", "M30", "H1"]},
            "profile":     {"type": "string", "enum": ["CONSERVATIVE", "MODERATE", "AGGRESSIVE"]},
            "direction":   {"type": "string", "enum": ["BUY", "SELL"]},
            "entry_price": {"type": "number"},
            "stop_loss":   {"type": "number"},
            "take_profit": {"type": "number"},
            "context":     {
                "type": "object",
                "description": (
                    "Optional extended indicators snapshot (33 cols Phase 2 + 5 meta). "
                    "Se assente: handler chiama MarketDataAdapter.get_extended_indicators "
                    "(deferred Wave 1 Plan 08-02)."
                ),
            },
        },
        "required": [
            "symbol", "timeframe", "profile", "direction",
            "entry_price", "stop_loss", "take_profit",
        ],
    },
)

GET_ML_CALIBRATION_TOOL = Tool(
    name="get_ml_calibration",
    description=(
        "Reliability data del classificatore corrente (metadata.json sidecar). "
        "Default: per-fold breakdown completo (10 fold, ~30-50KB) per audit-first "
        "(D-08-C1). Opt-out summary_only=true ritorna solo aggregate + "
        "threshold_by_profile + model_version + trained_at (~3-5KB, D-08-C2)."
    ),
    inputSchema={
        "type": "object",
        "properties": {
            "summary_only": {
                "type": "boolean",
                "default": False,
                "description": "Se true: payload ridotto (~3-5KB). Default false: per-fold full.",
            },
        },
        "required": [],
    },
)


# ─── Handler stub (Wave 0 — raise NotImplementedError; GREEN nei Wave 1-3) ─

def handle_train_ml_filter(
    args: dict,
    job_queue: Any,
    cfg: Any,
) -> dict:
    """MCP-04 — D-08-A1 async submit training.

    Wave 0 STUB: raise NotImplementedError. Plan 08-04 implementa.
    """
    raise NotImplementedError(
        "handle_train_ml_filter Wave 3 — implementato in Plan 08-04",
    )


def handle_predict_trade_quality(
    args: dict,
    ml_filter_singleton: Any,
    mt5_client: Any,
    cfg: Any,
) -> dict:
    """MCP-05 — D-12 wrapper read-only MLFilter.predict.

    Wave 0 STUB: raise NotImplementedError. Plan 08-02 implementa.
    """
    raise NotImplementedError(
        "handle_predict_trade_quality Wave 1 — implementato in Plan 08-02",
    )


def handle_get_ml_calibration(
    args: dict,
    cfg: Any,
) -> dict:
    """MCP-06 — D-08-C1 default per-fold; D-08-C2 summary_only opt-out.

    Wave 0 STUB: raise NotImplementedError. Plan 08-03 implementa.
    """
    raise NotImplementedError(
        "handle_get_ml_calibration Wave 2 — implementato in Plan 08-03",
    )


__all__ = [
    "TRAIN_ML_FILTER_TOOL",
    "PREDICT_TRADE_QUALITY_TOOL",
    "GET_ML_CALIBRATION_TOOL",
    "handle_train_ml_filter",
    "handle_predict_trade_quality",
    "handle_get_ml_calibration",
]
```

NB: Tool schema `data_source` enum lista solo `["baseline"]` per D-08-B2 (locked) — Plan 08-04 nel handler GREEN validerà letteralmente questo enum e ritornerà `validation_failed` se mismatch.

NB: NON aggiungere `ml_filter_singleton` o `MarketDataAdapter` import qui — sono Wave 1+ concerns. Stub firma include `ml_filter_singleton: Any` come placeholder per il singleton passato da server.py dispatch.
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent &amp;&amp; python -c "from mcp_tools.handlers.ml import TRAIN_ML_FILTER_TOOL, PREDICT_TRADE_QUALITY_TOOL, GET_ML_CALIBRATION_TOOL, handle_train_ml_filter, handle_predict_trade_quality, handle_get_ml_calibration; assert TRAIN_ML_FILTER_TOOL.name == 'train_ml_filter'; assert PREDICT_TRADE_QUALITY_TOOL.name == 'predict_trade_quality'; assert GET_ML_CALIBRATION_TOOL.name == 'get_ml_calibration'; assert TRAIN_ML_FILTER_TOOL.inputSchema['properties']['data_source']['enum'] == ['baseline']; assert set(PREDICT_TRADE_QUALITY_TOOL.inputSchema['required']) == {'symbol','timeframe','profile','direction','entry_price','stop_loss','take_profit'}; print('OK schemas')"</automated>
  </verify>
  <acceptance_criteria>
    - File `mcp_tools/handlers/ml.py` esiste, >=130 LOC effettive
    - `grep -c 'class \|def handle_' mcp_tools/handlers/ml.py` >= 3 (3 handler)
    - `grep -c 'Tool(' mcp_tools/handlers/ml.py` >= 3 (3 schema)
    - `grep -c 'NotImplementedError' mcp_tools/handlers/ml.py` >= 3 (1 per handler)
    - `grep -c 'enum' mcp_tools/handlers/ml.py` >= 3 (data_source + 3 enums input predict)
    - `python -c "import ast; ast.parse(open('mcp_tools/handlers/ml.py').read())"` exit 0
    - Comando automated PASS
  </acceptance_criteria>
  <done>
    ml.py creato con 3 Tool schema D-08 + 3 stub handler raise NotImplementedError firmati per Wave 1-3. Import smoke green. Nessuna business logic — pure scaffolding.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire mcp_tools/server.py con import + list_tools + dispatch + bootstrap singleton; aggiornare __init__.py barrel</name>
  <read_first>
    - mcp_tools/server.py (full 390 LOC — extension points lines 28-71 import, 156-293 list_tools, 298-381 dispatch, 75-78 singletons, 101-128 _bootstrap_state)
    - mcp_tools/__init__.py (5 LOC — verifica barrel)
    - .planning/phases/08-mcp-tools-part-2/08-PATTERNS.md §mcp_tools/server.py §Extension points 1-4 (verbatim)
    - .planning/phases/07-ml-classifier/07-05-PLAN.md lines 205-215 (MLFilter singleton + get_ml_filter pattern)
  </read_first>
  <files>mcp_tools/server.py, mcp_tools/__init__.py</files>
  <action>
**Step A — server.py import block** (dopo riga 70, prima di riga 72 commento `# Singleton globali`):
```python
# Phase 8 — ML control plane (MCP-04/05/06)
from mcp_tools.handlers.ml import (
    GET_ML_CALIBRATION_TOOL,
    PREDICT_TRADE_QUALITY_TOOL,
    TRAIN_ML_FILTER_TOOL,
    handle_get_ml_calibration,
    handle_predict_trade_quality,
    handle_train_ml_filter,
)
```

**Step B — server.py singleton** (dopo riga 82 `job_queue: "Any | None" = None`):
```python
# D-08 + Phase 7 D-13: MLFilter singleton, caricato a bootstrap se ENABLE_ML_FILTER=true
# e bundle file presente. None altrimenti (zero-impact rollout D-07-06-11).
ml_filter_singleton: "Any | None" = None
```

**Step C — server.py _bootstrap_state extension** (dopo riga 124 `log.info("MCP bootstrap state ready: ...")`, PRIMA della chiusura della funzione):
```python
    # Phase 8: MLFilter singleton load (best-effort, fallisce graceful)
    global ml_filter_singleton
    enable_ml = getattr(cfg, "ENABLE_ML_FILTER", False)
    bundle_path = getattr(cfg, "ML_MODEL_PATH", None)
    if enable_ml and bundle_path and Path(bundle_path).exists():
        try:
            from ml.inference import MLFilter
            ml_filter_singleton = MLFilter.load(bundle_path)
            log.info("MLFilter singleton caricato: %s", bundle_path)
        except Exception as exc:
            log.error("MLFilter load fallita (graceful): %s", exc)
            ml_filter_singleton = None
    else:
        log.info(
            "MLFilter singleton skip (ENABLE_ML_FILTER=%s bundle_exists=%s)",
            enable_ml, bool(bundle_path and Path(bundle_path).exists()),
        )
```
PRE-CONDIZIONE: aggiungere `from pathlib import Path` in cima a server.py se assente (grep verifica).
NB: `from ml.inference import MLFilter` è dentro try/except — se Phase 7 execute non completo l'import fallisce e si entra graceful path → `ml_filter_singleton=None` (test isolato green).

**Step D — server.py list_tools** (dopo riga 292 `GET_POSITION_STATE_TOOL,`, prima di riga 293 `]`):
```python
        # Phase 8 Wave 0 — ML control plane (MCP-04/05/06)
        TRAIN_ML_FILTER_TOOL,
        PREDICT_TRADE_QUALITY_TOOL,
        GET_ML_CALIBRATION_TOOL,
```

**Step E — server.py call_tool dispatch** (dopo riga 375 `return _text(handle_cancel_backtest(arguments, job_queue, cfg))`, prima di riga 377 `return _text({"error": f"unknown tool: {name}"})`):
```python
        # Phase 8 — ML control plane (MCP-04/05/06)
        if name in ("train_ml_filter", "predict_trade_quality", "get_ml_calibration"):
            if name == "train_ml_filter":
                if job_queue is None:
                    return _text(envelope(
                        ErrorCodes.INTERNAL_ERROR,
                        "JobQueue non inizializzata: chiamare _bootstrap_state() prima",
                        tool=name,
                    ))
                return _text(handle_train_ml_filter(arguments, job_queue, cfg))
            if name == "predict_trade_quality":
                return _text(handle_predict_trade_quality(
                    arguments, ml_filter_singleton, mt5, cfg,
                ))
            # get_ml_calibration
            return _text(handle_get_ml_calibration(arguments, cfg))
```

**Step F — mcp_tools/__init__.py** (verifica + extension):
Aprire mcp_tools/__init__.py. Se attualmente esporta solo nomi base (verifica con grep `__all__`), aggiungere export opzionale per `ml_filter_singleton` accessor lazy:
- Se esiste `__all__`: aggiungere "errors" e "schemas" se assenti.
- NESSUNA modifica funzionale: mcp_tools/__init__.py resta minimo, solo barrel.

Se file è solo `"""Package."""` 1-liner: NON modificare (compat shim Phase 6).
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent &amp;&amp; python -c "
import mcp_tools.server as s
import asyncio
tools = asyncio.run(s.list_tools())
names = [t.name for t in tools]
assert 'train_ml_filter' in names, f'missing train_ml_filter, got {names}'
assert 'predict_trade_quality' in names
assert 'get_ml_calibration' in names
assert s.ml_filter_singleton is None  # bootstrap not called → singleton None
print(f'OK list_tools={len(names)} tools, includes 3 ML tools')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TRAIN_ML_FILTER_TOOL\|PREDICT_TRADE_QUALITY_TOOL\|GET_ML_CALIBRATION_TOOL' mcp_tools/server.py` >= 6 (3 import + 3 list_tools)
    - `grep -c 'handle_train_ml_filter\|handle_predict_trade_quality\|handle_get_ml_calibration' mcp_tools/server.py` >= 6 (3 import + 3 dispatch)
    - `grep -c 'ml_filter_singleton' mcp_tools/server.py` >= 3 (1 declare + 1 bootstrap + 1 dispatch)
    - `grep -c 'from ml.inference import MLFilter' mcp_tools/server.py` == 1
    - server.py count list_tools entries >= 14 (11 esistenti Phase 6 + 3 nuovi Phase 8)
    - Comando automated PASS — list_tools async returns 3 nuove ML tool
    - `python -c "import mcp_tools.server"` exit 0 senza errori (graceful import-side)
  </acceptance_criteria>
  <done>
    server.py registra 3 ML Tool in list_tools, dispatch bucket condizionale `if name in ("train_ml_filter", ...)`, singleton `ml_filter_singleton` declared globale + lazy load in _bootstrap_state via try/except graceful (no fail se ml.inference non esiste Phase 7-pre-execute). Import smoke green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Creare tests/test_mcp_handlers_ml.py con 3 xfail test stub + 1 list_tools registration test green</name>
  <read_first>
    - tests/test_mcp_handlers_backtest.py (full ~219 LOC — analog fixture db_and_queue + pytest.importorskip pattern + concurrency cap test)
    - tests/test_mcp_handlers_proposal.py (full ~69 LOC — analog MagicMock cfg fixture)
    - mcp_tools/handlers/ml.py (creato Task 2 — 3 handler stub firme)
    - mcp_tools/server.py (modificato Task 3 — verifica list_tools async)
    - .planning/phases/08-mcp-tools-part-2/08-PATTERNS.md §tests/test_mcp_handlers_ml.py (test pattern table)
  </read_first>
  <files>tests/test_mcp_handlers_ml.py</files>
  <action>
Creare NUOVO file `tests/test_mcp_handlers_ml.py` con la seguente struttura. Italiano docstring; English identifier.

```python
"""Phase 8: ML handler tests (MCP-04/05/06) — Wave 0 scaffolding gate.

Wave 0 (Plan 08-01): 3 xfail test (stub raise NotImplementedError) +
1 green test (list_tools registration). Wave 1-3 (Plan 08-02/03/04)
flippano gli xfail in green.

Pattern analog tests/test_mcp_handlers_backtest.py.
"""
from __future__ import annotations

import asyncio
from unittest.mock import MagicMock

import pytest

pytest.importorskip("mcp_tools.handlers.ml")

from mcp_tools.handlers.ml import (  # noqa: E402
    GET_ML_CALIBRATION_TOOL,
    PREDICT_TRADE_QUALITY_TOOL,
    TRAIN_ML_FILTER_TOOL,
    handle_get_ml_calibration,
    handle_predict_trade_quality,
    handle_train_ml_filter,
)


# ─── Tool schema registration test (green Wave 0) ───────────────────────

def test_ml_tools_registered_in_list_tools():
    """Verifica che mcp_tools.server.list_tools() esponga i 3 nuovi tool ML."""
    import mcp_tools.server as srv
    tools = asyncio.run(srv.list_tools())
    names = {t.name for t in tools}
    assert "train_ml_filter" in names, f"train_ml_filter mancante, got {names}"
    assert "predict_trade_quality" in names
    assert "get_ml_calibration" in names


def test_train_ml_filter_tool_schema_data_source_enum():
    """D-08-B2: data_source enum locked a ['baseline'] in Phase 8."""
    assert TRAIN_ML_FILTER_TOOL.name == "train_ml_filter"
    props = TRAIN_ML_FILTER_TOOL.inputSchema["properties"]
    assert props["data_source"]["enum"] == ["baseline"]
    assert TRAIN_ML_FILTER_TOOL.inputSchema["required"] == []


def test_predict_trade_quality_tool_schema_required_fields():
    """MCP-05: 7 required fields + context optional."""
    required = set(PREDICT_TRADE_QUALITY_TOOL.inputSchema["required"])
    assert required == {
        "symbol", "timeframe", "profile", "direction",
        "entry_price", "stop_loss", "take_profit",
    }
    props = PREDICT_TRADE_QUALITY_TOOL.inputSchema["properties"]
    assert props["symbol"]["enum"] == ["EURUSD", "GBPUSD", "USDJPY"]
    assert props["profile"]["enum"] == ["CONSERVATIVE", "MODERATE", "AGGRESSIVE"]


def test_get_ml_calibration_tool_schema_summary_only_default_false():
    """D-08-C1: default per-fold (summary_only=False)."""
    props = GET_ML_CALIBRATION_TOOL.inputSchema["properties"]
    assert props["summary_only"]["type"] == "boolean"
    assert props["summary_only"]["default"] is False
    assert GET_ML_CALIBRATION_TOOL.inputSchema["required"] == []


# ─── Stub handler xfail (Wave 0; Plan 08-02/03/04 flippa in green) ──────

@pytest.mark.xfail(reason="Wave 1 Plan 08-02: handle_predict_trade_quality GREEN", strict=True)
def test_predict_trade_quality_stub_raises_not_implemented():
    """Wave 0: handler raise NotImplementedError (xfail).

    Plan 08-02 sostituisce raise con logica GREEN, e questo test flippa in
    PASS (strict=True → fail loud if green prima del tempo).
    """
    mock_cfg = MagicMock()
    mock_cfg.ENABLE_ML_FILTER = False
    handle_predict_trade_quality(
        args={"symbol": "EURUSD", "timeframe": "M15", "profile": "MODERATE",
              "direction": "BUY", "entry_price": 1.085,
              "stop_loss": 1.083, "take_profit": 1.090},
        ml_filter_singleton=None,
        mt5_client=MagicMock(),
        cfg=mock_cfg,
    )


@pytest.mark.xfail(reason="Wave 2 Plan 08-03: handle_get_ml_calibration GREEN", strict=True)
def test_get_ml_calibration_stub_raises_not_implemented():
    """Wave 0: handler raise NotImplementedError (xfail).

    Plan 08-03 sostituisce raise con logica GREEN (metadata.json reader).
    """
    mock_cfg = MagicMock()
    mock_cfg.ML_MODEL_PATH = "models/classifier_v1_latest.pkl"
    handle_get_ml_calibration(args={}, cfg=mock_cfg)


@pytest.mark.xfail(reason="Wave 3 Plan 08-04: handle_train_ml_filter GREEN", strict=True)
def test_train_ml_filter_stub_raises_not_implemented():
    """Wave 0: handler raise NotImplementedError (xfail).

    Plan 08-04 sostituisce raise con logica GREEN (worker + JobQueue.submit).
    """
    mock_cfg = MagicMock()
    mock_cfg.MCP_TRAINING_DATA_PATH = "data/training/baseline_decisions/part-0.parquet"
    mock_cfg.ML_MODEL_PATH = "models/classifier_v1_latest.pkl"
    mock_job_queue = MagicMock()
    handle_train_ml_filter(
        args={"data_source": "baseline"},
        job_queue=mock_job_queue,
        cfg=mock_cfg,
    )
```

NB: pytest `xfail(strict=True)` significa: se il test PASSA prima che il Wave 1-3 plan lo implementi (cioè la stub raise NotImplementedError viene rimossa per errore), pytest marca XPASSED → FAIL della suite. È il gate Nyquist Wave 0.
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent &amp;&amp; pytest tests/test_mcp_handlers_ml.py -v --no-header 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_mcp_handlers_ml.py` esiste, >=90 LOC
    - `grep -c 'xfail.*strict=True' tests/test_mcp_handlers_ml.py` == 3
    - `grep -c 'def test_' tests/test_mcp_handlers_ml.py` >= 7 (4 schema/registration green + 3 stub xfail)
    - `pytest tests/test_mcp_handlers_ml.py -v` exit 0 con 4 PASSED + 3 XFAIL (zero FAIL, zero XPASS)
    - Nessun test "magicamente verde" sui 3 xfail (xfail strict catch this)
  </acceptance_criteria>
  <done>
    Test file scrive 4 test green (list_tools registration + 3 schema validation) + 3 xfail strict gate sui stub handler. Suite green: 4 passed + 3 xfailed. Plan 08-02/03/04 dovranno rimuovere il decorator xfail quando handler GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → server.py dispatch | JSON-RPC args untrusted (Anthropic SDK / forex-trader-pro skill) |
| Env var → config.py | `ML_MODEL_PATH` / `MCP_TRAINING_DATA_PATH` developer-controlled, ma path arbitrary |
| server.py → ml.inference.MLFilter.load | Bundle joblib file (pickle deserialization) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01-01 | Information disclosure | Tool schema esposto su MCP list_tools include description in italiano (no PII) | accept | No PII / no segreto nei tool description; safe to expose |
| T-8-01-02 | Tampering | Stub handler `raise NotImplementedError` accidentalmente bypassato in dispatch | mitigate | xfail(strict=True) Task 4 catch XPASSED quando handler diventa green pre-tempo |
| T-8-01-03 | DoS | list_tools chiamato senza bootstrap → `ml_filter_singleton=None` → dispatch handle_predict_trade_quality passa None | accept | Task 2 stub raise NotImplementedError before consumare singleton; Wave 1 Plan 08-02 implementerà null-guard graceful |
| T-8-01-04 | Pickle deserialization | `MLFilter.load(bundle_path)` carica joblib pickle senza sha256 verification a bootstrap | accept (Wave 0) | Plan 08-04 `_validate_parquet_schema_v2` registra parquet sha256 in metadata.json; bundle sha256 verifica delegata a Plan 08-06 `_validate_bundle_pre_run` in run_ml_on_backtest.py. Wave 0 NON carica bundle reale (Phase 7 execute pre-condizione + try/except graceful). Decisione: ASVS L1 non blocca Wave 0 perché surface non attiva sin tanto che Phase 7 non produce bundle |
| T-8-01-05 | Env-driven config injection | `ML_MODEL_PATH` env var può puntare a path arbitrario | accept (Wave 0) | Plan 08-04 task `_validate_parquet_schema_v2` aggiungerà `path.resolve().is_relative_to(Path("data/training").resolve())` check per `MCP_TRAINING_DATA_PATH`; per `ML_MODEL_PATH` Plan 08-04 verificherà `is_relative_to(Path("models").resolve())`. Wave 0 carica solo se path esiste (Path.exists() check) + import try/except graceful — no escalation |
| T-8-01-06 | Repudiation | log MLFilter load success/fail su file (logger.py RotatingFileHandler) — audit OK | mitigate | Task 3 step C log.info su success, log.error su failure; pattern consistente con Phase 6 |
</threat_model>

<verification>
- `pytest tests/test_mcp_handlers_ml.py -v` → 4 PASSED + 3 XFAIL
- `python -c "import mcp_tools.server; import asyncio; print(len(asyncio.run(mcp_tools.server.list_tools())))"` → conteggio >= 14
- `python -c "from config import Config; c = Config(); print(c.ML_MODEL_PATH, c.MCP_TRAINING_DATA_PATH, c.MCP_ML_THRESHOLD_MARGIN_PCT)"` → 3 valori default
- `python -c "from mcp.errors import ErrorCodes; print(ErrorCodes.VALIDATION_FAILED, ErrorCodes.NOT_FOUND, ErrorCodes.INTERNAL_ERROR)"` → 3 string non-vuote
- `pytest tests/test_mcp_handlers_backtest.py -v` → suite Phase 6 backtest green (zero regressione)
- `pytest tests/test_mcp_tools_v2.py -v` → suite legacy green (zero regressione)
</verification>

<success_criteria>
- 3 Tool schema MCP-04/05/06 registrati in list_tools (verificato run-time)
- 3 stub handler raise NotImplementedError (xfail strict gate)
- 3 ErrorCodes nuovi disponibili per Wave 1-3
- 3 env var config Phase 8 caricate con default safe
- Suite globale post-08-01 NON regredisce (target: +4 passed + 3 xfailed, zero new FAIL)
- Wave 1+ unblocked: Plan 08-02 può procedere parallelo con Phase 7 execute (handler stub presente, signature stabile)
</success_criteria>

<output>
After completion, create `.planning/phases/08-mcp-tools-part-2/08-01-SUMMARY.md` with sezioni: Files Created/Modified (LOC count), Test Results (4 passed + 3 xfailed), Deviation (any Rule 1/2/3), Carry-forward to Plan 08-02/03/04 (signature lock + ErrorCodes available + bootstrap pattern).
</output>
