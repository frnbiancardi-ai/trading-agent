---
phase: 01-backtest-engine
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - backtest/broker.py
  - tests/test_backtest_broker.py
autonomous: true
requirements: [BACK-02, BACK-03]
tags: [backtest, broker, protocol]

must_haves:
  truths:
    - "BacktestBroker satisfies BrokerProtocol structurally (isinstance check passes)"
    - "Mt5Client satisfies BrokerProtocol structurally (regression — no live changes)"
    - "get_ohlc returns ONLY bars in the visible window — no future leakage (T-NoFutureLeak)"
    - "send_order registers a VirtualPosition; entry bar is NOT monitored for SL/TP on the same call (off-by-one Pitfall 1)"
    - "On a single bar that crosses BOTH SL and TP, SL fills first (conservative per D-09 / research §BacktestBroker Fill Semantics)"
    - "Gap-through scenario: bar.open beyond SL → fill at bar.open with reason SL_GAP"
    - "get_symbol_info (non-Protocol stub) returns SimpleNamespace with correct point/digits per symbol family"
  artifacts:
    - path: "backtest/broker.py"
      provides: "BacktestBroker, VirtualPosition. Implements BrokerProtocol + get_symbol_info stub."
      contains: "class BacktestBroker"
      min_lines: 120
    - path: "tests/test_backtest_broker.py"
      provides: "Protocol compliance + fill-semantics coverage"
      contains: "test_protocol_compliance"
  key_links:
    - from: "backtest/broker.py:BacktestBroker"
      to: "models.py:BrokerProtocol"
      via: "structural typing (no inheritance)"
      pattern: "class BacktestBroker"
    - from: "backtest/broker.py:BacktestBroker"
      to: "backtest/costs.py:CostModel"
      via: "constructor injection"
      pattern: "cost_model"
---

<objective>
Implement the BacktestBroker that powers BACK-02. It holds an in-memory bar window, implements the four BrokerProtocol methods (`get_ohlc`, `send_order`, `get_account_state`, `close_position`) plus the off-Protocol `get_symbol_info` stub, and applies the cost model on close. SL/TP monitoring with the conservative resolution policies from RESEARCH §BacktestBroker Fill Semantics. Plus protocol-compliance tests asserting both Mt5Client and BacktestBroker satisfy `BrokerProtocol`.

Purpose: Decouples strategy from MT5; enables Plan 05 engine to drive the strategy through historical bars.
Output: Full BacktestBroker + green tests.
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-backtest-engine/01-CONTEXT.md
@.planning/phases/01-backtest-engine/01-RESEARCH.md
@.planning/phases/01-backtest-engine/01-VALIDATION.md
@.planning/phases/01-backtest-engine/01-01-SUMMARY.md
@models.py
@mt5_client.py
@backtest/broker.py
@backtest/costs.py
@tests/test_backtest_broker.py

<interfaces>
<!-- BacktestBroker contract — used by Plan 05 (engine drives advance()) and validates BrokerProtocol from Plan 01. -->

```python
# backtest/broker.py
from collections import deque
from dataclasses import dataclass
from types import SimpleNamespace
from models import AccountState, OrderResult, BrokerProtocol  # Protocol satisfied structurally
from backtest.costs import CostModel
from backtest.loader import Bar

@dataclass
class VirtualPosition:
    position_id: int
    symbol: str
    direction: str           # "BUY" | "SELL"
    lots: float
    entry_price: float
    sl: float
    tp: float
    entry_time: int          # UTC unix timestamp
    entry_bar_index: int     # bar index at registration — used to skip same-bar SL/TP check (Pitfall 1)

class BacktestBroker:
    def __init__(
        self,
        symbol: str,
        timeframe: str,
        initial_balance: float,
        cost_model: CostModel,
        max_window: int = 500,
    ) -> None: ...

    # Engine-only API (not on Protocol)
    def advance(self, bar: Bar) -> list[dict]: ...   # returns closed-trade rows for this bar
    def get_symbol_info(self, symbol: str) -> SimpleNamespace: ...

    # BrokerProtocol API
    def get_ohlc(self, symbol: str, timeframe: str, n_bars: int) -> list[dict]: ...
    def send_order(self, symbol: str, direction: str, lots: float, sl: float, tp: float, comment: str = "") -> OrderResult: ...
    def get_account_state(self) -> AccountState: ...
    def close_position(self, position_id: int) -> OrderResult: ...
```

OrderResult / AccountState exist in models.py — read those before implementing to use real fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement BacktestBroker (full)</name>
  <read_first>
    - backtest/broker.py (stub from plan 01)
    - models.py (full — OrderResult, AccountState, PositionInfo, BrokerProtocol)
    - mt5_client.py (Mt5Client signatures: get_ohlc, send_order, get_account_state, close_position, get_symbol_info)
    - .planning/phases/01-backtest-engine/01-RESEARCH.md (§Pattern 3 Bar Window, §BacktestBroker Fill Semantics, §Pitfall 1 off-by-one, §Open Question 1 get_symbol_info stub)
    - backtest/loader.py (Bar dataclass)
    - backtest/costs.py (CostModel)
  </read_first>
  <files>backtest/broker.py</files>
  <behavior>
    - Constructor stores symbol/timeframe/balance/cost_model; initializes _window deque(maxlen=500), _positions dict, _next_id=1, _closed_trades list, _bar_index counter.
    - advance(bar) appends an OHLC dict to _window, increments _bar_index, then runs _check_sl_tp(bar) — but ONLY for positions whose entry_bar_index < current _bar_index (Pitfall 1: never close on the entry bar).
    - get_ohlc(symbol, timeframe, n_bars) returns list[dict] (last n_bars from _window). Asserts symbol/tf match (else returns []).
    - send_order: at bar close (current last bar in window), creates VirtualPosition with entry_price = last_bar.close, entry_time = last_bar.time, entry_bar_index = self._bar_index; assigns position_id from _next_id; returns OrderResult (use real fields from models.py — read first to confirm).
    - get_account_state: returns AccountState(balance=self._balance, equity=self._equity, free_margin=self._balance, open_positions=[PositionInfo(...) for each open], today_realized_pnl=0.0, starting_balance_of_day=self._initial_balance).
    - close_position(position_id): closes at last bar.close; applies cost model; updates _balance; returns OrderResult.
    - _check_sl_tp(bar): per RESEARCH §BacktestBroker Fill Semantics:
      * BUY: if bar.open <= sl → close at bar.open, reason "SL_GAP"; elif bar.low <= sl → close at sl, reason "SL"; elif bar.high >= tp → close at tp, reason "TP"
      * SELL: mirror (open >= sl → SL_GAP; high >= sl → SL; low <= tp → TP)
      * SL checked before TP when both crossed (conservative)
    - _close_virtual(pos_id, exit_price, reason, exit_time):
      * pnl_pips = (exit_price - entry_price) / pip_size if BUY else (entry_price - exit_price) / pip_size
      * gross_pnl_usd = pnl_pips * cost_model.pip_value_usd * lots
      * net_pnl_usd = gross_pnl_usd - cost_model.cost_usd(lots)
      * append closed-trade row dict {position_id, symbol, direction, lots, entry_price, exit_price, sl, tp, entry_time, exit_time, pnl_pips, pnl_usd (net), gross_pnl_usd, exit_reason} to _closed_trades and ALSO return it from advance().
      * update _balance += net_pnl_usd
      * delete from _positions
    - get_symbol_info(symbol): per research recommendation — return SimpleNamespace(point=0.001, digits=3, trade_tick_value=1.0, trade_tick_size=0.001, filling_mode=2, volume_step=0.01) for "JPY" in symbol; else point=0.00001, digits=5, trade_tick_value=1.0, trade_tick_size=0.00001, filling_mode=2, volume_step=0.01.
  </behavior>
  <action>
    Implement the full module per <behavior>. Important details:

    1. FIRST read `models.py` to copy the actual `OrderResult` / `AccountState` / `PositionInfo` field signatures — DO NOT GUESS. The plan author cannot show full signatures here; the executor must use the real ones.
    2. Module imports: `from __future__ import annotations`, stdlib (collections.deque, dataclasses.dataclass, types.SimpleNamespace), `from models import OrderResult, AccountState, PositionInfo`, `from backtest.costs import CostModel`, `from backtest.loader import Bar`.
    3. Do NOT inherit from BrokerProtocol — typing.Protocol is structural, not nominal. Compliance is verified at test time.
    4. SL/TP same-bar conflict resolution: SL FIRST (conservative). Document with comment "D-09 conservative: SL before TP when both crossed in same bar."
    5. Pitfall 1 (off-by-one): _check_sl_tp must skip positions where `pos.entry_bar_index == self._bar_index` (entry happens on current bar; monitoring starts next bar).
    6. advance() returns the list of closed-trade dicts so the engine (Plan 05) can stream them to the ledger writer.
    7. _next_id starts at 1 and increments per send_order.
    8. Cost is deducted ONCE on close (round-trip cost in commission_pips_round_trip already covers both legs).
    9. No logging, no print, no DB calls. Pure in-memory state.
    10. Use `Bar` (frozen dataclass with attribute access `.open .high .low .close .time`) — NOT dict access.
  </action>
  <verify>
    <automated>python -c "from backtest.broker import BacktestBroker, VirtualPosition; from backtest.costs import CostModel; from backtest.loader import Bar; from models import BrokerProtocol; cm = CostModel(0.5,0.3,0.5,0.0001,10.0); b = BacktestBroker('EURUSD','H1',10000.0,cm); assert isinstance(b, BrokerProtocol), 'BacktestBroker fails Protocol check'; print('proto-ok'); from mt5_client import Mt5Client; assert isinstance(Mt5Client.__new__(Mt5Client), BrokerProtocol) or hasattr(Mt5Client,'get_ohlc'), 'Mt5Client surface check'; print('OK')"</automated>
  </verify>
  <done>
    - `isinstance(BacktestBroker(...), BrokerProtocol)` is True
    - All 4 Protocol methods present and typed
    - `get_symbol_info` returns SimpleNamespace (off-Protocol stub)
    - File compiles via `python -m py_compile backtest/broker.py`
    - `grep -c "import sqlite3" backtest/broker.py` returns 0 (broker has no DB)
    - `grep -c "logging" backtest/broker.py` returns 0 (broker has no logging)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real tests for BacktestBroker fill semantics + Protocol compliance</name>
  <read_first>
    - tests/test_backtest_broker.py (skip-stub)
    - .planning/phases/01-backtest-engine/01-RESEARCH.md (§BacktestBroker Fill Semantics, §Pitfall 1)
    - .planning/codebase/TESTING.md
    - backtest/broker.py (just implemented)
  </read_first>
  <files>tests/test_backtest_broker.py</files>
  <behavior>
    - test_protocol_compliance: BacktestBroker AND Mt5Client both satisfy BrokerProtocol structurally (isinstance check or runtime_checkable). Use a constructed BacktestBroker; for Mt5Client, build a MagicMock spec'd from the class — BUT structural Protocol check works on any object with matching methods, so use isinstance(broker_instance, BrokerProtocol).
    - test_no_future_leak: Push 5 bars via advance(); call get_ohlc with n_bars=10 — only see bars pushed so far. Push 1 more bar — now see 6.
    - test_send_order: Push 1 bar; send_order BUY at SL=last-50pips, TP=last+100pips, lots=0.1. Verify _positions has 1 entry, OrderResult succeeds, position.entry_price == bar.close.
    - test_sl_tp_priority: Construct a scenario where the NEXT bar's high crosses TP AND low crosses SL. After advance() of that conflicting bar, position must close at SL (reason "SL"), not TP. Verify pnl_usd is negative.
    - test_gap_fill: For BUY, next bar's open is BELOW SL. After advance(), close reason == "SL_GAP" and exit_price == bar.open.
    - test_no_same_bar_close: Pitfall 1 — push bar #1, send_order BUY with TP=bar.high (immediate-fill trap). advance() of bar #1 does NOT close. advance() of bar #2 (which crosses TP) does close.
    - test_cost_applied: Open and close a flat trade (entry == exit). Net pnl_usd should equal -cost_usd(lots).
  </behavior>
  <action>
    Replace the plan-01 skip-stub with concrete tests. Helper builders allowed (private, file-scoped, prefixed `_`). Important constraints:

    1. Build Bar instances directly (frozen dataclass — pass all 8 fields).
    2. Use `from models import BrokerProtocol` for the isinstance() Protocol check (works because BrokerProtocol is `@runtime_checkable`).
    3. For Mt5Client compliance test: do NOT instantiate the real MT5 client (would try to connect). Use the class itself: assert all 4 method names exist as callables on Mt5Client class (`hasattr` + `callable`). Document why this is sufficient (Protocol is structural).
    4. test_no_same_bar_close MUST fail-loud if the broker incorrectly closes on entry bar — that's the regression guard.
    5. CostModel for tests: spread=1.0 / slippage=0.0 / commission_round_trip=0.0 / pip_size=0.0001 / pip_value=10.0 → cost_usd(0.1) == 0.1 * 1.0 * 10.0 = $1.00.
    6. SL/TP priority test: BUY with SL=1.09000, TP=1.10000, entry on bar where close=1.09500. Next bar high=1.10000, low=1.08500 — both crossed. Expected close at SL=1.09000.
    7. Gap-fill test: BUY with SL=1.09000, entry close=1.09500. Next bar open=1.08000 → close at 1.08000, reason "SL_GAP".

    Remove module-level pytest.mark.skip.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_broker.py -x --tb=short -v</automated>
  </verify>
  <done>
    - All 6+ broker tests pass
    - Protocol isinstance check green for BacktestBroker
    - SL-before-TP conservative resolution verified
    - SL_GAP reason verified
    - Same-bar entry/close prevented (Pitfall 1 guard)
    - No regression in baseline suite
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine→broker.advance | Only the engine pushes bars. No external input. |
| strategy→broker.get_ohlc | Strategy reads broker; broker reads its own window. No future-leak path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-NoFutureLeak | Spoofing (data integrity) | get_ohlc | mitigate | Window is append-only via advance(); get_ohlc only reads from _window deque. test_no_future_leak asserts no bars beyond pushed count are visible. |
| T-1-07 | Repudiation | _closed_trades log | accept | Each closed trade row has entry_time/exit_time/exit_reason; persisted by ledger writer in Plan 05. |
| T-OffByOne | Tampering (semantic) | _check_sl_tp on entry bar | mitigate | entry_bar_index recorded; _check_sl_tp skips positions whose entry_bar_index == current _bar_index. test_no_same_bar_close enforces. |
</threat_model>

<verification>
- `pytest tests/test_backtest_broker.py -x --tb=short` green
- `python -c "from backtest.broker import BacktestBroker; from models import BrokerProtocol; from backtest.costs import CostModel; assert isinstance(BacktestBroker('EURUSD','H1',10000.0, CostModel(0.5,0.3,0.5,0.0001,10.0)), BrokerProtocol)"` succeeds
- No regression
</verification>

<success_criteria>
- BacktestBroker implements BrokerProtocol structurally
- SL/TP/SL_GAP resolution matches RESEARCH §BacktestBroker Fill Semantics exactly
- Pitfall 1 (same-bar close) prevented and tested
- Cost model deducted on close
- Test file fully unblocked
</success_criteria>

<output>
Create `.planning/phases/01-backtest-engine/01-04-SUMMARY.md` documenting:
- BrokerProtocol compliance proof (isinstance returns True)
- SL/TP conflict resolution example
- Cost deduction example
</output>
