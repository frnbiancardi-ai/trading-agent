---
phase: 03-patterns-catalog
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - strategy.py
autonomous: true
requirements: [PATT-07]
tags: [patterns, strategy, refactor, integration]
must_haves:
  truths:
    - "strategy.py imports load_pattern_config from patterns module"
    - "IntradayStrategy.__init__ instantiates self._pattern_cfg = load_pattern_config()"
    - "scan_patterns call at line ~229 passes cfg=self._pattern_cfg"
    - "All three dict-style accesses (lines ~315, ~318, ~620) replaced with attribute access (.direction)"
    - "indicators_snapshot['patterns'] is now list[PatternHit] (consumers receive dataclass instances)"
    - "Existing strategy.py tests pass after refactor (regression-clean)"
    - "Full pytest -q suite green (26+ patterns + 11 pattern_config + all existing tests)"
  artifacts:
    - path: "strategy.py"
      provides: "5 call-site refactor + import update + __init__ pattern_cfg instantiation"
      contains: "load_pattern_config"
  key_links:
    - from: "strategy.py:scan_patterns()"
      to: "patterns.py:scan_patterns(bars, last_n, cfg)"
      via: "self._pattern_cfg passed via cfg kwarg"
      pattern: "cfg=self\\._pattern_cfg"
    - from: "strategy.py confluence checks"
      to: "PatternHit.direction"
      via: "p.direction == 'bullish' / 'bearish'"
      pattern: "p\\.direction =="
---

<objective>
Refactor atomico di `strategy.py` per consumare il nuovo schema `PatternHit` (attribute access) e iniettare `PatternConfig` tramite `IntradayStrategy.__init__`. Tocca 5 call site + 1 import + 1 init line. Verifica regression completa pytest.

Purpose: Chiude il loop Phase 3 — i consumatori esistenti vedono il nuovo formato senza shim. Soddisfa Phase 3 success criterion #3 (callers updated, no adapter shim per CONTEXT r2).
Output: strategy.py modificato in modo chirurgico, suite di test globale verde, smoke test import pulito.
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/trading-agent/CLAUDE.md
@C:/trading-agent/.planning/phases/03-patterns-catalog/03-CONTEXT.md
@C:/trading-agent/.planning/phases/03-patterns-catalog/03-RESEARCH.md
@C:/trading-agent/.planning/phases/03-patterns-catalog/03-PATTERNS.md
@C:/trading-agent/strategy.py
@C:/trading-agent/patterns.py
@C:/trading-agent/.planning/phases/03-patterns-catalog/03-03-PLAN.md

<interfaces>
<!-- API Phase 3 finale (post Wave 3) -->

```python
# patterns.py exports
PatternHit            # frozen dataclass: name, bar_index, span_bars, extreme_price, confidence, direction
PatternConfig         # frozen dataclass nidificato (loaded from config/patterns.yaml)
load_pattern_config(path: str | Path | None = None) -> PatternConfig
scan_patterns(bars: list[dict], last_n: int = 5, cfg: PatternConfig | None = None) -> list[PatternHit]
```

<!-- Refactor surface esatta (RESEARCH.md "Strategy.py Refactor Surface") -->

```python
# strategy.py:32 (import)
- from patterns import scan_patterns
+ from patterns import scan_patterns, load_pattern_config

# strategy.py IntradayStrategy.__init__ around line 152-162
# Aggiungere come ultima istruzione del body:
+ self._pattern_cfg = load_pattern_config()

# strategy.py:229 (call site, cfg pass-through)
- patterns = scan_patterns(bars, last_n=cfg.PATTERN_CONFIRMATION_BARS) if cfg.ENABLE_CANDLESTICK_PATTERNS else []
+ patterns = scan_patterns(bars, last_n=cfg.PATTERN_CONFIRMATION_BARS, cfg=self._pattern_cfg) if cfg.ENABLE_CANDLESTICK_PATTERNS else []

# strategy.py:315 (attribute access)
- has_pattern_bull = any(p["direction"] == "bullish" for p in patterns)
+ has_pattern_bull = any(p.direction == "bullish" for p in patterns)

# strategy.py:318 (attribute access)
- has_pattern_bear = any(p["direction"] == "bearish" for p in patterns)
+ has_pattern_bear = any(p.direction == "bearish" for p in patterns)

# strategy.py:620 (attribute access in _score_confidence)
- has_aligned = any(p["direction"] == wanted for p in patterns)
+ has_aligned = any(p.direction == wanted for p in patterns)
```

<!-- Note: line 241 indicators_snapshot["patterns"] = patterns NON cambia (continua a essere assegnamento). Cambia solo il TIPO contenuto (PatternHit invece di dict). Documentare in summary. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor atomico strategy.py — import, __init__, 5 call sites</name>
  <read_first>
    - C:/trading-agent/strategy.py (riga 32, 152-162 init body, 229, 314-321, 619-621 — leggere range esatti per conferma offset)
    - C:/trading-agent/.planning/phases/03-patterns-catalog/03-RESEARCH.md (lines 671-702 — exact diff hint)
    - C:/trading-agent/patterns.py (verifica `load_pattern_config` esportato)
  </read_first>
  <files>strategy.py</files>
  <behavior>
    - Import line: aggiunge `load_pattern_config` accanto a `scan_patterns`
    - `IntradayStrategy.__init__` (la seconda __init__ nel file, line 152): aggiunge `self._pattern_cfg = load_pattern_config()` dopo `self.env = environment or StrategyEnvironment(cfg, self.log)`
    - Line 229: passa `cfg=self._pattern_cfg` a `scan_patterns`
    - Lines 315, 318, 620: `p["direction"]` → `p.direction`
    - Linea 241 invariata (assegnamento); cambiamento di tipo documentato nel summary del piano
    - `StrategyEnvironment` class (prima __init__ a linea 59) NON viene toccata — non costruisce IntradayStrategy
    - Nessun altro punto di strategy.py modificato
  </behavior>
  <action>
Apri `strategy.py` ed esegui le 5+2 modifiche puntuali con Edit tool, una alla volta. Conferma sempre l'unicità della stringa di ricerca prima di sostituire.

**1) Import (linea 32):**
- old: `from patterns import scan_patterns`
- new: `from patterns import scan_patterns, load_pattern_config`

**2) IntradayStrategy.__init__ body (cerca esattamente la linea seguente come hook unico):**
- old:
```
        self.env = environment or StrategyEnvironment(cfg, self.log)
```
- new:
```
        self.env = environment or StrategyEnvironment(cfg, self.log)
        self._pattern_cfg = load_pattern_config()  # Phase 3: carica catalog cfg una volta a startup
```

**3) Call site scan_patterns (linea ~229):**
- old:
```
        patterns = scan_patterns(bars, last_n=cfg.PATTERN_CONFIRMATION_BARS) if cfg.ENABLE_CANDLESTICK_PATTERNS else []
```
- new:
```
        patterns = scan_patterns(bars, last_n=cfg.PATTERN_CONFIRMATION_BARS, cfg=self._pattern_cfg) if cfg.ENABLE_CANDLESTICK_PATTERNS else []
```

**4) Direction check bullish (linea 314-316):**
- old:
```
        has_pattern_bull = any(
            p["direction"] == "bullish" for p in patterns
        )
```
- new:
```
        has_pattern_bull = any(
            p.direction == "bullish" for p in patterns
        )
```

**5) Direction check bearish (linea 317-319):**
- old:
```
        has_pattern_bear = any(
            p["direction"] == "bearish" for p in patterns
        )
```
- new:
```
        has_pattern_bear = any(
            p.direction == "bearish" for p in patterns
        )
```

**6) Direction check in _score_confidence (linea ~620):**
- old:
```
        has_aligned = any(p["direction"] == wanted for p in patterns)
```
- new:
```
        has_aligned = any(p.direction == wanted for p in patterns)
```

**Note critiche:**
- NON aggiungere altre modifiche oltre queste 6.
- NON toccare `StrategyEnvironment.__init__` (linea 59) — non è IntradayStrategy.
- Linea 241 (`"patterns": patterns`) NON cambia — il tipo contenuto è ora `list[PatternHit]` ma l'assegnamento è invariato.
- `identify_entry_setup(...)` accetta `patterns` come argomento; verifica con grep se al suo interno c'è altro accesso `p["..."]` — se sì, va aggiornato. Cerca `p\["` su tutto il file per essere sicuro di aver coperto tutti i siti.

**Sicurezza:** nessun nuovo input untrusted introdotto. `load_pattern_config()` con path None usa default + env (validati dal loader fail-closed del piano 01).
  </action>
  <verify>
    <automated>python -c "import strategy; print('OK')" && grep -c "p\.direction" strategy.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from patterns import scan_patterns, load_pattern_config" strategy.py`
    - `grep -q "self._pattern_cfg = load_pattern_config()" strategy.py`
    - `grep -q "cfg=self._pattern_cfg" strategy.py`
    - `grep -c "p\.direction" strategy.py` >= 3
    - `! grep -E 'p\["(pattern|direction|bar_index)"\]' strategy.py` (nessun residuo dict access su pattern hits)
    - `python -c "import strategy"` exit 0
  </acceptance_criteria>
  <done>strategy.py importa load_pattern_config, instanzia _pattern_cfg in __init__, passa cfg a scan_patterns, accede agli hit via attribute. Zero residui dict access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression full-suite + smoke import end-to-end</name>
  <read_first>
    - C:/trading-agent/tests/ (verifica presenza tests/test_strategy.py se esiste — non parte di Phase 3 ma da regredire)
    - C:/trading-agent/strategy.py (post Task 1)
    - C:/trading-agent/.planning/phases/03-patterns-catalog/03-VALIDATION.md (sampling phase gate)
  </read_first>
  <files></files>
  <behavior>
    - Full pytest suite verde: pattern_config 11 + patterns 26+ + tutti gli altri test esistenti
    - Smoke test importa strategy + patterns + verifica end-to-end pipeline su bars sintetici
    - Conferma che `indicators_snapshot["patterns"]` contiene `list[PatternHit]` (smoke run su _analyze_technical o equivalente)
  </behavior>
  <action>
Questo task è puramente verifica regressione — nessun file modificato. Esegui in sequenza:

**1) Full pytest suite:**
```bash
pytest -q --tb=short
```
Tutti i test devono passare. Se qualche test su `tests/test_strategy.py` (se esiste) fallisce per dict→attribute residuo, identifica il sito mancante con `grep -nE 'p\["(direction|pattern|bar_index)"\]' strategy.py tests/` e applicare la stessa attribute migration localmente.

**2) Smoke import:**
```bash
python -c "from patterns import scan_patterns, PatternHit, PatternConfig, load_pattern_config; from strategy import IntradayStrategy; print('imports OK'); cfg = load_pattern_config(); print('config loaded:', type(cfg).__name__); bars = [{'open':1.10+i*0.001,'high':1.101+i*0.001,'low':1.099+i*0.001,'close':1.1005+i*0.001,'tick_volume':100} for i in range(10)]; out = scan_patterns(bars, last_n=5, cfg=cfg); print('scan ok, hits:', len(out)); assert all(isinstance(h, PatternHit) for h in out); print('all PatternHit OK')"
```

**3) Grep negative checks (nessun residuo dict access):**
```bash
grep -nE 'p\["(direction|pattern|bar_index)"\]' strategy.py tests/test_patterns.py tests/test_pattern_config.py
```
Atteso: nessuna riga in output (exit code 1).

**4) Grep positive (Phase 3 markers presenti):**
```bash
grep -q "@dataclass(frozen=True)" patterns.py
grep -q "load_pattern_config" patterns.py strategy.py
grep -q "PatternHit" patterns.py
```
Tutti devono ritornare exit 0.

**Sicurezza:** nessun side effect; solo verifica.
  </action>
  <verify>
    <automated>pytest -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` exit 0 su intera suite
    - Smoke import block sopra exit 0 stampando "all PatternHit OK"
    - `! grep -E 'p\["(direction|pattern|bar_index)"\]' strategy.py tests/test_patterns.py tests/test_pattern_config.py` (no residui)
    - `grep -q "@dataclass(frozen=True)" patterns.py` exit 0
    - `grep -q "load_pattern_config" strategy.py` exit 0
    - `grep -q "PatternHit" patterns.py` exit 0
    - Phase 3 success criteria 1, 2, 3 (ROADMAP) tutti soddisfatti
  </acceptance_criteria>
  <done>Suite globale verde, smoke import end-to-end pulito, Phase 3 chiusa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy.py→patterns.py | Strategy chiama `load_pattern_config()` con path default → env → DEFAULT_CONFIG_PATH; loader fail-closed (T-3-01..02 mitigati da plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-10 | Tampering | __init__ chiama load_pattern_config senza try/except — config malformato fa crashare avvio strategy | accept | Comportamento desiderato: fail-fast all'avvio. Errori YAML/anchor visibili in log via stack trace. Opzionale future enhancement: wrapper try/except che logga e usa default minimo (deferred). |
| T-3-11 | Repudiation | Cambio di tipo `indicators_snapshot["patterns"]` da list[dict] a list[PatternHit] non logato | accept | Documentato nel SUMMARY del piano; consumatori downstream (Phase 7 ML) verranno costruiti contro il nuovo tipo. Nessun consumatore esterno (MCP-14 deferred). |
</threat_model>

<verification>
- `pytest -q` exit 0 (full suite)
- `python -c "import strategy; from patterns import PatternHit; s=__import__('strategy'); print('OK')"` exit 0
- `! grep -E 'p\["(direction|pattern|bar_index)"\]' strategy.py tests/`
- `grep -q "self._pattern_cfg" strategy.py`
- `grep -q "cfg=self._pattern_cfg" strategy.py`
</verification>

<success_criteria>
- 5 call-site refactor in strategy.py applicati in modo chirurgico (import + init + scan + 3 attribute access)
- Zero residui di accesso dict-style su pattern hits in tutto il repo
- Suite di test completa verde post-refactor
- Phase 3 ROADMAP success criteria #1, #2, #3 tutti soddisfatti:
  1. Each pattern: hand-crafted positive + near-miss negative test ✓ (piano 02 + 03)
  2. PatternHit dataclass returned with all 6 fields ✓ (piano 01 + 03)
  3. Existing scan_patterns callers updated (no shim) ✓ (questo piano)
</success_criteria>

<output>
After completion, create `.planning/phases/03-patterns-catalog/03-04-SUMMARY.md`
</output>
