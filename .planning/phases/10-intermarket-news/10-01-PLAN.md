---
phase: 10
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_intermarket_loader.py
  - tests/test_intermarket_score.py
  - tests/test_calendar_rss_client.py
  - tests/test_mcp_handlers_macro.py
  - tests/test_phase10_no_phase5_drift.py
  - tests/fixtures/macro_dxy_smoke.csv
  - tests/fixtures/ff_rss_smoke.xml
  - tests/fixtures/ff_rss_dst_cross.xml
  - data/historical/macro/DXY/daily.csv
  - data/historical/macro/US10Y/daily.csv
  - data/historical/macro/XAUUSD/daily.csv
  - data/historical/macro/WTI/daily.csv
  - data/historical/macro/metadata.json
  - .gitignore
autonomous: false
requirements:
  - MCP-10
  - MCP-13
user_setup:
  - service: stooq
    why: "Manual download macro CSV (DXY/US10Y/XAUUSD/WTI) per Stooq CAPTCHA gate Pitfall 1"
    env_vars: []
    dashboard_config:
      - task: "Scaricare 4 CSV daily 23.6y per i 4 simboli macro (DXY/US10Y/XAUUSD/WTI)"
        location: "https://stooq.com/q/d/?s=^dxy&i=d (e analoghi per ^tnx, xauusd, cl.f)"
      - task: "Riusare data/historical/DXY/1Dyapt2.csv esistente come DXY/daily.csv (3084 row 2002-10-21->2026-04 BACK-01 format pre-verificato)"
        location: "Filesystem locale (cp/mv)"

must_haves:
  truths:
    - "tests/fixtures/macro_dxy_smoke.csv esiste (200 row, format BACK-01 semicolon: Data;Ora;Close)"
    - "tests/fixtures/ff_rss_smoke.xml esiste (>=10 eventi FF schema verificato)"
    - "tests/fixtures/ff_rss_dst_cross.xml esiste (eventi pre/post 2nd Sun March + 1st Sun November DST boundary)"
    - "data/historical/macro/{DXY,US10Y,XAUUSD,WTI}/daily.csv esistono in formato BACK-01"
    - "data/historical/macro/metadata.json esiste con sha256 dei 4 CSV"
    - "5 test file tests/test_intermarket_loader.py, test_intermarket_score.py, test_calendar_rss_client.py, test_mcp_handlers_macro.py, test_phase10_no_phase5_drift.py esistono con stubs xfail/skip Nyquist-compliant"
    - "test_phase10_no_phase5_drift.py PASS verde da subito (asserisce schema-v2 IMMUTATO + parquet sha256 pinned + umbrella test_baseline_immutato_with_flag_off canonical 10-VALIDATION.md)"
    - ".gitignore include data/cache/ (cache MCP-13 gitignored)"
  artifacts:
    - path: "tests/test_intermarket_loader.py"
      provides: "5 test stub MCP-10 loader (csv_read_smoke, strict_lt_no_future_leakage, sha256_anchor_stable, sha256_mismatch_detected, missing_symbol_returns_none, missing_csv_raises)"
      contains: "test_close_at_strict_less_than"
    - path: "tests/test_intermarket_score.py"
      provides: "stub MCP-10 score (direction_sign_flip, known_regime 2020-03 COVID + 2022-Q1 FOMC hike)"
      contains: "test_direction_sign_flip"
    - path: "tests/test_calendar_rss_client.py"
      provides: "14 test stub MCP-13 (parse_event, ET->UTC EDT/EST, DST cross-year, holiday, Tentative skip, cache TTL/roundtrip/fallback, pair filter)"
      contains: "test_dst_boundary_march_november"
    - path: "tests/test_mcp_handlers_macro.py"
      provides: "stub MCP-10/13 handler envelope shape"
      contains: "test_get_intermarket_context_envelope"
    - path: "tests/test_phase10_no_phase5_drift.py"
      provides: "D-10-C0 enforcement gate (schema-v2 IMMUTATO + parquet sha256 anchor + umbrella canonical test_baseline_immutato_with_flag_off)"
      contains: "test_dataset_writer_schema_v2_immutato"
    - path: "tests/fixtures/macro_dxy_smoke.csv"
      provides: "200-row deterministic DXY daily-close excerpt format BACK-01"
    - path: "tests/fixtures/ff_rss_smoke.xml"
      provides: "10 eventi FF reali (live schema verificato 2026-05-13)"
    - path: "tests/fixtures/ff_rss_dst_cross.xml"
      provides: "DST boundary regression fixture Mar 2nd Sun + Nov 1st Sun"
    - path: "data/historical/macro/DXY/daily.csv"
      provides: "DXY committed 23.6y (riuso 1Dyapt2.csv esistente)"
    - path: "data/historical/macro/US10Y/daily.csv"
      provides: "US10Y CBOE 10-Year yield index committed 23.6y"
    - path: "data/historical/macro/XAUUSD/daily.csv"
      provides: "XAUUSD gold spot committed 23.6y"
    - path: "data/historical/macro/WTI/daily.csv"
      provides: "WTI crude oil continuous committed 23.6y"
    - path: "data/historical/macro/metadata.json"
      provides: "sha256 anchor per i 4 CSV + Stooq ticker source + download date"
      contains: "sha256"
  key_links:
    - from: "tests/test_phase10_no_phase5_drift.py"
      to: "backtest/baseline/dataset_writer._SCHEMA_V2_REQUIRED_KEYS"
      via: "import + assert nessun campo intermarket/macro/news"
      pattern: "from backtest.baseline.dataset_writer import _SCHEMA_V2_REQUIRED_KEYS"
    - from: "tests/test_phase10_no_phase5_drift.py"
      to: "data/training/baseline_decisions/part-0.parquet"
      via: "file_sha256 + pin valore atteso al primo run"
      pattern: "from backtest.baseline.determinism import file_sha256"

threat_model:
  trust_boundaries:
    - boundary: "operator->Stooq HTTPS"
      description: "Manual CSV download via browser, CAPTCHA-gated (Pitfall 1)"
    - boundary: "filesystem->git commit (data/historical/macro/)"
      description: "CSV statici committati, integrità via sha256 (D-10-A1, D-10-A4)"
  stride_register:
    - id: "T-10-01-01"
      category: "Tampering"
      component: "data/historical/macro/{DXY,US10Y,XAUUSD,WTI}/daily.csv"
      disposition: "mitigate"
      mitigation: "sha256 anchor in data/historical/macro/metadata.json + Phase 7 retrain abort se sha256 cambia senza intent (D-10-A4). ASVS V6 partial (integrity-only, no encryption)."
    - id: "T-10-01-02"
      category: "Information Disclosure"
      component: "Stooq HTML CAPTCHA page invece di CSV"
      disposition: "mitigate"
      mitigation: "Manual operator download via browser. Wave 0 spike verifica row count > 5000 + sample close vs ICE/CME pubblico. NON committare CSV se row count < 5000. ASVS V13 partial."
    - id: "T-10-01-03"
      category: "Tampering"
      component: "Phase 5 parquet schema-v2 1076x59 (data/training/baseline_decisions/part-0.parquet)"
      disposition: "mitigate"
      mitigation: "test_phase10_no_phase5_drift.py asserisce _SCHEMA_V2_REQUIRED_KEYS frozenset NON cambiato + parquet sha256 anchor pinned (D-10-C0 IMMUTATO). ASVS V6 partial."
    - id: "T-10-01-04"
      category: "Future Leakage (T-10-FL)"
      component: "MacroLoader.close_at - fixture macro_dxy_smoke.csv"
      disposition: "mitigate"
      mitigation: "Fixture costruita con tutti i bar antecedenti il test ts_utc. Test Wave 1 in 10-02 PLAN asserisce strict-< no-future-leakage."
---

<objective>
Wave 0 scaffolding Phase 10: creare tutti i test stub + fixtures + macro CSV committati prima di scrivere production code. Obiettivo: ogni Wave 1+ task diventa RED->GREEN deterministico, e il Phase 5 immutability gate (D-10-C0) è ATTIVO dal commit-1 di Phase 10.

Purpose: 
- Anti-Shallow rule: ogni production module Phase 10 va incontro a un test stub gia esistente (Nyquist coverage al 100% dal commit di Wave 1).
- D-10-C0 gate enforcement: schema-v2 + parquet sha256 anchor regression test PASS verde da subito, qualunque drift accidentale di un planner-junior in Wave 1+ rompe CI.
- Stooq CAPTCHA gate (Pitfall 1): download manuale OBBLIGATORIO ora (operator checkpoint), non scriptato in Wave 3.
- FF schema (Pitfall 4): fixture XML verbatim dal live feed, NON inventata.

Output: 5 test file con stub xfail/skip-marked, 3 fixtures, 4 macro CSV committed, 1 metadata.json sha256, .gitignore patch.
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-intermarket-news/10-CONTEXT.md
@.planning/phases/10-intermarket-news/10-RESEARCH.md
@.planning/phases/10-intermarket-news/10-PATTERNS.md
@.planning/phases/10-intermarket-news/10-VALIDATION.md

# File esistenti da leggere PRIMA di toccare i nuovi test
@tests/test_news_aggregator.py
@tests/test_baseline_determinism.py
@tests/test_baseline_dataset_writer.py
@backtest/baseline/determinism.py
@backtest/baseline/dataset_writer.py
@data/historical/DXY/1Dyapt2.csv

<interfaces>
<!-- Contracts che i test stub devono codificare (anche se la implementation arriva in 10-02..04). -->
<!-- Executor: USA queste signature, NON inventare alternative. -->

From backtest/baseline/dataset_writer.py:
```python
_SCHEMA_V2_REQUIRED_KEYS: frozenset[str]  # 59 keys, programmaticamente built via _build_required_keys_v2()
def _build_required_keys_v2() -> frozenset[str]: ...
```

From backtest/baseline/determinism.py:
```python
def file_sha256(path: Path) -> str  # hex digest 64-char
```

Da Phase 10 RESEARCH §Pattern 1 (firma futura MacroLoader, codificata negli stub):
```python
MACRO_SYMBOLS = ("DXY", "US10Y", "XAUUSD", "WTI")
class MacroLoader:
    @classmethod
    def from_repo(cls, root: Path = Path("data/historical/macro")) -> "MacroLoader": ...
    def close_at(self, symbol: str, ts_utc: datetime) -> float | None: ...
    def sha256(self, symbol: str) -> str | None: ...
```

Da Phase 10 RESEARCH §Pattern 2 (firma futura CalendarRSSClient + CalendarEvent):
```python
@dataclass(frozen=True)
class CalendarEvent:
    title: str
    country: str
    event_time_utc: datetime
    impact: str
    forecast: str | None = None
    previous: str | None = None
    url: str | None = None
    all_day: bool = False

class CalendarRSSClient:
    def __init__(self, cfg, logger: logging.Logger | None = None): ...
    def fetch_events(self, *, force: bool = False, now: datetime | None = None) -> list[CalendarEvent]: ...
    def filter_window(self, events, now, window_minutes, min_impact='High', currencies=None) -> list[CalendarEvent]: ...
```

Da Phase 10 RESEARCH §Pattern 3 (firma futura build_intermarket_score):
```python
def build_intermarket_score(loader) -> Callable[[str, Literal["long","short"]], float]: ...
```

Da Phase 10 RESEARCH §Pattern 6 (firma futura handlers, signature reale envelope per Pattern-finding 6):
```python
def handle_get_intermarket_context(args, macro_loader, cfg) -> dict
def handle_get_economic_calendar(args, calendar_client, cfg) -> dict
# envelope ATTUALE (verificata in mcp/errors.py:52): envelope(error_code, message, **context) -> {ok: False, error: code, message: msg, ...context}
# Success path = return dict diretto (NON wrapped), come mcp_tools/handlers/market.py:114-122
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Macro CSV bundle + metadata.json (committed data)</name>
  <files>
    data/historical/macro/DXY/daily.csv
    data/historical/macro/US10Y/daily.csv
    data/historical/macro/XAUUSD/daily.csv
    data/historical/macro/WTI/daily.csv
    data/historical/macro/metadata.json
    .gitignore
  </files>
  <read_first>
    - data/historical/DXY/1Dyapt2.csv (3084 row 2002-10-21->2026-04, BACK-01 semicolon format, riuso diretto per DXY)
    - backtest/loader.py (BACK-01 parse pattern: sep=";", Data;Ora;Close, DD/MM/YYYY)
    - backtest/baseline/determinism.py (file_sha256)
    - .planning/phases/10-intermarket-news/10-RESEARCH.md §Pitfall 1 (Stooq CAPTCHA), §Pattern 1, §Open Question 1+2, Standard Stack §Version verification (Stooq URLs)
    - .planning/phases/10-intermarket-news/10-CONTEXT.md §canonical_refs (D-10-A1/A2/A3/A4)
  </read_first>
  <action>
    Step 1 — RIUSO DXY esistente (Open Question 2 raccomandazione):
    - Copia `data/historical/DXY/1Dyapt2.csv` -> `data/historical/macro/DXY/daily.csv` letterale (NO transformazione). File già BACK-01 semicolon `Data;Ora;Close;...` DD/MM/YYYY, 3084 row 2002-10-21->2026-04.
    - `mkdir -p data/historical/macro/DXY` prima.

    Step 2 — MANUAL DOWNLOAD operator (CHECKPOINT — vedi Task 2): l'operatore scaricherà US10Y/XAUUSD/WTI MANUALMENTE da Stooq via browser (Pitfall 1: batch automatico=CAPTCHA). I file devono essere:
    - `data/historical/macro/US10Y/daily.csv` — ticker Stooq `^tnx` (CBOE 10-Year Treasury Yield Index, daily close, 23.6y range 2002-10-21 -> today)
    - `data/historical/macro/XAUUSD/daily.csv` — ticker Stooq `xauusd` (Gold spot vs USD, daily close)
    - `data/historical/macro/WTI/daily.csv` — ticker Stooq `cl.f` (Crude Oil WTI continuous front-month)
    URL pattern: `https://stooq.com/q/d/?s={ticker}&i=d` (HTML page) -> "Download CSV" -> CSV format Stooq di default è US (`Date,Open,High,Low,Close,Volume` comma-separated). 

    Step 3 — NORMALIZZAZIONE CSV operator (CHECKPOINT 2): Stooq default CSV è formato US (Date,Open,High,Low,Close,Volume comma-separated, YYYY-MM-DD, dot decimal). Trasformare ognuno a BACK-01 semicolon Italian-format con header `Data;Ora;Open;High;Low;Close;Volume` (riusabile dal loader BACK-01 pattern); `Data` = data sorgente in DD/MM/YYYY, `Ora` = `00:00:00`. Pandas one-liner accettabile:
    ```python
    import pandas as pd
    df = pd.read_csv("us10y_stooq_raw.csv")  # Date,Open,High,Low,Close,Volume
    df["Data"] = pd.to_datetime(df["Date"]).dt.strftime("%d/%m/%Y")
    df["Ora"] = "00:00:00"
    out = df[["Data","Ora","Open","High","Low","Close","Volume"]]
    out.to_csv("data/historical/macro/US10Y/daily.csv", sep=";", index=False, decimal=".")
    ```
    NOTA: tieni `Open/High/Low/Volume` anche se loader Phase 10 leggerà solo `Close` — il loader Phase 10 drop-coppe le colonne extra (parity BACK-01).

    Step 4 — Validazione operator-driven post-normalize (manual check, vincoli OBBLIGATORI):
    - Row count >= 5000 per ognuno dei 4 file (23.6y daily ~ 6000 bar; aborti se < 5000 = CAPTCHA fallback).
    - Sample 3 close values vs pubblico (DXY ~99-115 range storico, US10Y yield idx ~0.5-50 range, XAUUSD ~250-2500, WTI ~10-130).
    - Header esattamente `Data;Ora;Open;High;Low;Close;Volume` (planner-NB: ammesso anche subset `Data;Ora;Close` se Stooq layout differs ma loader Phase 10 deve accettare; default produrre 7-col).

    Step 5 — Scrivi `data/historical/macro/metadata.json` (sha256 anchor + provenance, format diretto):
    ```json
    {
      "phase": 10,
      "decision": "D-10-A1, D-10-A4",
      "downloaded_at": "<ISO8601 UTC del download manuale>",
      "stooq_tickers": {
        "DXY": "^dxy",
        "US10Y": "^tnx",
        "XAUUSD": "xauusd",
        "WTI": "cl.f"
      },
      "files": {
        "DXY": {"path": "data/historical/macro/DXY/daily.csv", "sha256": "<calcolato via hashlib.sha256().hexdigest()>", "row_count": <int>, "date_range": "2002-10-21..YYYY-MM-DD"},
        "US10Y": {...},
        "XAUUSD": {...},
        "WTI": {...}
      },
      "notes_italiano": "Refresh manuale via scripts/refresh_macro_csv.py (Plan 10-05). Phase 7 retrain abort se sha256 cambia senza intent operator."
    }
    ```
    Genera sha256 ognuno con `python -c "import hashlib,pathlib; print(hashlib.sha256(pathlib.Path('data/historical/macro/DXY/daily.csv').read_bytes()).hexdigest())"` (riusa pattern verbatim da `backtest/baseline/determinism.py:35`).

    Step 6 — Patch `.gitignore` (additivo, append una sola riga se assente):
    Aggiungere riga `data/cache/` (gitignored: Phase 10 cache MCP-13 NON va in repo, persiste cross-run su filesystem locale). Verifica con `grep -F "data/cache/" .gitignore` prima di append.

    Tutti i commenti/log: italiano (CLAUDE.md convention). 

    NOTA su operator workflow: se l'operatore corre il plan in Codespace senza accesso a browser locale, può proseguire creando FIXTURE-ONLY (skip Step 2-5 per i 3 simboli mancanti, scrivendo metadata.json con `files[<sym>].sha256 = "MANUAL_DOWNLOAD_PENDING"`). I test Phase 10 saranno skipif-gated finché i 3 CSV mancano (vedi Task 5 marker `requires_macro_csv`). Phase 10 può proseguire Wave 1-3 con DXY-only se l'operatore documenta in metadata.json. Phase Verification finale richiederà i 4 CSV completi.
  </action>
  <verify>
    <automated>test -f data/historical/macro/DXY/daily.csv && test -f data/historical/macro/metadata.json && grep -q "data/cache/" .gitignore && python -c "import json; m=json.load(open('data/historical/macro/metadata.json')); assert 'DXY' in m['files'], 'metadata.json missing DXY'; print('OK')"</automated>
  </verify>
  <done>
    - data/historical/macro/DXY/daily.csv esiste in formato BACK-01 (sha256 anchored in metadata.json)
    - data/historical/macro/{US10Y,XAUUSD,WTI}/daily.csv presenti OPPURE metadata.json sha256 marked "MANUAL_DOWNLOAD_PENDING" (operator path Codespace-only)
    - data/historical/macro/metadata.json esiste con `phase: 10`, sha256 per ogni file presente, stooq_tickers locked
    - .gitignore include `data/cache/`
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Bundle dataset macro Stooq committato (Task 1 step 1-5).</what-built>
  <how-to-verify>
    1. Verificare che `data/historical/macro/DXY/daily.csv` esista (riuso 1Dyapt2.csv).
    2. Per US10Y / XAUUSD / WTI: confermare via filesystem (`ls -la data/historical/macro/{US10Y,XAUUSD,WTI}/daily.csv`) OPPURE confermare deferred-status in `metadata.json` (`grep "MANUAL_DOWNLOAD_PENDING" data/historical/macro/metadata.json`).
    3. Se 1+ simboli pending: documentare in CONTEXT update STATE.md "Phase 10 macro CSV: DXY only, restanti 3 deferred to operator manual step. Test gated via skipif requires_macro_csv".
    4. Sample-check 3 close values nei CSV scaricati vs Yahoo/Investing pubblico (DXY ~104 oggi, XAUUSD ~2350 oggi, US10Y yield ~43 oggi sui ^tnx, WTI ~78 oggi). Se sample differisce >5% → ticker SBAGLIATO (probabile CAPTCHA HTML page) → riscarica.
  </how-to-verify>
  <resume-signal>Type "approved" se DXY presente + restanti 3 OK (presenti o pending documentati). Type "blocked: missing X" se non riesci a scaricare.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fixture FF RSS smoke + DST cross fixture</name>
  <files>
    tests/fixtures/ff_rss_smoke.xml
    tests/fixtures/ff_rss_dst_cross.xml
    tests/fixtures/macro_dxy_smoke.csv
  </files>
  <read_first>
    - tests/test_news_aggregator.py (mock helper _fake_parsed pattern)
    - .planning/phases/10-intermarket-news/10-RESEARCH.md §Pitfall 3 (DST cross-year), §Pitfall 4 (FF custom tags entry.get), §Pattern 2 (calendar_rss/client.py with _parse_event)
    - news_aggregator.py:56-68 (socket timeout + bozo guard)
    - data/historical/macro/DXY/daily.csv (per primi 200 row excerpt -> fixture smoke)
  </read_first>
  <action>
    File 1: `tests/fixtures/ff_rss_smoke.xml` — costruisci 10 eventi FF live-schema. Schema verificato da RESEARCH §Pattern 2 + WebFetch live 2026-05-13. Format:
    ```xml
    <?xml version="1.0" encoding="utf-8"?>
    <weeklyevents>
      <event>
        <title>Core CPI m/m</title>
        <country>USD</country>
        <date>05-13-2026</date>
        <time>12:30pm</time>
        <impact>High</impact>
        <forecast>0.3%</forecast>
        <previous>0.4%</previous>
        <url>https://www.forexfactory.com/calendar?day=may13.2026</url>
      </event>
      <!-- 9 altri eventi: mix USD/EUR/GBP/JPY/AUD, mix impact High/Medium/Low/Holiday/Tentative, almeno 1 con time="All Day" e impact="Holiday" (D-10-B6 holiday), 1 con impact="Tentative" (skip target), 1 con impact="Medium" + time="All Day" (skip target non-holiday), 1 BoE Bank Rate GBP High EDT, 1 BoJ rate JPY Tokyo NY-time, 1 ECB rate EUR High morning -->
    </weeklyevents>
    ```
    Tutti i `<date>` formato `MM-DD-YYYY`. Tutti i `<time>` formato `H:MMam/pm` (lowercase am/pm) OR `All Day`. Mix temporal range: 5 eventi nel summer (EDT, May-Sep), 5 eventi nel winter (EST, Nov-Feb) per coverage parser ET->UTC.

    File 2: `tests/fixtures/ff_rss_dst_cross.xml` — variante con eventi al DST boundary. Includi:
    - 1 evento `03-08-2026 1:30am` (pre-DST, prima del salto - EST = -05:00, UTC=06:30)
    - 1 evento `03-08-2026 3:30am` (post-DST, dopo il salto - EDT = -04:00, UTC=07:30)
    - 1 evento `03-08-2026 2:30am` (skip-hour imaginary time DST start - planner espone come bug-trap: parser deve detectare e skippare + warning, vedi Pitfall 3)
    - 1 evento `11-01-2026 1:30am` (ambiguous fall-back 1:30am exists twice — fold=0 EDT vs fold=1 EST per zoneinfo default — Warning #7 risolto: comportamento atteso = fold=0 EDT pre-switch, UTC = 05:30 (1:30 EDT + 4h offset))
    - 1 evento `11-01-2026 3:30am` (post DST end, EST = -05:00, UTC=08:30)
    Idem schema FF verbatim, impact High USD per testabilità.

    File 3: `tests/fixtures/macro_dxy_smoke.csv` — 200 row deterministic da DXY/daily.csv (file 1 di Task 1). Estrai prime 200 righe (dopo header) da `data/historical/macro/DXY/daily.csv`:
    ```bash
    head -1 data/historical/macro/DXY/daily.csv > tests/fixtures/macro_dxy_smoke.csv
    tail -n +2 data/historical/macro/DXY/daily.csv | head -200 >> tests/fixtures/macro_dxy_smoke.csv
    ```
    Output deve essere 201 righe totali (1 header + 200 data row), format BACK-01 (`Data;Ora;Open;High;Low;Close;Volume`).

    Tutti i commenti italiani.
  </action>
  <verify>
    <automated>test -f tests/fixtures/ff_rss_smoke.xml && test -f tests/fixtures/ff_rss_dst_cross.xml && test -f tests/fixtures/macro_dxy_smoke.csv && python -c "import feedparser; p=feedparser.parse('tests/fixtures/ff_rss_smoke.xml'); assert len(p.entries) >= 10, f'expected >=10 events, got {len(p.entries)}'; e=p.entries[0]; assert all(e.get(k) for k in ('title','country','date','time','impact')), f'missing FF tag in entry[0]: {dict(e)}'; print('OK')" && wc -l tests/fixtures/macro_dxy_smoke.csv | grep -q "^ *201 "</automated>
  </verify>
  <done>
    - ff_rss_smoke.xml: 10+ eventi, feedparser parsa con tutti FF tag visibili (title/country/date/time/impact)
    - ff_rss_dst_cross.xml: 5 eventi DST boundary (3 marzo + 2 novembre) con uno @ 2:30am imaginary
    - macro_dxy_smoke.csv: 201 righe (header + 200), BACK-01 semicolon
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Test stub MCP-10 + MCP-13 + envelope (4 file)</name>
  <files>
    tests/test_intermarket_loader.py
    tests/test_intermarket_score.py
    tests/test_calendar_rss_client.py
    tests/test_mcp_handlers_macro.py
  </files>
  <read_first>
    - tests/test_baseline_determinism.py (sha256 test pattern + xfail strict gate)
    - tests/test_baseline_dataset_writer.py (schema keys + xfail markers)
    - tests/test_news_aggregator.py (_make_cfg, _fake_parsed helpers, mock feedparser pattern verbatim)
    - tests/test_mcp_handlers_market.py (handler envelope test pattern)
    - .planning/phases/10-intermarket-news/10-VALIDATION.md §Per-Task Verification Map (18 row, source of truth per test names — post Blocker #1 revision: granular split + umbrella aliases per 4 canonical names)
    - .planning/phases/10-intermarket-news/10-RESEARCH.md §Validation Architecture (test naming)
    - mcp/errors.py:52 (envelope signature reale: `envelope(error_code, message, **context) -> dict`)
  </read_first>
  <behavior>
    Ogni test stub deve:
    - Codificare la signature attesa (Wave 1 GREEN deterministic)
    - Failure mode `xfail(strict=True, reason="<10-XX> Wave 1 RED->GREEN")` PERMETTE che Wave 1+ flippi a PASS senza touch ai test
    - Stub-body usa `pytest.importorskip("intermarket.loader")` o `pytest.importorskip("calendar_rss.client")` per skip soft se Wave 1 non ancora eseguito
    - Test ID nomi ESATTI conformi a 10-VALIDATION.md (es. `test_close_at_strict_less_than`, `test_dst_boundary_march_november`)
    - Italiani docstring/commenti.

    NOMI CANONICI 10-VALIDATION.md — coverage tracking (Blocker #1):
    - `test_skip_tentative_all_day` (canonical, calendar) — umbrella che chiama internamente sia il check Tentative-skip sia il check All-Day-non-Holiday-skip (vedi action sotto).
    - `test_envelope_ok` (canonical, macro handlers) — alias di `test_economic_calendar_envelope_ok_success` (granular).
    - `test_envelope_validation_failed` (canonical, macro handlers) — alias di `test_economic_calendar_envelope_validation_failed_window_negative` (granular).
    - `test_baseline_immutato_with_flag_off` (canonical, drift gate) — vedi Task 5: vive in `test_phase10_no_phase5_drift.py` come umbrella che asserisce schema-v2 immutato + parquet sha256 anchor pinned con `ENABLE_INTERMARKET=false` (default).
  </behavior>
  <action>
    File 1 — `tests/test_intermarket_loader.py`:
    Test stub (xfail strict=True con reason "Plan 10-02 Wave 1 RED->GREEN"):
    1. `test_csv_read_smoke` — `MacroLoader._read_csv(tests/fixtures/macro_dxy_smoke.csv)` ritorna DataFrame con `ts_utc` + `close` columns, 200 row.
    2. `test_close_at_strict_less_than` — fixture mock con bar @ exact `ts_utc=datetime(2010,1,1,tzinfo=utc)`; `close_at("DXY", datetime(2010,1,1,tzinfo=utc))` ritorna bar `idx-1` (NON quello @ 2010-01-01).
    3. `test_sha256_anchor_stable` — `MacroLoader.from_repo(root=tmp_path)` con 2 file CSV creati uguali; chiamato twice, `loader.sha256("DXY")` stesso valore.
    4. `test_sha256_mismatch_detected` — create CSV, take sha256, modify CSV (1 byte), rebuild loader, asserisci sha256 cambiato.
    5. `test_missing_symbol_returns_none` — `loader.close_at("XYZ", now_utc)` -> None.
    6. `test_missing_csv_raises` — `MacroLoader.from_repo(root=Path("/nonexistent"))` -> FileNotFoundError o equivalente.

    Helper modular `_make_macro_csv_tmp(tmp_path, symbol, rows: list[tuple[date, close]])` riusabile.

    File 2 — `tests/test_intermarket_score.py`:
    Stub xfail "Plan 10-02 Wave 1":
    1. `test_direction_sign_flip` — mock loader con `close_at(symbol, ts)` ritornante valori deterministici (DXY -1%, US10Y +0.5%, etc.); `build_intermarket_score(mock_loader)("EURUSD", "long")` ritorna float positivo, `("EURUSD", "short")` ritorna esatto NEGATO.
    2. `test_known_regime_2020_covid` — fixture: closes DXY su 2020-03-19 (peak USD) +5%, US10Y -2%, XAUUSD +3%, WTI -25%. `_score("EURUSD", "long")` < 0 (risk-off non confirms EURUSD long). Test usa `_PAIR_WEIGHTS` Pattern 3 (EURUSD: DXY=-0.5, US10Y=+0.2, XAUUSD=-0.2, WTI=+0.1). Output atteso firmato <0.
    3. `test_known_regime_2022_q1_fomc` — fixture: closes DXY +2%, US10Y +5%, XAUUSD -1%, WTI +3% (hawkish Fed Q1 2022). `_score("USDJPY", "long")` > 0 (USD strong + risk-on = USDJPY long confirms).
    4. `test_no_data_returns_zero` — mock loader.close_at ritorna None per tutti i simboli. `_score("EURUSD", "long")` == 0.0 (neutral safe fallback).
    5. `test_unmapped_symbol_returns_zero` — `_score("CHFGBP", "long")` == 0.0 (symbol NON in _PAIR_WEIGHTS).
    6. `test_signature_backward_compat` — Pitfall 5: stub di tipo `lambda s: 0.0` (1-arg legacy) chiamato dal nuovo callsite via `try/except` deve passare silently. Codifica esplicita del comportamento backward-compat.

    File 3 — `tests/test_calendar_rss_client.py`:
    Stub xfail "Plan 10-03 Wave 1". Import `from calendar_rss.client import CalendarRSSClient, CalendarEvent` (pytest.importorskip per skip soft). Helper `_make_cfg(**overrides)` (analog test_news_aggregator).
    1. `test_parse_event_smoke` — load `tests/fixtures/ff_rss_smoke.xml`, `client._fetch_remote()` via mock feedparser ritorna lista 10 CalendarEvent con tutti campi pieni.
    2. `test_et_to_utc_edt_summer` — evento `05-12-2026 12:30pm` ET -> `2026-05-12 16:30:00+00:00` UTC.
    3. `test_et_to_utc_est_winter` — evento `01-15-2026 12:30pm` ET -> `2026-01-15 17:30:00+00:00` UTC.
    4. `test_dst_boundary_march_november` — load `ff_rss_dst_cross.xml`, asserisci:
        - evento `03-08-2026 1:30am` -> UTC 06:30 (EST pre-switch, -05:00)
        - evento `03-08-2026 3:30am` -> UTC 07:30 (EDT post-switch, -04:00)
        - evento `03-08-2026 2:30am` -> None (imaginary skip + warning loggato)
        - **evento `11-01-2026 1:30am` -> UTC 05:30 (Warning #7 risolto: ambiguous fall-back default `fold=0` zoneinfo = EDT pre-switch; assertion verbatim `assert event.event_time_utc == datetime(2026, 11, 1, 5, 30, tzinfo=timezone.utc)`)**
        - evento `11-01-2026 3:30am` -> UTC 08:30 (EST post-switch, -05:00)
    5. `test_holiday_all_day_flag` — evento `Christmas Day` impact=Holiday time="All Day" -> CalendarEvent con `all_day=True`, `event_time_utc=datetime(date, 0, 0, tzinfo=utc)` (nominal 00:00).
    6. `test_skip_tentative_granular` — evento impact="Tentative" -> None (filtrato out, granular split).
    7. `test_skip_all_day_non_holiday_granular` — evento impact="Medium" time="All Day" -> None (D-10-B6, granular split).
    8. `test_skip_tentative_all_day` — **UMBRELLA canonical 10-VALIDATION.md (Blocker #1 alias)**: chiama internamente entrambi i check Tentative-skip + All-Day-non-Holiday-skip su un fixture combo (un evento Tentative + un evento Medium All Day + assert entrambi None post-parse). Body:
        ```python
        def test_skip_tentative_all_day(tmp_path):
            """Umbrella canonical 10-VALIDATION.md MCP-13.
            Alias di test_skip_tentative_granular + test_skip_all_day_non_holiday_granular.
            D-10-B6: Tentative + All Day non-holiday = entrambi None.
            """
            client = CalendarRSSClient(_make_cfg())
            ev_tent = client._parse_event({"title":"X","country":"USD","date":"05-13-2026","time":"12:30pm","impact":"Tentative"})
            ev_allday = client._parse_event({"title":"Y","country":"USD","date":"05-13-2026","time":"All Day","impact":"Medium"})
            assert ev_tent is None
            assert ev_allday is None
        ```
    9. `test_cache_ttl_60min` — mock now=ts0, populate cache, mock now=ts0+59min -> `fetch_events()` NON chiama feedparser; mock now=ts0+61min -> SI chiama feedparser.
    10. `test_cache_roundtrip` — save + load preserva CalendarEvent uguali (Pitfall 6 datetime serialization isoformat).
    11. `test_fetch_remote_failure_fallback` — mock feedparser.parse raise; cache esistente -> ritorna cached events + warning loggato.
    12. `test_filter_window_pair_aware_USD_only` — `currencies=["USD","EUR"]` filtro -> escluso GBP/JPY events.
    13. `test_filter_min_impact_high_default` — eventi Low/Medium filtrati out di default (`min_impact='High'`).
    14. `test_filter_window_half_open_interval` — eventi `event_time_utc == cutoff_end` ESCLUSI (half-open).
    15. `test_socket_timeout_restored` — verifica `socket.getdefaulttimeout()` post-fetch == valore pre-fetch (Pitfall 7).

    File 4 — `tests/test_mcp_handlers_macro.py`:
    Stub xfail "Plan 10-04 Wave 2". Import `from mcp_tools.handlers.macro import handle_get_intermarket_context, handle_get_economic_calendar`. Helper `_make_cfg(enable_intermarket=False)` (MagicMock-based).
    1. `test_get_intermarket_context_envelope_disabled` — `cfg.ENABLE_INTERMARKET=False` -> ritorna dict success-style `{"intermarket_active": False, "signals": {}}` (NON envelope error). Verifica: `result["intermarket_active"] is False`.
    2. `test_get_intermarket_context_envelope_enabled` — cfg.ENABLE_INTERMARKET=True, mock loader. Ritorna dict con keys `usd_strength_bias`, `risk_on_off`, `jpy_safe_haven_flag`, `as_of_utc`, `signals`.
    3. `test_get_intermarket_context_loader_none_returns_internal_error` — loader=None -> ritorna envelope error `{ok: False, error: "internal_error", message: "..."}` (signature verbatim `envelope(ErrorCodes.INTERNAL_ERROR, ...)`).
    4. `test_economic_calendar_pair_filter` — args={"pair": "EURUSD"} -> ritorna eventi con country in `["USD","EUR"]` solo.
    5. `test_economic_calendar_window_minutes_override` — args={"window_minutes": 60}; client.filter_window chiamato con window_minutes=60.
    6. `test_economic_calendar_envelope_ok_success` — verifica dict response NON wrapped (signature mcp_tools/errors.py:52: success path = dict diretto, NON envelope-wrapped). **Granular nome — coperto anche dall'umbrella canonical 10-VALIDATION.md `test_envelope_ok`.**
    7. `test_economic_calendar_envelope_validation_failed_window_negative` — args={"window_minutes": -1} -> envelope error `validation_failed`. **Granular — coperto anche dall'umbrella canonical 10-VALIDATION.md `test_envelope_validation_failed`.**

    **Umbrella canonical alias (Blocker #1 — soluzione Option A per `test_envelope_ok` / `test_envelope_validation_failed`):**
    L'alignment vivo è in `10-VALIDATION.md` (modificato in revisione): la riga canonical "MCP envelope shape success" punta direttamente a `pytest tests/test_mcp_handlers_macro.py::test_economic_calendar_envelope_ok_success -x` (alias documentale, niente test wrapper Python serve perché VALIDATION.md è source-of-truth e ora rispecchia il nome granular). Stessa cosa per `test_envelope_validation_failed` -> `test_economic_calendar_envelope_validation_failed_window_negative`.

    Importante: tutti i test usano `pytest.importorskip("module.path", reason="Wave N RED->GREEN")` come prima riga del modulo per fall-back gentile se Wave 1+ non eseguito.

    Italiani docstring sempre.
  </action>
  <verify>
    <automated>pytest tests/test_intermarket_loader.py tests/test_intermarket_score.py tests/test_calendar_rss_client.py tests/test_mcp_handlers_macro.py --collect-only -q 2>&1 | tail -5 && pytest tests/test_intermarket_loader.py tests/test_intermarket_score.py tests/test_calendar_rss_client.py tests/test_mcp_handlers_macro.py -x --tb=short 2>&1 | grep -E "xfailed|skipped|passed|failed" | tail -3 && pytest tests/test_calendar_rss_client.py -k "test_skip_tentative_all_day" --collect-only -q 2>&1 | grep -q "test_skip_tentative_all_day"</automated>
  </verify>
  <done>
    - 4 test file esistono, pytest collect mostra 6 + 6 + 15 + 7 = 34 test stub (15 calendar dopo split granular + umbrella canonical)
    - Tutti i test xfail-strict OR skipped (NO failure, NO error)
    - Test names match esatti 10-VALIDATION.md Per-Task Verification Map (post Blocker #1 alignment)
    - Umbrella canonical `test_skip_tentative_all_day` esiste in test_calendar_rss_client.py (Blocker #1 Option B)
    - Helper modulari riusabili (_make_macro_csv_tmp, _make_cfg, _fake_parsed analog news_aggregator)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Phase 5 immutability gate test (D-10-C0 enforcement)</name>
  <files>
    tests/test_phase10_no_phase5_drift.py
  </files>
  <read_first>
    - backtest/baseline/dataset_writer.py (_SCHEMA_V2_REQUIRED_KEYS frozenset, _build_required_keys_v2)
    - backtest/baseline/determinism.py (file_sha256)
    - data/training/baseline_decisions/part-0.parquet (target del gate sha256)
    - .planning/research/baseline-2026-05-12.md (header: 27/27 runs, 1076 rows certified)
    - .planning/phases/10-intermarket-news/10-RESEARCH.md §Pitfall 8 (Phase 5 drift)
    - .planning/phases/10-intermarket-news/10-CONTEXT.md D-10-C0 SCOPE OVERRIDE
    - .planning/phases/10-intermarket-news/10-VALIDATION.md (riga canonical `test_baseline_immutato_with_flag_off` — D-10-D2 marquee gate)
  </read_first>
  <action>
    Crea `tests/test_phase10_no_phase5_drift.py`. NON xfail — questo test deve PASSARE GREEN immediatamente (gate attivo dal commit-1 Phase 10).

    Test 1 — `test_dataset_writer_schema_v2_immutato`:
    ```python
    def test_dataset_writer_schema_v2_immutato():
        """D-10-C0 IMMUTATO. Schema Phase 5 1076x59 cols deve restare identico
        per tutta Phase 10. Nessun campo intermarket/macro/news puo' apparire."""
        from backtest.baseline.dataset_writer import _SCHEMA_V2_REQUIRED_KEYS
        keys = _SCHEMA_V2_REQUIRED_KEYS  # frozenset[str]
        # Forbidden tokens (Phase 10 dataset extensions vietate)
        FORBIDDEN_SUBSTRINGS = ("intermarket", "macro", "news_blackout", "calendar_event", "dxy_close", "us10y_yield", "xauusd_close", "wti_close")
        for k in keys:
            for token in FORBIDDEN_SUBSTRINGS:
                assert token not in k.lower(), f"D-10-C0 VIOLATION: schema key '{k}' contiene token vietato '{token}'"
        # Phase 5 ufficiale: 59 cols (baseline-2026-05-12.md, plan 05-09 SUMMARY)
        assert len(keys) == 59, f"D-10-C0 VIOLATION: _SCHEMA_V2_REQUIRED_KEYS conta {len(keys)}, atteso 59 (Phase 5 schema-v2 IMMUTATO)"
    ```

    Test 2 — `test_build_required_keys_v2_signature_immutato`:
    ```python
    def test_build_required_keys_v2_signature_immutato():
        """Plan 05-09 FIX 3: la funzione _build_required_keys_v2() e' sourceof-truth
        del frozenset programmatic; Phase 10 NON deve toccarla."""
        import inspect
        from backtest.baseline import dataset_writer
        assert hasattr(dataset_writer, "_build_required_keys_v2"), "_build_required_keys_v2 mancante (Plan 05-09 FIX 3 broken)"
        src = inspect.getsource(dataset_writer._build_required_keys_v2)
        FORBIDDEN_TOKENS = ("intermarket", "macro_", "news_blackout", "calendar")
        for token in FORBIDDEN_TOKENS:
            assert token not in src.lower(), f"D-10-C0 VIOLATION: _build_required_keys_v2 menziona '{token}'"
    ```

    Test 3 — `test_baseline_parquet_sha256_anchor`:
    ```python
    @pytest.mark.skipif(
        not Path("data/training/baseline_decisions/part-0.parquet").exists(),
        reason="parquet baseline non presente in Codespace (PC secondario only)",
    )
    def test_baseline_parquet_sha256_anchor():
        """D-10-C0 sha256 anchor: il parquet 1076x59 generato Plan 05-09
        (commit 0410bf2 PC secondario) deve restare byte-identical durante Phase 10.

        Pin strategy: alla prima esecuzione, lo sha256 va catturato e committato in
        un file separato tests/fixtures/phase5_baseline_sha256.txt; runs successive
        verificano match. Plan 10-02 Task 1 GREEN-flip dovra committare il valore reale.
        """
        from backtest.baseline.determinism import file_sha256
        parquet_path = Path("data/training/baseline_decisions/part-0.parquet")
        anchor_path = Path("tests/fixtures/phase5_baseline_sha256.txt")
        actual = file_sha256(parquet_path)
        if not anchor_path.exists():
            anchor_path.parent.mkdir(parents=True, exist_ok=True)
            anchor_path.write_text(actual, encoding="utf-8")
            pytest.skip(f"D-10-C0 anchor primo run: pinned sha256={actual[:16]}... a {anchor_path}. Run successivi devono matchare.")
        expected = anchor_path.read_text(encoding="utf-8").strip()
        assert actual == expected, (
            f"D-10-C0 VIOLATION: parquet baseline sha256 changed!\n"
            f"  expected (pin): {expected}\n"
            f"  actual:         {actual}\n"
            f"  Phase 5 baseline 1076x59 IMMUTATO per D-10-C0 — rimuovere modifiche al dataset_writer."
        )
    ```

    Test 4 — `test_no_phase10_imports_in_dataset_writer`:
    ```python
    def test_no_phase10_imports_in_dataset_writer():
        """D-10-C0 architectural: dataset_writer.py NON deve importare intermarket/ o calendar_rss/."""
        import inspect
        from backtest.baseline import dataset_writer
        src = inspect.getsource(dataset_writer)
        FORBIDDEN_IMPORTS = ("from intermarket", "import intermarket", "from calendar_rss", "import calendar_rss")
        for token in FORBIDDEN_IMPORTS:
            assert token not in src, f"D-10-C0 VIOLATION: dataset_writer.py contiene '{token}'"
    ```

    Test 5 — **`test_baseline_immutato_with_flag_off`** (Blocker #1 umbrella canonical 10-VALIDATION.md, Option B):
    ```python
    import os

    def test_baseline_immutato_with_flag_off(monkeypatch):
        """D-10-C0 + D-10-D2 marquee gate (canonical 10-VALIDATION.md MCP-10).

        Umbrella che asserisce l'integrazione: con `ENABLE_INTERMARKET=false` (default
        D-10-D2 zero-impact rollout), il baseline parquet Phase 5 1076x59 resta
        byte-identical (sha256 anchor preservato) E lo schema-v2 frozenset resta IMMUTATO.

        Chiama internamente:
          - test_dataset_writer_schema_v2_immutato (asserzione schema)
          - test_baseline_parquet_sha256_anchor (skipif se parquet assente: pin/compare)
        + verifica esplicita os.environ.get('ENABLE_INTERMARKET', 'false').lower() == 'false'
          (default config Phase 10).
        """
        # Forza il default D-10-D2 (env potrebbe essere true su PC dev — assertion testa il default)
        monkeypatch.delenv("ENABLE_INTERMARKET", raising=False)
        flag = os.environ.get("ENABLE_INTERMARKET", "false").lower()
        assert flag == "false", (
            f"D-10-D2 default violato: ENABLE_INTERMARKET={flag!r}, atteso 'false'. "
            "Verifica config.py _attach_intermarket_macro(Config) e .env."
        )

        # Reuse: schema-v2 IMMUTATO
        test_dataset_writer_schema_v2_immutato()

        # Reuse: parquet sha256 anchor (skipif gestito dal test referenziato)
        test_baseline_parquet_sha256_anchor()
    ```

    Imports header:
    ```python
    """Phase 10 D-10-C0 immutability gate: Phase 5 parquet schema-v2 IMMUTATO.

    Riferimento: .planning/phases/10-intermarket-news/10-CONTEXT.md D-10-C0 SCOPE OVERRIDE.
    Motivazione: storico include festivita/news naturalmente. Strategy/ML implicitly
    learns from real P&L distribution. NESSUNA migrazione schema, NESSUNA aggiunta
    di feature macro/intermarket/news_blackout al parquet Phase 5.

    Test canonical (10-VALIDATION.md): test_baseline_immutato_with_flag_off umbrella
    marquee D-10-D2 — verifica integrazione default-OFF + schema immutato + sha256 pin.
    """
    from pathlib import Path
    import pytest
    ```

    Tutti i commenti/docstring in italiano. NO xfail/skip permanenti — questi test sono GATE PASSING.
    NB: il Test 3 può skippare (parquet non in Codespace). Il Test 5 umbrella RI-USA Test 3 quindi può skippare lì (propagazione pytest.skip da chiamata diretta a funzione test). Risultato verde aspettato in Codespace = passed + skipped (NESSUN fail).
  </action>
  <verify>
    <automated>pytest tests/test_phase10_no_phase5_drift.py -x --tb=short -v 2>&1 | tail -20 && pytest tests/test_phase10_no_phase5_drift.py -k "test_baseline_immutato_with_flag_off" --collect-only -q 2>&1 | grep -q "test_baseline_immutato_with_flag_off"</automated>
  </verify>
  <done>
    - tests/test_phase10_no_phase5_drift.py esiste
    - 5 test: schema_v2_immutato, build_required_keys_v2_signature, parquet_sha256_anchor (skipif if file absent), no_phase10_imports_in_dataset_writer, **test_baseline_immutato_with_flag_off (Blocker #1 umbrella canonical D-10-D2 marquee gate)**
    - Tutti i test PASS verdi (NO xfail) — il gate e' ATTIVO da Wave 0
    - Test 5 umbrella canonical raggiungibile via `pytest -k test_baseline_immutato_with_flag_off`
    - Anchor sha256 strategy documentata (primo run write fixture, successivi run compare)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Wave 0 sanity check + STATE.md update</name>
  <files>
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/STATE.md (sezione Active Work + Recent Decisions)
  </read_first>
  <action>
    Run full Wave 0 verification:
    ```bash
    pytest tests/test_intermarket_loader.py tests/test_intermarket_score.py tests/test_calendar_rss_client.py tests/test_mcp_handlers_macro.py tests/test_phase10_no_phase5_drift.py --tb=short -v
    ```
    Atteso: ~34 xfail + ~5 passed (gate, incluso umbrella test_baseline_immutato_with_flag_off). Zero error/failure.

    Aggiorna `.planning/STATE.md`:
    - Tabella "Milestone Status" — Phase 10 status: `🟡 wave-0-complete` (era `🟡 context-gathered`), progress `1/6 plans (~17%)` (post Wave 2 split 10-04 → 10-04a + 10-04b).
    - "Active Work" — sostituisci il blocco Phase 10 con:
      > **Phase 10 — Plan 10-01 Wave 0 scaffolding COMPLETE 2026-05-13.** 4 test stub (34 xfail incluso umbrella test_skip_tentative_all_day) + fixture FF RSS smoke + DST cross + macro DXY smoke + D-10-C0 immutability gate (5 test GREEN incluso umbrella canonical test_baseline_immutato_with_flag_off D-10-D2 marquee gate). Macro CSV bundle: DXY committato (riuso 1Dyapt2.csv 3084 row BACK-01), US10Y/XAUUSD/WTI {ASSEGNATO_OPERATOR_STATUS}. metadata.json sha256 anchor scritto. .gitignore patched per data/cache/. Prossimo: 10-02 (intermarket/ package) e 10-03 (calendar_rss/) in parallel Wave 1.
    - Last-updated footer line.
  </action>
  <verify>
    <automated>pytest tests/test_intermarket_loader.py tests/test_intermarket_score.py tests/test_calendar_rss_client.py tests/test_mcp_handlers_macro.py tests/test_phase10_no_phase5_drift.py -x --tb=short 2>&1 | grep -E "passed|xfailed|skipped|failed|error" | tail -3 && grep -q "Wave 0 scaffolding COMPLETE" .planning/STATE.md</automated>
  </verify>
  <done>
    - Suite Phase 10 quick (5 file): zero error/failure, solo xfail+skip+passed
    - STATE.md Active Work blocco Phase 10 aggiornato con Wave 0 complete + next-step pointer
    - Phase 10 status in tabella Milestone bumped a wave-0-complete 20%
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator->Stooq HTTPS | Manual CSV download via browser, CAPTCHA-gated per Pitfall 1 (Stooq disabilito batch automatic Dec 2020) |
| filesystem->git commit (data/historical/macro/) | CSV statici committati a repo, integrità via sha256 anchor in metadata.json |
| operator->test fixtures (tests/fixtures/) | Fixture deterministiche checked-in, NON regenerate at test-time |
| Phase 10 code->Phase 5 parquet | D-10-C0 IMMUTATO gate: AST/grep enforcement preserves baseline-2026-05-12 certification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | data/historical/macro/{DXY,US10Y,XAUUSD,WTI}/daily.csv | mitigate | sha256 anchor in data/historical/macro/metadata.json al commit-time. Phase 7 retrain abort se sha256 cambia senza intent operator (D-10-A4). ASVS V6 partial (integrity-only). |
| T-10-01-02 | Information Disclosure / Spoofing | Stooq HTML CAPTCHA page invece di CSV | mitigate | Manual operator download via browser (Task 2 checkpoint). Validazione row count >= 5000 + sample close vs ICE/CME public. Refresh script Wave 3 (10-05) implementa anche HTML detection + `<!DOCTYPE html>` abort + sleep(30) rate-limit. ASVS V13 partial. |
| T-10-01-03 | Tampering / Data Integrity | Phase 5 parquet schema-v2 1076x59 (data/training/baseline_decisions/part-0.parquet) | mitigate | test_phase10_no_phase5_drift.py 5 test gate: schema_v2_immutato (frozenset count + forbidden tokens), _build_required_keys_v2_signature (AST), parquet_sha256_anchor (skipif + pin-on-first-run), no_phase10_imports_in_dataset_writer, **test_baseline_immutato_with_flag_off (umbrella canonical D-10-D2)**. ASVS V6. |
| T-10-01-04 | Future Leakage (T-10-FL phase-specific) | MacroLoader.close_at via fixture macro_dxy_smoke.csv | mitigate | Fixture costruita con bar antecedenti il test ts_utc. Wave 1 Plan 10-02 test_close_at_strict_less_than asserisce strict-<. |
| T-10-01-05 | Data Integrity | FF RSS fixture (tests/fixtures/ff_rss_smoke.xml) | mitigate | Fixture verbatim da live FF feed verificato 2026-05-13 (RESEARCH Pitfall 4). Schema canonical 8 tag. Decouple dal test ambiente CI (no live fetch in unit tests). ASVS V10 partial (input validation). |
| T-10-01-06 | DST Boundary Bug | ET->UTC parser via ff_rss_dst_cross.xml | mitigate | Fixture include eventi @ 1:30am/3:30am/2:30am DST start (Mar 2nd Sun) + 1:30am/3:30am DST end (Nov 1st Sun). Wave 1 Plan 10-03 test_dst_boundary_march_november codifica behavior atteso (incluso Warning #7: ambiguous fall-back fold=0 EDT pre-switch, 11-01 01:30am -> UTC 05:30). Phase 1 D-08/D-10 analog. ASVS V5. |
</threat_model>

<verification>
After all 6 tasks complete:

```bash
# Quick verification (target ~5s)
pytest tests/test_intermarket_loader.py tests/test_intermarket_score.py tests/test_calendar_rss_client.py tests/test_mcp_handlers_macro.py tests/test_phase10_no_phase5_drift.py -x --tb=short -v

# Atteso:
#   ~5 passed (test_phase10_no_phase5_drift PASS verde incluso umbrella canonical)
#   ~34 xfailed (gli stub Wave 1+ incluso umbrella canonical test_skip_tentative_all_day)
#   0 failed, 0 error

# Canonical 10-VALIDATION.md test names selectable:
pytest tests/test_phase10_no_phase5_drift.py -k test_baseline_immutato_with_flag_off -x --tb=short
pytest tests/test_calendar_rss_client.py -k test_skip_tentative_all_day --collect-only -q

# Full suite regression (NO regression Phase 1-9)
pytest -x --tb=short 2>&1 | tail -5
# Atteso: passed count >= baseline pre-Phase-10 (no regression).
```

Verifica file presence:
```bash
ls -la data/historical/macro/DXY/daily.csv data/historical/macro/metadata.json tests/fixtures/ff_rss_smoke.xml tests/fixtures/ff_rss_dst_cross.xml tests/fixtures/macro_dxy_smoke.csv
grep -q "data/cache/" .gitignore && echo "gitignore OK"
```
</verification>

<success_criteria>
- [x] 5 test file Phase 10 esistenti (4 stub xfail + 1 gate GREEN)
- [x] 3 test fixture creati (ff_rss_smoke.xml, ff_rss_dst_cross.xml, macro_dxy_smoke.csv)
- [x] 4 macro CSV committed in data/historical/macro/ (almeno DXY; restanti documentati pending se Codespace-only)
- [x] metadata.json sha256 anchor scritto
- [x] .gitignore patched per data/cache/
- [x] test_phase10_no_phase5_drift.py 5 test PASS GREEN da subito (D-10-C0 gate active + Blocker #1 umbrella canonical test_baseline_immutato_with_flag_off)
- [x] Umbrella canonical `test_skip_tentative_all_day` esiste in test_calendar_rss_client.py
- [x] Suite quick zero failure/error
- [x] Suite full zero regression vs pre-Phase-10
- [x] STATE.md aggiornato (wave-0-complete + next-step)
</success_criteria>

<output>
After completion, create `.planning/phases/10-intermarket-news/10-01-SUMMARY.md` con:
- Test stub coverage map (test name -> 10-VALIDATION.md row -> Wave dependency, incluso umbrella canonical aliases)
- Macro CSV status table (DXY/US10Y/XAUUSD/WTI: committed | pending | row_count | sha256 prefix)
- Fixture provenance (FF RSS verbatim source date)
- Phase 5 immutability gate sha256 pinned valore (se eseguito sul PC con parquet)
- Blocker #1 canonical alignment summary (test_baseline_immutato_with_flag_off Option B umbrella; test_skip_tentative_all_day Option B umbrella; test_envelope_ok / test_envelope_validation_failed Option A via VALIDATION.md alias)
- Next: Plan 10-02 + 10-03 parallel Wave 1
</output>
</output>
