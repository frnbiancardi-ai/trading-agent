---
phase: 05-baseline-backtest
plan: 08
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06a, 05-06b, 05-07]
files_modified:
  - .planning/research/baseline-2026-05-08.md
  - .planning/research/baseline-equity-curves/
  - data/training/baseline_decisions/
  - data/training/baseline_drafts/
  - logs/trades.db
autonomous: false   # 1 checkpoint:human-verify per qualitative PNG inspection
requirements: [BACK-07, INT-01]
tags: [phase-5, baseline-backtest, wave-4, smoke, deliverable, phase-gate]

phase1_dependency_blocker: |
  ⚠️ ESECUZIONE BLOCCATA fino a Phase 1-4 completion.

  REVISIONE 2026-05-08 (BLOCKER 6 user-locked decision): Phase 1 INCOMPLETE — l'intero
  progetto è in planning, NESSUN codice prodotto. Dependency chain critica:
  - Phase 1 plan 01-04: BacktestBroker + BrokerProtocol compliance + force_close
  - Phase 1 plan 01-05: BacktestEngine.run() + LedgerWriter funzionante
  - Phase 1 plan 01-07: BacktestMetrics.compute_metrics
  - Phase 1 plan 01-08: smoke fixture + cleanup
  - Phase 2 ALL plans: indicators.compute_all_extended (ExtendedIndicators API D-04)
  - Phase 3 ALL plans: detector A/B/C/D
  - Phase 4 ALL plans: strategy.evaluate_proposal_for_bar, build_ctx_backtest

  Pre-flight check obbligatorio (Task 1 sotto): invoca `scripts/preflight_phase5.py`
  (Plan 05-01 Task 4) che fail-fast con stderr message se anche un solo modulo è
  mancante o ha signature errata. Wave 4 ABORTA con exit ≠ 0 se preflight fallisce.

  Operator availability (INFO 16): il checkpoint human-verify Task 3 richiede ~5-10 min
  di operatore disponibile — pianificare l'esecuzione di Wave 4 in finestra dove
  l'operatore puo' rispondere entro 30 min.

must_haves:
  truths:
    - "scripts/preflight_phase5.py exit 0 PRIMA che lo smoke run parta (BLOCKER 6 hard gate)"
    - "scripts/run_baseline_backtest.py --force completa con exit 0; WALL_CLOCK_SECONDS stampato"
    - "WALL_CLOCK_SECONDS ≤ 1800 (30 min hard gate per BACK-07 SC#1) — OPPURE --no-time-gate set per debug"
    - "27 PNG file esistono in .planning/research/baseline-equity-curves/"
    - ".planning/research/baseline-2026-05-08.md esiste con tabella 27-row + appendix"
    - "data/training/baseline_decisions/ è directory con ≥1 part-*.parquet (BLOCKER 1 fix: directory layout)"
    - "data/training/baseline_drafts/ è directory con ≥1 part-*.parquet"
    - "decision_count ≥ 1000 (HARD gate); WARNING-but-pass se <10k (SOFT target SC#3 BLOCKER 2 fix)"
    - "Checkpoint human-verify: PNG sample qualitative OK (no flat-line, drawdown coerente)"
    - "git commit dei deliverable .planning/research/* + data/training/* (gitignore review)"
  artifacts:
    - path: ".planning/research/baseline-2026-05-08.md"
      provides: "INT-01 baseline metrics report"
      contains: "Slice Metrics"
      contains: "Appendix"
    - path: ".planning/research/baseline-equity-curves/EURUSD_M15_MODERATE.png"
      provides: "INT-01 equity curve sample (1 di 27)"
    - path: "data/training/baseline_decisions"
      provides: "INT-01 D-02 dataset for ML Phase 7 (directory layout)"
    - path: "data/training/baseline_drafts"
      provides: "INT-01 D-03 dataset for ML Phase 7 (directory layout)"
  key_links:
    - from: ".planning/research/baseline-2026-05-08.md"
      to: ".planning/REQUIREMENTS.md INT-01"
      via: "report committato chiude requirement"
    - from: "data/training/baseline_decisions/"
      to: "Phase 7 ML training pipeline"
      via: "parquet dataset directory consumato da feature extractor + LightGBM via pd.read_parquet(dir)"
---

<objective>
Wave 4 — esecuzione end-to-end + commit dei deliverable INT-01.

1. **Pre-flight check** Phase 1-4 completion (BLOCKER 6 fix — `scripts/preflight_phase5.py`).
2. **Smoke run reale**: `.venv\Scripts\python.exe scripts\run_baseline_backtest.py --force`
   sul dataset 23.5y completo. WARNING 10 fix: wall-clock automated check via
   `--max-wall-clock 1800`.
3. **Verifica deliverable**: 27 PNG + 1 report MD + 2 parquet **directory** (BLOCKER 1 fix)
   esistenti. SC#3 hard gate ≥1000 / soft target ≥10000 decisions (BLOCKER 2 fix).
4. **Checkpoint human-verify**: utente apre 2-3 PNG sample, conferma qualità (no flat-line,
   no negative equity, drawdown shaded coerente).
5. **Git commit** dei deliverable in `.planning/research/`. Per `data/training/` e
   `logs/trades.db`: review `.gitignore` — molto probabilmente già escluse (file grandi).
   Documentare in commit message i path esclusi.

Purpose: chiudere INT-01 (`.planning/research/baseline-{date}.md` committato) e BACK-07
(<30 min wall-clock). Plan-gate Phase 5: tutto verde → Phase 6 sbloccata.

Operator availability (INFO 16): finestra di ~30 min necessaria. Checkpoint human-verify
Task 3 richiede operatore disponibile ~5-10 min per ispezione visiva PNG.
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/trading-agent/.planning/STATE.md
@C:/trading-agent/.planning/ROADMAP.md
@C:/trading-agent/.planning/REQUIREMENTS.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-CONTEXT.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-VALIDATION.md
@C:/trading-agent/.gitignore
@C:/trading-agent/scripts/preflight_phase5.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight contract probe Phase 1-4 (BLOCKER 6 hard gate)</name>
  <files>(no file modified — read-only verification)</files>
  <read_first>
    - C:/trading-agent/scripts/preflight_phase5.py (Plan 05-01 Task 4)
    - C:/trading-agent/.planning/STATE.md
    - C:/trading-agent/.planning/ROADMAP.md
    - C:/trading-agent/.planning/REQUIREMENTS.md
  </read_first>
  <action>
    **BLOCKER 6 fix**: invoca `scripts/preflight_phase5.py` PRIMA di tutto. Se exit ≠ 0,
    ABORTA Wave 4 con error chiaro all'utente: lista i contract probe falliti, suggerisce
    quale Phase plan deve essere completato.

    Comando:
    ```bash
    .venv\Scripts\python.exe scripts\preflight_phase5.py
    ```

    Output atteso (success): stampa "SUCCESS: tutti i contract probe Phase 1-4 verdi.
    Wave 4 sblocca." e exit 0.

    Output atteso (failure): stderr "FAILED: N check non passati." + lista per-check;
    exit 1.

    **Se preflight fallisce:**
    1. STOP immediato. NON eseguire Task 2 (smoke run).
    2. Logga in dettaglio quale Phase plan è incompleto.
    3. Crea `checkpoint:human-action` chiedendo all'utente:
       - "Confermi pause Phase 5 fino al completamento Phase X-YY?"
       - oppure "Procedi forzato (con consapevolezza che smoke fallirà)?"
    4. Se utente sceglie "pause": esci da Plan 05-08, suggerisci `/gsd-execute-phase X`
       per la phase mancante.

    **Se preflight passa:** procedi a Task 2.

    Verifica supplementare via grep su REQUIREMENTS.md (sanity, non hard gate poiché il
    preflight è authoritative):
    1. `grep "BACK-0[1-6]" .planning/REQUIREMENTS.md | grep "\[x\]"` ≥ 6
    2. `grep "INDIC-" .planning/REQUIREMENTS.md | grep "\[x\]"` ≥ 14
    3. `grep "PATT-" .planning/REQUIREMENTS.md | grep "\[x\]"` ≥ 7
    4. `grep "STRAT-" .planning/REQUIREMENTS.md | grep "\[x\]"` ≥ 9
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe scripts\preflight_phase5.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/preflight_phase5.py` exit 0 (BLOCKER 6 hard gate — Wave 4 sblocca solo qui)
    - Stdout contiene "SUCCESS"
    - Sanity REQUIREMENTS.md grep counts (non-hard, solo cross-check):
      BACK ≥6 [x], INDIC = 14 [x], PATT = 7 [x], STRAT = 9 [x]
  </acceptance_criteria>
  <done>
    Pre-flight verde. Phase 1-4 confermate complete via contract probe. Wave 4 sblocca Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Smoke run end-to-end + verify deliverable + SC#3 hard/soft gates</name>
  <files>.planning/research/baseline-2026-05-08.md, .planning/research/baseline-equity-curves/*.png (×27), data/training/baseline_decisions/, data/training/baseline_drafts/, logs/trades.db</files>
  <read_first>
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-CONTEXT.md §domain (output paths)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-VALIDATION.md §Manual-Only Verifications
  </read_first>
  <action>
    1. **Pre-clean (idempotency safety):** se `logs/trades.db` esiste e contiene run baseline
       di un'esecuzione precedente fallita parziale, NON cancellarlo (idempotency D-14
       gestisce skip). Lasciare al `--force` flag il job di pulire run_id specifici.

    2. **Eseguire run reale (WARNING 10 wall-clock automated):**
       ```bash
       .venv\Scripts\python.exe scripts\run_baseline_backtest.py --force --max-wall-clock 1800
       ```
       Aspettative:
       - Wall-clock: <30 min (1800s) hard gate via CLI flag (WARNING 10 fix). Se sforato,
         lo script esce con code 2 (NON 0) — Task fallisce. Per debug usare `--no-time-gate`.
       - Stdout: tqdm progress bar `slice: 9/9 [...]`.
       - Stdout finale: `WALL_CLOCK_SECONDS={N}` (parseable per audit).
       - Exit code: 0 (success) OR 2 (wall-clock superato).

    3. **Verifica artefatti** (BLOCKER 1: directory layout):
       - `.planning/research/baseline-2026-05-08.md` esiste, size >5KB
       - `.planning/research/baseline-equity-curves/` contiene 27 file `.png`, ognuno >1KB
       - **`data/training/baseline_decisions/` è DIRECTORY (BLOCKER 1 fix)** con ≥1 file
         `part-*.parquet` (pyarrow.dataset layout)
       - **`data/training/baseline_drafts/` è DIRECTORY** con ≥1 file `part-*.parquet`
       - `logs/trades.db` contiene 27 righe in `backtest_runs` con run_id `baseline_2026-05-08_*`
         e relative trade rows in `backtest_trades`

    4. **Verifica integrità dati (BLOCKER 2 SC#3 hard/soft gates):**
       ```bash
       .venv\Scripts\python.exe -c "import pyarrow.dataset as ds; n = ds.dataset('data/training/baseline_decisions').count_rows(); print(f'DECISIONS_COUNT={n}'); assert n >= 1000, f'HARD GATE FAIL: {n} < 1000'; (print(f'WARN: {n} < 10000 SOFT target — Phase 7 ML guidance: retune detector aggressiveness') if n < 10000 else None)"
       ```

       Politica BLOCKER 2:
       - **HARD gate**: decision_count ≥ 1000 → Plan procede.
       - **SOFT target**: decision_count ≥ 10000 → Plan passa pulito.
       - Se 1000 ≤ N < 10000: Plan passa CON WARNING — log esplicito + nota in
         appendix report MD: "decision_count {N} sotto target SC#3 (10000); guidance
         Phase 7: retune detector aggressiveness". NON è phase failure.
       - Se N < 1000: Plan FAIL hard.

       Numero PNG: `Get-ChildItem .planning/research/baseline-equity-curves/*.png | Measure-Object | Select-Object -ExpandProperty Count` == 27

    5. **Sanity report content:**
       - `Select-String "Slice Metrics" .planning/research/baseline-2026-05-08.md` ≥ 1 match
       - `Select-String "baseline_2026-05-08" .planning/research/baseline-2026-05-08.md | Measure-Object` ≥27
       - `Select-String "Appendix" .planning/research/baseline-2026-05-08.md` ≥ 1 match
       - `Select-String "cost_yaml_sha256" .planning/research/baseline-2026-05-08.md` ≥ 1 (WARNING 12 verified)

    Italiano log nei messaggi user-facing (CLAUDE.md §Convenzioni).
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -c "from pathlib import Path; import pyarrow.dataset as ds; pngs = list(Path('.planning/research/baseline-equity-curves').glob('*.png')); assert len(pngs) == 27, f'PNG count {len(pngs)}'; assert Path('.planning/research/baseline-2026-05-08.md').exists(); assert Path('data/training/baseline_decisions').is_dir(), 'BLOCKER 1: must be directory'; assert len(list(Path('data/training/baseline_decisions').glob('*.parquet'))) > 0, 'BLOCKER 1: at least 1 part-*.parquet'; assert Path('data/training/baseline_drafts').is_dir(); n = ds.dataset('data/training/baseline_decisions').count_rows(); assert n >= 1000, f'BLOCKER 2 HARD GATE: decisions {n} < 1000'; print(f'OK decisions={n} (soft target 10000)')"</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight Task 1 exit 0 (BLOCKER 6 hard gate)
    - Exit code di `scripts/run_baseline_backtest.py --force --max-wall-clock 1800` == 0
    - Stdout contiene `WALL_CLOCK_SECONDS=` (WARNING 10 verified)
    - WALL_CLOCK_SECONDS ≤ 1800 (BACK-07 SC#1 hard gate via CLI)
    - 27 PNG file (`Get-ChildItem ...png | Measure-Object` == 27)
    - 1 file `baseline-2026-05-08.md` size >5KB
    - **BLOCKER 1**: `Path("data/training/baseline_decisions").is_dir()` == True AND
      `len(list(Path("data/training/baseline_decisions").glob("*.parquet"))) > 0`
    - **BLOCKER 1**: identico per `data/training/baseline_drafts/`
    - **BLOCKER 2 HARD**: `pyarrow.dataset.dataset(...).count_rows()` ≥ 1000
    - **BLOCKER 2 SOFT**: log WARNING (no fail) se 1000 ≤ count < 10000
    - `backtest_runs` tabella ha 27 righe con prefix `baseline_2026-05-08_`
    - Report MD contiene ≥27 occorrenze run_id pattern + sezioni Header/Metrics/Per-slice/Appendix
    - Report appendix contiene `cost_yaml_sha256` (WARNING 12)
  </acceptance_criteria>
  <done>
    Run completata con exit 0. Wall-clock entro hard gate. SC#3 ≥1000 (BLOCKER 2 hard).
    Tutti i deliverable INT-01 esistenti, popolati e in directory layout (BLOCKER 1).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint human-verify — qualità PNG + sanity report</name>
  <what-built>
    - 27 equity curve PNG in `.planning/research/baseline-equity-curves/`
    - 1 report MD `.planning/research/baseline-2026-05-08.md`
    - 2 parquet directory in `data/training/` (BLOCKER 1 layout)

    Tutti generati automaticamente dallo smoke run (Task 2). Nessuna verifica numerica
    automatizzabile per la **qualità qualitativa** delle curve (smoothness, no spike
    artificiosi, drawdown coerente con trend) — richiede ispezione visiva umana.

    INFO 16 acknowledged: operator window ~5-10 min necessaria.
  </what-built>
  <how-to-verify>
    1. **Apri 3 PNG sample** in image viewer Windows:
       - `.planning/research/baseline-equity-curves/EURUSD_M15_MODERATE.png`
       - `.planning/research/baseline-equity-curves/GBPUSD_H1_AGGRESSIVE.png`
       - `.planning/research/baseline-equity-curves/USDJPY_M30_CONSERVATIVE.png`

       Per ogni PNG verifica:
       - Curve equity (top subplot, steelblue) NON è flat-line (qualche trade è avvenuto)
       - Curve equity NON va negativa (equity_eur ≥ 0 sempre — sizing dinamico ok)
       - Drawdown subplot (bottom, indianred shaded) ha valori ≤ 0 e coerenti con dip
         dell'equity sopra
       - X-axis date leggibili, no overlap label

    2. **Apri report** `.planning/research/baseline-2026-05-08.md`:
       - Header: data run, comando CLI, git_sha, wall-clock totale visibili
       - Tabella `## Slice Metrics`: 27 righe, valori numerici (no NaN/None)
       - Sezione `## Per-slice details`: 27 sub-sezioni, ognuna con link al PNG
       - Appendix: cost_yaml_sha256 (full 64-char), strategy_yaml_sha256, baseline_yaml_sha256
         visibili (WARNING 12)

    3. **Sanity sui dati:** verifica che almeno 1 slice ha n_trades >100 (edge non-trivial).
       Se TUTTI i 27 run hanno 0 trades → bug nei detector / risk_engine / engine.

    Se uno qualsiasi dei check fallisce → riportare nei commenti del checkpoint quale.
    Se tutto OK → digitare "approved" per sbloccare Task 4 (commit).
  </how-to-verify>
  <resume-signal>
    Type "approved" se PNG e report OK. Altrimenti descrivi il problema (es. "EURUSD_M15
    PNG mostra flat line dal 2010 in poi" → triggera retro Wave 2 debug).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Git commit deliverable INT-01 + .gitignore review</name>
  <files>.planning/research/baseline-2026-05-08.md, .planning/research/baseline-equity-curves/*.png, .gitignore (eventualmente)</files>
  <read_first>
    - C:/trading-agent/.gitignore
  </read_first>
  <action>
    1. **Verifica .gitignore:**
       - `data/training/` deve essere in `.gitignore` (file grandi, no commit)
       - `logs/trades.db` deve essere in `.gitignore`
       - `.planning/research/baseline-equity-curves/*.png` NO gitignore (commit per audit)
       - `.planning/research/baseline-*.md` NO gitignore (commit per INT-01)

       Se manca regola, aggiungere a `.gitignore`:
       ```
       data/training/
       logs/trades.db
       logs/*.db
       ```

    2. **Stage commit:**
       ```bash
       git add .planning/research/baseline-2026-05-08.md
       git add .planning/research/baseline-equity-curves/*.png
       git add .gitignore  # se modificato
       git status  # verifica nessun file da data/training/ o logs/ staged
       ```

    3. **Commit message** (Italian, COMMIT_CONVENTIONS.md, NO Co-Authored-By per
       MEMORY.md regola):
       ```
       feat(05): baseline backtest 27-run completato (BACK-07, INT-01)

       Esegue baseline pre-ML su 23.5y × 3 pair × 3 TF × 3 profile = 27 run.
       Wall-clock effettivo: {WALL_CLOCK}s (target SC#1 ≤1800s hard gate).
       Decision count: {N} (hard ≥1000, soft target ≥10000).

       Output:
       - .planning/research/baseline-2026-05-08.md — metrics report 27-row
       - .planning/research/baseline-equity-curves/ — 27 PNG equity+drawdown
       - data/training/baseline_decisions/ — parquet directory dataset (gitignored)
       - data/training/baseline_drafts/ — parquet directory dataset (gitignored)
       - logs/trades.db — 27 righe backtest_runs + trade rows (gitignored)

       Closes INT-01.
       ```

    4. **NO push automatico in questo task** — il push viene gestito dall'orchestrator
       Phase 5 finale (MEMORY.md regola: "Push to origin after each validated phase",
       triggered da `/gsd-verify-work`).

    5. **Update REQUIREMENTS.md:**
       - `BACK-07: [ ]` → `[x]`
       - `INT-01: [ ]` → `[x]`
       Anche colonna "Status" tabella Traceability.

    6. **Update STATE.md:**
       - Tabella Milestone Status riga 5 Baseline Backtest: status `◐ in-progress` o
         `○ pending` → `✓ complete`. Plans `0/0` → `9/9` (8 originali + 1 split: 06a/06b).
         Progress 100%.
       - "Active Work" sezione: rimuovi righe Phase 5 (sospesa).
       - Last updated date.

    7. Stage gli aggiornamenti REQUIREMENTS.md + STATE.md nello stesso commit (atomic
       phase closure).
  </action>
  <verify>
    <automated>git log --oneline -1 | findstr /C:"BACK-07" && git diff HEAD~1 --stat | findstr /C:"baseline-2026-05-08.md"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` contiene "INT-01" e "BACK-07"
    - `git show HEAD --stat` include `.planning/research/baseline-2026-05-08.md` + ≥27 PNG
    - `git status` clean (no untracked tra i deliverable)
    - `data/training/` NON nel commit (gitignored)
    - `logs/trades.db` NON nel commit (gitignored)
    - REQUIREMENTS.md `BACK-07: [x]` + `INT-01: [x]`
    - STATE.md Phase 5 status complete
  </acceptance_criteria>
  <done>
    Phase 5 deliverable committati. INT-01 + BACK-07 chiusi. STATE.md + REQUIREMENTS.md
    aggiornati. Phase gate verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke run output → filesystem | Path-controlled (.planning/research/, data/training/, logs/) — no traversal |
| git commit → repository | Atomic commit; .gitignore previene leak di file grandi (logs/trades.db, parquet) |
| preflight subprocess → main | Exit code authoritative (0 = pass, ≠0 = fail). No stdin/stdout injection in main flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-21 | Information Disclosure | data/training/baseline_*.parquet committed by accident | mitigate | .gitignore review obbligatoria in Task 4; `git status` check pre-commit |
| T-05-22 | Denial of Service | smoke run >30 min, dev laptop unusable | mitigate | WARNING 10 fix: `--max-wall-clock 1800` hard gate; exit code 2 se sforato. `--no-time-gate` solo per debug. |
| T-05-23 | Repudiation | report MD senza git_sha | mitigate | report writer (05-06b) include `meta["git_sha"]` in Header — audit trail completo |
| T-05-27 | Tampering | preflight bypass via missing file | mitigate | preflight `inspect.signature` cross-check — fail se classe esiste ma signature errata |
</threat_model>

<verification>
- 4 task: pre-flight + smoke + checkpoint qualitative + commit
- BLOCKER 6: Phase 1-4 completion gate via contract probe BEFORE smoke run
- BLOCKER 1: directory layout per entrambi i parquet dataset
- BLOCKER 2: SC#3 hard gate ≥1000 + soft target ≥10000 (warning-not-fail tra le due soglie)
- WARNING 10: wall-clock automated check via CLI flag
- 27 PNG + 1 report + 2 parquet directory creati
- INT-01 deliverable committato
- BACK-07 SC#1 wall-clock hard-gated ≤1800s
- REQUIREMENTS.md + STATE.md aggiornati
- Phase 5 chiusa
</verification>

<success_criteria>
**SC#1 (BACK-07 wall-clock):** smoke run exit 0 con WALL_CLOCK_SECONDS ≤ 1800 (HARD GATE
via WARNING 10 fix). Sforamento → exit code 2 → Plan fail. Bypass solo via `--no-time-gate`
(debug only).

**SC#2 (per-slice metrics):** report MD con tabella 27-row + appendix hashes (full sha256).

**SC#3 (decision dataset, BLOCKER 2 hard/soft):**
- HARD gate: ≥1000 righe in `data/training/baseline_decisions/` → Plan procede.
- SOFT target: ≥10000 righe → Plan pulito (no warning).
- 1000 ≤ N < 10000 → Plan passa con WARNING + appendix nota guidance Phase 7 ML retune detector.
- N < 1000 → Plan FAIL.

**SC#4 (equity curves):** 27 PNG generati e qualitative-OK (checkpoint human-verify).

**INT-01 closure:** `.planning/research/baseline-{date}.md` committato in git.
</success_criteria>

<output>
Dopo completamento: `.planning/phases/05-baseline-backtest/05-08-SUMMARY.md`.
Sezioni:
- Pre-flight Result (Phase 1-4 contract probe — BLOCKER 6 closure)
- Wall-Clock Effettivo vs HARD Gate 1800s (WARNING 10 closure)
- Deliverable Generated (counts: 27 PNG, 1 MD, 2 parquet DIRECTORY — BLOCKER 1, X righe ledger)
- SC#3 Decisions Count (HARD ≥1000 / SOFT ≥10000 — BLOCKER 2)
- Checkpoint human-verify Outcome
- Git Commit SHA + Files Committed
- INT-01 + BACK-07 closure status
- Phase 5 Gate Result (verde / pending issue)
- Next Phase: 06-mcp-tools-part-1
</output>
</content>
</invoke>