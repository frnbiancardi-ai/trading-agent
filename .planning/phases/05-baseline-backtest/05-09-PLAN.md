---
phase: 05-baseline-backtest
plan: 09
type: execute
wave: 5
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06a, 05-06b, 05-07, 05-08]
files_modified:
  - backtest/baseline/dataset_writer.py
  - backtest/baseline/slice_worker.py
  - data/configs/baseline.yaml
  - tests/test_baseline_dataset_writer.py
  - tests/test_baseline_runner.py
files_created:
  - scripts/run_baseline_05_09.py
  - tests/test_baseline_no_leakage_extended.py
autonomous: true       # nessun checkpoint: planning + writing + test, esecuzione 27/27 NON e' in questo plan
requirements: [BACK-07, INT-01]
tags: [phase-5, baseline-backtest, wave-5, dataset-remediation, additive-plan, blocks-phase-7]

decisions:
  # Plan 05-09 e' un re-opening additivo di Phase 5 chiuso 2026-05-08.
  # Le D-decisions Phase 5 originali (D-01..D-23) restano in vigore.
  # Sotto, deltas specifici introdotti da 05-09:
  - id: D-09-A
    title: "schema parquet baseline_decisions v2 (chiusura D-02 originale)"
    rationale: |
      Phase 5 D-02 specificava schema completo (identita + ProposalDraft +
      ExtendedIndicators 30+ campi + ctx + outcome). Plan 05-08 ha
      consegnato solo 17 colonne flat (vedi 05-08-SUMMARY §Numeri /
      `data/training/baseline_decisions/part-0.parquet`). Phase 7 RESEARCH
      §"Critical Finding: Dataset Schema Gap" (riga ~498-542) documenta
      mancanze field-by-field. Plan 05-09 chiude il gap scrivendo TUTTE
      le 37 chiavi prodotte da `indicators.compute_all_extended()` (lista
      canonica in `indicators/aggregate.py:96-141`, estratta
      programmaticamente — vedi FIX 3 / Task 1) + 4 meta (`profile`,
      `run_id`, `decision_ts_utc` UTC, `entry_ts_utc`, `exit_ts_utc`)
      come colonne top-level flat nel parquet (NON dentro
      `decision_context_json`).

      Bump esplicito: `dataset_schema_version: 2` in `baseline.yaml`.
      v1 = 17 cols flat + ctx_json (Plan 05-08 output, archiviato come
      `baseline_decisions.pre-05-09` per backup).
      v2 = 17 cols base + 33 cols extended (37 keys totali, di cui 4 gia'
      presenti nelle base via legacy compute_all) + 5 cols meta/timestamp
      = ~50+ cols (anchor principale).
  - id: D-09-B
    title: "no-leakage anchor: indicator snapshot al bar entry_time-1 (anchor conservativa vs D-22 baseline)"
    rationale: |
      Vincolo non rinegoziabile esplicitamente locked in planning_context.
      L'engine event-driven (`backtest/engine.py:170` for-loop su
      `(idx, bar)`) processa il bar corrente dentro un singolo iterato:
      `broker.advance(bar)` -> `strat.analyze_symbol(...)` -> proposal ->
      `broker.send_order(...)`. Lo `_build_ctx` attuale (engine.py:305-325)
      cattura `setup.indicators` che e' uno snapshot calcolato dalla strategy
      ALLO STESSO bar idx (e quindi include OHLC del bar di entry).

      Plan 05-09 introduce in slice_worker un secondo snapshot extended
      calcolato POST-engine su una vista pre-warm `bars_dict[:idx]`
      (esclusivo: il bar `idx` di entry NON entra nel calcolo). Questo
      replica la condizione "alla chiusura del bar precedente" che il
      classificatore ML dovra' vedere live (analyze_symbol live decide
      all'open di una nuova barra, basandosi sulla history conclusa).

      **FIX C iter 3 — chiarimento semantica `decision_ts_utc` vs D-22:**

      Convenzione MT5 bar-discrete: `bar.time = bar OPEN timestamp` (NON
      close). Di conseguenza, in stretta convenzione MT5:
        - close di idx-1 == open di idx == `bars_dict[idx]["time"]` == entry_ts
        - open di idx-1                  == `bars_dict[idx-1]["time"]`

      D-22 Phase 5 ("Decision_ts = bar close timestamp UTC") richiederebbe
      quindi `bars_dict[idx]["time"]` (close di idx-1 == open di idx), che
      coincide ESATTAMENTE con entry_ts_utc (gap=0 in convenzione MT5).

      **Plan 05-09 adotta `bars_dict[idx_entry - 1]["time"]` (open di idx-1)
      come `decision_ts_utc`, scelta deliberatamente PIU' CONSERVATIVA di
      D-22.** Razionale tecnico:
        - 1 bar di safety buffer al feature snapshot Phase 7
        - Previene edge case dove "close di idx-1" e "open di idx" differiscono
          per gap di mercato (weekend, holiday, gap di apertura sessione)
        - Phase 7 walk-forward fold usera' `decision_ts_utc` come boundary
          temporale: usare 1 bar prima del minimo richiesto e' accettabile
          come buffer (riduce features disponibili di 1 bar ma elimina
          rischio leakage residuo da gap-di-sessione)

      Il delta vs D-22 stretta e' esplicitamente documentato: gap atteso
      = timeframe_delta (15min per M15, 30min per M30, 60min per H1).
      Test `test_no_leakage_invariant_decision_before_entry` enforce
      l'invariante `gap == timeframe_delta` come contratto di
      "anchor 1-bar-prima-di-entry".

      Test mirato `tests/test_baseline_no_leakage_extended.py` verifica
      l'invariante: dati 100 bar sintetici monotonicamente crescenti, lo
      snapshot al bar k usa `bars[:k]` e NON `bars[:k+1]`. Implementato
      via mock di `compute_all_extended` che registra `len(bars_arg)` ad
      ogni call.
  - id: D-09-C
    title: "writer signature backward-compat con caller Plan 05-08"
    rationale: |
      `write_decisions_shard(rows, run_id, shard_dir)` (firma corrente)
      resta invariata. Il caller slice_worker passa un `list[dict]` dove
      ogni dict puo' avere KEYS extra (le 33 col extended + 5 meta).
      pandas.DataFrame le materializza automaticamente come colonne; le
      righe che hanno solo le 17 keys originali producono NaN nelle
      colonne extended (caso impossibile in produzione 05-09, ma garantito
      backward-compat per chiamanti Phase 5 originali).

      Fallback documentato in dataset_writer: se rows[0] manca dei meta
      `profile`/`run_id`/`decision_ts_utc`, scrive comunque (NON solleva)
      ed emette un singolo `_log.warning("schema-v2 fields missing: %s")`
      con lista delle keys mancanti. Lo schema-validation post-run del
      wrapper script catchera' il problema.
  - id: D-09-D
    title: "script wrapper stand-alone scripts/run_baseline_05_09.py"
    rationale: |
      Workflow distribuito locked in RESUME-PLAN.md rev 4: PC primario fa
      planning + Phase 6/8/9/10/11 prep mentre PC secondario esegue solo
      lo script Python in finestra notturna (~3h18m wall-clock atteso).
      Il PC secondario non ha Claude Code. Lo script DEVE:
      - Auto-detect repo root (`Path(__file__).resolve().parent.parent`)
      - argparse con: --smoke, --force, --only-runs, --max-wall-clock,
        --no-time-gate
      - Smoke mode: 1 pair (EURUSD) x 1 TF (M15) x 3 profile x **3 mesi**
        (date_start = "2024-02-01", date_end = "2024-05-01") — finestra
        allargata per probabilita' >=1 trade aggregato (FIX 2 plan-checker
        iter 1); output `data/training/_smoke_05_09/` (NON sovrascrive
        prod); wall-clock target <180s sul PC secondario (Ryzen 7 5800H),
        >300s indica regressione.
      - Full mode: invoca `run_baseline(force=...)` riusando il runner
        esistente (NESSUN duplicato logica).
      - Recovery mode: `--only-runs run_id1,run_id2,...` esegue
        pre-delete SQL su `backtest_runs` + `backtest_trades` (schema
        reale Phase 5; NO `trades_log` — quello vive in `logs/trades.db`
        live trader senza colonna run_id) per i run_id whitelistati,
        poi lancia `run_baseline(force=False)`: D-14 idempotency
        skippera' i run completi, rilanciando SOLO quelli appena
        cancellati. Implementazione reale via **riuso di
        `slice_worker._force_clear_run`** (FIX A iter 3), NON best-effort,
        NON pattern SQL custom duplicato.
      - Schema-validation post-run: ricarica parquet finalizzato, asserisce
        - >= 30 colonne indicators (subset noto) presenti
        - 5 meta presenti senza NULL (run_id, profile, regime_state,
          decision_ts_utc, entry_ts_utc, exit_ts_utc)
        - profile in {CONSERVATIVE, MODERATE, AGGRESSIVE}
        - decision_ts_utc dtype timezone-aware oppure ISO8601 con Z
        - row count >= 1050 (HARD gate, margine 2.5% sopra parity Plan
          05-08 SC#3 che ha prodotto 1076 trade — vedi FIX D iter 3 per
          razionale del bump da 1000 a 1050)
        - Smoke-tolerant mode (min_rows=0): dir mancante o vuota -> OK
          warning (FIX 2 plan-checker iter 1)
      - Exit code 0 se OK, !=0 se anomalia (schema KO o run failed).
      - Log strutturato con livello INFO, tqdm visibile.
  - id: D-09-E
    title: "max_workers locked a 9, NO worker pool tuning"
    rationale: |
      `data/configs/baseline.yaml:16` mantiene `max_workers: 9`. Plan 05-08
      ha validato 9 worker su Ryzen 7 5800H (8c/16t + 16GB) senza thrash di
      memoria (peak ~1.8GB stimato). SETUP-SECONDARY-PC.md §0.1 vincola RAM
      min 8 GB. Plan 05-09 NON modifica max_workers ne' introduce
      auto-detect cpu_count (out of scope, rischio regressione).
  - id: D-09-F
    title: "scope chirurgico: NO engine perf-opt, NO drafts capture"
    rationale: |
      Gap Plan 05-08 documentati:
      (a) wall-clock 11922s vs 1800s hard gate (Rule 4 user-accepted, defer
          plan 01-09 vectorization);
      (b) `drafts_rows = []` (FORMING/NONE non catturati, defer Phase 9
          failure analysis).

      Plan 05-09 NON tocca ne' (a) ne' (b). Resta CPU-only event-driven.
      Cattura solo trade chiusi (READY -> TP_HIT/SL_HIT/TIMEOUT/BE), parity
      Plan 05-08. Date range invariato (`2015-05-08` -> `2025-05-08`,
      10y window Option B).
  - id: D-09-G
    title: "Field coverage parziale D-02 — non-indicators field derivable downstream in Phase 7"
    rationale: |
      D-02 originale enumera 38 fields nel parquet baseline. Plan 05-09 chiude
      33 indicators (compute_all_extended) + 4 meta (profile, regime_state,
      run_id, decision_ts_utc/entry_ts_utc/exit_ts_utc) = 36-37 cols. Restano
      NON-coperti by design (decisione user 2026-05-11):
        - factors_trend_alignment, factors_setup_pattern, factors_momentum,
          factors_volatility_regime, factors_spread_session (5 bool da
          ProposalDraft serializzati in `decision_context_json` struct)
        - grade, setup_name (presenti in decision_context_json)
        - outcome (derivabile: exit_reason + pnl_pips)
        - bars_held (derivabile: bar_index_exit - bar_index_entry)
        - sr_dist_pips, spread_at_entry_pips, sentiment_proxy,
          recent_trades_outcome_5 (D-02 context fields, non in scope Phase 5)
      Rationale: estrarli come top-level cols obbligherebbe a toccare engine.py
      (Phase 1) e slice_worker enrichment, violando D-09-F (scope chirurgico
      Phase 5-only). Phase 7 plan dovra' includere come Wave 0 un task di
      feature extraction che derivi questi campi da decision_context_json +
      pnl_pips + bar timestamps. Stima 4-8 ore di lavoro umano addizionali
      a STEP 13 di RESUME-PLAN.md (accettato dall'user).
    consequences: |
      - Phase 7 plan dovra' esporre un feature_extraction.py step iniziale
      - Walk-forward fold Phase 7 usa decision_ts_utc (D-22 disciplinato)
      - Bar-close discipline: factors_* leggibili da decision_context_json
        struct[idx] (parquet permette query nested)
      - Acceptable trade-off vs invasivita' cross-phase scope.
    status: locked

dependency_graph: |
  Task 1 (writer extension + unit test schema + extraction programmatica)
    -> Task 2 (slice_worker integration: estrai snapshot extended + meta + 3 timestamps)
       -> Task 3 (baseline.yaml dataset_schema_version: 2 + commento bump)
          -> Task 4 (scripts/run_baseline_05_09.py wrapper CLI + --only-runs pre-delete)
             -> Task 5 (no-leakage test + smoke test runner integration)
                -> Task 6 (recovery docs sezione + suite verde finale)

  Seriale: ogni task aggiunge la funzionalita' che il successivo testa.
  La separation of concerns garantisce che Task 1 sia immediatamente
  verificabile (unit test scrive righe sintetiche) prima di toccare
  slice_worker.

  Files_modified overlap interno al plan: dataset_writer (Task 1),
  slice_worker (Task 2), baseline.yaml (Task 3), tests/* (Task 1+5).
  Nessun overlap con altri plan (Phase 5 chiusa, Phase 6 plan scritti ma
  non eseguiti, Phase 7 paused). Wave 5 standalone.

must_haves:
  truths:
    - "parquet finale baseline_decisions/ ha >= 30 colonne indicators top-level (no ctx_json)"
    - "5 meta presenti: profile (in {CONSERVATIVE,MODERATE,AGGRESSIVE}), regime_state (canonical, NON regime alias — FIX 9), run_id, decision_ts_utc (UTC-aware, bar idx-1 open per anchor conservativa vs D-22 — vedi D-09-B), entry_ts_utc (bar idx open), exit_ts_utc (variable per exit_reason)"
    - "indicator snapshot ML calcolato su bars_view = bars_dict[:idx], NON include bar di entry (no-leakage by construction)"
    - "row count >= 1050 (parity con Plan 05-08 = 1076 trade, margine 2.5% — vedi FIX D iter 3 per razionale bump da 1000)"
    - "scripts/run_baseline_05_09.py --smoke completa in <180s wall-clock sul PC secondario (>300s indica regressione)"
    - "scripts/run_baseline_05_09.py senza flags = full run 27/27 ok, wall-clock atteso 2h45m-3h30m su Ryzen 7 5800H"
    - "scripts/run_baseline_05_09.py --only-runs <list> esegue pre-delete via riuso di slice_worker._force_clear_run (NO SQL custom, NO trades_log) + relaunch (D-14 idempotency skip i completi)"
    - "tutti i test Phase 5 pre-esistenti restano verdi (backward-compat writer signature)"
    - "suite globale (~436 test pre-Phase 6) verde post-Task 5"
    - "dataset_schema_version: 2 documentato in baseline.yaml"
    - "decision_ts_utc < entry_ts_utc rigorosamente, gap == timeframe_delta (15min per M15 — anchor conservativa D-09-B, 1 bar pre-entry buffer)"
    - "_SCHEMA_V2_REQUIRED_KEYS costruito programmaticamente da compute_all_extended() interface (FIX 3 — previene drift)"
    - "indicators_full cache (chiamata top-level `compute_all_extended(bars_dict)` in slice_worker:270) RESTA INVARIATA — NON passa regime_cfg (firma backward-compat). Il flow engine principale (strategy detector -> trade generation) resta IDENTICO a Plan 05-08. Count trade atteso == 1076 ± 0 (FIX D iter 3). `regime_cfg` per-symbol caricato in slice_worker viene usato SOLO in `_extract_extended_snapshot_at_entry` post-engine per riempire le colonne `regime_state`/`regime_atr_pct` del parquet — flusso enrichment isolato, zero side-effect sul detector."
  artifacts:
    - path: "backtest/baseline/dataset_writer.py"
      provides: "writer esteso, schema-v2 con whitelist extraction (chiude D-02 + D-09-A)"
      contains: "_SCHEMA_V2_REQUIRED_KEYS"
      contains: "_build_required_keys_v2"
    - path: "backtest/baseline/slice_worker.py"
      provides: "extended snapshot @ bar idx-1 (D-09-B) + 5 meta injected nei decisions_rows + 3 timestamps distinti (D-22 anchor conservativa)"
      contains: "_extract_extended_snapshot_at_entry"
      contains: "_build_bars_index_by_time"
    - path: "data/configs/baseline.yaml"
      provides: "schema bump marker (D-09-A)"
      contains: "dataset_schema_version: 2"
    - path: "scripts/run_baseline_05_09.py"
      provides: "wrapper stand-alone PC secondario (D-09-D) con --only-runs riuso _force_clear_run (FIX A iter 3)"
      contains: "argparse"
      contains: "--smoke"
      contains: "--only-runs"
      contains: "_force_clear_run"
    - path: "tests/test_baseline_dataset_writer.py"
      provides: "unit test schema-v2 esteso + extraction programmatica anti-drift"
    - path: "tests/test_baseline_no_leakage_extended.py"
      provides: "AST/runtime invariant: snapshot a bars[:idx] (D-09-B) + decision_ts < entry_ts (gap == timeframe_delta)"
    - path: "tests/test_baseline_runner.py"
      provides: "smoke integration test invoca wrapper --smoke + --only-runs unit test (su tabella backtest_trades reale)"
  key_links:
    - from: "scripts/run_baseline_05_09.py"
      to: "backtest.baseline.runner.run_baseline"
      via: "import + invocation"
      pattern: "from backtest.baseline.runner import run_baseline"
    - from: "scripts/run_baseline_05_09.py"
      to: "backtest.baseline.slice_worker._force_clear_run"
      via: "riuso pattern pre-delete (FIX A iter 3 — NO SQL custom)"
      pattern: "from backtest.baseline.slice_worker import _force_clear_run"
    - from: "scripts/run_baseline_05_09.py::_run_smoke"
      to: "backtest.baseline.runner._csv_path_for"
      via: "path CSV canonical (FIX B iter 3 — NO path hardcoded)"
      pattern: "from backtest.baseline.runner import _csv_path_for"
    - from: "backtest/baseline/slice_worker.py::run_slice_3profiles"
      to: "indicators.compute_all_extended"
      via: "snapshot al bar entry_time-1 (D-09-B) — bars_view explicit slice"
      pattern: "bars_view\\s*=\\s*bars_dict\\[:idx\\]"
    - from: "backtest/baseline/slice_worker.py::run_slice_3profiles"
      to: "indicators.volatility.load_regime_config"
      via: "regime_cfg per-symbol (FIX 1 plan-checker iter 1) — usato SOLO in _extract_extended_snapshot_at_entry, NON in compute_all_extended top-level (FIX D iter 3)"
      pattern: "load_regime_config\\(symbol,"
    - from: "data/training/baseline_decisions/ (output)"
      to: "Phase 7 ML training pipeline (ML-01 feature extraction)"
      via: "parquet schema-v2 consumato da pd.read_parquet(dir)"
      pattern: "n/a (Phase 7 reader)"
---

<objective>
Chiusura del gap D-02 inherited da Plan 05-08 (parquet baseline 17 cols
flat -> target 50+ cols con TUTTI gli indicatori extended + 5 meta/timestamps) +
produzione di uno script wrapper Python stand-alone per esecuzione
notturna sul PC secondario (workflow distribuito RESUME-PLAN.md rev 4).

Scope chirurgico: tocca SOLO file Phase 5 (`backtest/baseline/`,
`data/configs/baseline.yaml`, `scripts/`, `tests/`). NON tocca
`backtest/engine.py` (Phase 1), NON tocca `strategy/` (Phase 4), NON
tocca `indicators/` (Phase 2 — solo lettura di interface
`compute_all_extended()` + `load_regime_config(symbol, yaml_path)`).

Purpose:
- Sbloccare Phase 7 ML training (RESEARCH §"Critical Finding: Dataset
  Schema Gap" — mancano ~24+ ExtendedIndicators fields + 5 context
  fields; ML-01 feature extraction blocked finche' il parquet non e'
  schema-completo per i 33 indicators + 4 meta. Gli altri 8 field
  D-02 sono deferred-by-design via D-09-G a Phase 7 feature_extraction).
- Mantenere la trace di reproducibilita' (re-run su PC secondario produce
  parquet equivalente, hash baseline.yaml diverso da Plan 05-08 grazie
  al bump `dataset_schema_version: 2`).
- Garantire no-leakage by construction (indicator snapshot @
  `bar[entry_time-1]`, MAI sul bar di entry o successivi).
- Discipline temporale anchor conservativa vs D-22: 3 timestamp distinti
  (`decision_ts_utc` = bar idx-1 open == 1 bar pre-entry,
  `entry_ts_utc` = bar idx open, `exit_ts_utc` = exit), prerequisito
  per walk-forward Phase 7 senza leakage.

Output:
- `backtest/baseline/dataset_writer.py` esteso (whitelist programmaticamente
  costruita da compute_all_extended interface)
- `backtest/baseline/slice_worker.py` modificato (extended snapshot +
  meta injection + 3 timestamps distinti)
- `data/configs/baseline.yaml` con `dataset_schema_version: 2`
- `scripts/run_baseline_05_09.py` (NUOVO) wrapper CLI standalone con
  `--only-runs` via riuso `_force_clear_run`
- `tests/test_baseline_dataset_writer.py` esteso (schema-v2 expectations
  + drift-prevention test)
- `tests/test_baseline_no_leakage_extended.py` (NUOVO) invariant test
  + decision_ts < entry_ts asserzioni
- `tests/test_baseline_runner.py` esteso (smoke integration via
  `--smoke` + `--only-runs` unit test)

Phase 5 closure dopo questo plan: 9/9 plans complete. Phase 7 sbloccato
per planning (con awareness D-09-G).

NB: l'esecuzione full 27/27 NON e' parte di questo plan. Il plan
produce gli artefatti di codice. L'esecuzione effettiva avviene allo
STEP 3 di RESUME-PLAN.md (PC secondario, notturna).
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/trading-agent/CLAUDE.md
@C:/trading-agent/.planning/STATE.md
@C:/trading-agent/.planning/ROADMAP.md
@C:/trading-agent/.planning/REQUIREMENTS.md
@C:/trading-agent/.planning/RESUME-PLAN.md
@C:/trading-agent/.planning/SETUP-SECONDARY-PC.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-CONTEXT.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-08-PLAN.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-08-SUMMARY.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md
@C:/trading-agent/.planning/phases/07-ml-classifier/07-CONTEXT.md
@C:/trading-agent/.planning/phases/07-ml-classifier/07-RESEARCH.md
@C:/trading-agent/backtest/baseline/dataset_writer.py
@C:/trading-agent/backtest/baseline/slice_worker.py
@C:/trading-agent/backtest/baseline/runner.py
@C:/trading-agent/backtest/engine.py
@C:/trading-agent/backtest/ledger.py
@C:/trading-agent/data/configs/baseline.yaml
@C:/trading-agent/data/configs/regime.yaml
@C:/trading-agent/indicators/aggregate.py
@C:/trading-agent/indicators/volatility.py
@C:/trading-agent/indicators/__init__.py
@C:/trading-agent/scripts/run_baseline_backtest.py
@C:/trading-agent/tests/test_baseline_dataset_writer.py

<interfaces>
<!-- Contracts che l'executor deve usare verbatim. NO scavenger hunt: tutto qui. -->

### `indicators.compute_all_extended(bars, regime_cfg=None) -> dict`

Source: `indicators/aggregate.py:49-142`. Accetta `list[dict]` con keys
`{time, open, high, low, close, volume}`. Ritorna dict con queste chiavi
canoniche (NB: i nomi sono SCAMBIABILI con la stringa esatta — NON
inventare aliases):

```text
# Legacy (4 da compute_all):
sma_20, ema_50, rsi_14, atr_14

# Bollinger (5):
bb_upper, bb_middle, bb_lower, bb_bbw, bb_squeeze

# Keltner (2):
kc_upper, kc_lower

# ADX (3):
adx_14, plus_di_14, minus_di_14

# MACD (3):
macd, macd_signal, macd_hist

# Stochastic (2):
stoch_k, stoch_d

# Donchian (2):
donch_upper, donch_lower

# Pivots (3):
pivot_p, pivot_camarilla_h3, pivot_camarilla_l3

# Fibonacci (4):
fib_0382, fib_0500, fib_0618, fib_direction

# VWAP/Volume (2):
vwap, avg_volume_20

# Bars/NR/CS (4):
nr4, nr7, boomer, closing_score

# Hurst (1):
hurst

# Regime (2 — None se regime_cfg=None):
regime_state, regime_atr_pct
```

**Totale: 37 chiavi** (33 sempre presenti + 2 regime opt + 4 legacy gia'
contate nelle 33 — ovvero 33 + 4 legacy = 37 quando regime_cfg=None,
oppure 35 + 4 legacy = 37 se contiamo regime_state/atr_pct sempre
presenti come keys con value None). FIX 3 plan-checker iter 1:
l'enumerazione manuale non e' la fonte canonica; Task 1 costruira'
`_SCHEMA_V2_REQUIRED_KEYS` programmaticamente invocando
`compute_all_extended(dummy_bars, regime_cfg=None)` UNA volta
a import-time del modulo `dataset_writer.py`.

### `indicators.volatility.load_regime_config(symbol, yaml_path) -> dict`

**FIX 1 plan-checker iter 1**: la firma reale (verificata
`indicators/volatility.py:249-269`) e':

```python
def load_regime_config(symbol: str, yaml_path: Path | str) -> dict:
    """Carica config regime PER SIMBOLO con fallback default."""
```

Schema YAML `data/configs/regime.yaml` (verificato):
- `default: {window, compressed_below, expanded_above}`
- `symbols.{EURUSD|GBPUSD|USDJPY}: {window, compressed_below, expanded_above}`

Il dict ritornato e' **per-symbol**. Plan 05-09 lo invoca UNA VOLTA per
worker dentro `run_slice_3profiles`, dove `symbol` e' disponibile come
argomento. I 3 profile sequenziali nello stesso worker condividono
`regime_cfg` (immutable dict).

**FIX D iter 3 — isolamento regime_cfg dal flow engine principale:**

`regime_cfg` viene caricato per-symbol in `run_slice_3profiles` ma e'
usato ESCLUSIVAMENTE all'interno di `_extract_extended_snapshot_at_entry`
(post-engine enrichment). La chiamata top-level
`compute_all_extended(bars_dict)` a `slice_worker.py:270` RESTA
INVARIATA (zero argomenti extra). Conseguenze:
  - Indicator cache `indicators_full` usata dalla strategy = identica a Plan 05-08
  - Flow detector -> proposal -> ledger row = identico a Plan 05-08
  - Count trade atteso == 1076 ± 0 (parity stretta, NO drift)
  - regime_cfg per-symbol popola SOLO le colonne `regime_state` /
    `regime_atr_pct` del parquet, calcolate post-engine

```python
# CORRETTO:
regime_cfg = load_regime_config(symbol, Path("data/configs/regime.yaml"))

# ERRATO (signature obsoleta, non esiste):
regime_cfg = load_regime_config()
```

### `backtest/baseline/dataset_writer.py::write_decisions_shard(rows, run_id, shard_dir)`

Source: `backtest/baseline/dataset_writer.py:24-38`. Firma INVARIATA
(D-09-C). Il writer materializza `rows: list[dict]` in pandas DataFrame
e scrive parquet snappy. Plan 05-09 NON cambia signature; aggiunge solo
una whitelist `_SCHEMA_V2_REQUIRED_KEYS` (costruita programmaticamente,
FIX 3) per documentation + un warning log se rows[0] manca le keys-v2.

### `backtest.baseline.slice_worker.run_slice_3profiles(...) -> list[dict]`

Source: `backtest/baseline/slice_worker.py:196-410`. Il caller passa
`bars: list[Bar]`, calcola `bars_dict` (linee 259-269), invoca
`compute_all_extended(bars_dict)` UNA VOLTA per slice (linea 270, **SENZA
regime_cfg — FIX D iter 3 isolamento**). Plan 05-09 estende: dopo aver
ottenuto `engine_result["trades"]`, per ogni trade arricchisce il dict con:
  (1) snapshot extended ricalcolato sulla sotto-finestra
      `bars_view = bars_dict[:entry_idx]` (NON inclusivo del bar entry — D-09-B);
  (2) 5 meta/timestamps: `profile`, `regime_state` (canonical, NO alias
      `regime` — FIX 9), `run_id`, `decision_ts_utc` (bar idx-1 open
      == 1 bar pre-entry, anchor conservativa vs D-22 — vedi D-09-B),
      `entry_ts_utc` (bar idx open, gia' in engine row),
      `exit_ts_utc` (variable per exit_reason, gia' in engine row).

### `backtest.baseline.slice_worker._force_clear_run(db_path, run_id) -> None`

**FIX A iter 3**: pattern canonical pre-delete per Phase 5 schema reale.

Source verbatim `backtest/baseline/slice_worker.py:59-74`:

```python
def _force_clear_run(db_path: Path, run_id: str) -> None:
    """RESEARCH §Pitfall: AUTOINCREMENT su backtest_trades duplica righe in --force.

    DELETE FROM backtest_trades + backtest_runs PRIMA di re-insert. Single connection
    con busy_timeout — WARNING 8 fix: non wrappare con `with_retry` (riapre conn ogni
    retry); usa busy_timeout built-in.
    """
    conn = sqlite3.connect(db_path, timeout=30.0)
    try:
        conn.execute("PRAGMA busy_timeout=30000")
        conn.execute("DELETE FROM backtest_trades WHERE run_id=?", (run_id,))
        conn.execute("DELETE FROM backtest_runs WHERE run_id=?", (run_id,))
        conn.commit()
    finally:
        conn.close()
```

Tabelle target reali Phase 5 (verificate `backtest/ledger.py:19-79`):
  - `backtest_runs(run_id PK, ...)` — schema completo runs ledger
  - `backtest_trades(trade_id AUTOINCREMENT PK, run_id FK, ...)` — schema trade ledger

**NON esiste** `trades_log` con colonna `run_id`: `trades_log` vive in
`logs/trades.db` ma e' lo schema del live trader (NO `run_id`). Tentare
`DELETE FROM trades_log WHERE run_id=?` solleverebbe
`OperationalError: no such column: run_id`. Per `--only-runs` il wrapper
RIUSA `_force_clear_run` (single source of truth).

### `backtest.baseline.runner._csv_path_for(symbol, tf) -> Path`

**FIX B iter 3**: convenzione canonical path CSV storici.

Source verbatim `backtest/baseline/runner.py:105-115`:

```python
def _csv_path_for(symbol: str, tf: str) -> Path:
    """Convention reale repo: data/historical/{SYMBOL}/{TF}.csv (Phase 1 BACK-01).

    Plan 05-08 deviation Rule 3 — Blocker: il placeholder originale
    `data/{symbol}_{tf}.csv` (slice_worker default + runner pre-Plan 05-08)
    NON corrisponde al layout reale dei CSV storici 23.5y, che vivono in
    `data/historical/EURUSD/M15.csv`, `data/historical/GBPUSD/H1.csv`, ecc.
    Questa funzione viene chiamata dal runner come csv_path esplicito al
    worker (vedi 05-06a-SUMMARY deviation #4 e 05-07-SUMMARY).
    """
    return Path("data") / "historical" / symbol / f"{tf}.csv"
```

**FIX B iter 3** richiede che lo smoke wrapper RIUSI questa funzione
invece di hardcodare il path: previene drift se Phase 1 cambia convenzione
e fornisce error message coerente con il runner.

### `backtest.baseline.runner.run_baseline(force=False) -> list[dict]`

Source: `backtest/baseline/runner.py` (Plan 05-07 + bridge 05-08).
Riusato come-is dal nuovo wrapper. NO modifiche al runner. Recovery
`--only-runs` implementato via riuso `_force_clear_run` (FIX A iter 3),
NO duplicate logic SQL.

**Ordine init `runner.py:170+179`** (FIX F iter 3 — cosmetic alignment):

```python
enable_sqlite_wal(ledger_path)              # PRIMA (WAL mode persiste cross-connection)
LedgerWriter(ledger_path).ensure_schema()   # POI (schema setup su WAL gia' attivo)
```

Lo smoke wrapper deve seguire lo stesso ordine.

### `backtest.loader.load_bars(csv_path, symbol, tf, date_start, date_end) -> list[Bar]`

Source: `backtest/loader.py:30-31` (verificato). Supporta `datetime |
None` per filtri inclusive (date_start) / exclusive (date_end). Plan
05-09 riusa la stessa firma per smoke mode (date_start=2024-02-01,
date_end=2024-05-01, **range allargato** vs 1 mese originale — FIX 2).

### `backtest.ledger` schema (Phase 5 reale) — riferimento per --only-runs

Source: `backtest/ledger.py:19-79` (verbatim DDL).

```python
_DDL_BACKTEST_RUNS = """
CREATE TABLE IF NOT EXISTS backtest_runs (
    run_id      TEXT PRIMARY KEY,
    ...
    status      TEXT NOT NULL DEFAULT 'running'
)
"""

_DDL_BACKTEST_TRADES = """
CREATE TABLE IF NOT EXISTS backtest_trades (
    trade_id     INTEGER PRIMARY KEY AUTOINCREMENT,
    run_id       TEXT NOT NULL REFERENCES backtest_runs(run_id),
    fold_index   INTEGER,
    entry_time   TEXT NOT NULL,
    exit_time    TEXT NOT NULL,
    symbol       TEXT NOT NULL,
    timeframe    TEXT NOT NULL,
    direction    TEXT NOT NULL,
    entry_price  REAL NOT NULL,
    exit_price   REAL NOT NULL,
    sl           REAL,
    tp           REAL,
    lot_size     REAL NOT NULL,
    pnl_pips     REAL,
    pnl_usd      REAL,
    risk_usd     REAL,
    exit_reason  TEXT,
    setup_type   TEXT,
    confidence   REAL,
    decision_context_json TEXT
)
"""
```

**SOLO** queste due tabelle hanno `run_id`. `trades_log` (live trader)
NON ha `run_id` ed e' fuori dallo scope di `--only-runs`.

### `data/configs/baseline.yaml` schema (post-Plan 05-08)

```yaml
equity_initial_eur: 10000
slippage_seed: 42
timeout_bars: {M15: 96, M30: 96, H1: 120}
warm_up_min_bars: 200
max_workers: 9
parquet_compression: snappy
force_rerun: false
progress_bar: true
date_start: "2015-05-08"
date_end:   "2025-05-08"
training_data_dir: "data/training"
report_dir: ".planning/research"
equity_curves_dir: ".planning/research/baseline-equity-curves"
```

Plan 05-09 aggiunge UNA chiave: `dataset_schema_version: 2` (D-09-A).
NESSUNA altra modifica al file.

### Engine output contract (engine.run() -> dict)

Source: `backtest/engine.py:296-301`. Ritorna dict con keys: `run_id,
trades, equity_curve, bars_processed`. `trades` e' `list[dict]` con
schema `_row_for_ledger` (engine.py:327-360): include `entry_time`
(ISO8601 string), `exit_time`, `symbol`, `direction`, `entry_price`,
`exit_price`, `sl`, `tp`, `lot_size`, `pnl_pips`, `pnl_usd`,
`exit_reason`, `setup_type`, `confidence`, `decision_context_json`.

**Mapping `entry_time` -> bar index per snapshot extended:** la stringa
ISO8601 va riconvertita a unix timestamp via `datetime.fromisoformat()`
poi confrontata con `bar.time` (int unix) per trovare l'indice nel
`bars_dict` cache. Helper documentato in Task 2.

**`_iso(ts)` helper** (engine.py:75-76, READ-ONLY, non toccare):
```python
def _iso(ts: int) -> str:
    return datetime.fromtimestamp(ts, tz=timezone.utc).isoformat()
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estendi dataset_writer con whitelist schema-v2 (programmatic extraction) + unit test esteso</name>
  <files>
    backtest/baseline/dataset_writer.py,
    tests/test_baseline_dataset_writer.py
  </files>
  <read_first>
    - C:/trading-agent/backtest/baseline/dataset_writer.py (file completo, 85 LOC)
    - C:/trading-agent/tests/test_baseline_dataset_writer.py (test attuali 128 LOC)
    - C:/trading-agent/indicators/aggregate.py:49-142 (compute_all_extended signature + keys canoniche)
  </read_first>
  <behavior>
    - Test 1: `write_decisions_shard` con riga schema-v2 completa (le 37 chiavi extended + 5 meta + 17 chiavi base) round-trip ok via `pd.read_parquet`.
    - Test 2 (FIX 3 plan-checker iter 1): la whitelist `_SCHEMA_V2_REQUIRED_KEYS` e' costruita PROGRAMMATICAMENTE da `_build_required_keys_v2()` che invoca `compute_all_extended(dummy_bars, regime_cfg=None)` a import-time, restituendo `frozenset(snapshot.keys())`. Asserzione: `set(snapshot.keys()) == _SCHEMA_V2_REQUIRED_KEYS - {meta_keys}` (cioe' le keys-v2 indicators coincidono ESATTAMENTE con l'output di compute_all_extended). Previene drift se Phase 2 estende l'interface in futuro.
    - Test 3 (NUOVO FIX 3 plan-checker iter 1): `test_schema_v2_keys_match_compute_all_extended()` invoca compute_all_extended con 200 bar sintetici + regime_cfg valido (caricato da `data/configs/regime.yaml` con `symbol="EURUSD"`) e asserisce `set(snapshot.keys()) | {"profile","run_id","regime_state","decision_ts_utc","entry_ts_utc","exit_ts_utc"} == _SCHEMA_V2_REQUIRED_KEYS`.
    - Test 4: backward-compat: scrivere una riga con SOLO le 17 keys originali (mock di 05-08) NON fa crash; emette un singolo log.warning con prefisso `"schema-v2 fields missing:"` (per-shard, non per-row).
    - Test 5: edge: rows vuoto e/o regime_state=None -> nessun crash (parity con `test_empty_rows_no_crash` esistente).
  </behavior>
  <action>
    1. Editare `backtest/baseline/dataset_writer.py`:
       - Aggiungere helper modulo-level `_build_required_keys_v2() -> frozenset[str]`:
         ```python
         def _build_required_keys_v2() -> frozenset[str]:
             """Costruisce whitelist v2 dinamicamente da compute_all_extended() interface.

             Eseguito UNA VOLTA a import-time del modulo per evitare drift se
             Phase 2 estende compute_all_extended in futuro (FIX 3 plan-checker
             iter 1 Plan 05-09). regime_cfg=None ammesso (vedi
             indicators/aggregate.py:93 ternary).

             Aggiunge i 5 meta/timestamps Plan 05-09 (profile, run_id,
             decision_ts_utc, entry_ts_utc, exit_ts_utc). `regime_state`
             e' gia' presente in compute_all_extended output, qui idempotent.
             """
             from indicators import compute_all_extended
             dummy_bars = [
                 {
                     "time": 1700000000 + i * 60,
                     "open":  1.10,
                     "high":  1.11,
                     "low":   1.09,
                     "close": 1.10,
                     "volume": 100,
                 }
                 for i in range(200)
             ]
             snapshot_keys = set(compute_all_extended(dummy_bars, regime_cfg=None).keys())
             meta_keys = {
                 "profile",          # CONSERVATIVE|MODERATE|AGGRESSIVE
                 "run_id",           # baseline_{date}_{symbol}_{tf}_{profile}
                 "decision_ts_utc",  # bar idx-1 open (anchor conservativa vs D-22 — D-09-B)
                 "entry_ts_utc",     # bar idx open
                 "exit_ts_utc",      # variable per exit_reason
             }
             # regime_state e' gia' in snapshot_keys (compute_all_extended la include
             # come None se regime_cfg=None). NON aggiungiamo "regime" alias (FIX 9).
             return frozenset(snapshot_keys | meta_keys)


         _SCHEMA_V2_REQUIRED_KEYS: frozenset[str] = _build_required_keys_v2()
         ```
       - In `write_decisions_shard`, dopo il guard `if not rows`: log warning una volta per shard se `set(rows[0].keys()).isdisjoint(_SCHEMA_V2_REQUIRED_KEYS)` (cioe' nessuna delle keys-v2 presente — indica chiamante 05-08 legacy). Il warning NON e' un errore; il writer continua.
       - Aggiornare il docstring del modulo: aggiungere paragrafo "Plan 05-09: schema-v2" che cita D-09-A + FIX 3 + lista i 5 meta + rimanda a `_SCHEMA_V2_REQUIRED_KEYS` per la whitelist completa.
       - **Italiano** in commenti + log (CLAUDE.md §Convenzioni). Identificatori in inglese.

    2. Estendere `tests/test_baseline_dataset_writer.py`:
       - Aggiungere 5 nuovi test:
         * `test_decisions_schema_v2_round_trip(tmp_path)`: scrive 1 riga con TUTTE le keys-v2 (5 meta + 37 indicator + 17 base, valori reali plausibili), legge parquet, asserisce che `set(df.columns).issuperset(_SCHEMA_V2_REQUIRED_KEYS)`.
         * `test_schema_v2_required_keys_documented()`: importa `_SCHEMA_V2_REQUIRED_KEYS`, asserisce membership di ciascuna delle 5 meta (`profile, run_id, decision_ts_utc, entry_ts_utc, exit_ts_utc`) + un campione di 6 indicators chiave (`atr_14, ema_50, rsi_14, bb_upper, adx_14, macd`).
         * `test_schema_v2_keys_match_compute_all_extended()` (NUOVO FIX 3): invoca compute_all_extended con 200 bar sintetici + regime_cfg da `data/configs/regime.yaml` (symbol="EURUSD"), asserisce che `set(snapshot.keys())` e' subset di `_SCHEMA_V2_REQUIRED_KEYS` e che la differenza e' SOLO `{profile, run_id, decision_ts_utc, entry_ts_utc, exit_ts_utc}` (cioe' i 5 meta aggiunti dal writer).
         * `test_legacy_v1_shard_emits_warning(tmp_path, caplog)`: scrive una riga con SOLO le 17 keys legacy (es. usando `_DECISIONS_KEYS` esistente del file di test ma RIMUOVENDO i meta); asserisce che `caplog.records` contiene un WARNING con substring `"schema-v2 fields missing"`.
         * `test_regime_none_no_crash(tmp_path)`: scrive una riga con `regime_state=None` e `regime_atr_pct=None` (caso `regime_cfg=None`); legge parquet; asserisce `df.iloc[0]["regime_state"]` is NaN o None.

       NON modificare i 6 test pre-esistenti (backward-compat).

    3. Eseguire localmente per validare RED/GREEN:
       ```bash
       pytest tests/test_baseline_dataset_writer.py -x -v
       ```
       Atteso: 11 test pass (6 esistenti + 5 nuovi).

    4. Commit:
       ```
       feat(05-09): writer schema-v2 + whitelist programmatic from compute_all_extended (closes D-02, FIX 3)
       ```
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent && pytest tests/test_baseline_dataset_writer.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - 11/11 test pass in tests/test_baseline_dataset_writer.py (6 esistenti + 5 nuovi).
    - `_SCHEMA_V2_REQUIRED_KEYS` esportata da `backtest.baseline.dataset_writer`, costruita via `_build_required_keys_v2()` programmaticamente.
    - Docstring modulo aggiornato con riferimento Plan 05-09 D-09-A + FIX 3.
    - Drift-prevention test verde: la whitelist coincide ESATTAMENTE con compute_all_extended output (+ 5 meta).
    - Backward-compat verificata: chiamante 05-08 legacy produce solo un warning, nessun errore.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integra extended snapshot @ bar idx-1 + 5 meta/timestamps in slice_worker (FIX 1/4/9 + isolamento regime_cfg FIX D iter 3)</name>
  <files>
    backtest/baseline/slice_worker.py
  </files>
  <read_first>
    - C:/trading-agent/backtest/baseline/slice_worker.py (file completo, 411 LOC, focus su righe 252-411)
    - C:/trading-agent/backtest/engine.py:75-76 (_iso helper, READ-ONLY) + righe 296-360 (engine.run() output schema + `_row_for_ledger`)
    - C:/trading-agent/indicators/aggregate.py:49-142 (compute_all_extended signature)
    - C:/trading-agent/indicators/volatility.py:249-269 (load_regime_config signature reale — symbol + yaml_path)
    - C:/trading-agent/data/configs/regime.yaml (per-symbol thresholds)
  </read_first>
  <behavior>
    - Slice_worker.run_slice_3profiles produce decisions_rows arricchite: ogni trade del `engine_result["trades"]` ha aggiunto:
      - 5 meta/timestamps:
        * `profile` (str)
        * `run_id` (str)
        * `regime_state` (str canonical — derivato da snapshot, NO alias `regime` — FIX 9 plan-checker iter 1)
        * `decision_ts_utc` (str ISO8601 UTC = bar idx-1 open == 1 bar pre-entry, anchor conservativa vs D-22 — vedi D-09-B; FIX C iter 3 chiarisce semantica)
        * `entry_ts_utc` (str ISO8601 UTC = bar idx open, gia' calcolato da engine `entry_time`)
        * `exit_ts_utc` (str ISO8601 UTC = exit timestamp variable, gia' calcolato da engine `exit_time`)
      - 33 colonne extended-indicator: TUTTI i ritorni di `compute_all_extended(bars_view, regime_cfg=regime_cfg)` dove `bars_view = bars_dict[:idx]` (esclusivo: bar idx NON entra).
    - NO-LEAKAGE: `idx` calcolato come `bars_idx_by_time[trade["entry_time"]]`. Verifica esplicita che `bars_view = bars_dict[:idx]` ha lunghezza == idx (cioe' esclude il bar idx).
    - **ISOLAMENTO regime_cfg dal flow engine principale (FIX D iter 3):** la chiamata top-level `compute_all_extended(bars_dict)` a `slice_worker.py:270` RESTA INVARIATA (zero argomenti extra). `regime_cfg` per-symbol viene caricato in `run_slice_3profiles` ma usato SOLO dentro `_extract_extended_snapshot_at_entry` (post-engine enrichment). Conseguenze:
      * Indicator cache `indicators_full` usata dalla strategy detector resta identica a Plan 05-08
      * Flow detector -> proposal -> engine `_row_for_ledger` -> trade count = identico a Plan 05-08
      * Count trade atteso == 1076 ± 0 (parity stretta su 10y window, validato post-execution PC secondario)
      * `regime_state` / `regime_atr_pct` per-symbol popolano SOLO le colonne parquet, calcolate al post-engine snapshot
    - BAR-CLOSE DISCIPLINE (anchor conservativa D-09-B vs D-22 stretta, chiarito FIX C iter 3): `decision_ts_utc` = `_iso(bars_dict[idx-1]["time"])` (open del bar PRECEDENTE l'entry, in convenzione MT5 `bar.time = bar OPEN`). `entry_ts_utc` = `entry_time` engine row (open del bar idx). Gap atteso == timeframe_delta (15min per M15). Test invariant in Task 5 asserisce `decision_ts_utc < entry_ts_utc` rigoroso e gap == timeframe delta.
    - REGIME PER-SYMBOL (FIX 1): `regime_cfg = load_regime_config(symbol, Path("data/configs/regime.yaml"))` invocato UNA VOLTA per worker. I 3 profile sequenziali condividono `regime_cfg`. **NON passato a `compute_all_extended(bars_dict)` top-level (FIX D iter 3 isolamento).**
    - Backward-compat: il `decision_context_json` carry-forward dal `_build_ctx` engine resta invariato (solo lettura, non sovrascritto). I 5 meta + indicators sono colonne TOP-LEVEL nuove (D-09-A).
    - DEDUPLICATION (FIX 9): NO colonna alias `regime`. Si usa SOLO `regime_state` come canonical name (matcha compute_all_extended output). Documentazione: la colonna "profile" e' meta-livello (CONSERVATIVE/MODERATE/AGGRESSIVE), la colonna "regime_state" e' feature-livello (compressed/normal/expanded ATR-based).
  </behavior>
  <action>
    1. Imports da aggiungere nello slice_worker (lazy, sotto blocco esistente `# noqa: E402`):
       - `from indicators.volatility import load_regime_config` (lazy in funzione)
       - `from datetime import datetime, timezone` (gia' lazy in `_build_equity_dataframe`)

    2. Nuovo helper modulo-level `_build_bars_index_by_time(bars_dict: list[dict]) -> dict[int, int]`:
       Ritorna `{bar["time"]: idx for idx, bar in enumerate(bars_dict)}`. Pre-computato UNA VOLTA per slice in `run_slice_3profiles` dopo il warm-up slicing (linea 245). Italiano docstring spiega il pattern.

    3. Nuovo helper modulo-level `_extract_extended_snapshot_at_entry(bars_dict, bars_idx_by_time, entry_time_iso, regime_cfg) -> dict`:
       ```python
       def _extract_extended_snapshot_at_entry(
           bars_dict: list[dict],
           bars_idx_by_time: dict[int, int],
           entry_time_iso: str,
           regime_cfg: dict | None,
       ) -> dict:
           """D-09-B: snapshot extended al bar PRIMA del entry (no-leakage).

           bars_view = bars_dict[:idx] e' EXCLUSIVE di bars_dict[idx], cioe' il
           bar di entry NON entra nel calcolo. Replica le condizioni live: la
           strategy decide all'open di una nuova barra basandosi sulla
           history conclusa.

           FIX D iter 3 — isolamento: questa funzione e' L'UNICO consumer di
           `regime_cfg` per-symbol. La chiamata top-level
           `compute_all_extended(bars_dict)` a `slice_worker.py:270` resta
           senza regime_cfg, preservando parity con Plan 05-08 sul detector.

           Pattern: `bars_view = bars_dict[:idx]` (chiavata da key_links
           regex, FIX 8 plan-checker iter 1).
           """
           # Converti ISO -> unix int (compatibile con bars_dict[i]["time"])
           # entry_time_iso e' formato `datetime.fromtimestamp(...).isoformat()`
           # (vedi engine._iso linea 75-76).
           dt = datetime.fromisoformat(entry_time_iso)
           if dt.tzinfo is None:
               dt = dt.replace(tzinfo=timezone.utc)
           entry_ts = int(dt.timestamp())
           idx = bars_idx_by_time.get(entry_ts)
           if idx is None or idx == 0:
               # Edge: entry_time non in cache (NON dovrebbe succedere: engine
               # itera sugli stessi bars) oppure entry al primo bar (impossibile
               # post-warmup ma defensive). Ritorna dict vuoto -> writer
               # produrra' NaN nelle colonne extended.
               return {}
           # No-leakage: vista fino a (exclusive) idx — variabile esplicita
           # `bars_view` per key_links regex (FIX 8).
           bars_view = bars_dict[:idx]
           if not bars_view:
               return {}
           from indicators import compute_all_extended  # lazy
           snapshot = compute_all_extended(bars_view, regime_cfg=regime_cfg)
           return snapshot
       ```

    4. Modificare `run_slice_3profiles` (intorno alla linea 270 dopo
       `indicators_full = compute_all_extended(bars_dict)`):
       - **FIX 1 plan-checker iter 1 + FIX D iter 3 isolamento**: caricare regime_cfg PER-SYMBOL una volta (cache locale, scope worker). **NON passarlo a `compute_all_extended(bars_dict)` top-level — usarlo SOLO in `_extract_extended_snapshot_at_entry` post-engine.**
         ```python
         # FIX 1 Plan 05-09 iter 1: load_regime_config(symbol, yaml_path)
         # firma reale (indicators/volatility.py:249). regime_cfg per-symbol;
         # i 3 profile sequenziali nello stesso worker condividono questo dict
         # (immutable, safe da riusare).
         #
         # FIX D iter 3 — ISOLAMENTO: regime_cfg viene caricato qui ma usato
         # SOLO dentro _extract_extended_snapshot_at_entry (post-engine).
         # La chiamata `compute_all_extended(bars_dict)` di sopra (linea 270)
         # NON riceve regime_cfg, preservando esatta parity con Plan 05-08
         # sul flow strategy detector -> proposal -> trade generation.
         # Count trade atteso == 1076 ± 0 (invariante).
         from indicators.volatility import load_regime_config  # noqa: E402
         regime_cfg = load_regime_config(symbol, Path("data/configs/regime.yaml"))
         ```
       - Pre-computare `bars_idx_by_time = _build_bars_index_by_time(bars_dict)`.

       **CRITICAL — INVARIANT da NON violare:** la riga
       `indicators_full = compute_all_extended(bars_dict)` (slice_worker.py:270)
       RESTA INVARIATA. NON aggiungere `regime_cfg=regime_cfg` alla chiamata
       top-level. L'unico consumer di regime_cfg deve essere
       `_extract_extended_snapshot_at_entry` post-engine.

    5. Nel ciclo `for profile in PROFILES`, DOPO `engine_result = engine.run()` e
       PRIMA di `write_decisions_shard(decisions_rows, ...)` (intorno linea 338):
       ```python
       # D-09-A + D-09-B + FIX 4 plan-checker iter 1: arricchisci decisions_rows
       # con extended snapshot @ bar idx-1 + 5 meta/timestamps distinti
       # (anchor conservativa vs D-22, vedi D-09-B FIX C iter 3).
       # Iterazione list[dict] in-place (mutate).
       trades = engine_result.get("trades", []) or []
       for trade in trades:
           entry_iso = trade.get("entry_time")
           if not entry_iso:
               continue  # defensive: trade senza entry_time non puo' essere snapshotato
           snapshot = _extract_extended_snapshot_at_entry(
               bars_dict, bars_idx_by_time, entry_iso, regime_cfg,
           )
           # FIX C iter 3 — anchor conservativa vs D-22:
           # - decision_ts_utc = open del bar idx-1 (== 1 bar PRE-entry in
           #   convenzione MT5 bar.time = bar OPEN). PIU' conservativa di D-22
           #   stretta (che richiederebbe close di idx-1 == open di idx == entry_ts).
           #   Razionale: 1 bar di safety buffer al feature snapshot Phase 7.
           # - entry_ts_utc = open del bar di entry (idx) — gia' calcolato da engine
           # - exit_ts_utc = exit timestamp (variable per exit_reason)
           # Phase 7 walk-forward split usa decision_ts_utc (NON entry_ts_utc)
           # per evitare leakage: il classifier vede solo info disponibili
           # 1 bar prima dell'open di entry.
           # Gap atteso = timeframe_delta (15min M15, 30min M30, 60min H1).
           dt = datetime.fromisoformat(entry_iso)
           if dt.tzinfo is None:
               dt = dt.replace(tzinfo=timezone.utc)
           entry_ts_unix = int(dt.timestamp())
           idx_entry = bars_idx_by_time.get(entry_ts_unix)
           if idx_entry is not None and idx_entry >= 1:
               # Bar idx-1 open = decision timestamp (anchor conservativa)
               decision_ts_unix = bars_dict[idx_entry - 1]["time"]
               decision_ts_iso = datetime.fromtimestamp(
                   decision_ts_unix, tz=timezone.utc
               ).isoformat()
           else:
               # Edge defensivo: caso impossibile post-warmup. Fallback su entry_iso
               # (mantenibile per safety; test invariant catchera' se accade).
               decision_ts_iso = entry_iso
           # 5 meta/timestamps (D-09-A + FIX 4 + FIX 9)
           trade["run_id"] = run_id
           trade["profile"] = profile
           trade["decision_ts_utc"] = decision_ts_iso  # bar idx-1 open (anchor conservativa D-09-B)
           trade["entry_ts_utc"] = entry_iso            # bar idx open (engine row)
           trade["exit_ts_utc"] = trade.get("exit_time")  # exit timestamp (variable)
           # FIX 9: NO alias `regime` (deduplicate). Usa regime_state come canonical
           # name (gia' presente in snapshot da compute_all_extended). setdefault
           # garantisce che se snapshot e' vuoto (edge) NON soprascriva con None.
           if "regime_state" not in trade and snapshot:
               trade["regime_state"] = snapshot.get("regime_state")
           # Merge snapshot completo (le 37 keys); setdefault evita override
           # di keys gia' presenti nel _row_for_ledger (paranoia: al momento
           # NON ci sono collisioni — engine usa entry_time/exit_price/ecc.,
           # disjoint dalle keys di compute_all_extended).
           for k, v in snapshot.items():
               trade.setdefault(k, v)
       decisions_rows = trades  # nome riusato per chiarezza, lista mutata in-place
       ```

       NOTA: `setdefault` previene override paranoico — se in futuro
       compute_all_extended dovesse aggiungere una key che collide con
       _row_for_ledger, il merge NON sovrascrive il valore engine.

    6. **FIX E iter 3 — dedup test:** NON aggiungere `test_regime_cfg_resolved_per_symbol` in `tests/test_baseline_runner.py` da questo task. Il test e' centralizzato esclusivamente in Task 5 (suite test_baseline_no_leakage_extended /  test_baseline_runner — single definition, no duplicate function name).

       Eseguire la suite Phase 5 esistente per verifica backward-compat:
       ```bash
       pytest tests/test_baseline_runner.py tests/test_baseline_dataset_writer.py tests/test_baseline_no_future_leakage.py -x
       ```

    7. **Italiano** in tutti i commenti e log nuovi.

    8. Commit:
       ```
       feat(05-09): slice_worker extended snapshot @ bar idx-1 + 5 meta/timestamps (D-09-A/B/G + FIX 1/4/9 + isolamento regime_cfg FIX D iter 3)
       ```
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent && pytest tests/test_baseline_runner.py tests/test_baseline_dataset_writer.py tests/test_baseline_no_future_leakage.py -x 2>&1 | tail -30 && grep -n "compute_all_extended(bars_dict)" backtest/baseline/slice_worker.py | grep -v regime_cfg</automated>
  </verify>
  <done>
    - `_build_bars_index_by_time` e `_extract_extended_snapshot_at_entry` definiti come helper modulo-level in slice_worker.
    - `load_regime_config(symbol, yaml_path)` invocato con 2 argomenti corretti (FIX 1).
    - **INVARIANTE FIX D iter 3:** `compute_all_extended(bars_dict)` top-level a linea 270 RESTA SENZA regime_cfg (verificato da grep).
    - Il ciclo `for profile in PROFILES` arricchisce ogni trade con 5 meta/timestamps + 33 indicator extended cols.
    - `bars_view = bars_dict[:idx]` slicing exclusivo (no bar di entry).
    - `decision_ts_utc` = open del bar idx-1 (anchor conservativa D-09-B), `entry_ts_utc` = open bar idx.
    - NO colonna alias `regime`; solo `regime_state` canonical (FIX 9).
    - Suite Phase 5 pre-esistente verde (backward-compat).
    - Italiano in commenti/log.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bump dataset_schema_version in baseline.yaml</name>
  <files>data/configs/baseline.yaml</files>
  <read_first>
    - C:/trading-agent/data/configs/baseline.yaml (34 righe, file completo)
  </read_first>
  <action>
    1. Editare `data/configs/baseline.yaml` aggiungendo, DOPO la riga
       `progress_bar: true` (linea 20) e PRIMA del blocco `# Date range`:

       ```yaml
       # Plan 05-09: schema-v2 bump (D-09-A).
       # v1 = 17 cols flat (Plan 05-08, archiviato in
       #      data/training/baseline_decisions.pre-05-09 per backup).
       # v2 = 17 base + ~33 extended (compute_all_extended) + 5 meta
       #      (profile, run_id, decision_ts_utc bar idx-1 open == 1 bar
       #      pre-entry anchor conservativa D-09-B, entry_ts_utc bar idx
       #      open, exit_ts_utc) = ~55+ cols totali.
       #      `regime_state` (canonical, NO alias `regime`) e' gia' parte
       #      delle 33 extended (compute_all_extended output).
       # Modificare questo valore invalida baseline_yaml_hash -> re-run
       # baseline obbligatorio (auditabile in backtest_runs.baseline_yaml_hash).
       dataset_schema_version: 2
       ```

    2. NON modificare nessun'altra chiave:
       - `equity_initial_eur`, `slippage_seed`, `timeout_bars`,
         `warm_up_min_bars`, `max_workers (9 — D-09-E locked)`,
         `parquet_compression`, `force_rerun`, `progress_bar`,
         `date_start`, `date_end`, `training_data_dir`, `report_dir`,
         `equity_curves_dir` -> TUTTE invariate.

    3. NON caricare `dataset_schema_version` in `BaselineConfig` dataclass
       (out of scope: nessun consumer attualmente legge questo campo; e'
       puramente documentativo + entry per hash invalidation). Plan 05-09
       lo aggiunge come marker, futuri plan possono leggerlo se serve.

    4. Verifica yaml valido:
       ```bash
       python -c "import yaml; cfg = yaml.safe_load(open('data/configs/baseline.yaml')); assert cfg.get('dataset_schema_version') == 2, cfg; print('OK schema_version =', cfg['dataset_schema_version'])"
       ```

    5. Commit:
       ```
       chore(05-09): baseline.yaml dataset_schema_version: 2 (hash invalidation)
       ```
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent && python -c "import yaml; cfg = yaml.safe_load(open('data/configs/baseline.yaml')); assert cfg.get('dataset_schema_version') == 2, cfg; assert cfg.get('max_workers') == 9, 'D-09-E violated'; print('OK schema_version =', cfg['dataset_schema_version'], 'max_workers =', cfg['max_workers'])"</automated>
  </verify>
  <done>
    - `data/configs/baseline.yaml` contiene `dataset_schema_version: 2`.
    - `max_workers: 9` invariato (D-09-E enforce).
    - yaml.safe_load non solleva errori.
    - Baseline.yaml hash invalidera' i Plan 05-08 run_id (force-rerun necessario su PC secondario).
  </done>
</task>

<task type="auto">
  <name>Task 4: scripts/run_baseline_05_09.py wrapper CLI standalone (FIX 2/5/6/7/10 + FIX A/B/F iter 3)</name>
  <files>scripts/run_baseline_05_09.py</files>
  <read_first>
    - C:/trading-agent/scripts/run_baseline_backtest.py (81 LOC, pattern wrapper esistente)
    - C:/trading-agent/backtest/baseline/runner.py (run_baseline + _csv_path_for signature)
    - C:/trading-agent/backtest/baseline/slice_worker.py:59-74 (_force_clear_run pattern canonical — FIX A iter 3)
    - C:/trading-agent/backtest/ledger.py:19-79 (schema reale Phase 5: backtest_runs + backtest_trades, NO trades_log con run_id)
    - C:/trading-agent/backtest/baseline/wal_setup.py:22-35 (enable_sqlite_wal signature)
    - C:/trading-agent/.planning/SETUP-SECONDARY-PC.md §0-3 (constraint operativi PC secondario)
  </read_first>
  <action>
    Creare `scripts/run_baseline_05_09.py` (NUOVO file) seguendo il
    template di `scripts/run_baseline_backtest.py` esteso con le 10 FIX
    plan-checker iter 1 + le 6 FIX iter 3 (A/B/C/D/E/F).

    ```python
    r"""Plan 05-09 wrapper backtest baseline — schema-v2 + re-run notturno.

    Eseguibile stand-alone sul PC secondario (no dipendenza da Claude Code).
    Riusa backtest.baseline.runner.run_baseline come backend; aggiunge:
      - --smoke: 3 mesi x 1 pair x 1 TF x 3 profile (FIX 2 plan-checker
        iter 1: range allargato da 1 mese -> 3 mesi per probabilita' >=1
        trade aggregato; target <180s, >300s indica regressione)
      - --only-runs run_id1,run_id2,...: whitelist recovery (FIX 6
        plan-checker iter 1 + FIX A iter 3: implementazione REALE via
        riuso di slice_worker._force_clear_run — pattern canonical
        Phase 5 con tabelle reali `backtest_runs` + `backtest_trades`,
        NO `trades_log` (quello e' live trader, schema senza run_id).
      - schema-validation post-run (>=30 indicators cols + 5 meta non-null);
        smoke-tolerant mode (FIX 2) per smoke con 0 trade non bloccante

    Uso:
        # Smoke locale (validazione veloce wrapper su PC primario o secondario):
        python scripts/run_baseline_05_09.py --smoke

        # Full run notturno (PC secondario):
        python scripts/run_baseline_05_09.py --force

        # Recovery dopo crash parziale (FIX 6 + FIX A iter 3:
        # riuso _force_clear_run + relaunch):
        python scripts/run_baseline_05_09.py --only-runs baseline_2026-05-11_EURUSD_M15_AGGRESSIVE

    Exit code:
        0 = OK (full o smoke completati, schema validato, count >= soglia)
        1 = errore generico (eccezione runtime, import fallito)
        2 = wall-clock superato (--max-wall-clock gate)
        3 = schema-validation KO (post-run, mancano col indicators o meta)
        4 = decision count sotto hard gate 1050 (margine 2.5% vs 1076 — FIX D iter 3)
    """
    from __future__ import annotations

    import argparse
    import logging
    import sys
    import time as _time
    from pathlib import Path

    ROOT = Path(__file__).resolve().parent.parent
    if str(ROOT) not in sys.path:
        sys.path.insert(0, str(ROOT))

    _log = logging.getLogger("run_baseline_05_09")


    def _validate_parquet_schema(parquet_dir: Path, min_rows: int) -> tuple[bool, str]:
        """D-09-D: schema-validation post-run.

        FIX 2 plan-checker iter 1: smoke-tolerant mode quando min_rows=0.
        - Se parquet_dir non esiste -> ritorna OK (smoke con 0 trade)
        - Se 0 rows -> ritorna OK (no schema check)
        Quando min_rows > 0 (full run): comportamento standard (errore se
        mancano colonne o row count basso).

        FIX 2C bug fix: usa pd.isna() invece di `is None` per check NULL meta.

        Ritorna (ok, message).
        """
        import pandas as pd
        import pyarrow.dataset as ds
        from backtest.baseline.dataset_writer import _SCHEMA_V2_REQUIRED_KEYS

        # SMOKE-TOLERANT MODE (FIX 2 plan-checker iter 1)
        if min_rows == 0:
            if not parquet_dir.exists():
                _log.warning(
                    "smoke: parquet dir %s mancante -> 0 trade su window, "
                    "schema validation skipped (non-blocking).",
                    parquet_dir,
                )
                return True, "smoke: 0 trade su window, schema validation skipped (non-blocking)"
            try:
                dataset = ds.dataset(str(parquet_dir), format="parquet")
                n = dataset.count_rows()
            except Exception:  # noqa: BLE001 — corruption o dir vuota
                _log.warning("smoke: parquet dir vuota o non leggibile -> skip validation")
                return True, "smoke: 0 trade, no schema check"
            if n == 0:
                return True, "smoke: 0 trade, no schema check"
            # Smoke con >=1 trade: applica schema check completo
            # (caso atteso: 3 mesi window dovrebbe produrre >=1 trade)

        # FULL/SMOKE-WITH-DATA MODE
        if not parquet_dir.exists():
            return False, f"parquet dir mancante: {parquet_dir}"
        try:
            dataset = ds.dataset(str(parquet_dir), format="parquet")
        except Exception as exc:  # noqa: BLE001
            return False, f"pyarrow load fallito: {exc}"
        n = dataset.count_rows()
        if n < min_rows:
            return False, f"row count {n} < hard gate {min_rows}"
        schema_names = set(dataset.schema.names)
        missing = _SCHEMA_V2_REQUIRED_KEYS - schema_names
        if missing:
            return False, f"missing cols schema-v2: {sorted(missing)[:10]}... (total {len(missing)})"

        # FIX 10 INFO 2: single head(1) call (era 2 query separate, marginal saving)
        table = dataset.head(1)
        df = table.to_pandas()
        # FIX 2C: pd.isna() invece di `is None` (Python None check non match pd.NaT)
        for meta in ("run_id", "profile", "regime_state", "decision_ts_utc",
                     "entry_ts_utc", "exit_ts_utc"):
            if meta not in df.columns:
                return False, f"meta {meta} mancante (col assente)"
            if pd.isna(df[meta].iloc[0]):
                return False, f"meta {meta} NULL nella prima riga"
        # profile validation: leggi UNIQUE profiles da seconda query (necessaria
        # perche' head(1) ha solo 1 valore; non possiamo accorpare in 1 read)
        profiles_table = dataset.to_table(columns=["profile"]).to_pandas()
        bad = set(profiles_table["profile"].unique()) - {"CONSERVATIVE", "MODERATE", "AGGRESSIVE"}
        if bad:
            return False, f"profile valori inattesi: {bad}"
        return True, f"OK schema-v2: {n} rows, {len(schema_names)} cols"


    def _run_smoke() -> int:
        """Smoke: 1 pair x 1 TF x 3 profile x 3 mesi (FIX 2: range allargato).

        Wall-clock target <180s sul PC secondario (Ryzen 7 5800H);
        >300s indica regressione (FIX 10 INFO 3).

        FIX B iter 3: csv_path via _csv_path_for() del runner (NO hardcoded).
        FIX F iter 3: ordine init = enable_sqlite_wal() PRIMA, LedgerWriter() POI
        (allinea a runner.py:170-179).
        """
        import shutil
        from dataclasses import replace
        from backtest.baseline.runner import load_baseline_config, _csv_path_for
        from backtest.baseline.slice_worker import run_slice_3profiles
        from backtest.baseline.dataset_writer import finalize_parquet_shards
        from backtest.baseline.wal_setup import enable_sqlite_wal
        from backtest.ledger import LedgerWriter

        baseline_cfg = load_baseline_config(ROOT / "data/configs/baseline.yaml")

        # FIX 7 plan-checker iter 1: path absolute per smoke (evita CWD-dependence)
        smoke_data = str(ROOT / "data" / "training" / "_smoke_05_09")
        smoke_equity = str(ROOT / ".planning" / "research" / "_smoke_equity_curves")

        # FIX 2 plan-checker iter 1: range allargato da 1 mese -> 3 mesi
        # per probabilita' >=1 trade aggregato su 3 profile in MODERATE/AGGRESSIVE.
        smoke_cfg = replace(
            baseline_cfg,
            date_start="2024-02-01",
            date_end="2024-05-01",  # 3 mesi
            training_data_dir=smoke_data,
            equity_curves_dir=smoke_equity,
        )
        smoke_dir = Path(smoke_data)
        smoke_equity_dir = Path(smoke_equity)
        if smoke_dir.exists():
            shutil.rmtree(smoke_dir)
        if smoke_equity_dir.exists():
            shutil.rmtree(smoke_equity_dir)
        smoke_dir.mkdir(parents=True, exist_ok=True)
        smoke_equity_dir.mkdir(parents=True, exist_ok=True)

        ledger_db = ROOT / "logs/trades_smoke_05_09.db"
        if ledger_db.exists():
            ledger_db.unlink()
        # FIX F iter 3: ordine init allineato a runner.py:170-179.
        # WAL prima (persiste cross-connection), LedgerWriter dopo (crea schema
        # sulla WAL gia' attiva).
        enable_sqlite_wal(ledger_db)
        LedgerWriter(ledger_db)

        # FIX B iter 3: csv_path via _csv_path_for(symbol, tf) del runner.
        # Convenzione canonical: data/historical/{SYMBOL}/{TF}.csv. NO hardcoded.
        # Se il file manca produciamo un error log esplicito e fallback exit code.
        csv_path = _csv_path_for("EURUSD", "M15")
        # _csv_path_for ritorna Path relativo ("data/historical/EURUSD/M15.csv").
        # Risolviamo rispetto a ROOT per absolute path (consistenza con FIX 7).
        csv_path_abs = (ROOT / csv_path).resolve()
        if not csv_path_abs.exists():
            _log.error(
                "Smoke fallback: CSV %s non trovato. Verifica layout data/historical/.",
                csv_path_abs,
            )
            return 2

        _log.info(
            "smoke 05-09: EURUSD M15 (3 profile) 2024-02-01 -> 2024-05-01 (3 mesi), csv=%s",
            csv_path_abs,
        )
        results = run_slice_3profiles(
            symbol="EURUSD",
            tf="M15",
            baseline_cfg=smoke_cfg,
            costs_cfg_path=ROOT / "data/configs/costs.yaml",
            strategy_cfg_path=ROOT / "config/strategy.yaml",
            force=True,
            ledger_db_path=ledger_db,
            run_date="2026-05-11-smoke",
            csv_path=csv_path_abs,
        )
        finalize_parquet_shards(smoke_dir)
        n_ok = sum(1 for r in results if r.get("status") == "OK")
        _log.info("smoke results: %d/3 ok", n_ok)
        # FIX 2 plan-checker iter 1: smoke-tolerant validation (min_rows=0)
        parquet_dir = smoke_dir / "baseline_decisions"
        ok, msg = _validate_parquet_schema(parquet_dir, min_rows=0)
        if not ok:
            print(f"SMOKE SCHEMA KO: {msg}", file=sys.stderr)
            return 3
        print(f"SMOKE OK: {msg}")
        return 0


    def _only_runs_pre_delete(ledger_db: Path, run_ids: list[str]) -> None:
        """FIX A iter 3 — riuso di slice_worker._force_clear_run.

        Schema reale Phase 5 (vedi backtest/ledger.py:19-79):
          - backtest_runs(run_id PK)
          - backtest_trades(run_id FK virtuale, AUTOINCREMENT trade_id)

        `trades_log` (logs/trades.db, schema live trader) NON ha run_id e
        NON deve essere toccata da --only-runs.

        Pattern canonical: per ciascun run_id, invoca _force_clear_run
        che esegue DELETE FROM backtest_trades + backtest_runs in single
        connection con busy_timeout=30s. Riuso garantisce single source
        of truth con il --force path del runner.
        """
        from backtest.baseline.slice_worker import _force_clear_run

        _log.info(
            "FIX A iter 3 --only-runs: pre-delete via _force_clear_run per %d run_id",
            len(run_ids),
        )
        for rid in run_ids:
            _force_clear_run(ledger_db, rid)
        _log.info(
            "Pre-delete completato per %d run_id (--only-runs whitelist).",
            len(run_ids),
        )


    def _run_full(args, ROOT_PATH: Path) -> int:
        """Full 27/27 run con supporto --only-runs (FIX 6 + FIX A iter 3).

        FIX A iter 3: --only-runs implementato via riuso _force_clear_run
        (slice_worker.py:59-74) — single source of truth, NO SQL custom.
        Sfrutta D-14 idempotency (Plan 05-08): dopo il pre-delete, il
        run_baseline(force=False) skippera' i run completi e rilancera'
        solo quelli appena cancellati.
        """
        from backtest.baseline.runner import load_baseline_config, run_baseline

        cfg = load_baseline_config(ROOT_PATH / "data/configs/baseline.yaml")

        # FIX 6 plan-checker iter 1 + FIX A iter 3: --only-runs riuso pattern canonical
        if args.only_runs:
            run_ids = [rid.strip() for rid in args.only_runs.split(",") if rid.strip()]
            if run_ids:
                ledger_db_path = ROOT_PATH / "logs" / "trades.db"
                _only_runs_pre_delete(ledger_db_path, run_ids)
                # Pulizia parquet shard per quei run_id (se ancora presenti
                # pre-finalize) — il re-finalize sovrascrivera' part-*.parquet
                # con existing_data_behavior gia' impostato in dataset_writer.py.
                training_dir = ROOT_PATH / "data" / "training"
                for rid in run_ids:
                    shard = training_dir / "baseline_decisions" / (
                        f"baseline_decisions_{rid}.parquet"
                    )
                    if shard.exists():
                        shard.unlink()

        start = _time.monotonic()
        results = run_baseline(force=args.force)
        wall = _time.monotonic() - start
        n_total = len(results)
        n_ok = sum(1 for r in results if r.get("status") == "OK")
        n_failed = sum(1 for r in results if r.get("status") == "FAILED")
        n_skipped = sum(1 for r in results if r.get("status") == "SKIPPED")

        print(f"WALL_CLOCK_SECONDS={wall:.0f}")
        print(f"RESULTS={n_ok}/{n_total} ok (skipped={n_skipped} failed={n_failed})")

        if not args.no_time_gate and wall > args.max_wall_clock:
            print(
                f"WARN: wall-clock {wall:.0f}s > max {args.max_wall_clock}s "
                f"(BACK-07 SC#1 violato — Rule 4 deviation Plan 05-08 already accepted).",
                file=sys.stderr,
            )
            # Plan 05-09 NON failha su wall-clock (Phase 5 perf opt deferred);
            # ritorna 0 se 27/27 ok, ma warning visibile in stderr.

        # Schema validation post-run — FIX D iter 3: min_rows=1050 (parity 1076 + 2.5% margine)
        parquet_dir = ROOT_PATH / "data/training/baseline_decisions"
        ok, msg = _validate_parquet_schema(parquet_dir, min_rows=1050)
        if not ok:
            print(f"SCHEMA KO: {msg}", file=sys.stderr)
            return 3
        print(f"SCHEMA OK: {msg}")

        if n_failed > 0:
            print(f"WARN: {n_failed} run falliti — vedi log", file=sys.stderr)
            return 1
        return 0


    def main() -> int:
        logging.basicConfig(
            level=logging.INFO,
            format="%(asctime)s %(levelname)s %(name)s: %(message)s",
        )
        parser = argparse.ArgumentParser(
            description="Plan 05-09 baseline wrapper (schema-v2 re-run + smoke).",
        )
        parser.add_argument(
            "--smoke", action="store_true",
            help="Smoke mini-run (1 pair x 1 TF x 3 profile x 3 mesi) per validare wrapper.",
        )
        parser.add_argument(
            "--force", action="store_true",
            help="Re-run anche se run_id esiste (overwrite ledger + parquet).",
        )
        parser.add_argument(
            "--only-runs", default="",
            help="Comma-separated whitelist di run_id (recovery, riuso _force_clear_run).",
        )
        parser.add_argument(
            "--max-wall-clock", type=int, default=14400,
            help="Soft warning su wall-clock seconds (default 14400 = 4h, "
                 "atteso ~3h18m sul PC secondario; NO hard gate Plan 05-09).",
        )
        parser.add_argument(
            "--no-time-gate", action="store_true",
            help="Disabilita time-gate (debug).",
        )
        args = parser.parse_args()

        if args.smoke:
            return _run_smoke()

        return _run_full(args, ROOT)


    if __name__ == "__main__":
        raise SystemExit(main())
    ```

    **Note operative cruciali:**
    - Lo smoke mode bypassa il runner ProcessPool e invoca DIRETTAMENTE
      `run_slice_3profiles` (single-process) — questo lo rende veloce e
      debuggabile sul PC primario. Il full mode usa `run_baseline` (Pool).
    - Lo smoke usa un `ledger_db` separato (`logs/trades_smoke_05_09.db`)
      per non contaminare il ledger di produzione `logs/trades.db`.
    - **FIX 6 + FIX A iter 3**: `--only-runs` IMPLEMENTATO via riuso di
      `slice_worker._force_clear_run` (single source of truth con il
      --force path del runner). NO SQL custom, NO loop su `trades_log`
      (che non ha run_id). Schema reale: `backtest_runs` + `backtest_trades`.
    - **FIX B iter 3**: smoke `csv_path` via `_csv_path_for("EURUSD", "M15")`
      del runner — NO path hardcoded. Convenzione canonical:
      `data/historical/EURUSD/M15.csv`. Error log esplicito se manca.
    - **FIX F iter 3**: ordine init `enable_sqlite_wal()` PRIMA,
      `LedgerWriter()` DOPO — allinea a `runner.py:170-179`.
    - **FIX 2**: smoke range allargato a 3 mesi (2024-02-01 -> 2024-05-01)
      per garantire >=1 trade nella validation; smoke-tolerant mode in
      `_validate_parquet_schema` evita falsi negativi se 0 trade.
    - **FIX 7**: tutti i path smoke sono absolute (`ROOT / "data" / ...`),
      cleanup esteso a `_smoke_equity_curves/` insieme a `_smoke_05_09/`.
    - **FIX D iter 3**: hard gate row count = 1050 (parity 1076 con margine 2.5%).
    - Il time-gate NON e' hard come Plan 05-08 (BACK-07 SC#1 gia' violato
      e Rule 4 user-accepted); resta solo come warning visibile.

    **Italiano** in docstring + log + commenti.

    Commit:
    ```
    feat(05-09): scripts/run_baseline_05_09.py wrapper CLI con --only-runs riuso _force_clear_run (D-09-D + FIX 2/5/6/7/10 + FIX A/B/F iter 3)
    ```
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent && python scripts/run_baseline_05_09.py --help 2>&1 | head -30 && python -c "import importlib.util, sys; spec = importlib.util.spec_from_file_location('rb05', 'scripts/run_baseline_05_09.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert callable(m.main), 'main missing'; assert callable(m._run_smoke), 'smoke missing'; assert callable(m._run_full), 'full missing'; assert callable(m._validate_parquet_schema), 'validator missing'; assert callable(m._only_runs_pre_delete), 'only_runs_pre_delete missing'; print('OK module surface')"</automated>
  </verify>
  <done>
    - `scripts/run_baseline_05_09.py` esiste, importa senza errori.
    - `python scripts/run_baseline_05_09.py --help` mostra le 5 flags (`--smoke, --force, --only-runs, --max-wall-clock, --no-time-gate`).
    - Funzioni `_run_smoke`, `_run_full`, `_validate_parquet_schema`, `_only_runs_pre_delete` esposte e callable.
    - `--only-runs` IMPLEMENTATO via riuso `_force_clear_run` (FIX A iter 3): single source of truth, NO SQL custom su `trades_log`.
    - Smoke usa `_csv_path_for("EURUSD", "M15")` (FIX B iter 3) — NO path hardcoded.
    - Ordine init WAL prima, LedgerWriter dopo (FIX F iter 3) — allinea runner.py:170-179.
    - Smoke usa range 3 mesi (FIX 2) + path absolute (FIX 7) + cleanup _smoke_equity_curves (FIX 7).
    - `_validate_parquet_schema` ha smoke-tolerant mode (FIX 2) + `pd.isna()` (FIX 2C) + min_rows=1050 full (FIX D iter 3).
    - NO esecuzione effettiva del run in questo task (esecuzione su PC secondario, STEP 3 RESUME-PLAN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: no-leakage invariant test + decision_ts&lt;entry_ts (anchor conservativa D-09-B) + smoke integration + --only-runs unit test (FIX A/E iter 3)</name>
  <files>
    tests/test_baseline_no_leakage_extended.py,
    tests/test_baseline_runner.py
  </files>
  <read_first>
    - C:/trading-agent/backtest/baseline/slice_worker.py (focus sui nuovi helper Task 2 + _force_clear_run riga 59-74)
    - C:/trading-agent/backtest/ledger.py:19-79 (schema reale backtest_runs + backtest_trades per setup test)
    - C:/trading-agent/tests/test_baseline_no_future_leakage.py (test pattern esistente)
    - C:/trading-agent/tests/test_baseline_runner.py (per estensione smoke test)
    - C:/trading-agent/scripts/run_baseline_05_09.py (Task 4)
  </read_first>
  <behavior>
    - Test A (`test_baseline_no_leakage_extended.py`, NUOVO): l'helper `_extract_extended_snapshot_at_entry`, dato un `bars_dict` di 100 bar sintetici monotonicamente crescenti + `bars_idx_by_time` precomputato + `entry_time_iso` corrispondente al bar 50, invoca `compute_all_extended` con `len(bars_arg) == 50` (NON 51). Mock di compute_all_extended che cattura `len()` di arg[0] e lo registra in un list[int]. Invarianti:
      - len(captured) == 1 (chiamato esattamente una volta)
      - captured[0] == 50 (no leakage: bar 50 escluso)
      - dict ritornato dal mock e' veicolato verbatim al caller
    - Test B (`test_baseline_no_leakage_extended.py`): edge `idx == 0` -> ritorna `{}` senza chiamare compute_all_extended.
    - Test C (`test_baseline_no_leakage_extended.py`): edge `entry_ts non in bars_idx_by_time` -> ritorna `{}`.
    - **Test D (`test_baseline_no_leakage_extended.py`, FIX 4 + FIX C iter 3):** `test_no_leakage_invariant_decision_before_entry()` simula il ciclo di Task 2 §5 con bars sintetici M15 (gap 900s), entry al bar idx=50. Asserisce:
      - `decision_ts_utc < entry_ts_utc` rigorosamente (datetime comparison)
      - `entry_ts_utc - decision_ts_utc == timedelta(minutes=15)` (timeframe M15 delta esatto)
      - In convenzione MT5 (`bar.time = bar OPEN`), `decision_ts` corrisponde a `bars_dict[idx-1]["time"]` (open di idx-1 = anchor conservativa D-09-B; piu' conservativa di D-22 stretta che richiederebbe `bars_dict[idx]["time"]` == entry_ts gap=0)
    - **Test E (`test_baseline_runner.py`, FIX 1 + FIX E iter 3):** `test_regime_cfg_resolved_per_symbol` SINGLE DEFINITION in `tests/test_baseline_runner.py` (NO duplicato da Task 2). Verifica signature `load_regime_config(symbol, yaml_path)` reale.
    - Test F (`test_baseline_runner.py`, ESTESO): `test_smoke_05_09_wrapper_runs()` invoca `scripts/run_baseline_05_09.py --smoke` via subprocess, asserisce exit code 0 e stdout contains "SMOKE OK". TIMEOUT pytest 240s (smoke target <180s; margine OS overhead 60s).
    - **Test G (`test_baseline_runner.py`, FIX 6 + FIX A iter 3):** `test_only_runs_pre_delete_and_relaunch()` con mock di `run_baseline` + tmp_path SQLite db con schema REALE Phase 5 (`backtest_runs` + `backtest_trades` matchando `_DDL_BACKTEST_TRADES` di `backtest/ledger.py:56-78`). Pre-seedato con riga `backtest_runs(run_id='abc', status='done')` + riga `backtest_trades(run_id='abc', ...)`. Invoca `_run_full(args=Namespace(only_runs='abc', force=False, ...))` e verifica:
      - Prima del run, le query `SELECT * FROM backtest_runs WHERE run_id='abc'` e `SELECT * FROM backtest_trades WHERE run_id='abc'` ritornavano 1 riga ciascuna
      - Dopo il pre-delete (eseguito da `_only_runs_pre_delete` che riusa `_force_clear_run`), ENTRAMBE le tabelle hanno 0 righe per quel run_id
      - `run_baseline` viene chiamato esattamente 1 volta con `force=False`
      - **NO accesso a `trades_log`** (verifica defensive: se il pre-delete tentasse di toccare `trades_log` con colonna `run_id`, solleverebbe `OperationalError: no such column`)
  </behavior>
  <action>
    1. Creare `tests/test_baseline_no_leakage_extended.py`:
       ```python
       """Test invariante no-leakage: snapshot extended @ bar idx-1 (Plan 05-09 D-09-B + FIX 4 + FIX C iter 3).

       Verifica che _extract_extended_snapshot_at_entry usi `bars_dict[:idx]`
       (EXCLUSIVE del bar di entry), garantendo che gli indicatori del trade
       riflettano la history visibile alla strategy PRIMA dell'open del bar
       in cui il trade entra.

       Inoltre (FIX 4 + FIX C iter 3): decision_ts_utc < entry_ts_utc rigorosamente,
       gap == timeframe delta (15min per M15) — anchor CONSERVATIVA vs D-22 stretta
       (1 bar di safety buffer pre-entry) — prerequisito per walk-forward Phase 7
       senza off-by-one-bar leakage residuo da gap-di-sessione.
       """
       from __future__ import annotations

       from datetime import datetime, timedelta, timezone
       from unittest.mock import patch

       import pytest


       def _make_bars_dict(n: int = 100, interval_seconds: int = 900) -> list[dict]:
           """Bar sintetici monotonicamente crescenti.

           Default 900s = 15min (M15 timeframe) per testare invariante
           anchor conservativa D-09-B (gap = timeframe delta).
           """
           base_ts = int(datetime(2024, 1, 1, tzinfo=timezone.utc).timestamp())
           return [
               {
                   "time": base_ts + i * interval_seconds,
                   "open": 1.10 + i * 0.0001,
                   "high": 1.10 + i * 0.0001 + 0.0005,
                   "low": 1.10 + i * 0.0001 - 0.0005,
                   "close": 1.10 + i * 0.0001 + 0.0002,
                   "volume": 1000.0,
               }
               for i in range(n)
           ]


       def test_snapshot_excludes_entry_bar() -> None:
           """D-09-B: il bar di entry NON entra nel calcolo."""
           from backtest.baseline.slice_worker import (
               _build_bars_index_by_time,
               _extract_extended_snapshot_at_entry,
           )

           bars_dict = _make_bars_dict(100)
           idx_map = _build_bars_index_by_time(bars_dict)
           entry_bar = bars_dict[50]
           entry_iso = datetime.fromtimestamp(entry_bar["time"], tz=timezone.utc).isoformat()

           captured_lens: list[int] = []

           def _fake_cae(bars_arg, regime_cfg=None):
               captured_lens.append(len(bars_arg))
               return {"sma_20": 1.11, "atr_14": 0.001, "regime_state": "normal"}

           with patch("indicators.compute_all_extended", side_effect=_fake_cae):
               snap = _extract_extended_snapshot_at_entry(
                   bars_dict, idx_map, entry_iso, regime_cfg=None,
               )

           assert len(captured_lens) == 1, "compute_all_extended chiamato esattamente 1 volta"
           assert captured_lens[0] == 50, (
               f"NO-LEAKAGE: deve usare bars[:50] (50 elementi), trovato {captured_lens[0]}"
           )
           assert snap["sma_20"] == 1.11
           assert snap["regime_state"] == "normal"


       def test_snapshot_idx_zero_returns_empty() -> None:
           """Edge: entry al primo bar -> dict vuoto senza chiamare cae."""
           from backtest.baseline.slice_worker import (
               _build_bars_index_by_time,
               _extract_extended_snapshot_at_entry,
           )

           bars_dict = _make_bars_dict(10)
           idx_map = _build_bars_index_by_time(bars_dict)
           entry_iso = datetime.fromtimestamp(bars_dict[0]["time"], tz=timezone.utc).isoformat()

           called: list[bool] = []
           with patch("indicators.compute_all_extended", side_effect=lambda *a, **k: called.append(True)):
               snap = _extract_extended_snapshot_at_entry(
                   bars_dict, idx_map, entry_iso, regime_cfg=None,
               )
           assert snap == {}
           assert called == [], "compute_all_extended NON deve essere invocato per idx=0"


       def test_snapshot_unknown_entry_ts_returns_empty() -> None:
           """Edge: entry_ts non in cache -> dict vuoto."""
           from backtest.baseline.slice_worker import (
               _build_bars_index_by_time,
               _extract_extended_snapshot_at_entry,
           )

           bars_dict = _make_bars_dict(10)
           idx_map = _build_bars_index_by_time(bars_dict)
           # Timestamp arbitrario non presente in idx_map
           fake_iso = datetime(1999, 1, 1, tzinfo=timezone.utc).isoformat()
           snap = _extract_extended_snapshot_at_entry(
               bars_dict, idx_map, fake_iso, regime_cfg=None,
           )
           assert snap == {}


       def test_no_leakage_invariant_decision_before_entry() -> None:
           """FIX 4 + FIX C iter 3: decision_ts < entry_ts rigorosamente, gap == timeframe_delta.

           Anchor CONSERVATIVA D-09-B vs D-22 stretta. In convenzione MT5
           (`bar.time = bar OPEN`):
             - decision_ts = open di idx-1 == bars_dict[idx-1]["time"]
             - entry_ts    = open di idx   == bars_dict[idx]["time"]
             - gap = open(idx) - open(idx-1) = timeframe_delta (900s per M15)

           D-22 stretta richiederebbe decision_ts = close di idx-1 ==
           open di idx == entry_ts (gap=0). Plan 05-09 sceglie ANCHOR
           CONSERVATIVA (1 bar prima) come safety buffer per Phase 7.

           Simula il flow di Task 2 §5: entry al bar idx=50, decision_ts
           calcolato come open del bar idx-1=49, entry_ts == open del bar
           idx=50 (== bars_dict[50]["time"] reso ISO). Gap atteso = 900s = 15min.
           """
           from backtest.baseline.slice_worker import _build_bars_index_by_time

           bars_dict = _make_bars_dict(100, interval_seconds=900)  # M15 = 15min = 900s
           idx_map = _build_bars_index_by_time(bars_dict)

           # Entry al bar 50 (simula trade ledger row)
           idx_entry = 50
           entry_ts_unix = bars_dict[idx_entry]["time"]
           entry_iso = datetime.fromtimestamp(entry_ts_unix, tz=timezone.utc).isoformat()

           # Decision_ts = open del bar idx-1 (anchor conservativa D-09-B / FIX C iter 3)
           decision_ts_unix = bars_dict[idx_entry - 1]["time"]
           decision_iso = datetime.fromtimestamp(decision_ts_unix, tz=timezone.utc).isoformat()

           # Invariante 1: decision_ts < entry_ts (strict)
           dt_decision = datetime.fromisoformat(decision_iso)
           dt_entry = datetime.fromisoformat(entry_iso)
           assert dt_decision < dt_entry, (
               f"D-09-B violato: decision {decision_iso} >= entry {entry_iso}"
           )

           # Invariante 2: gap == timeframe_delta (M15 = 15min)
           # Anchor conservativa = 1 bar pre-entry (NON gap=0 di D-22 stretta)
           gap = dt_entry - dt_decision
           assert gap == timedelta(minutes=15), (
               f"D-09-B violato: gap {gap} != 15min (timeframe M15 — anchor conservativa)"
           )
       ```

    2. Estendere `tests/test_baseline_runner.py` aggiungendo TRE nuovi test
       a fondo file (NON modificare i test pre-esistenti). **FIX E iter 3**:
       `test_regime_cfg_resolved_per_symbol` e' SINGOLO (non duplicato da Task 2):
       ```python
       def test_regime_cfg_resolved_per_symbol() -> None:
           """FIX 1 Plan 05-09 iter 1: load_regime_config invocato con symbol.

           SINGOLA definizione (FIX E iter 3 — Task 2 NON definisce un test
           omonimo; questo e' l'unico).

           Verifica che la signature reale (symbol, yaml_path) sia rispettata
           dal nuovo codice slice_worker.
           """
           import inspect
           from indicators.volatility import load_regime_config

           sig = inspect.signature(load_regime_config)
           params = list(sig.parameters.keys())
           assert params == ["symbol", "yaml_path"], (
               f"load_regime_config signature inattesa: {params}"
           )

           # Verifica che la firma e' invocabile con i 2 args attesi
           # (test di compatibilita' staticamente verificato).
           cfg = load_regime_config("EURUSD", "data/configs/regime.yaml")
           assert isinstance(cfg, dict)
           assert "window" in cfg


       def test_smoke_05_09_wrapper_runs(tmp_path) -> None:
           """Plan 05-09 D-09-D + FIX 2 + FIX B iter 3: scripts/run_baseline_05_09.py --smoke completa in <240s.

           Smoke con range allargato (3 mesi) + csv_path via _csv_path_for() +
           tolerant validation:
             - exit 0 anche se 0 trade (raro caso edge — smoke-tolerant FIX 2B)
             - exit 0 con schema check completo se >=1 trade (caso atteso
               per 3 mesi window MODERATE/AGGRESSIVE)
           """
           import subprocess
           import sys
           from pathlib import Path

           ROOT = Path(__file__).resolve().parent.parent
           script = ROOT / "scripts/run_baseline_05_09.py"
           assert script.exists(), f"script mancante: {script}"

           # Skip se CSV non disponibile (CI environment).
           # FIX B iter 3: path canonical via convenzione _csv_path_for
           # (data/historical/{SYMBOL}/{TF}.csv).
           csv_path = ROOT / "data" / "historical" / "EURUSD" / "M15.csv"
           if not csv_path.exists():
               pytest.skip(f"CSV non disponibile: {csv_path}")

           result = subprocess.run(
               [sys.executable, str(script), "--smoke"],
               cwd=str(ROOT),
               capture_output=True,
               text=True,
               timeout=240,  # smoke target <180s; margine 60s OS overhead
           )
           assert result.returncode == 0, (
               f"smoke exit={result.returncode}\nSTDOUT:\n{result.stdout}\nSTDERR:\n{result.stderr}"
           )
           assert "SMOKE OK" in result.stdout, (
               f"manca marker 'SMOKE OK' in stdout:\n{result.stdout}"
           )


       def test_only_runs_pre_delete_and_relaunch(tmp_path, monkeypatch) -> None:
           """FIX 6 + FIX A iter 3: --only-runs implementato via riuso _force_clear_run.

           Test scenario:
             1. Crea tmp SQLite con schema REALE Phase 5: `backtest_runs` +
                `backtest_trades` (matcha _DDL_BACKTEST_TRADES di
                backtest/ledger.py:56-78). NO `trades_log` (quello e' live
                trader, schema senza run_id — verificato in FIX A iter 3).
             2. Seed: 1 riga in `backtest_runs(run_id='abc', status='done')` +
                1 riga in `backtest_trades(run_id='abc', ...)`.
             3. Invoca _run_full(args=Namespace(only_runs='abc', force=False, ...))
                con run_baseline monkeypatched (no-op che ritorna []).
             4. Verifica:
                - PRE-run: entrambe le tabelle hanno 1 riga per run_id='abc'
                - POST-pre-delete (eseguito da _only_runs_pre_delete che
                  riusa slice_worker._force_clear_run): ENTRAMBE 0 righe
                - run_baseline chiamato esattamente 1 volta con force=False
           """
           import sqlite3
           import sys
           import types
           from pathlib import Path

           ROOT = Path(__file__).resolve().parent.parent
           sys.path.insert(0, str(ROOT))

           # Setup tmp db con schema REALE (FIX A iter 3 — NO trades_log fittizio)
           tmp_db = tmp_path / "trades.db"
           with sqlite3.connect(tmp_db) as conn:
               # Schema reale Phase 5 (subset minimo per il test, matcha DDL).
               # backtest/ledger.py:19-79 e' la fonte canonica.
               conn.execute(
                   "CREATE TABLE backtest_runs ("
                   "run_id TEXT PRIMARY KEY, "
                   "symbol TEXT NOT NULL DEFAULT 'EURUSD', "
                   "timeframe TEXT NOT NULL DEFAULT 'M15', "
                   "date_start TEXT NOT NULL DEFAULT '2020-01-01', "
                   "date_end TEXT NOT NULL DEFAULT '2020-12-31', "
                   "started_at TEXT NOT NULL DEFAULT 'now', "
                   "status TEXT NOT NULL DEFAULT 'running'"
                   ")"
               )
               conn.execute(
                   "CREATE TABLE backtest_trades ("
                   "trade_id INTEGER PRIMARY KEY AUTOINCREMENT, "
                   "run_id TEXT NOT NULL, "
                   "entry_time TEXT NOT NULL DEFAULT '2020-01-01', "
                   "exit_time TEXT NOT NULL DEFAULT '2020-01-02', "
                   "symbol TEXT NOT NULL DEFAULT 'EURUSD', "
                   "timeframe TEXT NOT NULL DEFAULT 'M15', "
                   "direction TEXT NOT NULL DEFAULT 'BUY', "
                   "entry_price REAL NOT NULL DEFAULT 1.0, "
                   "exit_price REAL NOT NULL DEFAULT 1.0, "
                   "lot_size REAL NOT NULL DEFAULT 0.01"
                   ")"
               )
               conn.execute(
                   "INSERT INTO backtest_runs (run_id, status) VALUES ('abc', 'done')"
               )
               conn.execute(
                   "INSERT INTO backtest_trades (run_id) VALUES ('abc')"
               )
               conn.commit()

           # Sanity check pre-run: entrambe le tabelle hanno la riga
           with sqlite3.connect(tmp_db) as conn:
               n_runs = conn.execute(
                   "SELECT COUNT(*) FROM backtest_runs WHERE run_id='abc'"
               ).fetchone()[0]
               n_trades = conn.execute(
                   "SELECT COUNT(*) FROM backtest_trades WHERE run_id='abc'"
               ).fetchone()[0]
               assert n_runs == 1 and n_trades == 1, (
                   f"seed fallito: runs={n_runs}, trades={n_trades}"
               )

           # Monkeypatch run_baseline (no-op tracker)
           run_baseline_calls: list[bool] = []

           def _fake_run_baseline(force=False):
               run_baseline_calls.append(force)
               return []

           # Fake ROOT con tmp_db come logs/trades.db
           tmp_root = tmp_path / "fake_root"
           (tmp_root / "logs").mkdir(parents=True)
           (tmp_root / "data" / "configs").mkdir(parents=True)
           (tmp_root / "data" / "training" / "baseline_decisions").mkdir(parents=True)
           # Copy tmp_db -> tmp_root/logs/trades.db (preserva schema reale)
           (tmp_root / "logs" / "trades.db").write_bytes(tmp_db.read_bytes())

           fake_runner = types.SimpleNamespace(
               run_baseline=_fake_run_baseline,
               load_baseline_config=lambda *a, **k: types.SimpleNamespace(
                   training_data_dir=str(tmp_root / "data/training"),
               ),
           )
           monkeypatch.setitem(
               sys.modules, "backtest.baseline.runner", fake_runner
           )

           # Import _run_full + stub validazione parquet
           import importlib.util
           spec = importlib.util.spec_from_file_location(
               "rb05", ROOT / "scripts/run_baseline_05_09.py"
           )
           m = importlib.util.module_from_spec(spec)
           spec.loader.exec_module(m)

           monkeypatch.setattr(m, "_validate_parquet_schema", lambda *a, **k: (True, "stub OK"))

           args = types.SimpleNamespace(
               only_runs="abc",
               force=False,
               max_wall_clock=14400,
               no_time_gate=False,
           )
           exit_code = m._run_full(args, tmp_root)

           # Verifica post-run: entrambe le tabelle ripulite (FIX A iter 3)
           with sqlite3.connect(tmp_root / "logs/trades.db") as conn:
               n_runs_after = conn.execute(
                   "SELECT COUNT(*) FROM backtest_runs WHERE run_id='abc'"
               ).fetchone()[0]
               n_trades_after = conn.execute(
                   "SELECT COUNT(*) FROM backtest_trades WHERE run_id='abc'"
               ).fetchone()[0]
           assert n_runs_after == 0, (
               f"FIX A iter 3 violato: riga backtest_runs 'abc' NON eliminata "
               f"(rimaste {n_runs_after})"
           )
           assert n_trades_after == 0, (
               f"FIX A iter 3 violato: riga backtest_trades 'abc' NON eliminata "
               f"(rimaste {n_trades_after}) — _force_clear_run pattern violato"
           )
           assert run_baseline_calls == [False], (
               f"run_baseline call inattesi: {run_baseline_calls}"
           )
           assert exit_code == 0
       ```

    3. Eseguire:
       ```bash
       pytest tests/test_baseline_no_leakage_extended.py tests/test_baseline_runner.py -x -v
       ```
       Atteso: 4 test pass nel file no_leakage_extended + 3 nuovi test pass (o smoke skipped se manca CSV) nel runner.

    4. Eseguire suite globale per backward-compat:
       ```bash
       pytest -x --tb=short 2>&1 | tail -40
       ```
       Atteso: 436+ pass (4 + 3 = 7 nuovi test aggiungono al totale, 0 regression). **FIX E iter 3**: NO `test_regime_cfg_resolved_per_symbol` duplicato (single definition in test_baseline_runner.py).

    5. **Italiano** in docstring + commenti.

    6. Commit:
       ```
       test(05-09): no-leakage + anchor conservativa D-09-B + smoke + --only-runs (D-09-B/D + FIX 1/2/4/6 + FIX A/B/C/E iter 3)
       ```
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent && pytest tests/test_baseline_no_leakage_extended.py tests/test_baseline_runner.py -x -v 2>&1 | tail -50 && grep -c "def test_regime_cfg_resolved_per_symbol" tests/test_baseline_runner.py</automated>
  </verify>
  <done>
    - 4 test pass in `tests/test_baseline_no_leakage_extended.py` (3 originali + 1 NUOVO FIX 4 / FIX C iter 3 anchor conservativa).
    - +3 test pass o skipped in `tests/test_baseline_runner.py` (regime_cfg signature FIX 1, smoke wrapper FIX 2+B, only-runs FIX 6+A).
    - **FIX E iter 3**: `test_regime_cfg_resolved_per_symbol` definito ESATTAMENTE 1 volta in test_baseline_runner.py (verificato da grep -c = 1, NO duplicato da Task 2).
    - Suite globale pre-Phase 6 (~436 test) verde, zero regression.
    - Invariante no-leakage D-09-B verificato a livello unit (bars_view = bars_dict[:idx] excludes entry bar).
    - Invariante anchor conservativa decision_ts < entry_ts + gap == timeframe verificato.
    - Test --only-runs usa schema REALE Phase 5 (backtest_trades, NON trades_log fittizio) e verifica entrambe le tabelle ripulite.
  </done>
</task>

<task type="auto">
  <name>Task 6: Documentazione recovery + suite verde finale + roadmap update</name>
  <files>.planning/phases/05-baseline-backtest/05-09-PLAN.md</files>
  <read_first>
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-08-SUMMARY.md (pattern SUMMARY)
    - C:/trading-agent/.planning/SETUP-SECONDARY-PC.md (sezione recovery referenziata)
  </read_first>
  <action>
    1. **NON modificare il PLAN.md** (questo file, gia' completo post-revision iter 3).
       Tutto cio' che il SUMMARY del plan 05-09 deve catturare lo
       documenta l'executor in `05-09-SUMMARY.md` post-esecuzione (vedi
       output section sotto).

    2. **Verifica suite globale verde** (ultima volta in questo plan):
       ```bash
       pytest -x --tb=short
       ```
       Atteso: 436+ pass, zero failed. Se rosso, identifica e fixa
       PRIMA di marcare il plan complete.

    3. **Sanity check artefatti** (manuale):
       ```bash
       ls -la backtest/baseline/dataset_writer.py                # extended Task 1
       ls -la backtest/baseline/slice_worker.py                  # extended Task 2
       grep "dataset_schema_version" data/configs/baseline.yaml  # Task 3
       ls -la scripts/run_baseline_05_09.py                      # NUOVO Task 4
       ls -la tests/test_baseline_no_leakage_extended.py         # NUOVO Task 5
       grep "_SCHEMA_V2_REQUIRED_KEYS" backtest/baseline/dataset_writer.py
       grep "_build_required_keys_v2" backtest/baseline/dataset_writer.py
       grep "_extract_extended_snapshot_at_entry" backtest/baseline/slice_worker.py
       grep "bars_view = bars_dict\[:idx\]" backtest/baseline/slice_worker.py
       grep "load_regime_config(symbol" backtest/baseline/slice_worker.py
       grep "_force_clear_run" scripts/run_baseline_05_09.py     # FIX A iter 3
       grep "_csv_path_for" scripts/run_baseline_05_09.py        # FIX B iter 3
       grep "pd.isna" scripts/run_baseline_05_09.py
       # FIX D iter 3: la chiamata top-level NON deve avere regime_cfg
       grep -n "compute_all_extended(bars_dict)" backtest/baseline/slice_worker.py | grep -v regime_cfg
       # FIX E iter 3: SINGLE definition test_regime_cfg
       test "$(grep -c 'def test_regime_cfg_resolved_per_symbol' tests/test_baseline_runner.py)" = "1"
       ```
       Tutti i comandi devono produrre output non-vuoto / exit 0.

    4. **Recovery documentation reminder** (per il SUMMARY post-esecuzione):
       Il SUMMARY del Plan 05-09 (executor produrra'
       `05-09-SUMMARY.md`) DEVE includere una sezione "Recovery
       Procedure" che documenta:
       - Comportamento crash mid-run: i parquet shard per-worker
         (`baseline_decisions_<run_id>.parquet`) gia' scritti sono safe;
         solo l'eventuale finalize step `finalize_parquet_shards` puo'
         essere ri-eseguito senza side effect (riscrive part-{i}.parquet).
       - Identificazione run_id mancanti: query SQL
         `SELECT run_id FROM backtest_runs ORDER BY started_at` su
         `logs/trades.db`; confrontare con i 27 run_id attesi
         (`baseline_<date>_<symbol>_<tf>_<profile>`).
       - **FIX 6 + FIX A iter 3 implementato**: re-lanciare run_id specifici con
         `python scripts/run_baseline_05_09.py --only-runs <run_id1>,<run_id2>`.
         Il wrapper esegue pre-delete via riuso `_force_clear_run` (DELETE
         FROM backtest_trades + backtest_runs) + relaunch (D-14 idempotency
         skippa i completi). NO `trades_log` (schema live trader senza run_id).
       - Alternativa manuale (NO --only-runs): cancellare manualmente le
         righe dei run_id che vogliamo rilanciare da entrambe le tabelle
         `backtest_runs` + `backtest_trades`, poi
         `python scripts/run_baseline_05_09.py` (senza `--force` cosi'
         da preservare i completati via D-14 idempotency).
       - Cross-ref: `SETUP-SECONDARY-PC.md` §3 (sezione "Esecuzione
         backtest notturno" — citata in RESUME-PLAN STEP 3).

    5. **Sentinel commit finale (atomic phase closure):**
       NON aggiornare STATE.md o ROADMAP.md in questo task — verra'
       fatto dall'orchestrator dopo che il PC secondario completera'
       il backtest e il SUMMARY sara' scritto (STEP 13 di
       RESUME-PLAN.md). Plan 05-09 chiude lo scope `plan-write` e
       lascia lo scope `plan-execute` al PC secondario.

       Tag opzionale ma raccomandato:
       ```bash
       git tag plan-05-09-write-complete -m "Plan 05-09 code artifacts ready for execution (10 FIX iter 1 + 6 FIX iter 3)"
       ```

    6. Eseguire test gate finale combinato (4 suite chiave):
       ```bash
       pytest tests/test_baseline_dataset_writer.py \
              tests/test_baseline_runner.py \
              tests/test_baseline_no_leakage_extended.py \
              tests/test_baseline_no_future_leakage.py \
              -x -v
       ```
       Atteso: ~21+ test pass (11 writer + 3-4 runner + 4 no_leakage_extended + 3-4 no_future_leakage).
  </action>
  <verify>
    <automated>cd /workspaces/trading-agent && pytest tests/test_baseline_dataset_writer.py tests/test_baseline_runner.py tests/test_baseline_no_leakage_extended.py tests/test_baseline_no_future_leakage.py -x 2>&1 | tail -20 && grep -c "_SCHEMA_V2_REQUIRED_KEYS\|_extract_extended_snapshot_at_entry\|dataset_schema_version\|_force_clear_run\|_csv_path_for\|load_regime_config(symbol" backtest/baseline/dataset_writer.py backtest/baseline/slice_worker.py data/configs/baseline.yaml scripts/run_baseline_05_09.py 2>/dev/null && test "$(grep -c 'def test_regime_cfg_resolved_per_symbol' tests/test_baseline_runner.py)" = "1" && echo "FIX E iter 3 OK: single test definition"</automated>
  </verify>
  <done>
    - Suite combinata 4 test files verde.
    - Tutti i sentinel grep producono >=1 match (writer ha `_SCHEMA_V2_REQUIRED_KEYS` + `_build_required_keys_v2`, slice_worker ha `_extract_extended_snapshot_at_entry` + `bars_view = bars_dict[:idx]` + `load_regime_config(symbol,...)` + top-level `compute_all_extended(bars_dict)` senza regime_cfg, yaml ha `dataset_schema_version`, script ha `_force_clear_run` + `_csv_path_for` + `pd.isna`).
    - **FIX E iter 3 enforced**: `grep -c def test_regime_cfg_resolved_per_symbol == 1` in test_baseline_runner.py.
    - Recovery procedure documentata nel plan (questo task) con FIX 6 + FIX A iter 3 implementato e da ribadire nel SUMMARY post-esecuzione.
    - Plan 05-09 scope `plan-write` complete: artefatti pronti per esecuzione PC secondario (RESUME-PLAN STEP 3).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compute_all_extended -> writer | dict snapshot produce columns parquet. Keys non validate runtime; mismatch silente fino a Phase 7 reader. Mitigazione: `_SCHEMA_V2_REQUIRED_KEYS` whitelist (programmatica, FIX 3) + warning log. |
| script CLI -> filesystem | Path controlled (`data/training/_smoke_05_09/`, `logs/trades_smoke_05_09.db`, repo-root relative tramite `ROOT = Path(__file__).resolve().parent.parent`). No traversal. FIX 7: path absolute per smoke per evitare CWD-dependence. FIX B iter 3: csv_path via `_csv_path_for()` (convenzione canonical, NO hardcoded). |
| PC secondario -> repo | git clone + push branch. SETUP-SECONDARY-PC.md §0.2 enforce: NO `.env` via git (gitignore). Mitigazione: `.env` trasferito out-of-band (USB/cloud privato). |
| smoke ledger -> prod ledger | `logs/trades_smoke_05_09.db` separato da `logs/trades.db` per evitare contamination. Mitigazione: pre-clean `if ledger.exists(): unlink()` in `_run_smoke()`. |
| --only-runs SQL injection | Input CLI parsato via `split(",")` poi passato a `_force_clear_run(db_path, run_id)` che usa query parametrized con `?` placeholder (FIX A iter 3 — riuso pattern canonical, NO f-string interpolation di run_id user-supplied). |
| --only-runs schema scope | Solo `backtest_runs` + `backtest_trades` (schema reale Phase 5). `trades_log` (live trader, schema senza run_id) NON toccato. FIX A iter 3 garantisce con riuso `_force_clear_run`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-31 | Tampering | Indicator snapshot include OHLC del bar di entry (leakage by bug) | mitigate | Test mirato `test_snapshot_excludes_entry_bar` (Task 5) verifica `len(bars_arg) == idx` (NON idx+1). Mock di `compute_all_extended` cattura args. |
| T-05-32 | Information Disclosure | `.env` committato per errore dal PC secondario | mitigate | `.gitignore` gia' include `.env` (verificato linea 1); SETUP-SECONDARY-PC.md §A.4/B.4 documenta trasferimento out-of-band; `git status` pre-commit obbligatorio. |
| T-05-33 | Denial of Service | Smoke run blocca PC secondario >5 min (regression performance) | mitigate | Timeout pytest 240s nel test smoke (Task 5); `--smoke` mode usa 3 mesi x 1 pair x 1 TF (carico ~3/27 del full run, target <180s; >300s indica regressione — FIX 2 + 10). |
| T-05-34 | Repudiation | Re-run senza tracciamento del bump schema-v2 | mitigate | `baseline_yaml_hash` in `backtest_runs` cambia automaticamente con `dataset_schema_version: 2` (Task 3); audit trail completo via D-17 (Plan 05-07). |
| T-05-35 | Tampering | Worker crash mid-write produce parquet shard zero-byte -> finalize fallisce | mitigate | Gia' mitigato da Plan 05-04 finalize_parquet_shards: filtro `stat().st_size > 0` (dataset_writer.py:69). Recovery documentato in Task 6 + FIX 6 + FIX A iter 3 --only-runs. |
| T-05-36 | Elevation of Privilege | Script wrapper esegue arbitrary code via --only-runs SQL injection | mitigate | argparse type=str + split(",") con strip; riuso `_force_clear_run` con query parametrized `?` placeholder (FIX A iter 3 — NO f-string user input). Worst case: run_id invalido viene loggato e DELETE no-op (zero righe matched). |
| T-05-37 | Tampering | Drift di compute_all_extended schema non rilevato | mitigate | FIX 3: `_SCHEMA_V2_REQUIRED_KEYS` costruito programmaticamente da compute_all_extended; test `test_schema_v2_keys_match_compute_all_extended` cattura drift se Phase 2 estende l'interface. |
| T-05-38 | Tampering | Walk-forward Phase 7 con leakage off-by-one-bar (entry_ts usato invece di decision_ts) | mitigate | FIX 4 + FIX C iter 3: 3 timestamp distinti scritti nel parquet (decision_ts_utc = anchor conservativa 1-bar-pre-entry, entry_ts_utc, exit_ts_utc); test `test_no_leakage_invariant_decision_before_entry` enforce gap == timeframe_delta rigoroso. |
| T-05-39 | Tampering | `--only-runs` tenta DELETE su `trades_log` (schema live trader senza run_id) -> OperationalError | mitigate | FIX A iter 3: riuso `_force_clear_run` (single source of truth) loop SOLO su `backtest_runs` + `backtest_trades`. Test `test_only_runs_pre_delete_and_relaunch` usa schema REALE e verifica entrambe le tabelle ripulite. |
| T-05-40 | Tampering | regime_cfg per-symbol passato accidentalmente a `compute_all_extended(bars_dict)` top-level -> drift detector (count trade != 1076) | mitigate | FIX D iter 3: invariante esplicito in Task 2 + truth in must_haves + verify grep `grep -v regime_cfg` sulla chiamata top-level. `regime_cfg` consumer SOLO `_extract_extended_snapshot_at_entry`. |

</threat_model>

<verification>
- 6 task seriali: writer schema-v2 programmatic + slice_worker snapshot @ idx-1 + 3 timestamps distinti + yaml bump + script wrapper + invariant test + suite gate.
- D-09-A: 5 meta + 33 indicators top-level (NON ctx_json) verificati da `test_decisions_schema_v2_round_trip` (Task 1) + `_validate_parquet_schema` (Task 4 script).
- D-09-B: no-leakage anchor @ bars[:idx] verificato unit-level da `test_snapshot_excludes_entry_bar` (Task 5).
- D-09-C: writer signature invariata; backward-compat verificata da `test_legacy_v1_shard_emits_warning` (Task 1) + suite Phase 5 esistente verde (Task 2 + 6).
- D-09-D: script wrapper stand-alone con --smoke verificato da `test_smoke_05_09_wrapper_runs` (Task 5).
- D-09-E: `max_workers: 9` locked verificato dal `python -c` assert in Task 3.
- D-09-F: scope chirurgico — files_modified frontmatter limita a `backtest/baseline/*`, `data/configs/baseline.yaml`, `scripts/*`, `tests/*`. Nessun file Phase 1/2/4 toccato.
- D-09-G (FIX 5): scope chirurgico esteso — 8 field D-02 deferred-by-design a Phase 7 feature_extraction, 4 field D-02 out-of-scope Phase 9/10. Tabella copertura in `<success_criteria>` sotto.
- FIX 1: `load_regime_config(symbol, yaml_path)` verificato da `test_regime_cfg_resolved_per_symbol` (Task 5, SINGLE definition — FIX E iter 3).
- FIX 2: smoke range allargato + smoke-tolerant validation verificati da `test_smoke_05_09_wrapper_runs` (Task 5).
- FIX 3: drift-prevention test `test_schema_v2_keys_match_compute_all_extended` (Task 1).
- FIX 4 + FIX C iter 3: 3 timestamp distinti con anchor conservativa D-09-B (1 bar pre-entry, gap == timeframe), verificati da `test_no_leakage_invariant_decision_before_entry` (Task 5).
- FIX 6 + FIX A iter 3: --only-runs riuso `_force_clear_run` (NO SQL custom, NO trades_log) verificato da `test_only_runs_pre_delete_and_relaunch` (Task 5) con schema REALE Phase 5.
- FIX B iter 3: csv_path canonical via `_csv_path_for("EURUSD", "M15")` (NO hardcoded) — sentinel grep Task 6.
- FIX D iter 3: isolamento regime_cfg dal flow engine principale — invariante `compute_all_extended(bars_dict)` top-level senza regime_cfg verificato da grep Task 2 verify + Task 6 sanity check. Count trade atteso == 1076 ± 0 (parity stretta).
- FIX E iter 3: dedup test `test_regime_cfg_resolved_per_symbol` (single definition in test_baseline_runner.py, NO duplicato Task 2) — verificato da `grep -c == 1` in Task 6.
- FIX F iter 3: ordine init `enable_sqlite_wal()` prima, `LedgerWriter()` dopo — allinea runner.py:170-179 (cosmetic, code review).
- FIX 7/8/9/10: path absolute, key_links regex, regime canonical, cosmetic — verificati da grep sentinel (Task 6).
- Test gate complessivo: suite globale (~436 test) verde post-Task 5.
- Sentinel grep finale (Task 6): tutti i 8+ marker presenti nei file attesi.
</verification>

<success_criteria>
**SC#1 — Writer schema-v2 (D-09-A):**
- `backtest/baseline/dataset_writer.py` espone `_SCHEMA_V2_REQUIRED_KEYS` costruito da `_build_required_keys_v2()` (FIX 3) con 5 meta + 33 indicators.
- Round-trip test passa: scrivere dict completo, leggere parquet, columns superset di whitelist.
- Drift-prevention: `set(compute_all_extended(dummy_bars).keys()) | meta_keys == _SCHEMA_V2_REQUIRED_KEYS`.

**SC#2 — No-leakage by construction (D-09-B + FIX C iter 3):**
- `_extract_extended_snapshot_at_entry` invoca `compute_all_extended(bars_view, ...)` dove `bars_view = bars_dict[:idx]` (exclusive).
- Test mock verifica `len(bars_arg) == idx` (NON idx+1) su 100 bar sintetici.
- FIX 4 + FIX C iter 3 (anchor conservativa): `decision_ts_utc < entry_ts_utc` rigoroso, gap == timeframe_delta (15min M15) — 1 bar pre-entry buffer vs D-22 stretta.

**SC#3 — Wrapper script funzionale (D-09-D):**
- `python scripts/run_baseline_05_09.py --help` mostra le flags `--smoke`, `--force`, `--only-runs`, `--max-wall-clock`, `--no-time-gate`.
- `python scripts/run_baseline_05_09.py --smoke` exit 0 con stdout `SMOKE OK` (target <180s; FIX 2+10; FIX B iter 3 csv via `_csv_path_for`).
- `python scripts/run_baseline_05_09.py --only-runs <id>` esegue pre-delete via riuso `_force_clear_run` + relaunch (FIX 6 + FIX A iter 3 implementato — NO SQL custom, NO trades_log).

**SC#4 — Backward-compat preservata (D-09-C):**
- Suite Phase 5 pre-esistente (`test_baseline_*`) verde post-Task 2.
- Suite globale ~436 test verde post-Task 5.
- **FIX D iter 3 — isolamento regime_cfg:** count trade atteso == 1076 ± 0 (parity stretta vs Plan 05-08, validato post-execution PC secondario).
- **FIX E iter 3 — dedup:** `test_regime_cfg_resolved_per_symbol` definito esattamente 1 volta.

**SC#5 — Hash invalidation (D-09-A trace):**
- `data/configs/baseline.yaml` contiene `dataset_schema_version: 2`.
- `sha256(baseline.yaml)` post-Task 3 != `sha256(baseline.yaml)` pre-Task 3 -> re-run obbligatorio (auditabile in `backtest_runs.baseline_yaml_hash` quando il PC secondario eseguira').

**SC#6 — Phase 7 ML unblocking (cross-phase) + scope chirurgico (D-09-G):**
- Schema parquet (post-esecuzione PC secondario, STEP 3 RESUME-PLAN) include 33+ colonne indicators top-level + 5 meta/timestamps non-null.
- Phase 7 RESEARCH §"Critical Finding: Dataset Schema Gap" risolto per la quota di scope: i 33 indicators + 4 timestamp/meta presenti; 8 field deferred a Phase 7 feature extraction per D-09-G; 4 field out-of-scope Phase 9/10.
- Phase 5 status 9/9 plans (closure 100%).
- Row count >= 1050 hard gate (FIX D iter 3 — margine 2.5% vs parity 1076).

### Field coverage matrix vs D-02 originale (Phase 5 CONTEXT.md)

FIX 5 plan-checker iter 1: scope chirurgico esplicito.

| D-02 field group | Plan 05-09 coverage | Source post-05-09 |
|---|---|---|
| Identity: symbol, timeframe, direction | Covered | _row_for_ledger (engine) |
| Levels: entry_price, exit_price, sl, tp, lot_size, risk_usd | Covered | _row_for_ledger |
| Outcome short: exit_reason, pnl_pips, pnl_usd, confidence, setup_type | Covered | _row_for_ledger |
| Timestamps: decision_ts_utc, entry_ts_utc, exit_ts_utc | Covered (FIX 4 + FIX C iter 3 anchor conservativa) | slice_worker enrichment (Task 2) |
| Profile/run_id | Covered | slice_worker enrichment |
| Regime: regime_state (canonical, NO alias `regime`) | Covered (FIX 9) | compute_all_extended output (consumato solo in `_extract_extended_snapshot_at_entry` — FIX D iter 3) |
| Indicators (33+): rsi_14, atr_14, bollinger_*, adx_*, macd_*, etc. | Covered (FIX 1+3) | compute_all_extended @ bars_view = bars_dict[:idx] |
| factors_trend_alignment + 4 altri factors bool | **Deferred (D-09-G)** | decision_context_json struct in Phase 7 feature_extraction |
| grade, setup_name | **Deferred (D-09-G)** | decision_context_json struct in Phase 7 |
| outcome in {WIN,LOSS,BE,TIMEOUT} | **Deferred (D-09-G)** | Phase 7 derive da exit_reason+pnl_pips |
| bars_held | **Deferred (D-09-G)** | Phase 7 derive da bar timestamps |
| sr_dist_pips, spread_at_entry_pips, sentiment_proxy, recent_trades_outcome_5 | **Out of scope** | Phase 9/10 (failure analysis + intermarket) |

**Out of scope (NON parte di SC):**
- Esecuzione full 27/27 (STEP 3 PC secondario, separata in pipeline).
- Wall-clock <30 min (Rule 4 deviation Plan 05-08 user-accepted, defer plan 01-09).
- Drafts FORMING/NONE capture (deferred Phase 9).
- Date range 23.5y (locked 10y window, vedi D-09-F).
- I 13 field D-02 deferred-by-design (8 a Phase 7 feature_extraction via D-09-G; 4 a Phase 9/10 out-of-scope; 1 deduplicato via FIX 9 — `regime` alias rimosso, solo `regime_state` canonical).

</success_criteria>

<output>
Dopo completamento di tutti i 6 task, l'executor produrra'
`.planning/phases/05-baseline-backtest/05-09-SUMMARY.md`.

Sezioni obbligatorie del SUMMARY:

1. **Risultato** (1 paragrafo): scope plan-write chiuso, artefatti
   pronti per esecuzione PC secondario (separazione esplicita tra
   "code ready" e "data produced"). Cita applicazione 10 FIX
   plan-checker iter 1 + 6 FIX iter 3 (A/B/C/D/E/F).

2. **Numeri**:
   - LOC delta per file (`dataset_writer.py`, `slice_worker.py`, ...)
   - Test totali aggiunti: 5 (writer) + 4 (no_leakage_extended) + 3 (runner: regime + smoke + only-runs) = **12 nuovi** (FIX E iter 3: single definition test_regime_cfg, count invariato post-dedup)
   - Suite globale: X passed (pre vs post-plan)
   - Commit count

3. **Schema-v2 whitelist** (tabella): le 37 chiavi `compute_all_extended` + 5 meta + 17 base = ~55+ colonne attese nel parquet post-esecuzione. Citare extraction programmatica (FIX 3) come anti-drift.

4. **No-leakage proof** (espanso FIX 4 + FIX C iter 3): cita test `test_snapshot_excludes_entry_bar` con assertion concreta `captured_lens[0] == 50` (per `idx=50`) + test `test_no_leakage_invariant_decision_before_entry` con assertion `gap == timedelta(minutes=15)` (anchor conservativa D-09-B 1 bar pre-entry vs D-22 stretta gap=0).

5. **Isolamento regime_cfg dal flow engine principale (FIX D iter 3)**: documenta che `compute_all_extended(bars_dict)` top-level resta senza regime_cfg, regime_cfg consumer SOLO `_extract_extended_snapshot_at_entry`. Count trade atteso == 1076 ± 0 (parity stretta validata).

6. **Field coverage matrix** (sezione obbligatoria, FIX 5 plan-checker iter 1):
   tabella 12-row con copertura D-02 originale vs Plan 05-09 (Covered /
   Deferred D-09-G / Out of scope Phase 9-10). Cross-ref D-09-G rationale.

7. **Recovery Procedure** (sezione obbligatoria, vedi Task 6 §4):
   - Crash mid-run -> shard safe -> finalize ri-eseguibile
   - Run_id mancanti identificabili via SQL su backtest_runs
   - **FIX 6 + FIX A iter 3 IMPLEMENTATO**: `--only-runs <ids>` esegue
     pre-delete via riuso `_force_clear_run` (NO SQL custom, NO `trades_log`)
     + relaunch tramite D-14 idempotency
   - Cross-ref SETUP-SECONDARY-PC.md §3

8. **Esecuzione full 27/27 NON in scope plan-write**: chiaramente
   marcato come deferred a STEP 3 RESUME-PLAN.md (PC secondario,
   notturna, ~3h18m wall-clock atteso).

9. **Cross-phase impact**: Phase 5 9/9 plans (closure 100%); Phase 7
   ML unblocked per `/gsd-plan-phase 7 --skip-research` con awareness
   D-09-G (feature_extraction Wave 0 task richiesto per derivare
   factors_*, outcome, bars_held da decision_context_json + pnl_pips +
   timestamps).

10. **Deviation log** (se applicabili): es. se durante Task 4 lo script
    si scopre richiede modifica al runner per --only-runs nativo, documentare
    come Rule 3 architectural deviation con justification.

11. **Plan-checker FIX applicate** (sezione obbligatoria post-revision):
    - 10 FIX iter 1 (BLOCKER 1-2, WARNING 1-5, INFO 1-4) con riferimento a Task/sezione
    - 6 FIX iter 3 (A: --only-runs riuso `_force_clear_run` schema reale;
      B: csv_path via `_csv_path_for`; C: chiarimento semantica
      decision_ts vs D-22 — anchor conservativa documentata; D:
      isolamento regime_cfg dal flow engine principale + truth +
      min_rows=1050; E: dedup `test_regime_cfg_resolved_per_symbol`;
      F: ordine init WAL/LedgerWriter aligned)

12. **Self-Check** finale: PASSED / FAILED con motivazione.

Path: `.planning/phases/05-baseline-backtest/05-09-SUMMARY.md`
</output>
