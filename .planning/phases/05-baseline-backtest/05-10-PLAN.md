---
phase: 05-baseline-backtest
plan: 10
type: execute
wave: 6
status: DRAFT (iter 0 — no plan-checker yet, no discuss-phase)
depends_on: [05-09]
files_modified:
  - data/configs/baseline.yaml         # date_start/date_end null/null (full history)
files_created:
  - .planning/phases/05-baseline-backtest/05-10-PLAN.md   # questo file
  - data/training/baseline_decisions.plan-05-09/          # archive del Plan 05-09 output prima di overwrite
files_overwritten_in_place:
  - data/training/baseline_decisions/part-0.parquet       # da 1076 rows -> ~2500-3000 rows full-history
autonomous: false      # esecuzione MANUALE su PC secondario (~8-15h wall-clock)
requirements: [BACK-07-ext]   # extension implicita BACK-07, formalizzazione a posteriori
tags: [phase-5, baseline-backtest, wave-6, full-history-extension, training-data-volume, blocks-phase-7-with-richer-data]

decisions:
  # Plan 05-10 deviazione ESPLICITA dal D-09-F lock-10y di Plan 05-09.
  # Justification: massimizzare training data Phase 7 (user priority
  # "training ML data integrity = priority assoluta" in memory).

  - id: D-10-A
    title: "date range full-history (null/null) — supersede D-09-F 10y lock"
    rationale: |
      Plan 05-09 D-09-F locked 10y window (2015-05-08 → 2025-05-08, 1076 trades).
      Plan 05-10 rilassa esplicitamente: `date_start: null` + `date_end: null`
      in `baseline.yaml` -> loader.load_bars con None/None -> full coverage
      CSV (2002-10-21 → 2026-05-04 = ~23.6 anni).

      Trade-off accettato:
      - Pro: ~2.36x trade count atteso (~2500-3000 rows) -> Phase 7 ML
        classifier ha training set ~2.5x più grande -> migliore generalization
        attesa, copertura regimi storici aggiuntiva (crisi 2008, COVID 2020,
        era pre-EUR maturity 2002-2005).
      - Contro: wall-clock proiezione 8-15h (engine perf O(N²) nota in
        baseline.yaml:36 commento). Plan 01-09 vectorization-perf
        deferred -> stiamo accettando il costo per ora.
      - Contro: perdita parity stretta con Plan 05-09 output. baseline_yaml_hash
        invariato a livello schema, ma date_start/date_end cambia ->
        baseline_runs SHA di yaml file cambia -> idempotency D-14 invalida
        tutti i 27 run_id precedenti.

  - id: D-10-B
    title: "archive Plan 05-09 output prima di overwrite (D-23 audit trail)"
    rationale: |
      `data/training/baseline_decisions/part-0.parquet` (1076 × 59) è il
      deliverable Plan 05-09. Overwrite in place perde audit trail. PRIMA
      del run, copia in `data/training/baseline_decisions.plan-05-09/`
      (pattern coerente con `baseline_decisions.pre-05-09/` già esistente
      per Plan 05-08 archive).

      Sul PC secondario, comando archive da lanciare PRIMA del run:
      ```powershell
      Copy-Item -Recurse data\training\baseline_decisions data\training\baseline_decisions.plan-05-09
      ```

  - id: D-10-C
    title: "schema-v2 INVARIANTE — no code changes, solo config"
    rationale: |
      Plan 05-10 NON tocca:
      - backtest/baseline/dataset_writer.py (schema-v2 D-09-A locked)
      - backtest/baseline/slice_worker.py (no-leakage D-09-B locked)
      - scripts/run_baseline_05_09.py (riusato as-is; date range viene da yaml)

      Plan 05-10 modifica SOLO `data/configs/baseline.yaml` (2 righe:
      date_start/date_end). Output parquet stesso schema (59 cols
      ~), solo row count diverso.

  - id: D-10-D
    title: "hard gate row count rilassato — sanity check ≥2000 (non parity stretta)"
    rationale: |
      `run_baseline_05_09.py` validation gate è hard-coded `min_rows=1050`
      (parity Plan 05-08 1076 ± margine). Su full-history 23.6y, atteso
      ~2500-3000 rows. Sanity gate Plan 05-10: rows >= 2000 (margine
      conservativo, evita falsi positivi se la stima 2500 risulta troppo
      ottimistica). Validation post-run manuale (vedi §Validation criteria
      sotto), NON modifica codice gate.

  - id: D-10-E
    title: "execution informale stanotte, formalizzazione plan-check tomorrow"
    rationale: |
      User ha esplicitamente richiesto "DRAFT only, no plan-checker, no
      discuss-phase" per Plan 05-10. Lancio stanotte è iter 0 / informal
      execution. Domani (o quando il run finisce) plan-check iter 1
      review retroattivo: verifica esiti, formalizzazione baseline.yaml
      committed-or-revert, decisione finale su modalità (committed yaml
      vs separate baseline_full.yaml vs CLI flag).

scope:
  - Re-run completo 27/27 baseline backtest su 23.6y full-history
  - Output: data/training/baseline_decisions/part-0.parquet (~2500-3000 rows × 59 cols schema-v2)
  - Archive Plan 05-09 output: data/training/baseline_decisions.plan-05-09/
  - Audit trail: nuova entry in backtest_runs tabella con baseline_yaml_sha256
    diverso (yaml modificato)

out_of_scope:
  - Modifiche al codice (dataset_writer, slice_worker, runner)
  - Modifiche allo script wrapper run_baseline_05_09.py
  - Plan-check formale stanotte (rinviato a iter 1 post-execution)
  - Phase 7 ML re-training (Plan 07-* sblocca DOPO Plan 05-10 conferma esiti)
  - Vectorization Phase 1 perf optimization (Plan 01-09 deferred, fuori scope)

execution_steps_secondary:
  - id: STEP-1
    desc: "Pull latest da branch feature/update-pythono-pure-strategy (include 05-10-PLAN.md)"
    cmd: |
      cd C:\dev\git\trading-agent
      git pull origin feature/update-pythono-pure-strategy

  - id: STEP-2
    desc: "Archive Plan 05-09 output (D-10-B audit)"
    cmd: |
      Copy-Item -Recurse data\training\baseline_decisions data\training\baseline_decisions.plan-05-09
      # Verifica
      Test-Path data\training\baseline_decisions.plan-05-09\part-0.parquet
      # Atteso: True

  - id: STEP-3
    desc: "Edit baseline.yaml LOCALLY (no commit stasera)"
    instructions: |
      Apri `data\configs\baseline.yaml` e modifica le righe 40-41:

      DA:
        date_start: "2015-05-08"
        date_end:   "2025-05-08"

      A:
        date_start: null
        date_end:   null

      Salva. NO `git commit`. La modifica resta locale per stanotte.
      (Plan-check iter 1 deciderà se/come commitare la modifica finale.)

  - id: STEP-4
    desc: "Verifica config load + date range nullo"
    cmd: |
      python -c "from backtest.baseline.runner import load_baseline_config; c=load_baseline_config(); print('date_start:', c.date_start, 'date_end:', c.date_end)"
      # Atteso: date_start: None date_end: None

  - id: STEP-5
    desc: "Lancio full run (8-15h wall-clock proiettato)"
    cmd: |
      .venv\Scripts\Activate.ps1
      python scripts\run_baseline_05_09.py 2>&1 | Tee-Object -FilePath 05-10-RUN.log

  - id: STEP-6
    desc: "Al risveglio: verifica output"
    cmd: |
      python -c "import pyarrow.parquet as pq, pandas as pd; t=pq.read_table('data/training/baseline_decisions/part-0.parquet'); df=t.to_pandas(); print(f'Rows: {t.num_rows}, Cols: {t.num_columns}'); print(df.groupby('profile').size()); print('Time range:', df['decision_ts_utc'].min(), '->', df['decision_ts_utc'].max())"

validation_criteria:
  - id: VAL-1
    desc: "27/27 run ok"
    check: |
      Log finale dello script: `RESULTS=27/27 ok (skipped=0 failed=0)`
  - id: VAL-2
    desc: "Schema preserved schema-v2 (~59 cols)"
    check: |
      Parquet `data/training/baseline_decisions/part-0.parquet`:
      n_cols >= 55, contiene 'rsi_14', 'atr_14', 'profile', 'regime_state',
      'run_id', 'decision_ts_utc'.
  - id: VAL-3
    desc: "Row count sanity (>=2000, atteso 2500-3000)"
    check: |
      Parquet rows >= 2000. Se rows in [1500, 2000) -> investigate
      (forse data CSV non copre tutto il range presunto, o date filter
      ha edge case).
  - id: VAL-4
    desc: "Time coverage 2002-2026"
    check: |
      df['decision_ts_utc'].min() <= 2003-12-31 AND df['decision_ts_utc'].max() >= 2025-12-31
      (margine perché early-2002 ha warmup bars skip e il decision_ts è
      sempre dopo il warmup `max(200, longest_lookback)`).
  - id: VAL-5
    desc: "Wall-clock dentro 16h (cap soft warning)"
    check: |
      WALL_CLOCK_SECONDS <= 57600 (16h). Se >16h -> probabile saturazione
      RAM (swap kicks in) -> investigate.
  - id: VAL-6
    desc: "Distribuzione profili coerente (AGGRESSIVE > MODERATE > CONSERVATIVE)"
    check: |
      AGGRESSIVE count > MODERATE count > CONSERVATIVE count
      (sanity check: stessa logica di Plan 05-09 dove era 540 > 448 > 88
      -> ora atteso scaling ~2.5x: 1300 > 1100 > 220 indicative).

risks:
  - id: R-1
    desc: "Wall-clock supera 15h"
    mitigation: |
      Stima proiezione lineare O(N²) approssimativa. Se >16h, valutare
      kill + scope reduction (es: 15y window invece di 23.6y) e re-run.
  - id: R-2
    desc: "RAM 16 GB satura -> swap -> degrado wall-clock 3-5x"
    mitigation: |
      Pre-flight: chiudi applicazioni pesanti (SETUP-SECONDARY-PC.md §3.0).
      Worker = 9 (baseline.yaml:16) non cambia, ma window 2.5x più larga
      -> picco RAM stimato ~14 GB. Margine ~2 GB stretto.
      Mitigazione futura: ridurre max_workers a 6 in baseline.yaml come
      knob "ridotta-RAM" per Plan 05-10 se R-2 si materializza.
  - id: R-3
    desc: "Early-2002 data ha warmup bars problem"
    mitigation: |
      `warm_up_min_bars: 200` (baseline.yaml:14) + longest_lookback_required.
      Su M15: 200 bar = 50h ≈ 2 giorni di trading -> decision_ts effettivo
      parte da ~2002-10-23 (ok). Su H1: 200 bar = ~8 giorni trading
      -> primo decision ~2002-10-31. Trascurabile, già coperto da test
      `test_warmup_skips_first_lookback_bars` in suite.
  - id: R-4
    desc: "Determinism reduced - slippage seed derivato da run_id che ora include nuove date"
    mitigation: |
      `slippage_seed: 42` resta (baseline.yaml:7) ma run_id include
      first.time/last.time hash -> different da Plan 05-09. Output non
      bit-comparable col Plan 05-09 by design. Re-run dello STESSO
      Plan 05-10 con stesso CSV deve essere bit-deterministic.

success_criteria:
  - id: SC-1
    desc: "Parquet finale schema-v2 produced, row count >= 2000"
    proof: VAL-2 + VAL-3
  - id: SC-2
    desc: "Wall-clock < 16h"
    proof: VAL-5
  - id: SC-3
    desc: "Audit trail integro (Plan 05-09 archived, baseline_runs SHA tracked)"
    proof: |
      `data/training/baseline_decisions.plan-05-09/part-0.parquet` esiste con
      1076 rows.
      `logs/trades.db` contiene rows in `backtest_runs` con
      `baseline_yaml_sha256` diverso da quello di Plan 05-09 (config
      cambiata).
  - id: SC-4
    desc: "Distribuzione coerente (sanity)"
    proof: VAL-6

deliverables:
  - data/training/baseline_decisions/part-0.parquet (~2500-3000 × 59)
  - data/training/baseline_decisions.plan-05-09/part-0.parquet (1076 × 59, audit backup)
  - 05-10-RUN.log (full execution log)
  - .planning/research/baseline-2026-05-13.md (auto-generato dal report_writer)
  - 05-10-SUMMARY.md (post-execution, redatto domani)

post_execution_actions:
  - Commit + push deliverables sul secondario:
    `git add data/training/baseline_decisions/ data/training/baseline_decisions.plan-05-09/ data/configs/baseline.yaml 05-10-RUN.log .planning/research/baseline-2026-05-13.md`
    `git commit -m "feat(05-10): full-history baseline backtest — 23.6y, ~XXXX rows, schema-v2"`
    `git push origin feature/update-pythono-pure-strategy`
  - Sul primario: `git pull`
  - Plan-check iter 1 retroattivo (decide se baseline.yaml stays committed
    con null/null o se serve baseline_full.yaml separato o CLI flag)
  - Update STATE.md con esito Plan 05-10
  - Re-evaluate Phase 7 plans con dataset ~2.5x più grande (potenziale impact su
    fold size walk_forward, calibration sample size, p95 latency benchmark)

iteration_log:
  - iter: 0
    date: 2026-05-12
    action: DRAFT creato per supportare execution informale stanotte sul PC secondario
    plan_checker: SKIPPED (user decision)
  - iter: 1 (planned)
    date: 2026-05-13
    action: plan-check retroattivo + formalizzazione baseline.yaml change strategy
