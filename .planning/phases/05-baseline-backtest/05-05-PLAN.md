---
phase: 05-baseline-backtest
plan: 05
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - backtest/engine.py
  - backtest/broker.py    # WARNING 9 conditional: solo se preflight rileva force_close mancante
  - backtest/metrics.py
  - tests/test_baseline_no_future_leakage.py
autonomous: true
requirements: [BACK-07, INT-01]
tags: [phase-5, baseline-backtest, wave-2, engine-extension, metrics]

phase1_dependency_risk: |
  Modifica additive a backtest/engine.py + backtest/metrics.py — Phase 1 plans 01-05 (engine)
  e 01-07 (metrics) PENDING. Coordinamento: questo Plan EXTENDS i moduli ASSUMENDO la
  signature corrente (verificata via Read di backtest/engine.py:91-103 — esistente).
  Se Phase 1 04-05 viene committato prima di Phase 5 05-05, eseguire merge: i nuovi kwargs
  Phase 5 sono additive con default None — backwards compat garantita.

must_haves:
  truths:
    - "BacktestEngine.__init__ accetta indicators_full, risk_profile, timeout_bars, equity_initial come kwargs additive (default None)"
    - "Phase 1 caller esistenti (run_backtest) continuano a funzionare invariati"
    - "Quando risk_profile è impostato, cfg.RISK_MODE viene popolato per risk_engine.evaluate_trade"
    - "Quando timeout_bars è impostato, posizioni aperte oltre timeout chiudono con exit_reason='TIMEOUT_CLOSE'"
    - "BacktestMetrics include longest_dd_days (compute_metrics popolato)"
    - "compute_all_extended(bars[:i+1]).field[-1] == compute_all_extended(bars).field[i] (no future leakage invariant)"
  artifacts:
    - path: "backtest/engine.py"
      provides: "engine esteso con 4 kwargs Phase 5 + timeout enforcement"
      contains: "indicators_full"
      contains: "risk_profile"
      contains: "timeout_bars"
      contains: "equity_initial"
      contains: "TIMEOUT_CLOSE"
    - path: "backtest/metrics.py"
      provides: "BacktestMetrics con longest_dd_days"
      contains: "longest_dd_days"
  key_links:
    - from: "backtest/engine.py::BacktestEngine"
      to: "backtest/baseline/slice_worker.py (Wave 2 — 05-06)"
      via: "slice_worker istanzia engine con kwargs Phase 5"
    - from: "backtest/metrics.py::compute_metrics"
      to: "backtest/baseline/report_writer.py (Wave 2 — 05-06)"
      via: "report popola colonna longest_dd_days nella tabella 27-row"
---

<objective>
Wave 2 (parte engine) — estensione additive a `backtest/engine.py` per accettare i 4 kwargs
richiesti dal slice_worker Phase 5 (`indicators_full`, `risk_profile`, `timeout_bars`,
`equity_initial`) e implementare l'enforcement del timeout per-TF (D-05). Estensione di
`backtest/metrics.py` per includere `longest_dd_days` nella tabella report (D-18).

Inoltre: implementa il test invariant `test_indicator_full_slice_equals_recompute` (D-21
no-future-leakage) — questo è il gate critico Phase 5 SC#3 dataset integrity.

Output:
- `backtest/engine.py` esteso (additive — Phase 1 callers invariati)
- `backtest/metrics.py` esteso con `longest_dd_days`
- 1 test no-future-leakage implementato (test_indicator_full_slice_equals_recompute) — gli
  altri 2 stub di `test_baseline_no_future_leakage.py` rimangono skip → 05-06 (slice_worker)

Purpose: senza estensione engine, slice_worker (05-06) non può iniettare la cache indicators
pre-computed (D-15) né attivare timeout per-TF (D-05). Senza `longest_dd_days`, report
writer (05-06) non può popolare la colonna richiesta da D-18 schema.
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-CONTEXT.md
@C:/trading-agent/.planning/phases/01-backtest-engine/01-CONTEXT.md
@C:/trading-agent/backtest/engine.py
@C:/trading-agent/backtest/broker.py
@C:/trading-agent/backtest/metrics.py
@C:/trading-agent/risk_engine.py

<interfaces>
From backtest/engine.py (existing signature, lines 91-103):
```python
def __init__(
    self, bars: list[Bar], symbol: str, timeframe: str, cost_model: CostModel,
    cfg: Config | None = None, initial_balance: float = 10_000.0,
    run_id: str | None = None, ledger: LedgerWriter | None = None,
    fold_index: int | None = None, cost_yaml_hash: str = "nohash",
) -> None:
```

From PATTERNS.md §`backtest/engine.py (MODIFY)` lines 453-483:
- Phase 5 additions sono additive: 4 nuovi kwargs default-None
- Profile injection: `if risk_profile is not None: self.cfg.RISK_MODE = risk_profile`
- Timeout enforcement: extend `_check_sl_tp` o aggiungere step prima nel loop principale

From backtest/metrics.py (existing dataclass — verificare che `avg_r` esista):
- `BacktestMetrics` deve esporre: sharpe, sortino, max_drawdown_pct, **longest_dd_days (NEW)**, hit_rate, expectancy_usd, profit_factor, avg_r, total_trades, total_pnl_usd
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backtest/engine.py — 4 kwargs Phase 5 + timeout enforcement</name>
  <files>backtest/engine.py</files>
  <read_first>
    - C:/trading-agent/backtest/engine.py (file completo — signature corrente + main loop)
    - C:/trading-agent/backtest/broker.py (VirtualPosition.entry_bar_index — già presente Phase 1)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md §`backtest/engine.py (MODIFY)` lines 453-483
    - C:/trading-agent/risk_engine.py (verifica `RISK_MODE` consumption)
  </read_first>
  <action>
    **Sub-task 1a — Estendi `BacktestEngine.__init__` signature.**

    Modifica solo la firma + body. Phase 1 callers (`run_backtest()` line 309-342)
    invocano senza i 4 nuovi kwargs → default None preserva comportamento.

    Patch:
    ```python
    def __init__(
        self,
        bars: list[Bar],
        symbol: str,
        timeframe: str,
        cost_model: CostModel,
        cfg: Config | None = None,
        initial_balance: float = 10_000.0,
        run_id: str | None = None,
        ledger: LedgerWriter | None = None,
        fold_index: int | None = None,
        cost_yaml_hash: str = "nohash",
        # ── Phase 5 additions (additive, default None) ─────────────────
        indicators_full: dict | None = None,
        risk_profile: str | None = None,
        timeout_bars: int | None = None,
        equity_initial: float | None = None,
    ) -> None:
        if not bars:
            raise ValueError("BacktestEngine requires at least one bar")
        self.bars = bars
        self.symbol = symbol
        self.timeframe = timeframe
        self.cost_model = cost_model
        self.cfg = _always_open_cfg(cfg)
        # Phase 5: equity_initial alias di initial_balance per chiarezza orchestrator
        self.initial_balance = float(equity_initial if equity_initial is not None else initial_balance)
        self.fold_index = fold_index
        self.cost_yaml_hash = cost_yaml_hash
        self.ledger = ledger
        self.run_id = run_id or _compute_run_id(
            symbol, timeframe, bars[0].time, bars[-1].time, cost_yaml_hash,
        )
        # Phase 5 additions
        self.indicators_full = indicators_full
        self.timeout_bars = timeout_bars
        # Profile injection: imposta RISK_MODE consumato da risk_engine.evaluate_trade
        if risk_profile is not None:
            self.cfg.RISK_MODE = risk_profile
    ```

    **Sub-task 1b — Timeout enforcement.**

    Nel main loop di `BacktestEngine.run()` (lines 119+), localizzare il punto dove
    `broker.advance(bar)` viene chiamato. PRIMA o DOPO advance, aggiungere check timeout:

    ```python
    # Phase 5 D-05: timeout per-TF
    if self.timeout_bars is not None:
        # broker.virtual_positions è la list of open positions (Phase 1 broker.py)
        for pos in list(broker.virtual_positions):
            bars_held = current_bar_index - pos.entry_bar_index
            if bars_held >= self.timeout_bars:
                # Force-close at current bar.close, exit_reason="TIMEOUT_CLOSE"
                broker.force_close(pos, current_bar, exit_reason="TIMEOUT_CLOSE")
    ```

    NB (WARNING 9 fix): il metodo `broker.force_close(pos, bar, exit_reason)` potrebbe non
    esistere in Phase 1 BacktestBroker. PRIMA di scrivere questo task, esegui
    `scripts/preflight_phase5.py` (Plan 05-01 Task 4): il probe segnala se `force_close`
    e `virtual_positions` (o equivalenti `open_positions` / `_positions`) esistono
    sulla classe corrente. Output preflight indica a quale strada proseguire:
      - Se `force_close` e `virtual_positions` ESISTONO: usali verbatim (engine timeout
        path scritto contro questi simboli).
      - Se `force_close` MANCA: AGGIUNGILO in `backtest/broker.py` come thin wrapper su
        `_close_position(pos, bar.close, exit_reason)` con signature
        `force_close(position_id, exit_price: float, exit_reason: str) -> ClosedTrade`.
        Includi questa modifica nei `files_modified` di Plan 05-05 al momento della
        execution (additive a Phase 1 plan 01-04).
      - Se `virtual_positions` MANCA ma esiste `open_positions` o `_positions`: usa quello
        e annota nel SUMMARY.
    Pattern: leverage existing close primitive, just pass il nuovo `exit_reason` constant.

    Definisci costante module-level: `TIMEOUT_CLOSE_REASON = "TIMEOUT_CLOSE"` e usala
    sia qui che in eventual reference Phase 5.

    **Sub-task 1c — Indicators cache injection (BLOCKER 5 fix: dual-branch).**

    Se `self.indicators_full` è impostato, l'engine NON ricalcola `compute_all_extended`
    bar-by-bar. Invece, per ogni step `i`, slicing della cache via due branch supportati
    (decisi in base alla shape effettiva di Phase 2 D-04 ExtendedIndicators API):

    ```python
    if self.indicators_full is not None:
        # BLOCKER 5 fix: dual-branch — branch detection runtime via hasattr.
        # `scripts/preflight_phase5.py` (Plan 05-01 Task 4) probe la shape e
        # documenta quale branch è attivo. Engine adapter è tolerant.
        if hasattr(self.indicators_full, "slice_until"):
            # Phase 2 dataclass-of-lists con method dedicato
            indicators_at_i = self.indicators_full.slice_until(i)
        else:
            # Phase 2 dict-of-lists (fallback)
            indicators_at_i = {
                k: v[:i+1] if isinstance(v, list) else v
                for k, v in self.indicators_full.items()
            }
    else:
        indicators_at_i = compute_all_extended(bars_so_far)  # Phase 1 default path
    ```

    Note plan-body (BLOCKER 5):
      - Fino a Phase 2 D-04 landed: test usa fixture dict-of-lists (synthetic_indicators_full
        in conftest.py).
      - Se Phase 2 sceglie dataclass-of-lists con slice_until(): branch automatico no
        change engine.
      - Se Phase 2 sceglie shape diversa (object con attribute access lazy): engine adapter
        DA RIVEDERE — flag esplicito in 05-05 SUMMARY per re-verification a Phase 2 done.
        `scripts/preflight_phase5.py` rileva e logga il branch in uso.

    NB: questo CONTRO Phase 4 D-13 drive-bar pattern. Verifica: lo slicing della cache
    fino a `i+1` produce risultato IDENTICO al recompute, finché Phase 2 indicator non
    ha "future leakage" by construction (Phase 2 D-09 garantisce). Test
    `test_indicator_full_slice_equals_recompute` (Task 3 sotto) valida invariant.

    **Sub-task 1d — Italiano commenti CLAUDE.md §Convenzioni.**
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -c "from backtest.engine import BacktestEngine; import inspect; sig = inspect.signature(BacktestEngine.__init__); params = set(sig.parameters); assert {'indicators_full', 'risk_profile', 'timeout_bars', 'equity_initial'}.issubset(params); print('OK', params)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "indicators_full" backtest/engine.py` ≥ 3 (param + assignment + usage)
    - `grep -c "risk_profile" backtest/engine.py` ≥ 2
    - `grep -c "timeout_bars" backtest/engine.py` ≥ 3
    - `grep -c "equity_initial" backtest/engine.py` ≥ 2
    - `grep -c "TIMEOUT_CLOSE" backtest/engine.py` ≥ 1
    - Phase 1 test esistenti `pytest tests/test_backtest_engine.py -x` exit 0 (regressione zero — backwards compat)
    - Smoke instanziare BacktestEngine senza i 4 kwargs nuovi → no exception
  </acceptance_criteria>
  <done>
    Engine esteso. 4 kwargs disponibili. Timeout enforcement attivo se `timeout_bars` impostato.
    Phase 1 test verdi (no regression).
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend backtest/metrics.py — longest_dd_days</name>
  <files>backtest/metrics.py</files>
  <read_first>
    - C:/trading-agent/backtest/metrics.py (file completo)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md §`backtest/metrics.py (MODIFY)` lines 486-509
  </read_first>
  <action>
    1. Aggiungere campo `longest_dd_days: float = 0.0` al dataclass `BacktestMetrics`
       (subito dopo `max_drawdown_pct`).

    2. In `compute_metrics(...)`, calcolare longest_dd_days come max run-length consecutiva
       di "underwater state" (equity < running peak) misurata in bar units.

       NB: il campo si chiama "_days" ma il calcolo dipende dal timeframe. Convenzione:
       calcolare la run-length massima in BAR UNITS, poi convertire a days assumendo
       timeframe attivo. Per ora restituire bar units come float — la conversione
       (M15→0.0104, M30→0.0208, H1→0.0416 days/bar) è applicata in `report_writer.py`
       via parametro `bars_per_day`.

       Implementazione:
       ```python
       def _longest_underwater_run(equity_curve: list[float]) -> int:
           """Conta lunghezza massima di run consecutivo dove equity[t] < running_peak."""
           if not equity_curve:
               return 0
           peak = equity_curve[0]
           current_run = 0
           max_run = 0
           for eq in equity_curve:
               if eq > peak:
                   peak = eq
                   current_run = 0
               else:
                   current_run += 1
                   max_run = max(max_run, current_run)
           return max_run
       ```

    3. In `compute_metrics(...)`, dopo il calcolo di `max_drawdown_pct`, aggiungere:
       ```python
       longest_dd_bars = _longest_underwater_run(equity_curve_values)
       # NB: caller (report_writer) può convertire bar→days; qui restituiamo bar units
       # come float per compatibilità tipo del dataclass
       longest_dd_days = float(longest_dd_bars)
       ```
       e popolare il nuovo campo del dataclass.

    4. Italiano commenti.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -c "from backtest.metrics import BacktestMetrics; from dataclasses import fields; names = {f.name for f in fields(BacktestMetrics)}; assert 'longest_dd_days' in names; print('OK', names)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "longest_dd_days" backtest/metrics.py` ≥ 2 (dataclass field + compute_metrics assignment)
    - `grep -c "_longest_underwater_run" backtest/metrics.py` ≥ 2 (def + call)
    - `pytest tests/test_backtest_metrics.py -x` exit 0 se file esiste (regression Phase 1)
    - Smoke: `BacktestMetrics(...).longest_dd_days` accessibile
  </acceptance_criteria>
  <done>
    Metrics esteso con longest_dd_days. Phase 1 test verdi.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_baseline_no_future_leakage.py — implementa test_indicator_full_slice_equals_recompute</name>
  <files>tests/test_baseline_no_future_leakage.py</files>
  <read_first>
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-VALIDATION.md row D-21 (riga 47)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-CONTEXT.md §D-21 (line 95-96)
    - C:/trading-agent/tests/conftest.py (synthetic_bars + synthetic_indicators_full fixtures)
  </read_first>
  <behavior>
    - Test 1 (BLOCKER 4 REAL D-21): `test_indicator_full_slice_equals_recompute(synthetic_bars)` — D-21 no-future-leakage REAL invariant.

      Importa `compute_all_extended` da `indicators` (Phase 2). Per i ∈ {30, 60, 99}:
      verifica `compute_all_extended(synthetic_bars[:i+1]).atr[-1]` == `compute_all_extended(synthetic_bars).atr[i]`
      entro `np.isclose(rtol=1e-9)`. Stesso check su `ema20`, `rsi` (3 indicator sample).

      **BLOCKER 4 fix**: il test precedente era una tautologia di indicizzazione lista
      (full[:i+1][-1] == full[i] sempre vero per costruzione). Il D-21 reale verifica
      che il calcolo da `bars[:i+1]` e il valore i-esimo del calcolo full siano IDENTICI
      (no future leakage from indicator computation). Solo Phase 2 può fornire questa
      garanzia.

      **Phase 2 dependency**: se Phase 2 ExtendedIndicators API non è ancora landed, il test
      è marcato `@pytest.mark.skip(reason="awaits Phase 2 D-04 ExtendedIndicators API + Plan 05-05 reactivation")`.
      Quando Phase 2 plan 02-XX è completo, rimuovere lo skip e il test diventa attivo.

      Pattern test (BLOCKER 4 reale):
      ```python
      def test_indicator_full_slice_equals_recompute(synthetic_bars):
          """D-21 REAL no-future-leakage: compute_all_extended invariant per i in {30,60,99}."""
          import numpy as np
          try:
              from indicators import compute_all_extended  # type: ignore
          except ImportError:
              pytest.skip("Phase 2 indicators.compute_all_extended non landed")

          full = compute_all_extended(synthetic_bars)
          for key in ("atr", "ema20", "rsi"):
              full_series = getattr(full, key, None) if not isinstance(full, dict) else full.get(key)
              if full_series is None:
                  pytest.skip(f"Phase 2 ExtendedIndicators non espone {key}")
              for i in (30, 60, 99):
                  partial = compute_all_extended(synthetic_bars[:i+1])
                  partial_series = (getattr(partial, key, None) if not isinstance(partial, dict)
                                    else partial.get(key))
                  assert np.isclose(partial_series[-1], full_series[i], rtol=1e-9),                       f"D-21 violato: key={key} i={i} partial_last={partial_series[-1]} full_at_i={full_series[i]}"
      ```
  </behavior>
  <action>
    Aprire `tests/test_baseline_no_future_leakage.py` (creato da 05-02 come stub).

    Implementare `test_indicator_full_slice_equals_recompute` (body come in `<behavior>` Test 1).

    **BLOCKER 4 condizionale**: se `from indicators import compute_all_extended` solleva
    ImportError (Phase 2 non landed), il test esegue `pytest.skip(...)` runtime invece
    di fallire — degradazione graceful. Quando Phase 2 plan 02-XX completa, il test
    diventa effettivo automaticamente (no skip marker da rimuovere — il guard è dentro
    il body).

    In alternativa, se Phase 2 e' chiaramente lontana al momento dell'esecuzione di Plan
    05-05: apporre `@pytest.mark.skip(reason="awaits Phase 2 D-04 ExtendedIndicators API + Plan 05-05 reactivation")`
    al test e creare TODO esplicito per la reactivation gate (commit reattivazione quando
    Phase 2 e' verde).

    Le altre 2 stub functions (`test_decision_dataset_temporal_ordering` e
    `test_entry_at_next_bar_open`) RIMANGONO skipped — saranno implementate in 05-06
    (slice_worker) quando il dataset è prodotto end-to-end.

    Italiano docstring esteso, inglese function name (snake_case Python).
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_baseline_no_future_leakage.py::test_indicator_full_slice_equals_recompute -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@pytest.mark.skip" tests/test_baseline_no_future_leakage.py` == 2 (3 stub - 1 implementato)
    - `pytest tests/test_baseline_no_future_leakage.py::test_indicator_full_slice_equals_recompute -x` exit 0
    - Test funzione body contiene loop su >=3 indicator keys (verificabile via grep `for key in`)
  </acceptance_criteria>
  <done>
    Slicing semantics verificata. Wave 3 può confidare che cache lookup è equivalente a recompute.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BacktestEngine kwargs (esterni → engine) | Validazione in `__init__` (None defaults safe) |
| indicators_full dict (cache → engine) | Schema validation deferita — assume dict-of-lists Phase 2 D-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | risk_profile string injection | mitigate | Caller (slice_worker) usa solo costanti hard-coded `["CONSERVATIVE", "MODERATE", "AGGRESSIVE"]` — no user input vector |
| T-05-13 | Information Disclosure | indicators_full memory share | accept | per-process cache (worker spawn isolato) — no cross-process leak |
| T-05-14 | Denial of Service | timeout_bars=0 → close immediato | mitigate | `if bars_held >= self.timeout_bars` — `timeout_bars=None` skip enforcement; `timeout_bars=0` chiude tutto al primo bar (intentional, no DoS) |
</threat_model>

<verification>
- BacktestEngine signature estesa con 4 kwargs (verifica via `inspect.signature`)
- Phase 1 test backward compat green (no regression)
- BacktestMetrics include longest_dd_days
- Test no-future-leakage primo invariant verde (slicing semantics)
</verification>

<success_criteria>
- D-05 timeout per-TF activabile via kwargs
- D-15 indicator cache injectable senza modificare loop bar-by-bar
- D-18 longest_dd_days disponibile per report writer
- Phase 1 backwards compat preserved
</success_criteria>

<output>
Dopo completamento: `.planning/phases/05-baseline-backtest/05-05-SUMMARY.md`.
Sezioni: Files Modified, New Engine kwargs, Timeout Enforcement, longest_dd_days impl, Phase 1 Regression Status, Next Wave (05-06 slice_worker + report).
</output>
