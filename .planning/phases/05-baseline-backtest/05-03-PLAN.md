---
phase: 05-baseline-backtest
plan: 03
type: execute
wave: 1
depends_on: [05-01, 05-02]
files_modified:
  - backtest/baseline/determinism.py
  - backtest/baseline/wal_setup.py
  - tests/test_baseline_determinism.py
  - tests/test_baseline_wal.py
autonomous: true
requirements: [BACK-07, INT-01]
tags: [phase-5, baseline-backtest, wave-1, determinism, sqlite-wal]

must_haves:
  truths:
    - "seed_for_run_id('baseline_2026-05-08_EURUSD_M15_MODERATE') ritorna lo stesso int 31-bit cross-process e cross-run"
    - "file_sha256(path) ritorna hex hash stabile (sha256, non md5, non hash())"
    - "enable_sqlite_wal(db) attiva PRAGMA journal_mode=wal + busy_timeout=30000"
    - "with_retry(fn) ritenta fino a 5 volte solo su sqlite3.OperationalError 'locked'"
    - "9 worker concorrenti che inseriscono in logs/trades.db non perdono righe e non sollevano OperationalError finale"
  artifacts:
    - path: "backtest/baseline/determinism.py"
      provides: "seed_for_run_id, file_sha256, make_rng (sostituisce hash() Python)"
      contains: "hashlib.sha256"
      contains: "def seed_for_run_id"
      contains: "def file_sha256"
      contains: "def make_rng"
      min_lines: 30
    - path: "backtest/baseline/wal_setup.py"
      provides: "enable_sqlite_wal, open_worker_connection, with_retry"
      contains: "PRAGMA journal_mode=WAL"
      contains: "PRAGMA busy_timeout"
      contains: "def with_retry"
      contains: "OperationalError"
      min_lines: 40
  key_links:
    - from: "backtest/baseline/determinism.py::seed_for_run_id"
      to: "backtest/baseline/slice_worker.py (Wave 2) — passa seed a BacktestBroker"
      via: "make_rng(run_id) per slippage uniform [-S, +S]"
    - from: "backtest/baseline/wal_setup.py::enable_sqlite_wal"
      to: "backtest/baseline/runner.py (Wave 3) — chiamato una volta dal main"
      via: "main process invoca prima del ProcessPoolExecutor"
    - from: "backtest/baseline/wal_setup.py::with_retry"
      to: "backtest/ledger.py LedgerWriter.insert_trades"
      via: "wrapper su INSERT batch — retry su SQLITE_BUSY"
---

<objective>
Wave 1 — modulo determinismo + setup WAL. Implementa due moduli foundation indipendenti che
non hanno dependencies cross-modulo dentro la phase:

1. **`backtest/baseline/determinism.py`** — sostituisce `hash()` Python (PYTHONHASHSEED randomized,
   non riproducibile cross-run) con `hashlib.sha256` esplicito. Espone `seed_for_run_id`,
   `file_sha256`, `make_rng`.

2. **`backtest/baseline/wal_setup.py`** — abilita SQLite WAL + busy_timeout per multi-writer
   concorrente (9 worker). Espone `enable_sqlite_wal`, `open_worker_connection`, `with_retry`
   (riusa shape `mt5_client._retry` con jitter + narrow exception).

Output: 2 moduli + 2 file test passanti (rimuovere `@pytest.mark.skip` dai 6 stub di 05-02).

Purpose: D-17 audit trail e D-16 multi-writer non hanno alternative in repo. Senza questi
moduli, il Wave 3 runner non può seedare deterministicamente né scrivere concorrentemente.
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-VALIDATION.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-CONTEXT.md
@C:/trading-agent/mt5_client.py
@C:/trading-agent/logger.py
@C:/trading-agent/backtest/engine.py

<interfaces>
From mt5_client.py (analog `_retry` decorator, lines 26-39):
```python
def _retry(n: int = 3):
    def decorator(fn):
        @functools.wraps(fn)
        def wrapper(*args, **kwargs):
            last_exc = None
            for attempt in range(n):
                try:
                    return fn(*args, **kwargs)
                except Exception as exc:
                    last_exc = exc
                    logger.warning("retry %d/%d for %s: %s", attempt + 1, n, fn.__name__, exc)
            raise last_exc
        return wrapper
    return decorator
```

From logger.py (analog WAL pragma block, lines 61-63):
```python
with sqlite3.connect(_db_path) as conn:
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute(_TRADES_LOG_SCHEMA)
    conn.commit()
```

From backtest/engine.py (existing md5 to UPGRADE to sha256, lines 59-69):
```python
def _compute_run_id(symbol, timeframe, first_time, last_time, cost_hash):
    payload = f"{symbol}|{timeframe}|{first_time}|{last_time}|{cost_hash}"
    return hashlib.md5(payload.encode("utf-8")).hexdigest()[:16]
```
NB: questo Phase 1 helper NON va modificato (usato per run_id Phase 1). Il modulo Phase 5
`determinism.py` ne è separato e usa SHA256 — coesistono.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backtest/baseline/determinism.py — seed sha256 + file hashes + RNG</name>
  <files>backtest/baseline/determinism.py, tests/test_baseline_determinism.py</files>
  <read_first>
    - C:/trading-agent/backtest/engine.py lines 59-69 (md5 helper esistente — non modificare, riferimento)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md §`backtest/baseline/determinism.py` (lines 260-295)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md §Pitfall su Python hash() randomization
  </read_first>
  <behavior>
    - Test 1: `test_seed_reproducibility` — `seed_for_run_id("baseline_2026-05-08_EURUSD_M15_MODERATE")` chiamato 2× ritorna identico int. Range: 0 ≤ seed < 2**31.
    - Test 2: `test_seed_reproducibility_cross_process` — subprocess `.venv\Scripts\python.exe -c "from backtest.baseline.determinism import seed_for_run_id; print(seed_for_run_id('baseline_2026-05-08_EURUSD_M15_MODERATE'))"` ritorna stesso intero del processo padre (questo dimostra che PYTHONHASHSEED non influenza il risultato — sha256 è deterministico).
    - Test 3: `test_config_hash_matches` — `file_sha256(tmp_path / 'cfg.yaml')` == `hashlib.sha256(content).hexdigest()` (sha256, non md5; full hex, non troncato).
    - Test extra: `test_make_rng_reproducible` — `make_rng("rid").uniform(-1, 1)` chiamato 2× su 2 RNG distinti ritorna stessa sequenza.
  </behavior>
  <action>
    **Sub-task 1a — Crea `backtest/baseline/determinism.py`:**

    Contenuto verbatim da PATTERNS.md (lines 277-295), con docstring italiano:

    ```python
    """Determinism helpers per baseline backtest (D-17).

    Sostituisce Python `hash()` (PYTHONHASHSEED randomized, non riproducibile cross-run)
    con `hashlib.sha256` esplicito. Espone:
      - seed_for_run_id(run_id): int 31-bit deterministico per BacktestBroker slippage RNG
      - file_sha256(path): hex digest completo per audit trail (cost.yaml, strategy.yaml,
        baseline.yaml hashes scritti in backtest_runs)
      - make_rng(run_id): np.random.Generator pre-seedato

    Source: hashlib stdlib, numpy.random.default_rng. RESEARCH.md §Pitfall 1.
    """
    from __future__ import annotations

    import hashlib
    from pathlib import Path

    import numpy as np


    def seed_for_run_id(run_id: str) -> int:
        """31-bit seed deterministico cross-run, cross-machine.

        Usa SHA256 dei primi 4 byte → int big-endian → mask 0x7FFFFFFF (positive 31-bit).
        """
        digest = hashlib.sha256(run_id.encode("utf-8")).digest()
        return int.from_bytes(digest[:4], "big") & 0x7FFFFFFF


    def file_sha256(path: Path) -> str:
        """Config file hash per backtest_runs audit trail (D-17).

        Hex digest completo (64 char). Letto come bytes — encoding-agnostic, line-ending
        sensible (CRLF vs LF cambia hash; gestito a CI livello).
        """
        return hashlib.sha256(Path(path).read_bytes()).hexdigest()


    def make_rng(run_id: str) -> np.random.Generator:
        """Crea numpy Generator pre-seedato deterministicamente da run_id."""
        return np.random.default_rng(seed_for_run_id(run_id))
    ```

    **Sub-task 1b — Implementa i 4 test in `tests/test_baseline_determinism.py`:**

    Rimuovere il marker `@pytest.mark.skip` dagli stub di 05-02. Implementa i body:

    ```python
    """Tests for backtest.baseline.determinism (D-17)."""
    from __future__ import annotations

    import hashlib
    import subprocess
    import sys
    from pathlib import Path

    import pytest

    from backtest.baseline.determinism import seed_for_run_id, file_sha256, make_rng


    def test_seed_reproducibility() -> None:
        """D-17: stesso run_id → stesso seed dentro lo stesso processo."""
        rid = "baseline_2026-05-08_EURUSD_M15_MODERATE"
        s1 = seed_for_run_id(rid)
        s2 = seed_for_run_id(rid)
        assert s1 == s2
        assert 0 <= s1 < 2**31


    def test_seed_reproducibility_cross_process(tmp_path: Path) -> None:
        """D-17: subprocess deve ottenere stesso seed (no PYTHONHASHSEED dependency)."""
        rid = "baseline_2026-05-08_EURUSD_M15_MODERATE"
        expected = seed_for_run_id(rid)
        # Invocare .venv python child
        result = subprocess.run(
            [sys.executable, "-c",
             "from backtest.baseline.determinism import seed_for_run_id; "
             f"print(seed_for_run_id('{rid}'))"],
            capture_output=True, text=True, check=True,
        )
        child_seed = int(result.stdout.strip())
        assert child_seed == expected


    def test_config_hash_matches(tmp_path: Path) -> None:
        """D-17: file_sha256 == hashlib.sha256(content).hexdigest()."""
        path = tmp_path / "cfg.yaml"
        content = b"foo: bar\nbaz: 42\n"
        path.write_bytes(content)
        expected = hashlib.sha256(content).hexdigest()
        assert file_sha256(path) == expected
        assert len(file_sha256(path)) == 64  # SHA256 = 64 hex chars


    def test_make_rng_reproducible() -> None:
        """D-17: 2 RNG dallo stesso run_id producono sequenza identica."""
        rng1 = make_rng("rid")
        rng2 = make_rng("rid")
        seq1 = [rng1.uniform(-1, 1) for _ in range(5)]
        seq2 = [rng2.uniform(-1, 1) for _ in range(5)]
        assert seq1 == seq2
    ```
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_baseline_determinism.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - File `backtest/baseline/determinism.py` esiste, ≥30 righe
    - `grep -c "hashlib.sha256" backtest/baseline/determinism.py` ≥ 2 (in seed + in file_sha256)
    - `grep -c "PYTHONHASHSEED" backtest/baseline/determinism.py` == 0 (NO `hash()` Python)
    - `pytest tests/test_baseline_determinism.py -x` exit 0 (4 test pass, 0 skip rimossi)
    - `python -c "from backtest.baseline.determinism import seed_for_run_id; print(seed_for_run_id('x'))"` exit 0
  </acceptance_criteria>
  <done>
    Modulo determinism implementato + 4 test verdi cross-process. Phase 5 D-17 audit-trail
    primitive disponibili.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: backtest/baseline/wal_setup.py — WAL + busy_timeout + retry-with-jitter</name>
  <files>backtest/baseline/wal_setup.py, tests/test_baseline_wal.py</files>
  <read_first>
    - C:/trading-agent/mt5_client.py lines 26-39 (`_retry` decorator analog)
    - C:/trading-agent/logger.py lines 61-63 (WAL pragma block analog)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md §`backtest/baseline/wal_setup.py` lines 299-357
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md §Pattern 3 SQLite WAL multi-writer
  </read_first>
  <behavior>
    - Test 1: `test_wal_pragma_enabled(tmp_db_with_wal)` — dopo `enable_sqlite_wal(db)`, query `PRAGMA journal_mode` ritorna stringa `"wal"` (case-insensitive). `PRAGMA busy_timeout` ≥ 30000.
    - Test 2: `test_wal_concurrent_writes(tmp_path)` — 3 multiprocessing.Process worker eseguono 50 INSERT ciascuno su shared db; final SELECT count(*) == 150; nessun OperationalError unhandled.
    - Test 3: `test_with_retry_on_locked()` — funzione che fallisce 2 volte con `OperationalError("database is locked")` poi succede; `with_retry(fn, n=5)` ritorna risultato.
    - Test 4: `test_with_retry_propagates_non_lock()` — `OperationalError("syntax error")` (NON lock) → propagata immediatamente, no retry.
    - Test 5: `test_with_retry_max_attempts()` — fn sempre fallisce con "locked"; `with_retry(fn, n=3)` raise dopo 3 tentativi.
  </behavior>
  <action>
    **Sub-task 2a — Crea `backtest/baseline/wal_setup.py`:**

    Verbatim PATTERNS.md (lines 327-357) con docstring italiano:

    ```python
    """SQLite WAL setup + retry-with-jitter per 9-writer concorrenti (D-16).

    Riusa pattern mt5_client._retry (lines 26-39) — decoratore generico → funzione
    `with_retry(fn)` riapplicabile. Narrow exception class a sqlite3.OperationalError
    "locked" per evitare retry su syntax error / corrupt DB.

    Source: sqlite.org/wal.html, RESEARCH.md §Pattern 3.
    """
    from __future__ import annotations

    import logging
    import random
    import sqlite3
    import time
    from pathlib import Path
    from typing import Callable, TypeVar

    T = TypeVar("T")
    _log = logging.getLogger(__name__)


    def enable_sqlite_wal(db_path: Path) -> None:
        """Una tantum dal main process. WAL mode persiste cross-connection.

        - journal_mode=WAL: N reader + 1 writer simultanei (vs default rollback journal)
        - busy_timeout=30000ms: SQLite attende lock fino a 30s prima di sollevare BUSY
        - synchronous=NORMAL: WAL-safe + ~3x faster di FULL (durabilità OS-crash invece
          di power-loss; accettabile per backtest non-prod)
        """
        with sqlite3.connect(db_path) as conn:
            conn.execute("PRAGMA journal_mode=WAL")
            conn.execute("PRAGMA busy_timeout=30000")
            conn.execute("PRAGMA synchronous=NORMAL")
            conn.commit()


    def open_worker_connection(db_path: Path) -> sqlite3.Connection:
        """Per-worker connection. Ogni worker apre la propria — mai sharing cross-process."""
        conn = sqlite3.connect(db_path, timeout=30.0)  # 30s busy wait built-in
        conn.execute("PRAGMA busy_timeout=30000")
        conn.execute("PRAGMA synchronous=NORMAL")
        return conn


    def with_retry(fn: Callable[[], T], n: int = 5, base_delay: float = 0.05) -> T:
        """Riusa shape mt5_client._retry. Exponential backoff + jitter su SQLITE_BUSY.

        Narrow exception: solo `sqlite3.OperationalError` con messaggio contenente
        'locked'. Altri OperationalError (syntax, corrupt) propagati immediatamente.
        """
        last_exc: Exception | None = None
        for attempt in range(n):
            try:
                return fn()
            except sqlite3.OperationalError as exc:
                if "locked" not in str(exc).lower():
                    raise
                last_exc = exc
                delay = base_delay * (2 ** attempt) + random.uniform(0, base_delay)
                _log.warning(
                    "retry %d/%d sqlite locked: %s (sleep %.3fs)",
                    attempt + 1, n, exc, delay,
                )
                time.sleep(delay)
        assert last_exc is not None  # unreachable: range(n) >= 1
        raise last_exc
    ```

    **Sub-task 2b — Implementa 5 test in `tests/test_baseline_wal.py`:**

    Rimuove skip + implementa body. Test 2 (concurrent_writes) usa
    `multiprocessing.Process` + helper top-level (non lambda — pickle compat Windows spawn):

    ```python
    """Tests for backtest.baseline.wal_setup (D-16)."""
    from __future__ import annotations

    import multiprocessing as mp
    import sqlite3
    from pathlib import Path

    import pytest

    from backtest.baseline.wal_setup import enable_sqlite_wal, open_worker_connection, with_retry


    def test_wal_pragma_enabled(tmp_db_with_wal: Path) -> None:
        with sqlite3.connect(tmp_db_with_wal) as conn:
            mode = conn.execute("PRAGMA journal_mode").fetchone()[0]
            timeout = conn.execute("PRAGMA busy_timeout").fetchone()[0]
        assert mode.lower() == "wal"
        assert timeout >= 30000


    def _worker_insert(db_path: str, n_rows: int) -> None:
        """Top-level helper per multiprocessing spawn (Windows non picklerizza closures).

        WARNING 7 fix: NO lambda con `expr and conn.commit()` (truthiness bug — Cursor.execute
        ritorna oggetto cursor che e' truthy ma non documenta semantica). Helper esplicito.
        """
        from backtest.baseline.wal_setup import open_worker_connection, with_retry
        conn = open_worker_connection(Path(db_path))
        conn.execute("CREATE TABLE IF NOT EXISTS t (id INTEGER PRIMARY KEY, v INTEGER)")
        conn.commit()
        for i in range(n_rows):
            def _do_insert(_conn=conn, _i=i):
                _conn.execute("INSERT INTO t(v) VALUES(?)", (_i,))
                _conn.commit()
            with_retry(_do_insert)
        conn.close()


    def test_wal_concurrent_writes(tmp_path: Path) -> None:
        db = tmp_path / "trades.db"
        enable_sqlite_wal(db)
        # Init schema dal main per evitare race su CREATE
        with sqlite3.connect(db) as conn:
            conn.execute("CREATE TABLE IF NOT EXISTS t (id INTEGER PRIMARY KEY, v INTEGER)")
            conn.commit()
        ctx = mp.get_context("spawn")
        procs = [ctx.Process(target=_worker_insert, args=(str(db), 50)) for _ in range(3)]
        for p in procs:
            p.start()
        for p in procs:
            p.join(timeout=60)
            assert p.exitcode == 0, f"worker failed exit={p.exitcode}"
        with sqlite3.connect(db) as conn:
            count = conn.execute("SELECT count(*) FROM t").fetchone()[0]
        assert count == 150  # 3 worker × 50 row


    def test_with_retry_on_locked() -> None:
        attempts = {"n": 0}
        def fn():
            attempts["n"] += 1
            if attempts["n"] < 3:
                raise sqlite3.OperationalError("database is locked")
            return "ok"
        result = with_retry(fn, n=5, base_delay=0.001)
        assert result == "ok"
        assert attempts["n"] == 3


    def test_with_retry_propagates_non_lock() -> None:
        def fn():
            raise sqlite3.OperationalError("syntax error near 'foo'")
        with pytest.raises(sqlite3.OperationalError, match="syntax"):
            with_retry(fn, n=5, base_delay=0.001)


    def test_with_retry_max_attempts() -> None:
        def fn():
            raise sqlite3.OperationalError("database is locked")
        with pytest.raises(sqlite3.OperationalError, match="locked"):
            with_retry(fn, n=3, base_delay=0.001)
    ```
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_baseline_wal.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - File `backtest/baseline/wal_setup.py` esiste, ≥40 righe
    - `grep -c "PRAGMA journal_mode=WAL" backtest/baseline/wal_setup.py` ≥ 1
    - `grep -c "busy_timeout" backtest/baseline/wal_setup.py` ≥ 2
    - `grep -c "def with_retry" backtest/baseline/wal_setup.py` == 1
    - `grep -c "def enable_sqlite_wal" backtest/baseline/wal_setup.py` == 1
    - `grep -c "def open_worker_connection" backtest/baseline/wal_setup.py` == 1
    - `pytest tests/test_baseline_wal.py -x` exit 0 (5 test pass, anche `test_wal_concurrent_writes` su Windows spawn)
  </acceptance_criteria>
  <done>
    WAL setup + retry implementati + 5 test verdi (incluso multi-process concurrency su
    Windows spawn).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_id string → seed integer | Input non trusted lato library, ma confinato — runner.py costruisce run_id da costanti hard-coded (PAIRS, TFS, PROFILES, date) |
| sqlite3 OperationalError messaggio | String matching su "locked" — fragile a localizzazione SQLite (Python stdlib emette stringhe inglesi sempre) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | seed_for_run_id input | accept | run_id costruito da costanti hard-coded in runner — no user input |
| T-05-07 | Denial of Service | with_retry infinite loop | mitigate | `n=5` max attempts hard cap; backoff ≤ base_delay × 2**4 + jitter ≈ 0.85s totale max wait |
| T-05-08 | Repudiation | file_sha256 collision | accept | SHA256 collision cryptographically infeasible per audit trail purpose |
</threat_model>

<verification>
- 2 moduli baseline core implementati (determinism, wal_setup)
- 9 test totali pass (4 determinism + 5 wal)
- Cross-process determinism dimostrato via subprocess
- Multi-writer SQLite concurrency dimostrato via mp.Process (Windows spawn ctx)
- VALIDATION.md rows D-16 + D-17 spostate da pending → green
</verification>

<success_criteria>
- D-17 audit trail primitives esposte e testate
- D-16 multi-writer SQLite WAL configurato e testato
- Wave 2 può consumare: `slice_worker.py` userà `seed_for_run_id` per RNG slippage,
  `file_sha256` per audit hash; `runner.py` userà `enable_sqlite_wal` + `with_retry`
</success_criteria>

<output>
Dopo completamento: `.planning/phases/05-baseline-backtest/05-03-SUMMARY.md`.
Sezioni: Files Created, Tests Passing (9/9), Cross-Process Verification, Multi-Writer Verification, Next Wave (05-04 parallel + 05-05).
</output>
