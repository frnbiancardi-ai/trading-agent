---
phase: 05-baseline-backtest
plan: 02
type: execute
wave: 0
depends_on: [05-01]
files_modified:
  - tests/conftest.py
  - tests/test_baseline_runner.py
  - tests/test_baseline_dataset_writer.py
  - tests/test_baseline_no_future_leakage.py
  - tests/test_baseline_determinism.py
  - tests/test_baseline_wal.py
  - tests/test_baseline_plot_writer.py
  - tests/test_baseline_report_writer.py
  - tests/test_baseline_warmup.py
autonomous: true
requirements: [BACK-07, INT-01]
tags: [phase-5, baseline-backtest, wave-0, test-scaffolding, nyquist]

must_haves:
  truths:
    - "tests/conftest.py espone fixtures synthetic_bars, synthetic_indicators_full, tmp_db_with_wal"
    - "8 file tests/test_baseline_*.py + 1 file tests/test_baseline_warmup.py esistono con stub xfail/skip per ogni criterio VALIDATION.md"
    - "tests/test_baseline_warmup.py copre BLOCKER 3 D-07 (longest_lookback_required) — l'unico test NON-skipped del Plan 05-02 (helper già implementato in 05-01)"
    - "pytest tests/test_baseline_*.py --collect-only completa exit 0 (collection senza errori)"
    - "Ogni test stub (escluso warmup) ha @pytest.mark.skip(reason=...) FINO a Wave dove modulo target nasce"
  artifacts:
    - path: "tests/conftest.py"
      provides: "shared fixtures: synthetic_bars, synthetic_indicators_full, tmp_db_with_wal, mock_baseline_cfg"
      contains: "synthetic_bars"
      contains: "synthetic_indicators_full"
      contains: "tmp_db_with_wal"
    - path: "tests/test_baseline_runner.py"
      provides: "BACK-07 perf budget, D-14 idempotency, D-15 cache reuse, D-23 cost"
    - path: "tests/test_baseline_dataset_writer.py"
      provides: "INT-01 D-02/D-03 schema parquet"
    - path: "tests/test_baseline_no_future_leakage.py"
      provides: "D-21 no future, D-22 bar-close, D-22 entry-at-next-open"
    - path: "tests/test_baseline_determinism.py"
      provides: "D-17 seed reproducibility + config hash"
    - path: "tests/test_baseline_wal.py"
      provides: "D-16 concurrent writes, no `database is locked`"
    - path: "tests/test_baseline_plot_writer.py"
      provides: "D-19 PNG structure, D-20 Agg backend"
    - path: "tests/test_baseline_report_writer.py"
      provides: "D-18 report header + 27-row + appendix"
    - path: "tests/test_baseline_warmup.py"
      provides: "D-07 BLOCKER 3 fix — longest_lookback helper unit test"
      contains: "longest_lookback_required"
  key_links:
    - from: "tests/conftest.py"
      to: "tutti i test_baseline_*.py"
      via: "pytest fixture autodiscover"
    - from: "tests/test_baseline_*.py"
      to: "VALIDATION.md §Per-Task Verification Map"
      via: "ogni `<automated>` comando esegue questi test"
---

<objective>
Wave 0 test scaffolding (Nyquist compliance). Creare 8 stub test file + 1 test attivo
(`test_baseline_warmup.py`, BLOCKER 3 fix) + `conftest.py` con fixtures condivise. Ogni
test stub dichiara la struttura (signature + docstring + skip), così `pytest --collect-only`
riconosce i test e i Plan Wave 1+ possono "togliere lo skip" quando il modulo target è creato.

Purpose: Nyquist requirement — ogni `<verify><automated>` di tutti i Plan Wave 1+ punta
a uno di questi file. Senza scaffolding W0, Wave 1 task non hanno target verificabile e
violano la regola "ogni `<automated>` deve esistere prima del task target o creare il file".

REVISIONE 2026-05-08 (BLOCKER 3): aggiunto `tests/test_baseline_warmup.py` ATTIVO (NON
skip) — verifica unit `longest_lookback_required` implementato in Plan 05-01 Task 3b.

Output: 7 file `tests/test_baseline_*.py` (stub) + 1 `tests/test_baseline_warmup.py`
(non-stub, attivo) + `tests/conftest.py` (fixtures fully implemented — non-stub: i fixtures
sono shared infrastruttura).
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-VALIDATION.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-CONTEXT.md
@C:/trading-agent/tests/test_backtest_engine.py
@C:/trading-agent/tests/test_backtest_ledger.py
@C:/trading-agent/CLAUDE.md

<interfaces>
From tests/test_backtest_engine.py (Phase 1 — verbatim pattern):
- `_uptrend_bars(n: int) -> list[Bar]` helper synthetic-bar generator
- `tmp_path` fixture pytest builtin
- `_engine_5bar_fixture` shape: synthetic bars → engine.run() → assert ledger row

From tests/test_backtest_ledger.py:
- `tmp_path / "trades.db"` pattern, never touch `logs/trades.db` from tests
- Round-trip insert + select assert

From backtest/baseline/warmup.py (Plan 05-01 Task 3b):
- `longest_lookback_required(strategy_cfg) -> int`
- Default fallback: 200 se strategy_cfg None / non-parseable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/conftest.py — fixtures condivise (synthetic_bars, indicators, tmp_db_with_wal)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - C:/trading-agent/tests/test_backtest_engine.py (helper `_uptrend_bars` da copiare verbatim)
    - C:/trading-agent/tests/test_backtest_ledger.py (pattern tmp_path SQLite)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md §`tests/conftest.py`
  </read_first>
  <action>
    File NUOVO `tests/conftest.py`. Contenuto completo (NB: questo è infrastruttura — NON stub):

    ```python
    """Shared pytest fixtures per Phase 5 baseline test (D-15, D-16, D-21).

    Fixtures additive, NO autouse=True (evita pollution cross-test).
    """
    from __future__ import annotations

    import sqlite3
    from datetime import datetime, timezone
    from pathlib import Path

    import pytest


    @pytest.fixture
    def synthetic_bars() -> list:
        """100 bar uptrend determ. Mirror tests/test_backtest_engine.py::_uptrend_bars."""
        from backtest.loader import Bar  # import locale per evitare side-effect collection
        bars: list = []
        base_price = 1.1000
        base_time = int(datetime(2024, 1, 1, tzinfo=timezone.utc).timestamp())
        for i in range(100):
            bars.append(Bar(
                time=base_time + i * 900,  # M15 = 900s
                open=base_price + i * 0.0001,
                high=base_price + i * 0.0001 + 0.0002,
                low=base_price + i * 0.0001 - 0.0001,
                close=base_price + i * 0.0001 + 0.0001,
                tick_volume=100 + i,
                spread=2,
                real_volume=0,
            ))
        return bars


    @pytest.fixture
    def synthetic_indicators_full(synthetic_bars):
        """Mock ExtendedIndicators dataclass-of-lists. Phase 2 D-04 schema.

        Restituisce dict con chiavi minime per detector A/B/C/D + risk_engine.
        """
        n = len(synthetic_bars)
        # Lista lunga n con valori monotoni — placeholder finchè Phase 2 non finalizza.
        return {
            "atr": [0.0010] * n,
            "ema20": [b.close for b in synthetic_bars],
            "ema50": [b.close for b in synthetic_bars],
            "ema200": [b.close for b in synthetic_bars],
            "ema50_slope": [0.0001] * n,
            "rsi": [55.0] * n,
            "adx": [25.0] * n,
            "regime": ["normal"] * n,
        }


    @pytest.fixture
    def tmp_db_with_wal(tmp_path: Path) -> Path:
        """Tmp SQLite con WAL mode già attivato. Per test multi-writer."""
        db_path = tmp_path / "trades.db"
        with sqlite3.connect(db_path) as conn:
            conn.execute("PRAGMA journal_mode=WAL")
            conn.execute("PRAGMA busy_timeout=30000")
            conn.execute("PRAGMA synchronous=NORMAL")
            conn.commit()
        return db_path


    @pytest.fixture
    def mock_baseline_cfg(tmp_path: Path) -> Path:
        """Tmp baseline.yaml minimale per test runner."""
        path = tmp_path / "baseline.yaml"
        path.write_text(
            "equity_initial_eur: 10000\n"
            "slippage_seed: 42\n"
            "timeout_bars:\n"
            "  M15: 96\n"
            "  M30: 96\n"
            "  H1: 120\n"
            "warm_up_min_bars: 200\n"
            "max_workers: 2\n"  # ridotto per test
            "parquet_compression: snappy\n"
            "force_rerun: false\n"
            "progress_bar: false\n"
            'training_data_dir: "data/training"\n'
            'report_dir: ".planning/research"\n'
            'equity_curves_dir: ".planning/research/baseline-equity-curves"\n',
            encoding="utf-8",
        )
        return path
    ```
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/conftest.py --collect-only --tb=short 2>&1 | findstr /C:"errors" /C:"error"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` esiste
    - `pytest --collect-only -q tests/` exit 0 (no collection errors da conftest)
    - `grep -c "def synthetic_bars" tests/conftest.py` == 1
    - `grep -c "def synthetic_indicators_full" tests/conftest.py` == 1
    - `grep -c "def tmp_db_with_wal" tests/conftest.py` == 1
    - `grep -c "def mock_baseline_cfg" tests/conftest.py` == 1
    - `grep -c "autouse=True" tests/conftest.py` == 0 (NESSUN autouse — vincolo VALIDATION.md)
  </acceptance_criteria>
  <done>
    conftest carica senza errori, 4 fixtures dichiarate, no autouse=True.
  </done>
</task>

<task type="auto">
  <name>Task 2: 7 stub test file (test_baseline_*.py) + 1 test attivo (warmup) per Nyquist validation</name>
  <files>tests/test_baseline_runner.py, tests/test_baseline_dataset_writer.py, tests/test_baseline_no_future_leakage.py, tests/test_baseline_determinism.py, tests/test_baseline_wal.py, tests/test_baseline_plot_writer.py, tests/test_baseline_report_writer.py, tests/test_baseline_warmup.py</files>
  <read_first>
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-VALIDATION.md §Per-Task Verification Map (matrice 16 criteri)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md §Test files lines 569-633
    - C:/trading-agent/tests/test_backtest_ledger.py (header convention)
    - C:/trading-agent/backtest/baseline/warmup.py (helper implementato in Plan 05-01)
  </read_first>
  <action>
    Creare 7 file stub + 1 file attivo. Ogni file stub: header docstring + import block + N
    test stub con `@pytest.mark.skip(reason="W{N}: implementato dal plan 05-{NN}")`.

    **Pattern stub identico per ogni test:**
    ```python
    @pytest.mark.skip(reason="Wave 1: 05-03/04 implementa modulo target")
    def test_<name>(...) -> None:
        """<docstring spiegando criterio VALIDATION.md>"""
        raise NotImplementedError  # rimosso quando plan target imple
    ```

    ---

    **File 1: `tests/test_baseline_runner.py`** — copre BACK-07, D-14, D-15, D-23.

    Header:
    ```python
    """Tests for backtest.baseline.runner (BACK-07, D-14, D-15, D-23)."""
    from __future__ import annotations
    import pytest
    ```

    Stub functions (signatures + skip + docstring):
    - `test_single_slice_perf_budget(synthetic_bars)` — VALIDATION row "BACK-07 single-slice perf <2 min". Skip → 05-07.
    - `test_idempotency_skip_and_force(tmp_db_with_wal)` — D-14. Skip → 05-07.
    - `test_indicator_cache_reused_across_profiles(synthetic_bars, synthetic_indicators_full)` — D-15. Skip → 05-07.
    - `test_cost_deduction(synthetic_bars)` — D-23 spread+commission+slippage. Skip → 05-07.

    ---

    **File 2: `tests/test_baseline_dataset_writer.py`** — copre INT-01 D-02/D-03.

    Header (verbatim PATTERNS.md):
    ```python
    """Tests for backtest.baseline.dataset_writer (D-01, D-02, D-03)."""
    from __future__ import annotations
    import pandas as pd
    from pathlib import Path
    import pytest
    ```

    Stub functions:
    - `test_decisions_shard_round_trip(tmp_path)` — D-01 round-trip. Skip → 05-04.
    - `test_decisions_schema(tmp_path)` — D-02 column set. Skip → 05-04.
    - `test_drafts_schema(tmp_path)` — D-03 column set. Skip → 05-04.
    - `test_finalize_concatenates_shards(tmp_path)` — pyarrow.dataset.write_dataset finalize. Skip → 05-04.
    - `test_empty_rows_no_crash(tmp_path)` — empty rows shard. Skip → 05-04.
    - `test_finalize_handles_empty_shards(tmp_path)` — WARNING 11 fix: 1 empty shard + 1 row shard, finalize round-trip. Skip → 05-04.

    ---

    **File 3: `tests/test_baseline_no_future_leakage.py`** — D-21, D-22.

    Stub functions:
    - `test_indicator_full_slice_equals_recompute(synthetic_bars)` — D-21 invariant **REAL**: `compute_all_extended(bars[:i+1]).atr[-1]` vs `compute_all_extended(bars).atr[i]` (BLOCKER 4 fix). Skip → 05-05 (con `@pytest.mark.skip(reason="awaits Phase 2 D-04 ExtendedIndicators API + Plan 05-05 reactivation")`).
    - `test_decision_dataset_temporal_ordering()` — D-21 row invariant `decision_ts ≤ entry_ts < exit_ts`. Skip → 05-06.
    - `test_entry_at_next_bar_open(synthetic_bars)` — D-22 entry semantica. Skip → 05-05.

    ---

    **File 4: `tests/test_baseline_determinism.py`** — D-17.

    Stub functions:
    - `test_seed_reproducibility()` — `seed_for_run_id("baseline_2026-05-08_EURUSD_M15_MODERATE")` chiamato 2 volte → identico. Skip → 05-03.
    - `test_seed_reproducibility_cross_process(tmp_path)` — subprocess `.venv\Scripts\python.exe -c "..."` digest identico. Skip → 05-03.
    - `test_config_hash_matches(tmp_path)` — `file_sha256(path)` matches `hashlib.sha256(content).hexdigest()`. Skip → 05-03.

    ---

    **File 5: `tests/test_baseline_wal.py`** — D-16.

    Stub functions:
    - `test_wal_pragma_enabled(tmp_db_with_wal)` — `PRAGMA journal_mode` ritorna `wal`. Skip → 05-03.
    - `test_wal_concurrent_writes(tmp_db_with_wal)` — 3 multiprocessing.Process insert N rows ciascuno → final count == 3*N, no `OperationalError`. Skip → 05-03.
    - `test_with_retry_on_locked()` — mock `sqlite3.OperationalError("database is locked")` → retry succeeds. Skip → 05-03.

    ---

    **File 6: `tests/test_baseline_plot_writer.py`** — D-19, D-20.

    Stub functions:
    - `test_plot_creates_png(tmp_path)` — D-19 PNG ≥1KB esistente. Skip → 05-04.
    - `test_equity_png_structure(tmp_path)` — D-19 2-subplot equity + DD. Skip → 05-04.
    - `test_agg_backend_in_worker()` — D-20 matplotlib backend == "Agg" dopo import. Skip → 05-04.

    ---

    **File 7: `tests/test_baseline_report_writer.py`** — D-18.

    Stub functions:
    - `test_report_structure(tmp_path)` — D-18 header + tabella 27-row + appendix presenti. Skip → 05-06.
    - `test_report_contains_all_27_run_ids(tmp_path)` — INT-01 ogni run_id citato. Skip → 05-06.
    - `test_report_appendix_hashes(tmp_path)` — D-17 hash row in appendix. Skip → 05-06.

    ---

    **File 8 — ATTIVO (NON stub): `tests/test_baseline_warmup.py`** — BLOCKER 3 D-07 fix.

    Helper `longest_lookback_required` GIÀ implementato in Plan 05-01 Task 3b → questi test
    sono attivi (no skip):

    ```python
    """Tests for backtest.baseline.warmup (D-07, BLOCKER 3 user-locked decision)."""
    from __future__ import annotations

    from pathlib import Path

    import pytest

    from backtest.baseline.warmup import longest_lookback_required


    def test_longest_lookback_default_when_none() -> None:
        """D-07: cfg None → default fallback 200."""
        assert longest_lookback_required(None) == 200


    def test_longest_lookback_max_indicator_period() -> None:
        """D-07 BLOCKER 3 fix: max(periods) across config block."""
        cfg = {
            "indicators": {
                "ema_periods": [20, 50, 200],
                "atr_period": 14,
                "bb_period": 20,
                "donchian_period": 55,
                "macd_slow": 26,
            },
        }
        # Atteso: max([20,50,200,14,20,55,26]) == 200
        assert longest_lookback_required(cfg) == 200


    def test_longest_lookback_finds_largest() -> None:
        """D-07: lookback maggiore di 200 viene rispettato (no clamp)."""
        cfg = {"indicators": {"ema_periods": [20, 250], "atr_period": 14}}
        assert longest_lookback_required(cfg) == 250


    def test_longest_lookback_yaml_path(tmp_path: Path) -> None:
        """D-07: legge config da Path verso strategy.yaml."""
        p = tmp_path / "strategy.yaml"
        p.write_text(
            "indicators:\n"
            "  ema_periods: [20, 50, 100]\n"
            "  atr_period: 14\n",
            encoding="utf-8",
        )
        assert longest_lookback_required(p) == 100


    def test_longest_lookback_garbage_falls_back() -> None:
        """D-07: input non parseable → fallback 200, no exception."""
        # Dict senza alcuna chiave whitelisted
        assert longest_lookback_required({"foo": "bar", "baz": [1, 2, 3]}) == 200
    ```

    ---

    **Vincoli per ogni stub file:**
    - `from __future__ import annotations` (Pattern S1)
    - `import pytest` sempre
    - Italiano nei docstring estesi (CLAUDE.md §Convenzioni); test name in inglese ammesso (snake_case Python convention).
    - Ogni stub function ha body `raise NotImplementedError` + skip marker.
    - NESSUN import di moduli `backtest.baseline.*` (escluso `warmup` che è already-implemented
      in 05-01) — Wave 1+ non li ha ancora creati → collection error.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_baseline_runner.py tests/test_baseline_dataset_writer.py tests/test_baseline_no_future_leakage.py tests/test_baseline_determinism.py tests/test_baseline_wal.py tests/test_baseline_plot_writer.py tests/test_baseline_report_writer.py tests/test_baseline_warmup.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - 8 file `tests/test_baseline_*.py` esistenti (7 stub + 1 attivo: `test_baseline_warmup.py`)
    - `pytest --collect-only tests/test_baseline_*.py` exit 0
    - `pytest --collect-only tests/test_baseline_*.py 2>&1 | grep -c "test session starts"` ≥ 1
    - Ogni file stub ha ≥3 funzioni `def test_` (controllo via `grep -c "^def test_" {file}` ≥ 3 per ognuno; warmup ≥4)
    - **WARNING 13 fix**: `ls tests/test_baseline_*.py | wc -l` == 8 AND `for f in tests/test_baseline_runner.py tests/test_baseline_dataset_writer.py tests/test_baseline_no_future_leakage.py tests/test_baseline_determinism.py tests/test_baseline_wal.py tests/test_baseline_plot_writer.py tests/test_baseline_report_writer.py; do grep -l "@pytest.mark.skip" "$f"; done | wc -l` == 7 (ogni file stub ha almeno uno skip marker; warmup è attivo)
    - NESSUN import di `backtest.baseline.runner|determinism|wal_setup|dataset_writer|plot_writer|report_writer` nei file stub (verificato via `grep -E "from backtest.baseline\.(runner|determinism|wal_setup|dataset_writer|plot_writer|report_writer)" tests/test_baseline_runner.py tests/test_baseline_dataset_writer.py tests/test_baseline_no_future_leakage.py tests/test_baseline_determinism.py tests/test_baseline_wal.py tests/test_baseline_plot_writer.py tests/test_baseline_report_writer.py` ritorna 0)
    - `pytest tests/test_baseline_warmup.py -x` exit 0 (5 test pass — non skipped, helper già implementato in 05-01)
    - Test count totale: ≥28 stub + 5 attivi (warmup) = ≥33
  </acceptance_criteria>
  <done>
    8 file pronti (7 stub + 1 attivo). `pytest --collect-only` exit 0. Stub: ogni Wave 1+
    task può puntare al test corrispondente. Warmup: 5 test verdi (BLOCKER 3 chiuso).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest collection | Test file path traversal — restricted ai dir `tests/` per pytest.ini |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | tests/conftest.py fixtures | accept | fixtures usano `tmp_path` — no shared state cross-test |
| T-05-05 | Denial of Service | pytest collection time | mitigate | stub `raise NotImplementedError` + skip — no slow setup; collection <1s |
</threat_model>

<verification>
- 7 file test stub creati + 1 file warmup attivo, 1 conftest.py
- pytest collection green (0 errori)
- 28+ test stub registrati (target sampling N≥3 per modulo) + 5 warmup attivi
- BLOCKER 3 D-07 chiuso (warmup helper unit-tested)
- WARNING 13 fix: skip-marker presence-check robusto (per-file), non count-exact
- Nessun import circolare prodotto da stub (no `backtest.baseline.X` import top-level eccetto warmup)
</verification>

<success_criteria>
- Plan 05-02 completo: scaffolding test-first per Wave 1+
- Ogni `<verify><automated>` di Wave 1+ punta a un test esistente
- Nyquist compliance per VALIDATION.md raggiunta in W0
- BLOCKER 3 (warmup adattivo D-07) chiuso e validato
</success_criteria>

<output>
Dopo completamento: `.planning/phases/05-baseline-backtest/05-02-SUMMARY.md`.
Sezioni: Files Created, Test Count Per File, Skip Mapping (test → plan target),
Warmup Tests Active (5), Next Wave.
</output>
</content>
</invoke>