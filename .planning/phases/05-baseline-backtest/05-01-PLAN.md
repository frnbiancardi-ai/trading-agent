---
phase: 05-baseline-backtest
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - data/configs/baseline.yaml
  - backtest/baseline/__init__.py
  - backtest/baseline/warmup.py
  - backtest/ledger.py
  - scripts/preflight_phase5.py
autonomous: true
requirements: [BACK-07, INT-01]
tags: [phase-5, baseline-backtest, wave-0, scaffolding, preflight]

phase1_dependency_risk: |
  REVISIONE 2026-05-08: l'INTERA dependency chain (Phase 1, 2, 3, 4) è ancora in
  planning — nessun codice prodotto. Phase 5 dipende da:
    - Phase 1: BacktestEngine.run(), BacktestBroker (incluso force_close), LedgerWriter,
      load_cost_model, BacktestMetrics, _hash_cost_yaml.
    - Phase 2: indicators.compute_all_extended (ExtendedIndicators API D-04, dict-of-lists
      o dataclass-of-lists con eventuale slice_until()).
    - Phase 3: detector A/B/C/D (consumati da strategy.evaluate_proposal_for_bar).
    - Phase 4: strategy.evaluate_proposal_for_bar, strategy.adapters.backtest.build_ctx_backtest.

  Esecuzione Wave 4 (05-08 smoke) BLOCCATA fino a completamento Phase 1-4.
  Wave 0-3 (scaffolding, writers, runner) sono safe perché lavorano su moduli indipendenti
  e mockano BacktestEngine/compute_all_extended nei test (MagicMock against future contract).

  Pre-flight contract probe (`scripts/preflight_phase5.py`) introdotto in questo Plan
  Task 4 — fail-fast se la dependency chain non risponde alle signature attese.

must_haves:
  truths:
    - "pip install pyarrow matplotlib succede sul .venv corrente"
    - "import pyarrow / import matplotlib non sollevano ModuleNotFoundError"
    - "data/configs/baseline.yaml carica via yaml.safe_load senza eccezioni"
    - "backtest_runs ha colonne slippage_seed_effective, strategy_yaml_hash, baseline_yaml_hash, git_sha, cost_yaml_sha256, strategy_yaml_sha256, baseline_yaml_sha256"
    - "backtest/baseline package importabile (import backtest.baseline)"
    - "backtest.baseline.warmup.longest_lookback_required(strategy_cfg) ritorna int (max lookback configurato; default 200 se config block non parseable)"
    - "scripts/preflight_phase5.py exit 0 quando Phase 1-4 contracts presenti; exit ≠ 0 con error chiaro altrimenti"
  artifacts:
    - path: "data/configs/baseline.yaml"
      provides: "BaselineConfig YAML schema (D-15, D-17)"
      contains: "equity_initial_eur"
      contains: "slippage_seed: 42"
      contains: "timeout_bars"
    - path: "backtest/baseline/__init__.py"
      provides: "package marker (Phase 5 subpackage)"
    - path: "backtest/baseline/warmup.py"
      provides: "longest_lookback_required(strategy_cfg) helper (D-07 adaptive warm-up)"
      contains: "def longest_lookback_required"
    - path: "requirements.txt"
      provides: "deps pyarrow + matplotlib"
      contains: "pyarrow"
      contains: "matplotlib"
    - path: "backtest/ledger.py"
      provides: "schema esteso con 7 nuove colonne + _migrate_backtest_runs"
      contains: "slippage_seed_effective"
      contains: "strategy_yaml_hash"
      contains: "baseline_yaml_hash"
      contains: "git_sha"
      contains: "cost_yaml_sha256"
      contains: "strategy_yaml_sha256"
      contains: "baseline_yaml_sha256"
      contains: "_migrate_backtest_runs"
    - path: "scripts/preflight_phase5.py"
      provides: "contract probe Phase 1-4 — fail-fast se dependency chain incompleta"
      contains: "inspect.signature"
      contains: "BacktestEngine"
      contains: "compute_all_extended"
      contains: "evaluate_proposal_for_bar"
  key_links:
    - from: "data/configs/baseline.yaml"
      to: "backtest/baseline/runner.py (Wave 3)"
      via: "load_baseline_config() loader (frozen dataclass pattern)"
    - from: "backtest/ledger.py"
      to: "backtest/baseline/slice_worker.py (Wave 2)"
      via: "LedgerWriter.record_run() con 7 nuovi campi"
    - from: "backtest/baseline/warmup.py::longest_lookback_required"
      to: "backtest/baseline/slice_worker.py (Wave 2)"
      via: "warm_up = max(baseline_cfg.warm_up_min_bars, longest_lookback_required(strategy_cfg)); bars = bars[warm_up:]"
    - from: "scripts/preflight_phase5.py"
      to: "scripts/run_baseline_backtest.py (Wave 4 — Plan 05-08)"
      via: "preflight invocato PRIMA dello smoke; exit ≠ 0 aborta la run"
---

<objective>
Wave 0 scaffolding: installare deps mancanti (pyarrow, matplotlib), creare il config YAML
`data/configs/baseline.yaml` con schema lockato (D-15, D-17, D-19), inizializzare il
package `backtest/baseline/`, implementare l'helper warm-up adattivo
`backtest/baseline/warmup.py::longest_lookback_required` (D-07), estendere lo schema
`backtest_runs` con 7 colonne richieste da D-17 audit trail (4 originali + 3 colonne
sha256 full-64 — see Warning 12), e creare il preflight contract probe
`scripts/preflight_phase5.py` per blocco Wave 4.

Purpose: sbloccare Wave 1+ — senza pyarrow/matplotlib i moduli writer non importano, senza
baseline.yaml il runner non parte, senza schema esteso `record_run()` fallisce con audit trail,
senza warm-up adattivo (D-07) il slice_worker rischia bias da indicator non-warmed, senza
preflight la Wave 4 smoke esegue su dependency mancanti senza messaggio chiaro.

Output:
- `requirements.txt` aggiornato con `pyarrow` + `matplotlib`
- `data/configs/baseline.yaml` (NEW)
- `backtest/baseline/__init__.py` (NEW)
- `backtest/baseline/warmup.py` (NEW — D-07 adaptive helper, BLOCKER 3 fix)
- `backtest/ledger.py` esteso con `_migrate_backtest_runs` + 7 nuove colonne (4 originali +
  3 sha256 full-64; vedi Warning 12 fix)
- `scripts/preflight_phase5.py` (NEW — contract probe Phase 1-4; BLOCKER 6 fix)
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/trading-agent/.planning/PROJECT.md
@C:/trading-agent/.planning/ROADMAP.md
@C:/trading-agent/.planning/STATE.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-CONTEXT.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-VALIDATION.md
@C:/trading-agent/.planning/phases/01-backtest-engine/01-CONTEXT.md
@C:/trading-agent/CLAUDE.md
@C:/trading-agent/backtest/ledger.py
@C:/trading-agent/backtest/costs.py
@C:/trading-agent/requirements.txt

<interfaces>
From backtest/ledger.py (existing — Phase 1):
- `_DDL_BACKTEST_RUNS` (lines 19-43): CREATE TABLE schema. Phase 5 aggiunge 7 colonne.
- `_BT_RUNS_COLUMNS` (lines 80-86): tuple ordinata. Phase 5 appende 7 nomi alla fine.
- `_INSERT_RUN_SQL`: derivato da `_BT_RUNS_COLUMNS` — auto-aggiornato.
- Pattern `CREATE TABLE IF NOT EXISTS` su module init già presente.

From backtest/costs.py (esistente — Phase 1):
- `@dataclass(frozen=True) class CostModel`
- `def load_cost_model(yaml_path: Path) -> CostModel` — pattern verbatim per `BaselineConfig`.

From data/configs/costs.yaml (esistente):
- Header convention: `# data/configs/<name>.yaml\n# <description>`
- YAML schema flat: `EURUSD: {spread_pips: 0.5, commission_per_lot_usd: 7, slippage_pips: 0.3}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pyarrow + matplotlib + update requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - C:/trading-agent/requirements.txt (file completo, deve essere ≤30 righe)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md §Standard Stack lines 102-147 (verifica install + verifica versione)
  </read_first>
  <action>
    1. Eseguire install: `.venv\Scripts\python.exe -m pip install pyarrow matplotlib`
       (PowerShell — backslash path Windows, per CLAUDE.md ambiente).
    2. Verificare versioni installate: `.venv\Scripts\python.exe -m pip show pyarrow` e
       `.venv\Scripts\python.exe -m pip show matplotlib`. Documentare le versioni effettive
       nel commit message (es. "pyarrow 17.0.0, matplotlib 3.9.2").
    3. Editare `requirements.txt`: appendere alla fine due righe:
       ```
       pyarrow
       matplotlib
       ```
       NON pinnare versioni — convention repo (vedi RESEARCH.md §Installation lines 137-140).
    4. Smoke check: `.venv\Scripts\python.exe -c "import pyarrow; import matplotlib; print(pyarrow.__version__, matplotlib.__version__)"` deve stampare due versioni.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -c "import pyarrow, matplotlib; print('OK', pyarrow.__version__, matplotlib.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import pyarrow"` exit 0
    - `python -c "import matplotlib"` exit 0
    - `grep -c "^pyarrow$" requirements.txt` == 1
    - `grep -c "^matplotlib$" requirements.txt` == 1
  </acceptance_criteria>
  <done>
    pyarrow + matplotlib importabili. requirements.txt contiene entrambe le righe.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crea data/configs/baseline.yaml con schema lockato</name>
  <files>data/configs/baseline.yaml</files>
  <read_first>
    - C:/trading-agent/data/configs/costs.yaml (header convention + 3-righe-comment pattern)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-CONTEXT.md §specifics lines 287-310 (baseline.yaml schema)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md §Pattern 5 (determinism)
  </read_first>
  <action>
    Creare `data/configs/baseline.yaml` (file NUOVO) con schema esatto (D-15, D-17, D-19):

    ```yaml
    # data/configs/baseline.yaml
    # Phase 5 baseline backtest orchestration knobs (D-15, D-17, D-19, D-20).
    # Loaded da backtest.baseline.runner via load_baseline_config().
    # NB: lockato — modifiche invalidano baseline_yaml_hash → re-run baseline obbligatorio.

    equity_initial_eur: 10000      # D-10 — equity start per slice
    slippage_seed: 42              # D-17 — seed lockato (per-run derivato via sha256(run_id))

    timeout_bars:                  # D-05 — timeout policy per-TF
      M15: 96                      # ~24h
      M30: 96                      # ~48h
      H1: 120                      # ~5 giorni

    warm_up_min_bars: 200          # D-07 — warm-up adaptive: max(200, longest_lookback_required)

    max_workers: 9                 # D-15 — 9 task (symbol × tf), 3 profile sequenziali per worker
    parquet_compression: snappy    # D-20 default; alternativa zstd in deferred

    force_rerun: false             # D-14 — default safe; CLI flag --force overrides
    progress_bar: true             # tqdm su 27 run

    # Output paths
    training_data_dir: "data/training"
    report_dir: ".planning/research"
    equity_curves_dir: ".planning/research/baseline-equity-curves"
    ```

    Encoding: UTF-8, line endings consistenti col repo. Italiano commenti per CLAUDE.md
    §Convenzioni.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -c "import yaml; cfg = yaml.safe_load(open('data/configs/baseline.yaml', encoding='utf-8')); assert cfg['slippage_seed'] == 42; assert cfg['timeout_bars']['M15'] == 96; assert cfg['max_workers'] == 9; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `data/configs/baseline.yaml` esiste
    - `yaml.safe_load(...)['slippage_seed']` == 42
    - `yaml.safe_load(...)['timeout_bars']` ha chiavi `M15, M30, H1` con valori `96, 96, 120`
    - `yaml.safe_load(...)['equity_initial_eur']` == 10000
    - `yaml.safe_load(...)['max_workers']` == 9
    - `grep -c "force_rerun: false" data/configs/baseline.yaml` == 1
  </acceptance_criteria>
  <done>
    YAML carica deterministicamente con tutti i valori D-15/D-17/D-19 attesi.
  </done>
</task>

<task type="auto">
  <name>Task 3: Crea backtest/baseline/__init__.py + warmup.py + estendi backtest/ledger.py schema</name>
  <files>backtest/baseline/__init__.py, backtest/baseline/warmup.py, backtest/ledger.py</files>
  <read_first>
    - C:/trading-agent/backtest/__init__.py (analog verbatim 2-righe template)
    - C:/trading-agent/backtest/ledger.py lines 1-100 (schema corrente)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md §`backtest/ledger.py (MODIFY)` lines 408-449
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md §Pitfall about ALTER TABLE ADD COLUMN
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md §Open Question 5 (longest_lookback semantics)
  </read_first>
  <action>
    **Sub-task 3a — Crea `backtest/baseline/__init__.py`:**

    File NUOVO. Contenuto verbatim (2 righe):
    ```python
    """Backtest baseline subpackage: 27-run pre-ML dataset + report (Phase 5)."""
    __all__ = ["run_baseline"]
    ```
    NB: `run_baseline` sarà definito in Wave 3 — `__all__` è dichiarato in advance per
    documentare l'API pubblica. L'import effettivo NON è qui (no circular dep).

    **Sub-task 3b — Crea `backtest/baseline/warmup.py` (NUOVO — fix BLOCKER 3 D-07):**

    Helper adattivo. Tolerant: se Phase 2 espone `indicators.indicators_full.longest_lookback()`
    quello prevale; altrimenti fallback su parsing `config/strategy.yaml`. Default 200 se
    nulla parseable.

    ```python
    """Warm-up helper adattivo per baseline backtest (D-07).

    REVISIONE 2026-05-08 (BLOCKER 3 user-locked decision): implementa adaptive helper.
    `slice_worker` calcola: warm_up = max(baseline_cfg.warm_up_min_bars,
    longest_lookback_required(strategy_cfg)); bars = bars[warm_up:].

    Strategia tolerante:
      1. Se `indicators.indicators_full.longest_lookback()` è esposto da Phase 2 → usa quello.
      2. Altrimenti parsa `config/strategy.yaml` indicator block (ema_periods, atr_period,
         bb_period, donchian_period, etc.) e ritorna max valore.
      3. Default fallback: 200 (bias safe per coerenza con `warm_up_min_bars` D-15).
    """
    from __future__ import annotations

    import logging
    from pathlib import Path
    from typing import Any

    _log = logging.getLogger(__name__)

    _DEFAULT_FALLBACK = 200

    # Chiavi YAML che rappresentano lookback period (Phase 2 indicator config naming).
    # Aggiungere chiavi qui se Phase 2 introduce nuovi indicator con lookback.
    _LOOKBACK_KEYS = {
        "ema_period", "ema_periods", "atr_period", "rsi_period", "adx_period",
        "bb_period", "donchian_period", "keltner_period", "macd_slow",
        "stoch_period", "vwap_period", "fib_lookback", "hurst_window",
        "longest_lookback", "lookback",
    }


    def _flatten_numeric(obj: Any) -> list[int]:
        """DFS estrae tutti gli int da chiavi-lookback in un dict/list nested."""
        out: list[int] = []
        if isinstance(obj, dict):
            for k, v in obj.items():
                if k in _LOOKBACK_KEYS:
                    if isinstance(v, (int, float)):
                        out.append(int(v))
                    elif isinstance(v, list):
                        out.extend(int(x) for x in v if isinstance(x, (int, float)))
                else:
                    out.extend(_flatten_numeric(v))
        elif isinstance(obj, list):
            for item in obj:
                out.extend(_flatten_numeric(item))
        return out


    def longest_lookback_required(strategy_cfg: Any) -> int:
        """D-07 adaptive warm-up: max bars di lookback su tutti gli indicator configurati.

        strategy_cfg può essere:
          - dict (yaml.safe_load output)
          - Path/str (legge il file YAML)
          - None (usa default)

        Tentativi (in ordine):
          1. Phase 2 hook: indicators.indicators_full.longest_lookback() se esiste.
          2. Parsing dict: max sui valori delle chiavi in _LOOKBACK_KEYS (DFS nested).
          3. Default fallback: 200.
        """
        # Tentativo 1: Phase 2 hook (lazy import, optional)
        try:
            from indicators import indicators_full as _ind  # type: ignore
            fn = getattr(_ind, "longest_lookback", None)
            if callable(fn):
                val = int(fn())
                if val > 0:
                    return val
        except Exception:  # noqa: BLE001 — modulo non ancora landed (Phase 2 pending)
            pass

        # Tentativo 2: parsing dict
        cfg_dict: dict | None = None
        if isinstance(strategy_cfg, dict):
            cfg_dict = strategy_cfg
        elif isinstance(strategy_cfg, (str, Path)):
            try:
                import yaml
                with open(strategy_cfg, encoding="utf-8") as f:
                    cfg_dict = yaml.safe_load(f) or {}
            except Exception as exc:  # noqa: BLE001
                _log.warning("longest_lookback: impossibile leggere %s (%s)", strategy_cfg, exc)
                cfg_dict = None

        if cfg_dict:
            values = _flatten_numeric(cfg_dict)
            if values:
                return max(values)

        # Tentativo 3: fallback
        _log.info("longest_lookback: nessuna config parseable, uso fallback %d", _DEFAULT_FALLBACK)
        return _DEFAULT_FALLBACK
    ```

    **Sub-task 3c — Estendi `backtest/ledger.py`:**

    1. In `_DDL_BACKTEST_RUNS` (lines 19-43), prima della parentesi chiusa `)`,
       appendere 7 nuove colonne (NB: NESSUNA virgola dopo l'ultima):

       ```sql
       slippage_seed_effective INTEGER,
       strategy_yaml_hash TEXT,
       baseline_yaml_hash TEXT,
       git_sha TEXT,
       cost_yaml_sha256 TEXT,
       strategy_yaml_sha256 TEXT,
       baseline_yaml_sha256 TEXT
       ```

       Le ultime 3 colonne `*_sha256` sono la fix di **WARNING 12**: la colonna legacy
       `cost_yaml_hash` mantiene il formato Phase 1 (`md5[:16]`); le 3 nuove `*_sha256`
       memorizzano il sha256 completo (64 char). Phase 5 slice_worker scrive i 3 valori
       full sha256 nelle nuove colonne — drop della troncatura `[:16]`.

    2. In `_BT_RUNS_COLUMNS` (lines 80-86), appendere alla tuple le 7 colonne IN ORDINE
       IDENTICO al DDL:
       ```python
       _BT_RUNS_COLUMNS: tuple[str, ...] = (
           "run_id", "symbol", "timeframe", "date_start", "date_end",
           "cost_yaml_hash", "profile", "n_folds", "fold_mode", "train_ratio",
           "started_at", "finished_at", "total_trades", "sharpe", "sortino",
           "max_dd_pct", "hit_rate", "expectancy_usd", "profit_factor", "avg_r",
           "total_pnl_usd",
           # Phase 5 additions (D-17 audit trail) — additive, backwards-compat:
           "slippage_seed_effective", "strategy_yaml_hash", "baseline_yaml_hash", "git_sha",
           # Phase 5 sha256 full-64 (Warning 12 fix — coexist con legacy cost_yaml_hash md5[:16]):
           "cost_yaml_sha256", "strategy_yaml_sha256", "baseline_yaml_sha256",
       )
       ```

    3. Aggiungere funzione `_migrate_backtest_runs(conn)` PRIMA della prima funzione public
       in ledger.py:
       ```python
       def _migrate_backtest_runs(conn: sqlite3.Connection) -> None:
           """Additive migration: ALTER TABLE ADD COLUMN se mancante (Phase 5).

           SQLite ADD COLUMN richiede DEFAULT NULL — backwards-compatible con righe
           esistenti. Idempotente: ri-eseguire non duplica colonne.
           """
           existing = {r[1] for r in conn.execute("PRAGMA table_info(backtest_runs)")}
           additions = [
               ("slippage_seed_effective", "INTEGER"),
               ("strategy_yaml_hash", "TEXT"),
               ("baseline_yaml_hash", "TEXT"),
               ("git_sha", "TEXT"),
               # Warning 12 fix: full-64 sha256 columns
               ("cost_yaml_sha256", "TEXT"),
               ("strategy_yaml_sha256", "TEXT"),
               ("baseline_yaml_sha256", "TEXT"),
           ]
           for col, typ in additions:
               if col not in existing:
                   conn.execute(f"ALTER TABLE backtest_runs ADD COLUMN {col} {typ}")
       ```

    4. Trovare la funzione che già esegue `CREATE TABLE IF NOT EXISTS` per `backtest_runs`
       (tipicamente `LedgerWriter.__init__` o helper `_ensure_schema`). DOPO il
       `CREATE TABLE` invocare `_migrate_backtest_runs(conn)` PRIMA del `CREATE TABLE` per
       `backtest_trades` (così schema è completo prima di indexes).

    5. Italiano commenti.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -c "import backtest.baseline; import backtest.baseline.warmup as W; import backtest.ledger as L; assert 'slippage_seed_effective' in L._BT_RUNS_COLUMNS; assert 'cost_yaml_sha256' in L._BT_RUNS_COLUMNS; assert 'strategy_yaml_sha256' in L._BT_RUNS_COLUMNS; assert 'baseline_yaml_sha256' in L._BT_RUNS_COLUMNS; assert callable(W.longest_lookback_required); assert W.longest_lookback_required(None) == 200; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `backtest/baseline/__init__.py` esiste, contiene `__all__`
    - `backtest/baseline/warmup.py` esiste con `def longest_lookback_required`
    - `python -c "import backtest.baseline.warmup as w; assert w.longest_lookback_required(None) == 200"` exit 0
    - `python -c "from backtest.baseline.warmup import longest_lookback_required; print(longest_lookback_required({'ema_periods':[20,50,200],'atr_period':14}))"` stampa 200
    - `grep -c "slippage_seed_effective" backtest/ledger.py` ≥ 2 (DDL + tuple)
    - `grep -c "strategy_yaml_hash" backtest/ledger.py` ≥ 2
    - `grep -c "baseline_yaml_hash" backtest/ledger.py` ≥ 2
    - `grep -c "git_sha" backtest/ledger.py` ≥ 2
    - `grep -c "cost_yaml_sha256" backtest/ledger.py` ≥ 2
    - `grep -c "strategy_yaml_sha256" backtest/ledger.py` ≥ 2
    - `grep -c "baseline_yaml_sha256" backtest/ledger.py` ≥ 2
    - `grep -c "_migrate_backtest_runs" backtest/ledger.py` ≥ 2 (def + call site)
    - Test smoke: creare `tmp.db` via `sqlite3.connect`, eseguire DDL completo + migrate,
      verificare via `PRAGMA table_info(backtest_runs)` che le 7 colonne esistono.
  </acceptance_criteria>
  <done>
    Package `backtest.baseline` importabile + warmup.py D-07 operativo. `backtest_runs`
    schema esteso con 7 colonne (4 originali + 3 sha256 full-64). Migrazione idempotente
    verificata.
  </done>
</task>

<task type="auto">
  <name>Task 4: Crea scripts/preflight_phase5.py — contract probe Phase 1-4</name>
  <files>scripts/preflight_phase5.py</files>
  <read_first>
    - C:/trading-agent/scripts/dry_run_cycle.py (bootstrap pattern sys.path)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md (engine signature aspettata, ExtendedIndicators API)
  </read_first>
  <action>
    **Fix BLOCKER 6**: contract probe fail-fast PRIMA di qualsiasi smoke run. Verifica
    via `inspect.signature` che le callable Phase 1-4 esistano con la signature attesa.
    Verifica anche shape dell'output `compute_all_extended` (dict-of-lists VS dataclass-of-lists
    con `slice_until` — fix anche BLOCKER 5 / WARNING 9: probe `BacktestBroker.virtual_positions`
    e `force_close`).

    File NUOVO `scripts/preflight_phase5.py`:

    ```python
    r"""Phase 5 preflight: contract probe Phase 1-4 dependency chain.

    Esegue PRIMA dello smoke run (Plan 05-08). Fail-fast con error chiaro se uno
    qualsiasi dei moduli upstream non risponde alla signature attesa.

    Coperture:
      - backtest.engine.BacktestEngine: __init__ kwargs (Phase 5 additions)
      - backtest.broker.BacktestBroker: virtual_positions attr OR equivalente, force_close
      - backtest.ledger.LedgerWriter: insert_trades + record_run
      - backtest.metrics.compute_metrics + BacktestMetrics fields
      - strategy.evaluate_proposal_for_bar
      - strategy.adapters.backtest.build_ctx_backtest
      - indicators.compute_all_extended: shape probe (dict-of-lists | dataclass con slice_until)

    Exit 0 = preflight verde. Exit 1 = qualcosa manca (stderr lista i punti critici).

    Uso:
        .\.venv\Scripts\python.exe scripts\preflight_phase5.py
    """
    from __future__ import annotations

    import inspect
    import sys
    from pathlib import Path

    ROOT = Path(__file__).resolve().parent.parent
    if str(ROOT) not in sys.path:
        sys.path.insert(0, str(ROOT))


    REPORT: list[str] = []
    FAILED: list[str] = []


    def _ok(msg: str) -> None:
        REPORT.append(f"  OK    {msg}")


    def _fail(msg: str) -> None:
        REPORT.append(f"  FAIL  {msg}")
        FAILED.append(msg)


    def _check_engine() -> None:
        try:
            from backtest.engine import BacktestEngine
        except Exception as exc:  # noqa: BLE001
            _fail(f"import backtest.engine.BacktestEngine: {exc}")
            return
        try:
            sig = inspect.signature(BacktestEngine.__init__)
            params = set(sig.parameters)
            required_phase1 = {"bars", "symbol", "timeframe", "cost_model"}
            missing = required_phase1 - params
            if missing:
                _fail(f"BacktestEngine.__init__ manca params Phase 1: {missing}")
            else:
                _ok("BacktestEngine.__init__ ha parametri base Phase 1")
            # Phase 5 additions — opzionali se Plan 05-05 non ancora landed
            phase5 = {"indicators_full", "risk_profile", "timeout_bars", "equity_initial"}
            present_p5 = phase5 & params
            if present_p5 == phase5:
                _ok("BacktestEngine.__init__ ha tutti gli additions Phase 5")
            else:
                _fail(f"BacktestEngine.__init__ Phase 5 additions mancanti: {phase5 - params}")
        except Exception as exc:  # noqa: BLE001
            _fail(f"BacktestEngine.__init__ signature inspect: {exc}")


    def _check_broker() -> None:
        try:
            from backtest.broker import BacktestBroker
        except Exception as exc:  # noqa: BLE001
            _fail(f"import backtest.broker.BacktestBroker: {exc}")
            return
        # virtual_positions: attr OR property OR method-equivalent (Phase 1 D-04)
        has_vp = (hasattr(BacktestBroker, "virtual_positions")
                  or hasattr(BacktestBroker, "open_positions")
                  or hasattr(BacktestBroker, "_positions"))
        if has_vp:
            _ok("BacktestBroker espone virtual_positions/open_positions/_positions")
        else:
            _fail("BacktestBroker non espone collezione di posizioni open (virtual_positions)")
        # force_close: serve per timeout enforcement Phase 5
        if hasattr(BacktestBroker, "force_close"):
            _ok("BacktestBroker.force_close presente")
        else:
            _fail("BacktestBroker.force_close MANCA — Plan 05-05 deve aggiungere il metodo "
                  "(signature: force_close(position_id, exit_price, exit_reason: str) -> ClosedTrade)")


    def _check_ledger() -> None:
        try:
            from backtest.ledger import LedgerWriter, _BT_RUNS_COLUMNS  # type: ignore
        except Exception as exc:  # noqa: BLE001
            _fail(f"import backtest.ledger.LedgerWriter / _BT_RUNS_COLUMNS: {exc}")
            return
        for col in ("slippage_seed_effective", "strategy_yaml_hash", "baseline_yaml_hash",
                    "git_sha", "cost_yaml_sha256", "strategy_yaml_sha256",
                    "baseline_yaml_sha256"):
            if col in _BT_RUNS_COLUMNS:
                _ok(f"_BT_RUNS_COLUMNS contiene {col}")
            else:
                _fail(f"_BT_RUNS_COLUMNS manca {col} (Phase 5 schema migration)")


    def _check_metrics() -> None:
        try:
            from backtest.metrics import BacktestMetrics, compute_metrics  # noqa: F401
            from dataclasses import fields
        except Exception as exc:  # noqa: BLE001
            _fail(f"import backtest.metrics: {exc}")
            return
        names = {f.name for f in fields(BacktestMetrics)}
        for f in ("sharpe", "sortino", "max_drawdown_pct", "hit_rate",
                  "expectancy_usd", "profit_factor", "avg_r", "total_pnl_usd",
                  "longest_dd_days"):
            if f in names:
                _ok(f"BacktestMetrics.{f}")
            else:
                _fail(f"BacktestMetrics manca campo: {f}")


    def _check_strategy() -> None:
        try:
            from strategy import evaluate_proposal_for_bar  # type: ignore
            _ok(f"strategy.evaluate_proposal_for_bar callable={callable(evaluate_proposal_for_bar)}")
        except Exception as exc:  # noqa: BLE001
            _fail(f"import strategy.evaluate_proposal_for_bar: {exc}")
        try:
            from strategy.adapters.backtest import build_ctx_backtest  # type: ignore
            _ok(f"strategy.adapters.backtest.build_ctx_backtest callable={callable(build_ctx_backtest)}")
        except Exception as exc:  # noqa: BLE001
            _fail(f"import strategy.adapters.backtest.build_ctx_backtest: {exc}")


    def _check_indicators() -> None:
        try:
            from indicators import compute_all_extended  # type: ignore
        except Exception as exc:  # noqa: BLE001
            _fail(f"import indicators.compute_all_extended: {exc}")
            return
        _ok(f"indicators.compute_all_extended callable={callable(compute_all_extended)}")
        # Shape probe: chiamata con bars sintetici minimi → output deve essere dict-of-lists
        # OPPURE oggetto con .slice_until(i). Branch documentato.
        try:
            from backtest.loader import Bar  # type: ignore
            bars = [Bar(time=i*900, open=1.0, high=1.001, low=0.999, close=1.0,
                        tick_volume=10, spread=2, real_volume=0) for i in range(50)]
            out = compute_all_extended(bars)
            if hasattr(out, "slice_until"):
                _ok("compute_all_extended output ha slice_until() → engine usa branch slice_until")
            elif isinstance(out, dict):
                # Verifica almeno una chiave punta a list (dict-of-lists)
                has_list = any(isinstance(v, list) for v in out.values())
                if has_list:
                    _ok("compute_all_extended output dict-of-lists → engine usa branch slicing dict")
                else:
                    _fail("compute_all_extended output dict ma nessuna chiave list — "
                          "Phase 2 contract incerto, slice_worker engine adapter da rivedere")
            else:
                _fail(f"compute_all_extended output type {type(out).__name__} non riconosciuto "
                      "(atteso dict-of-lists OR oggetto con slice_until())")
        except Exception as exc:  # noqa: BLE001
            _fail(f"compute_all_extended shape probe: {exc}")


    def main() -> int:
        print("=== Phase 5 Preflight Contract Probe ===")
        print(f"ROOT = {ROOT}")
        print()
        for fn in (_check_engine, _check_broker, _check_ledger, _check_metrics,
                   _check_strategy, _check_indicators):
            fn()
        for line in REPORT:
            print(line)
        print()
        if FAILED:
            print(f"FAILED: {len(FAILED)} check non passati. Risolvi prima dello smoke run.",
                  file=sys.stderr)
            for f in FAILED:
                print(f"  - {f}", file=sys.stderr)
            return 1
        print("SUCCESS: tutti i contract probe Phase 1-4 verdi. Wave 4 sblocca.")
        return 0


    if __name__ == "__main__":
        raise SystemExit(main())
    ```

    Italiano commenti dove user-facing (CLAUDE.md §Convenzioni).
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe scripts\preflight_phase5.py 2>&1 | findstr /C:"Phase 5 Preflight"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/preflight_phase5.py` esiste
    - `python scripts/preflight_phase5.py` stampa header "=== Phase 5 Preflight Contract Probe ==="
    - Exit code 0 quando Phase 1-4 complete; exit 1 (con stderr message) altrimenti
    - `grep -c "inspect.signature" scripts/preflight_phase5.py` ≥ 1
    - `grep -c "BacktestEngine" scripts/preflight_phase5.py` ≥ 1
    - `grep -c "compute_all_extended" scripts/preflight_phase5.py` ≥ 1
    - `grep -c "evaluate_proposal_for_bar" scripts/preflight_phase5.py` ≥ 1
    - `grep -c "build_ctx_backtest" scripts/preflight_phase5.py` ≥ 1
    - `grep -c "force_close" scripts/preflight_phase5.py` ≥ 1
    - `grep -c "slice_until" scripts/preflight_phase5.py` ≥ 1
  </acceptance_criteria>
  <done>
    Preflight contract probe operativo. Plan 05-08 lo invocherà per gate Wave 4.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → Python loader | YAML config caricato via `yaml.safe_load` (no execution di tag custom) |
| Python → SQLite (logs/trades.db) | Schema migration via parametrized SQL — ALTER TABLE con f-string contiene SOLO costanti hard-coded (no user input) |
| preflight subprocess → ROOT path | sys.path injection limitata a `Path(__file__).resolve().parent.parent` — no traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | data/configs/baseline.yaml | mitigate | `baseline_yaml_sha256` in `backtest_runs` (D-17 + Warning 12) — modifica file rilevata via sha256 mismatch |
| T-05-02 | Information Disclosure | requirements.txt | accept | nessun secret, dependency lock public OK |
| T-05-03 | Tampering | backtest/ledger.py ALTER TABLE | mitigate | f-string ALTER TABLE contiene solo nomi colonna costanti hard-coded — no SQL injection vector. SQLite ADD COLUMN è idempotente safe. |
| T-05-24 | Tampering | longest_lookback_required parsing | mitigate | parser DFS limitato a chiavi whitelisted (_LOOKBACK_KEYS) — input malformati ricadono su fallback 200 |
</threat_model>

<verification>
- `pyarrow` e `matplotlib` importabili (Wave 1+ writer non bloccati)
- `data/configs/baseline.yaml` carica deterministicamente con tutti i valori CONTEXT lockati
- `backtest_runs` schema esteso con 7 colonne D-17 audit trail (4 originali + 3 sha256)
- Migrazione idempotente: re-run non duplica colonne (verificato via `PRAGMA table_info`)
- `backtest.baseline.warmup.longest_lookback_required` operativo (D-07, BLOCKER 3 fix)
- `scripts/preflight_phase5.py` operativo (BLOCKER 6 fix)
- Phase 1-4 dependency risk dichiarato in frontmatter `phase1_dependency_risk` — Wave 4 smoke
  bloccato fino a Phase 1-4 completion
</verification>

<success_criteria>
- Plan 05-01 completo: env + config + schema migrate + package init + warmup helper + preflight
- Wave 1 può iniziare: imports pyarrow/matplotlib non falliscono, `backtest.baseline`
  importabile, `LedgerWriter.record_run()` accetta i 7 nuovi campi, warmup adattivo disponibile
- BACK-07 / INT-01: scaffolding non blocca SC#1 (<30 min) né SC#2 (report) né SC#3 (parquet)
</success_criteria>

<output>
Dopo completamento: `.planning/phases/05-baseline-backtest/05-01-SUMMARY.md`
con sezioni: Files Modified, Decisions Encoded (D-15/D-17/D-07), Versions Installed (pyarrow, matplotlib),
Schema Migration (7 columns), Warmup Helper (D-07), Preflight Probe Status, Next Wave (Wave 1: 05-03 + 05-04 paralleli).
</output>
</content>
</invoke>