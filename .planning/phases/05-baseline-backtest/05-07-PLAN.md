---
phase: 05-baseline-backtest
plan: 07
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06a, 05-06b]
files_modified:
  - backtest/baseline/runner.py
  - scripts/run_baseline_backtest.py
  - scripts/profile_baseline_slice.py
autonomous: true
requirements: [BACK-07, INT-01]
tags: [phase-5, baseline-backtest, wave-3, runner, cli, profiler]

phase1_dependency_risk: |
  REVISIONE 2026-05-08: l'INTERA dependency chain Phase 1-4 ancora in planning. Runner
  orchestra ProcessPoolExecutor + invoca run_slice_3profiles. Tutti i test in questo
  Plan MOCKANO slice_worker (no real CSV, no real engine). Esecuzione end-to-end reale è
  task del Plan 05-08 (smoke run) — bloccato dal preflight `scripts/preflight_phase5.py`
  (Plan 05-01 Task 4) che fail-fast se Phase 1-4 modules absent.

must_haves:
  truths:
    - "scripts/run_baseline_backtest.py --force lancia 9 worker via ProcessPoolExecutor con mp_context spawn"
    - "runner.py:run_baseline(force) carica baseline.yaml + costs.yaml + strategy.yaml + abilita WAL una volta"
    - "tqdm progress bar attiva su as_completed(futures) (9 task) se baseline_cfg.progress_bar=True"
    - "Worker exception non kill pool — ritorna status=FAILED nel result list"
    - "Post-pool: finalize_parquet_shards + write_baseline_report invocati"
    - "scripts/profile_baseline_slice.py SYM TF PROFILE stampa wall-clock + memory peak"
    - "BaselineConfig dataclass + load_baseline_config() loader esposti da runner"
  artifacts:
    - path: "backtest/baseline/runner.py"
      provides: "run_baseline orchestrator + BaselineConfig + load_baseline_config"
      contains: "ProcessPoolExecutor"
      contains: "max_workers"
      contains: "mp.get_context(\"spawn\")"
      contains: "as_completed"
      contains: "finalize_parquet_shards"
      contains: "write_baseline_report"
      contains: "enable_sqlite_wal"
      contains: "@dataclass(frozen=True)"
      min_lines: 120
    - path: "scripts/run_baseline_backtest.py"
      provides: "CLI entry point con --force flag"
      contains: "argparse"
      contains: "--force"
      contains: "run_baseline"
    - path: "scripts/profile_baseline_slice.py"
      provides: "perf profiler single slice (Wave 1 sanity check)"
      contains: "psutil"
      contains: "wall_clock"
  key_links:
    - from: "backtest/baseline/runner.py::run_baseline"
      to: "scripts/run_baseline_backtest.py"
      via: "CLI invoca run_baseline(force=args.force)"
    - from: "backtest/baseline/runner.py"
      to: "backtest/baseline/slice_worker.py"
      via: "ProcessPoolExecutor.submit(run_slice_3profiles, ...)"
---

<objective>
Wave 3 — Top-level orchestrator + CLI entry-point + perf profiler.

1. **`backtest/baseline/runner.py`** — `run_baseline(force)` main orchestrator.
   Carica configs, abilita SQLite WAL, lancia ProcessPoolExecutor(9) su (symbol, tf), passa
   `run_slice_3profiles` come worker target, raccoglie 27 result via `as_completed` + tqdm,
   chiama `finalize_parquet_shards` + `write_baseline_report` post-pool. Definisce
   `BaselineConfig` dataclass + `load_baseline_config()` loader (pattern Phase 1 D-05
   `load_cost_model`).

2. **`scripts/run_baseline_backtest.py`** — CLI entry. argparse + sys.path bootstrap +
   `run_baseline(force=args.force)`.

3. **`scripts/profile_baseline_slice.py`** — Perf profiler single-slice (RESEARCH SC#1
   sanity check). Esegue 1 slice, misura wall-clock + memory peak via psutil.

Output: 3 file + integration smoke test in `tests/test_baseline_runner.py` (mocked worker).

Purpose: chiude Wave 3. Plan 05-08 successivo eseguirà la run reale end-to-end.
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-CONTEXT.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md
@C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md
@C:/trading-agent/scripts/dry_run_cycle.py
@C:/trading-agent/backtest/costs.py
@C:/trading-agent/backtest/baseline/slice_worker.py
@C:/trading-agent/backtest/baseline/dataset_writer.py
@C:/trading-agent/backtest/baseline/report_writer.py
@C:/trading-agent/backtest/baseline/wal_setup.py

<interfaces>
From scripts/dry_run_cycle.py (analog bootstrap pattern):
```python
from __future__ import annotations
import sys
from pathlib import Path
ROOT = Path(__file__).resolve().parent.parent
if str(ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))
# ... imports
```

From backtest/costs.py (pattern S4 frozen dataclass + YAML loader):
```python
@dataclass(frozen=True)
class CostModel: ...
def load_cost_model(yaml_path: Path) -> CostModel: ...
```

From backtest/baseline/slice_worker.py (Wave 2):
  - run_slice_3profiles(symbol, tf, baseline_cfg, costs_cfg, strategy_cfg_path,
                          force, ledger_db_path, run_date) → list[dict]
  - PROFILES tuple

PAIRS = ("EURUSD", "GBPUSD", "USDJPY")
TFS = ("M15", "M30", "H1")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backtest/baseline/runner.py — main orchestrator + BaselineConfig loader</name>
  <files>backtest/baseline/runner.py, tests/test_baseline_runner.py</files>
  <read_first>
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md §`runner.py` lines 52-95
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-RESEARCH.md §Pattern 1 lines 261-302
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-CONTEXT.md §specifics lines 251-263 (main skeleton)
    - C:/trading-agent/backtest/costs.py (frozen dataclass loader pattern)
  </read_first>
  <behavior>
    - Test 1: `test_run_baseline_orchestrates_27_runs(monkeypatch, tmp_path)` — Mocka
      `run_slice_3profiles` per ritornare 3 dummy result. Verifica che `run_baseline()`
      raccoglie 9*3=27 result totali e invoca `finalize_parquet_shards` + `write_baseline_report` 1 volta.
    - Test 2: `test_run_baseline_handles_worker_failure(monkeypatch)` — Mocka uno dei 9 worker
      a sollevare exception → verifica che pool completa, result include status=FAILED per
      quel worker (3 profile FAILED), altri 24 OK.
    - Test 3: `test_load_baseline_config(tmp_path)` — Crea YAML temp, chiama
      `load_baseline_config(path)`, verifica frozen dataclass con campi corretti.
  </behavior>
  <action>
    **Sub-task 1a — Crea `backtest/baseline/runner.py`:**

    ```python
    """Phase 5 baseline orchestrator: 27 run paralleli (BACK-07, INT-01).

    Hybrid orchestration D-15: ProcessPoolExecutor(9) su (symbol, tf), 3 profile
    sequenziali per worker riusando indicator cache (compute_all_extended UNA volta).
    Wall-clock target <30 min su 8-core dev laptop (SC#1).

    Source: PATTERNS.md §runner.py, RESEARCH.md §Pattern 1.
    """
    from __future__ import annotations

    import logging
    import multiprocessing as mp
    import time
    from concurrent.futures import ProcessPoolExecutor, as_completed
    from dataclasses import dataclass
    from datetime import date
    from itertools import product
    from pathlib import Path

    import yaml
    from tqdm import tqdm

    from backtest.baseline.slice_worker import run_slice_3profiles
    from backtest.baseline.wal_setup import enable_sqlite_wal
    from backtest.baseline.dataset_writer import finalize_parquet_shards
    from backtest.baseline.report_writer import write_baseline_report
    from backtest.baseline.determinism import file_sha256

    _log = logging.getLogger(__name__)

    PAIRS = ("EURUSD", "GBPUSD", "USDJPY")
    TFS = ("M15", "M30", "H1")


    @dataclass(frozen=True)
    class BaselineConfig:
        """Frozen config caricato da data/configs/baseline.yaml (D-15, D-17, D-19, D-20)."""
        equity_initial_eur: float
        slippage_seed: int
        timeout_bars: dict          # {"M15": 96, "M30": 96, "H1": 120}
        warm_up_min_bars: int
        max_workers: int
        parquet_compression: str
        force_rerun: bool
        progress_bar: bool
        training_data_dir: str
        report_dir: str
        equity_curves_dir: str


    def load_baseline_config(yaml_path: Path | None = None) -> BaselineConfig:
        """Carica baseline.yaml in dataclass frozen (pattern Phase 1 load_cost_model)."""
        path = yaml_path or Path("data/configs/baseline.yaml")
        with open(path, encoding="utf-8") as f:
            cfg = yaml.safe_load(f) or {}
        return BaselineConfig(
            equity_initial_eur=float(cfg["equity_initial_eur"]),
            slippage_seed=int(cfg["slippage_seed"]),
            timeout_bars={k: int(v) for k, v in cfg["timeout_bars"].items()},
            warm_up_min_bars=int(cfg["warm_up_min_bars"]),
            max_workers=int(cfg["max_workers"]),
            parquet_compression=str(cfg.get("parquet_compression", "snappy")),
            force_rerun=bool(cfg.get("force_rerun", False)),
            progress_bar=bool(cfg.get("progress_bar", True)),
            training_data_dir=str(cfg.get("training_data_dir", "data/training")),
            report_dir=str(cfg.get("report_dir", ".planning/research")),
            equity_curves_dir=str(cfg.get("equity_curves_dir",
                                            ".planning/research/baseline-equity-curves")),
        )


    def run_baseline(
        force: bool = False,
        baseline_yaml: Path | None = None,
        costs_yaml: Path | None = None,
        strategy_yaml: Path | None = None,
        ledger_db: Path | None = None,
    ) -> list[dict]:
        """Orchestrator main process. Hybrid orchestration D-15.

        - Carica configs, abilita SQLite WAL una volta
        - ProcessPoolExecutor(N=max_workers, mp_context=spawn) su (symbol, tf) cartesian
        - tqdm progress bar su as_completed se progress_bar=True
        - Per-worker exception → status=FAILED in result list (no pool kill)
        - Post-pool: finalize_parquet_shards + write_baseline_report
        """
        t0 = time.time()
        baseline_cfg = load_baseline_config(baseline_yaml)
        costs_path = costs_yaml or Path("data/configs/costs.yaml")
        strategy_path = strategy_yaml or Path("config/strategy.yaml")
        ledger_path = ledger_db or Path("logs/trades.db")

        with open(costs_path, encoding="utf-8") as f:
            costs_cfg = yaml.safe_load(f) or {}

        # Abilita WAL una tantum (D-16)
        enable_sqlite_wal(ledger_path)

        tasks = list(product(PAIRS, TFS))  # 9 (symbol, tf)
        run_date = date.today().isoformat()

        # Windows default è già spawn ma renderlo esplicito documenta il vincolo
        ctx = mp.get_context("spawn")
        all_results: list[dict] = []

        with ProcessPoolExecutor(max_workers=baseline_cfg.max_workers, mp_context=ctx) as pool:
            futures = {
                pool.submit(
                    run_slice_3profiles,
                    symbol, tf, baseline_cfg, costs_cfg, strategy_path,
                    force, ledger_path, run_date,
                ): (symbol, tf)
                for symbol, tf in tasks
            }
            iterator = as_completed(futures)
            if baseline_cfg.progress_bar:
                iterator = tqdm(iterator, total=len(futures), desc="slice")
            for fut in iterator:
                slice_id = futures[fut]
                try:
                    all_results.extend(fut.result())
                except Exception as exc:  # NON relanciare — permetti agli altri di completare
                    _log.error("slice %s fallita: %s", slice_id, exc, exc_info=True)
                    sym, tf = slice_id
                    for prof in ("CONSERVATIVE", "MODERATE", "AGGRESSIVE"):
                        all_results.append({
                            "run_id": f"baseline_{run_date}_{sym}_{tf}_{prof}",
                            "symbol": sym, "timeframe": tf, "profile": prof,
                            "status": "FAILED", "error": str(exc),
                        })

        # Post-pool: finalize parquet shards
        finalize_parquet_shards(Path(baseline_cfg.training_data_dir))

        # Post-pool: scrivi report MD
        wall_clock = time.time() - t0
        report_path = Path(baseline_cfg.report_dir) / f"baseline-{run_date}.md"
        meta = {
            "cli_command": "python scripts/run_baseline_backtest.py" + (" --force" if force else ""),
            "git_sha": _git_sha(),
            "total_wall_clock_seconds": wall_clock,
            # WARNING 12 fix: full 64-char sha256 keys
            "cost_yaml_sha256": file_sha256(costs_path),
            "strategy_yaml_sha256": file_sha256(strategy_path) if strategy_path.exists() else "n/a",
            "baseline_yaml_sha256": file_sha256(baseline_yaml or Path("data/configs/baseline.yaml")),
            "slippage_seed": baseline_cfg.slippage_seed,
            "warm_up_bars": {tf: baseline_cfg.warm_up_min_bars for tf in TFS},
            "longest_lookback": baseline_cfg.warm_up_min_bars,
            # SC#3 thresholds (BLOCKER 2)
            "min_decisions_hard": 1000,
            "target_decisions_soft": 10000,
        }
        write_baseline_report(all_results, report_path, meta)
        _log.info("baseline run completata: %.1fs, report=%s", wall_clock, report_path)
        return all_results


    def _git_sha() -> str:
        """Best-effort. Riusa pattern slice_worker._git_sha."""
        import subprocess
        try:
            return subprocess.check_output(
                ["git", "rev-parse", "HEAD"],
                cwd=str(Path(__file__).resolve().parents[2]),
                stderr=subprocess.DEVNULL,
            ).decode().strip()
        except Exception:
            return "unknown"
    ```

    **Sub-task 1b — Aggiungi 3 test in `tests/test_baseline_runner.py`** (NON sostituire i
    4 test esistenti — append):

    ```python
    # ── Wave 3 additions: orchestrator-level test ──────────────────────────────

    def test_load_baseline_config(tmp_path):
        from backtest.baseline.runner import load_baseline_config, BaselineConfig
        yaml_path = tmp_path / "baseline.yaml"
        yaml_path.write_text(
            "equity_initial_eur: 10000\n"
            "slippage_seed: 42\n"
            "timeout_bars: {M15: 96, M30: 96, H1: 120}\n"
            "warm_up_min_bars: 200\n"
            "max_workers: 9\n"
            "parquet_compression: snappy\n"
            "force_rerun: false\n"
            "progress_bar: true\n"
            'training_data_dir: "data/training"\n'
            'report_dir: ".planning/research"\n'
            'equity_curves_dir: ".planning/research/baseline-equity-curves"\n',
            encoding="utf-8",
        )
        cfg = load_baseline_config(yaml_path)
        assert isinstance(cfg, BaselineConfig)
        assert cfg.equity_initial_eur == 10000.0
        assert cfg.slippage_seed == 42
        assert cfg.timeout_bars == {"M15": 96, "M30": 96, "H1": 120}
        assert cfg.max_workers == 9


    def test_run_baseline_orchestrates_27_runs(monkeypatch, tmp_path):
        """Smoke: 9 task × 3 profile = 27 result. Mock worker → no real CSV/engine."""
        # baseline.yaml temp
        baseline_path = tmp_path / "baseline.yaml"
        baseline_path.write_text(
            "equity_initial_eur: 10000\nslippage_seed: 42\n"
            "timeout_bars: {M15: 96, M30: 96, H1: 120}\n"
            "warm_up_min_bars: 200\nmax_workers: 2\n"  # ridotto per test
            "parquet_compression: snappy\nforce_rerun: false\nprogress_bar: false\n"
            f'training_data_dir: "{tmp_path}"\n'
            f'report_dir: "{tmp_path}"\n'
            f'equity_curves_dir: "{tmp_path}"\n',
            encoding="utf-8",
        )
        costs_path = tmp_path / "costs.yaml"
        costs_path.write_text("EURUSD: {spread_pips: 0.5}\n", encoding="utf-8")
        strategy_path = tmp_path / "strategy.yaml"
        strategy_path.write_text("dummy: 1\n", encoding="utf-8")
        ledger_db = tmp_path / "trades.db"

        # Mock run_slice_3profiles a ritornare 3 result OK
        def fake_worker(sym, tf, *args, **kwargs):
            return [{"run_id": f"r_{sym}_{tf}_{p}", "symbol": sym, "timeframe": tf,
                     "profile": p, "status": "OK", "metrics": None,
                     "n_trades": 0, "n_drafts": 0, "equity_path": ""}
                    for p in ("CONSERVATIVE", "MODERATE", "AGGRESSIVE")]
        monkeypatch.setattr("backtest.baseline.runner.run_slice_3profiles", fake_worker)
        # Stub WAL setup + finalize_parquet
        monkeypatch.setattr("backtest.baseline.runner.enable_sqlite_wal", lambda p: None)
        monkeypatch.setattr("backtest.baseline.runner.finalize_parquet_shards", lambda p: None)

        from backtest.baseline.runner import run_baseline
        results = run_baseline(
            force=False, baseline_yaml=baseline_path, costs_yaml=costs_path,
            strategy_yaml=strategy_path, ledger_db=ledger_db,
        )
        assert len(results) == 27  # 9 (sym,tf) × 3 profile
        assert (tmp_path / f"baseline-{__import__('datetime').date.today().isoformat()}.md").exists()


    def test_run_baseline_handles_worker_failure(monkeypatch, tmp_path):
        """Worker exception → status=FAILED, pool continua."""
        baseline_path = tmp_path / "baseline.yaml"
        baseline_path.write_text(
            "equity_initial_eur: 10000\nslippage_seed: 42\n"
            "timeout_bars: {M15: 96, M30: 96, H1: 120}\n"
            "warm_up_min_bars: 200\nmax_workers: 2\nparquet_compression: snappy\n"
            "force_rerun: false\nprogress_bar: false\n"
            f'training_data_dir: "{tmp_path}"\nreport_dir: "{tmp_path}"\n'
            f'equity_curves_dir: "{tmp_path}"\n',
            encoding="utf-8",
        )
        (tmp_path / "costs.yaml").write_text("EURUSD: {}\n", encoding="utf-8")
        (tmp_path / "strategy.yaml").write_text("x: 1\n", encoding="utf-8")

        def flaky_worker(sym, tf, *args, **kwargs):
            if sym == "EURUSD" and tf == "M15":
                raise RuntimeError("synthetic worker failure")
            return [{"run_id": f"r_{sym}_{tf}_{p}", "status": "OK"}
                    for p in ("CONSERVATIVE", "MODERATE", "AGGRESSIVE")]
        monkeypatch.setattr("backtest.baseline.runner.run_slice_3profiles", flaky_worker)
        monkeypatch.setattr("backtest.baseline.runner.enable_sqlite_wal", lambda p: None)
        monkeypatch.setattr("backtest.baseline.runner.finalize_parquet_shards", lambda p: None)

        from backtest.baseline.runner import run_baseline
        results = run_baseline(
            baseline_yaml=baseline_path,
            costs_yaml=tmp_path / "costs.yaml",
            strategy_yaml=tmp_path / "strategy.yaml",
            ledger_db=tmp_path / "trades.db",
        )
        # 27 totali, 3 di EURUSD/M15 sono FAILED
        assert len(results) == 27
        failed = [r for r in results if r.get("status") == "FAILED"]
        assert len(failed) == 3
        assert all(r["symbol"] == "EURUSD" and r["timeframe"] == "M15" for r in failed)
    ```
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_baseline_runner.py::test_load_baseline_config tests/test_baseline_runner.py::test_run_baseline_orchestrates_27_runs tests/test_baseline_runner.py::test_run_baseline_handles_worker_failure -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - File `backtest/baseline/runner.py` esiste, ≥120 righe
    - `grep -c "ProcessPoolExecutor" backtest/baseline/runner.py` ≥ 1
    - `grep -c "mp.get_context(\"spawn\")" backtest/baseline/runner.py` == 1
    - `grep -c "as_completed" backtest/baseline/runner.py` ≥ 1
    - `grep -c "@dataclass(frozen=True)" backtest/baseline/runner.py` == 1
    - `grep -c "def load_baseline_config" backtest/baseline/runner.py` == 1
    - `grep -c "def run_baseline" backtest/baseline/runner.py` == 1
    - `grep -c "finalize_parquet_shards" backtest/baseline/runner.py` ≥ 1
    - `grep -c "write_baseline_report" backtest/baseline/runner.py` ≥ 1
    - `grep -c "enable_sqlite_wal" backtest/baseline/runner.py` ≥ 1
    - 3 nuovi test pass + i 4 di 05-06 ancora verdi
  </acceptance_criteria>
  <done>
    Runner orchestrator implementato + BaselineConfig loader + 3 test orchestrazione verdi.
  </done>
</task>

<task type="auto">
  <name>Task 2: scripts/run_baseline_backtest.py + scripts/profile_baseline_slice.py</name>
  <files>scripts/run_baseline_backtest.py, scripts/profile_baseline_slice.py</files>
  <read_first>
    - C:/trading-agent/scripts/dry_run_cycle.py (verbatim bootstrap pattern)
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-PATTERNS.md §`scripts/run_baseline_backtest.py` lines 524-565
    - C:/trading-agent/.planning/phases/05-baseline-backtest/05-VALIDATION.md row "(perf) Memory peak"
  </read_first>
  <action>
    **Sub-task 2a — Crea `scripts/run_baseline_backtest.py`:**

    ```python
    r"""Phase 5 baseline backtest runner — 27 run paralleli (BACK-07, INT-01).

    Esegue baseline pre-ML completo: 23.5y × 3 pair × 3 TF × 3 profile = 27 run.
    Output:
      - data/training/baseline_decisions/  (parquet directory, pyarrow.dataset format)
      - data/training/baseline_drafts/     (parquet directory)
      - .planning/research/baseline-{date}.md
      - .planning/research/baseline-equity-curves/{symbol}_{tf}_{profile}.png (×27)
      - logs/trades.db tabelle backtest_trades + backtest_runs

    Uso:
        .\.venv\Scripts\python.exe scripts\run_baseline_backtest.py
        .\.venv\Scripts\python.exe scripts\run_baseline_backtest.py --force
    """
    from __future__ import annotations

    import argparse
    import logging
    import sys
    from pathlib import Path

    ROOT = Path(__file__).resolve().parent.parent
    if str(ROOT) not in sys.path:
        sys.path.insert(0, str(ROOT))

    from backtest.baseline.runner import run_baseline  # noqa: E402


    def main() -> int:
        logging.basicConfig(
            level=logging.INFO,
            format="%(asctime)s %(levelname)s %(name)s: %(message)s",
        )
        parser = argparse.ArgumentParser(
            description="Phase 5 baseline backtest (27 run, 23.5y x 3 pair x 3 TF x 3 profile)",
        )
        parser.add_argument("--force", action="store_true",
                            help="re-run anche se run_id esiste (overwrite ledger + parquet)")
        parser.add_argument("--max-wall-clock", type=int, default=1800,
                            help="WARNING 10 fix: max wall-clock seconds (default 1800 = 30 min). "
                                 "Exit non-zero se wall > max (BACK-07 SC#1 hard gate).")
        parser.add_argument("--no-time-gate", action="store_true",
                            help="Disabilita il time-gate di --max-wall-clock (debug only).")
        args = parser.parse_args()

        import time as _time
        start = _time.monotonic()
        results = run_baseline(force=args.force)
        wall = _time.monotonic() - start
        print(f"WALL_CLOCK_SECONDS={wall:.0f}")

        # WARNING 10 fix: hard gate su wall-clock
        if args.no_time_gate:
            print("(time-gate disabled via --no-time-gate)")
        elif wall > args.max_wall_clock:
            print(f"FAIL: wall-clock {wall:.0f}s > max {args.max_wall_clock}s "
                  f"(BACK-07 SC#1 violato). Use --no-time-gate per bypass debug.",
                  file=__import__('sys').stderr)
            return 2
        return 0


    if __name__ == "__main__":
        raise SystemExit(main())
    ```

    **Sub-task 2b — Crea `scripts/profile_baseline_slice.py`:**

    ```python
    r"""Phase 5 perf profiler single-slice (BACK-07 SC#1 sanity).

    Esegue 1 slice (symbol, tf, profile) e misura:
      - wall_clock totale
      - memory peak via psutil (RSS)

    Output: stampa stdout per audit Wave 1.

    Uso:
        .\.venv\Scripts\python.exe scripts\profile_baseline_slice.py EURUSD M15 MODERATE
    """
    from __future__ import annotations

    import argparse
    import sys
    import time
    from pathlib import Path

    ROOT = Path(__file__).resolve().parent.parent
    if str(ROOT) not in sys.path:
        sys.path.insert(0, str(ROOT))

    try:
        import psutil
    except ImportError:
        print("WARNING: psutil non installato — pip install psutil per memory profiling")
        psutil = None  # type: ignore


    def main() -> int:
        parser = argparse.ArgumentParser(description="Profile single baseline slice")
        parser.add_argument("symbol")
        parser.add_argument("timeframe")
        parser.add_argument("profile", choices=["CONSERVATIVE", "MODERATE", "AGGRESSIVE"])
        args = parser.parse_args()

        from backtest.baseline.runner import load_baseline_config
        from backtest.baseline.slice_worker import run_slice_3profiles
        import yaml

        baseline_cfg = load_baseline_config()
        with open("data/configs/costs.yaml", encoding="utf-8") as f:
            costs_cfg = yaml.safe_load(f) or {}
        strategy_cfg_path = Path("config/strategy.yaml")

        proc = psutil.Process() if psutil else None
        rss_start = proc.memory_info().rss if proc else 0
        t0 = time.time()
        results = run_slice_3profiles(
            args.symbol, args.timeframe, baseline_cfg, costs_cfg, strategy_cfg_path,
            force=False, ledger_db_path=Path("logs/trades.db"),
        )
        wall_clock = time.time() - t0
        rss_end = proc.memory_info().rss if proc else 0
        rss_peak_mb = (rss_end - rss_start) / 1e6 if proc else 0

        # Filtra solo profile richiesto
        target = next((r for r in results if r["profile"] == args.profile), None)
        print(f"=== Profile {args.symbol} {args.timeframe} {args.profile} ===")
        print(f"wall_clock: {wall_clock:.2f}s")
        print(f"rss_delta_mb: {rss_peak_mb:.1f}")
        print(f"status: {target['status'] if target else 'N/A'}")
        if target and target.get("status") == "OK":
            print(f"n_trades: {target.get('n_trades', 0)}")
            print(f"n_drafts: {target.get('n_drafts', 0)}")
        return 0


    if __name__ == "__main__":
        raise SystemExit(main())
    ```

    **NB:** `psutil` può non essere installato. Lo script gestisce il fallback gracefully.
    Se vogliamo aggiungerlo come dep: `pip install psutil` e una riga in requirements.txt
    — facoltativo, NON bloccante per Phase 5.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe scripts\run_baseline_backtest.py --help 2>&1 | findstr /C:"--force" && .venv\Scripts\python.exe scripts\profile_baseline_slice.py --help 2>&1 | findstr /C:"symbol"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/run_baseline_backtest.py` esiste
    - File `scripts/profile_baseline_slice.py` esiste
    - `python scripts\run_baseline_backtest.py --help` exit 0, output contiene `--force` AND `--max-wall-clock` AND `--no-time-gate` (WARNING 10 fix)
    - `python scripts\profile_baseline_slice.py --help` exit 0, output contiene `symbol`, `timeframe`, `profile`
    - `grep -c "from backtest.baseline.runner import run_baseline" scripts/run_baseline_backtest.py` == 1
    - `grep -c "argparse" scripts/run_baseline_backtest.py` ≥ 1
    - `grep -c "psutil" scripts/profile_baseline_slice.py` ≥ 1
    - `grep -c "wall_clock" scripts/profile_baseline_slice.py` ≥ 1
    - `grep -c "ROOT = Path(__file__).resolve()" scripts/run_baseline_backtest.py` == 1 (bootstrap pattern)
  </acceptance_criteria>
  <done>
    CLI entry-point + perf profiler implementati. `--help` risponde correttamente.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (--force) → run_baseline | argparse validation; --force solo bool, no string injection |
| ProcessPool spawn → worker | Solo dati pickleable trasferiti (config dataclass + Path stringa) |
| psutil rss read | Read-only su processo padre — no privileged ops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | DoS | ProcessPool resource exhaustion | mitigate | `max_workers` limitato da baseline.yaml (default 9). RAM peak ~1.8GB documentato in RESEARCH §Hot Spots. |
| T-05-19 | Tampering | --force overwrite ledger | accept | User-driven flag — desired behavior per D-14. NON default. |
| T-05-20 | Information Disclosure | scripts/profile_baseline_slice.py stdout | accept | RSS + wall_clock — no secret material |
</threat_model>

<verification>
- 3 file Wave 3 implementati (runner.py, run_baseline_backtest.py, profile_baseline_slice.py)
- 3 nuovi test orchestrator pass (oltre ai 4 di 05-06 ancora verdi → totale 7)
- CLI `--help` funzionante per entrambi gli script
- Pattern bootstrap PATH (sys.path injection) replicato
- ProcessPoolExecutor configurato con `mp_context="spawn"` esplicito (Windows)
</verification>

<success_criteria>
- Wave 3 completa. End-to-end pipeline pronta (richiede solo Phase 1 BacktestEngine
  funzionante per smoke run reale).
- D-15 hybrid orchestration totalmente cablata: runner→pool→worker→engine→writer→report
- BACK-07 perf budget testabile via `scripts/profile_baseline_slice.py`
</success_criteria>

<output>
Dopo completamento: `.planning/phases/05-baseline-backtest/05-07-SUMMARY.md`.
Sezioni: Files Created, Tests Passing (7/7 runner), CLI surface, Profiler, Next Wave (05-08 smoke run + INT-01 deliverable commit).
</output>
