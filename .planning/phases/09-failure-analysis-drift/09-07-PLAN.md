---
phase: 09-failure-analysis-drift
plan: 07
type: execute
wave: 6
depends_on: [09-02, 09-03, 09-04, 09-05, 09-06]
files_modified:
  - tests/test_drift_integration_e2e.py
  - tests/test_cluster_drift_integration.py
  - tests/test_retrain_e2e_atomic.py
  - .planning/phases/09-failure-analysis-drift/09-VERIFICATION.md
autonomous: true
cross_phase_prereqs:
  - "phase-7 plan-07-05 (ml/inference.py exports MLFilter — required by E2E tests for atomic_promote singleton invalidate; gated via pytest.importorskip)"
  - "phase-7 plan-07-04 (ml/threshold.py exports sweep_expectancy_per_profile — required by E2E validation_gate flow; gated)"
  - "phase-8 plan-08-04 (mcp_tools/handlers/ml.py — required by E2E retrain dedup; gated)"
requirements:
  - ML-07
  - ML-08
  - ML-09
  - MCP-07
  - MCP-08
  - MCP-18
  - INT-03
must_haves:
  truths:
    - "B6 (Option A): MCP-08 _load_current_window + _load_reference_window in mcp_tools/handlers/drift.py sono GIA' IMPLEMENTATI in Plan 09-03 Task 3 (Wave 2 closure complete). Plan 09-07 NON re-implementa — verifies via integration test che 'raise NotImplementedError' returns 0 in source"
    - "E2E smoke 1 (rejected path): simulate drift breach → enqueue retrain via dedup → validation_gate.run_validation_gate REJECTS (synthetic candidate ECE regression > MAX_ECE_REGRESSION_PCT) → old model atomically preserved → validation_log.db row con passed=0 + reject_reason"
    - "E2E smoke 2 (promotion path): simulate drift breach → retrain → validation_gate passes → atomic_promote os.replace pair → MLFilter singleton invalidated → next get_ml_filter ricarica nuovo bundle (P8 verified)"
    - "E2E smoke 3 (P7 pair-crash): atomic_promote fails between metadata.replace + pkl.replace → final metadata = v2, final pkl = v1 → next load FeatureSchemaMismatchError-like surfaced"
    - "Cross-area integration: cluster artifact loaded + drift monitor running + MCP-18 ml_recheck rispettando ENABLE_ML_FILTER=false fallback (3 area coexist)"
    - "Full suite gate: pytest tests/ passes con zero regression vs Phase 1-8 baseline 458 passed (allowing Phase 1 perf test_smoke_12month_under_60s pre-existing fail)"
    - "Requirements coverage cross-check: ML-07/08/09 + MCP-07/08/18 + INT-03 ognuno ha ≥1 passing test"
    - "16 D-09-* decisions A1-A4 + B1-B4 + C1-C4 + D1-D4 ognuna touched in source o test"
    - "ROADMAP SC#1..4 ognuna ha ≥1 test green explicitly verifying il behavior"
    - "W14: VERIFICATION.md include 1-line explicit guard 'negative regression on Phase 7/8 requirements' — i Phase 7/8 closed requirements (ML-01..06, MCP-04..06, MCP-09..12, MCP-15..17) NON sono regressed da Phase 9 changes (grep + smoke test inclusi)"
    - "W13 monkeypatch stability: tests usano `retrain.promotion._invalidate_singleton` (module-level patch target — established in Plan 09-04 Task 2) NOT `ml.inference._invalidate_singleton`"
    - "VERIFICATION.md scritto con coverage table 7 req × 16 D × 4 SC × 15 P + W14 negative regression guard + sign-off per gate phase complete"
  artifacts:
    - path: "tests/test_drift_integration_e2e.py"
      provides: "Synthetic drift breach → retrain rejected/promoted full path (3 E2E test)"
      min_lines: 200
    - path: "tests/test_cluster_drift_integration.py"
      provides: "Cluster + drift coexistence integration"
      min_lines: 100
    - path: "tests/test_retrain_e2e_atomic.py"
      provides: "Atomic promote + singleton invalidation E2E + dedup race"
      min_lines: 150
    - path: ".planning/phases/09-failure-analysis-drift/09-VERIFICATION.md"
      provides: "Phase gate verification table + W14 negative regression check + sign-off"
      min_lines: 130
  key_links:
    - from: "tests/test_drift_integration_e2e.py"
      to: "drift.compute + drift.alarm + retrain.dedup + retrain.validation_gate + retrain.promotion"
      via: "compose full pipeline"
      pattern: "run_validation_gate|atomic_promote"
    - from: "tests/test_retrain_e2e_atomic.py"
      to: "retrain.promotion._invalidate_singleton (W13 module-level)"
      via: "monkeypatch su retrain.promotion._invalidate_singleton — stable target"
      pattern: "retrain.promotion._invalidate_singleton"
    - from: ".planning/phases/09-failure-analysis-drift/09-VERIFICATION.md"
      to: "Phase 9 closure phase gate + W14 negative regression Phase 7/8"
      via: "coverage table 7 req × SC × D-decisions × pitfalls audit + Phase 7/8 carry-forward smoke"
      pattern: "verification_passed|negative_regression_phase78"
---

<objective>
Wave 6 Phase 9 — integration + E2E smoke + phase gate verification.

**B6 Option A applied:** Plan 09-03 chiude MCP-08 fully (Task 3 implementa `_load_current_window` + `_load_reference_window` in-place). Plan 09-07 NON re-implementa quei reader (Wave 2 architectural deviation Rule 3 eliminata). Plan 09-07 ora si concentra solo su:
1. 3 E2E test file (Task originale 2)
2. 09-VERIFICATION.md phase gate + W14 negative regression Phase 7/8 (Task originale 3)

Aggiunge 3 nuovi test file E2E (drift→retrain rejected + drift→retrain promoted + cluster/drift coexistence + dedup race) usando **W13 monkeypatch stability** (`retrain.promotion._invalidate_singleton` module-level target), e produce `09-VERIFICATION.md` con coverage matrix 7 req × 16 D × 4 SC × 15 pitfalls + W14 negative regression Phase 7/8 guard + sign-off.

Purpose: chiude Phase 9 con full-suite green + zero regression vs baseline Phase 1-8. ROADMAP SC#1..4 tutti verificati explicitly.

Output: 3 E2E test file + VERIFICATION.md phase gate. **NO Task 1 redundant** (B6 Option A risolto in 09-03).
</objective>

<execution_context>
@/workspaces/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-CONTEXT.md
@/workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-RESEARCH.md
@/workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-PATTERNS.md
@/workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-02-PLAN.md
@/workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-03-PLAN.md
@/workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-04-PLAN.md
@/workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-05-PLAN.md
@/workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-06-PLAN.md
@/workspaces/trading-agent/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3 E2E integration test files (drift→retrain rejected, drift→retrain promoted, cluster/drift coexistence, dedup race) — W13 monkeypatch stability</name>
  <files>tests/test_drift_integration_e2e.py, tests/test_cluster_drift_integration.py, tests/test_retrain_e2e_atomic.py</files>
  <read_first>
    - /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-CONTEXT.md § scenarios E2E (full document context)
    - /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-RESEARCH.md § Failure Mode Coverage (lines 722-733)
    - /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-04-PLAN.md (retrain pipeline full + W13 module-level `_invalidate_singleton` import in retrain/promotion.py)
    - /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-05-PLAN.md (MCP-18 + fallback)
  </read_first>
  <action>
**Crea `tests/test_drift_integration_e2e.py`** (~250 LOC). 3 scenari E2E.

`pytest.importorskip("scipy")` + `pytest.importorskip("joblib")` + (per `test_drift_breach_pair_crash` aggiungi `pytest.importorskip("ml.inference")`) al TOP del file.

```python
"""Phase 9 Wave 6 — E2E integration test: drift breach → retrain → validation gate.

3 path:
1. REJECTED: synthetic candidate con ECE regression > MAX_ECE_REGRESSION_PCT → old preserved
2. PROMOTED: synthetic candidate passa tutti i check → atomic_promote + singleton invalidate
3. P7 PAIR-CRASH: failure between os.replace metadata + pkl → final.metadata=v2, final.pkl=v1
"""
from __future__ import annotations

import json
import os
import sqlite3
from pathlib import Path
from types import SimpleNamespace
from unittest.mock import MagicMock, patch

import pytest


pytest.importorskip("scipy")
pytest.importorskip("joblib")


@pytest.fixture
def mock_cfg(tmp_path):
    return SimpleNamespace(
        DRIFT_KS_PVALUE_THRESHOLD=0.01,
        DRIFT_ECE_DELTA_THRESHOLD=0.02,
        DRIFT_FEATURE_FRACTION_THRESHOLD=0.30,
        DRIFT_ECE_N_BINS=10,
        DRIFT_MIN_SAMPLES_CURRENT=30,
        DRIFT_TOP_K_FEATURES=15,
        DRIFT_DEFAULT_WINDOW_DAYS=30,
        DRIFT_SYNTH_SEED=42,
        MAX_ECE_REGRESSION_PCT=10.0,
        KS_CANDIDATE_DIVERGENCE_THRESHOLD=0.15,
        EXPECTANCY_REGRESSION_PCT=5.0,
        ML_MODEL_PATH=str(tmp_path / "classifier_v1.pkl"),
        MCP_TRAINING_DATA_PATH="data/training/baseline_decisions/part-0.parquet",
        ENABLE_ML_FILTER=False,
        OPERATING_TIMEZONE="UTC",
    )


def _create_dummy_bundle(path: Path, ece: float, version: str) -> None:
    import joblib
    bundle = {"model": MagicMock()}
    bundle["model"].predict_proba = MagicMock(return_value=[[0.5, 0.5]])
    joblib.dump(bundle, path)
    meta = {
        "model_version": version,
        "aggregate": {"ece": ece},
        "feature_importance_": {"atr": 0.3, "adx": 0.2},
        "feature_list": ["atr", "adx"],
        "categorical_encodings": {},
        "dataset_hash": "sha256:dummy",
    }
    path.with_suffix(".metadata.json").write_text(json.dumps(meta))


def test_drift_breach_retrain_rejected_old_preserved(mock_cfg, tmp_path, monkeypatch):
    """E2E 1: candidate ECE regression > threshold → reject + old preserved."""
    from retrain.validation_gate import run_validation_gate, ensure_validation_log_db

    ref_path = tmp_path / "classifier_v1.pkl"
    cand_path = tmp_path / "classifier_v2.pkl.candidate"
    _create_dummy_bundle(ref_path, ece=0.041, version="v1")
    _create_dummy_bundle(cand_path, ece=0.063, version="v2_candidate")  # 53.6% regression

    log_db = tmp_path / "validation_log.db"
    ensure_validation_log_db(log_db)

    held_out_df = MagicMock()
    # B3 expectancy hard-fail: monkeypatch ml.threshold absent → check 3 fails con
    # 'expectancy_check_unavailable'. ECE check 1 fails also (regression > 10%).
    import sys
    monkeypatch.setitem(sys.modules, "ml.threshold", None)

    result = run_validation_gate(
        cand_path, ref_path, held_out_df, mock_cfg,
        triggered_by_sources=["drift_breach"],
        log_db_path=str(log_db),
    )

    assert result["passed"] is False
    # Almeno una causa di reject deve essere presente
    assert result["reject_reason"] is not None

    # Old preserved
    assert ref_path.exists()
    assert json.loads(ref_path.with_suffix(".metadata.json").read_text())["model_version"] == "v1"

    # Audit row in validation_log.db
    with sqlite3.connect(str(log_db)) as c:
        rows = c.execute("SELECT check_name, passed FROM validation_log").fetchall()
    check_names = {r[0] for r in rows}
    assert "ece_regression" in check_names


def test_drift_breach_retrain_promoted_singleton_invalidated(mock_cfg, tmp_path, monkeypatch):
    """E2E 2: candidate passa validation → atomic_promote + singleton invalidate (P8 + W13)."""
    pytest.importorskip("ml.inference")
    from retrain.promotion import atomic_promote

    ref_path = tmp_path / "classifier_v1.pkl"
    cand_path = tmp_path / "classifier_v2.pkl"
    cand_temp = tmp_path / "classifier_v2.pkl.candidate"
    _create_dummy_bundle(ref_path, ece=0.041, version="v1")
    _create_dummy_bundle(cand_temp, ece=0.040, version="v2")

    # W13 STABLE TARGET: monkeypatch su retrain.promotion._invalidate_singleton
    # (module-level import — Plan 09-04 Task 2 stability)
    invalidate_counter = {"calls": 0}
    def _mock_invalidate():
        invalidate_counter["calls"] += 1
    monkeypatch.setattr("retrain.promotion._invalidate_singleton", _mock_invalidate)

    atomic_promote(cand_temp, cand_path)

    assert not cand_temp.exists()
    assert not cand_temp.with_suffix(".metadata.json").exists()
    assert cand_path.exists()
    final_meta = json.loads(cand_path.with_suffix(".metadata.json").read_text())
    assert final_meta["model_version"] == "v2"
    # W13 singleton invalidated (call counter)
    assert invalidate_counter["calls"] >= 1


def test_drift_breach_pair_crash_inter_pair_raises_on_load(mock_cfg, tmp_path, monkeypatch):
    """P7 + W13: simulate crash between os.replace metadata + os.replace pkl."""
    pytest.importorskip("ml.inference")
    from retrain.promotion import atomic_promote

    ref_path = tmp_path / "classifier_v1.pkl"
    cand_path = tmp_path / "classifier_v2.pkl"
    cand_temp = tmp_path / "classifier_v2.pkl.candidate"
    _create_dummy_bundle(ref_path, ece=0.041, version="v1")
    _create_dummy_bundle(cand_path, ece=0.041, version="v1")  # final pre-existing v1
    _create_dummy_bundle(cand_temp, ece=0.040, version="v2")

    # W13: monkeypatch su retrain.promotion._invalidate_singleton (NO leak)
    monkeypatch.setattr("retrain.promotion._invalidate_singleton", lambda: None)

    call_count = {"n": 0}
    original_replace = os.replace
    def flaky_replace(src, dst):
        call_count["n"] += 1
        if call_count["n"] == 2:
            raise OSError("simulated crash inter-pair")
        return original_replace(src, dst)
    monkeypatch.setattr(os, "replace", flaky_replace)

    with pytest.raises(OSError):
        atomic_promote(cand_temp, cand_path)

    # P7: metadata FIRST succeeded (v2), .pkl SECOND failed (still v1)
    final_meta = json.loads(cand_path.with_suffix(".metadata.json").read_text())
    assert final_meta["model_version"] == "v2"
```

**Crea `tests/test_cluster_drift_integration.py`** (~120 LOC) — invariato rispetto al piano originale eccetto B5 importorskip al top.

**Crea `tests/test_retrain_e2e_atomic.py`** (~150 LOC) — invariato eccetto W13 + B5 import gates.

```python
# B5: importorskip Phase 8 Plan 08-04 dep
pytest.importorskip("mcp_tools.handlers.ml", reason="Phase 8 Plan 08-04 prerequisite")
```
  </action>
  <verify>
    <automated>pytest tests/test_drift_integration_e2e.py tests/test_cluster_drift_integration.py tests/test_retrain_e2e_atomic.py -x -q 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_drift_integration_e2e.py -x -q` exit 0 (3 tests; OR skip-cleanly se Codespace no deps)
    - `pytest tests/test_cluster_drift_integration.py -x -q` exit 0 (2 tests)
    - `pytest tests/test_retrain_e2e_atomic.py -x -q` exit 0 (1 test)
    - `grep -c "test_drift_breach_retrain_rejected" /workspaces/trading-agent/tests/test_drift_integration_e2e.py` returns at least 1
    - `grep -c "test_drift_breach_retrain_promoted" /workspaces/trading-agent/tests/test_drift_integration_e2e.py` returns at least 1
    - `grep -c "test_drift_breach_pair_crash" /workspaces/trading-agent/tests/test_drift_integration_e2e.py` returns at least 1 (P7)
    - **W13 stability:** `grep -c "retrain.promotion._invalidate_singleton" /workspaces/trading-agent/tests/test_drift_integration_e2e.py` returns at least 2 (test_promoted + test_pair_crash patch target)
    - `grep -c "ml.inference._invalidate_singleton" /workspaces/trading-agent/tests/test_drift_integration_e2e.py` returns 0 (W13: NEVER patch directly su ml.inference)
    - `grep -c "test_mcp18_enable_ml_filter_false" /workspaces/trading-agent/tests/test_cluster_drift_integration.py` returns at least 1
    - `grep -c "test_retrain_concurrent_3_triggers" /workspaces/trading-agent/tests/test_retrain_e2e_atomic.py` returns at least 1
  </acceptance_criteria>
  <done>3 E2E test file con full scenario coverage: rejected/promoted/pair-crash + cluster-drift coexistence + 3-source dedup. W13 monkeypatch stability (target `retrain.promotion._invalidate_singleton` ONLY). Skip-cleanly su Codespace senza Phase 7/8 deps.</done>
</task>

<task type="auto">
  <name>Task 2: 09-VERIFICATION.md phase gate + W14 negative regression Phase 7/8 + full-suite smoke + REQUIREMENTS/ROADMAP/STATE update</name>
  <files>.planning/phases/09-failure-analysis-drift/09-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - /workspaces/trading-agent/.planning/phases/02-indicators-library/02-VERIFICATION.md (analog phase-gate document)
    - /workspaces/trading-agent/.planning/REQUIREMENTS.md (Traceability table per ML-07/08/09 + MCP-07/08/18 + INT-03 + Phase 7/8 closed reqs)
    - /workspaces/trading-agent/.planning/ROADMAP.md (Phase 9 entry to mark complete)
    - /workspaces/trading-agent/.planning/STATE.md (Active Work + Milestone Status table)
  </read_first>
  <action>
**Step 1 — Crea `.planning/phases/09-failure-analysis-drift/09-VERIFICATION.md`** (~250 LOC) — verbatim template originale + W14 section.

Aggiunge sezione **"Phase 7/8 Negative Regression Guard (W14)"** prima del sign-off:

```markdown
## Phase 7/8 Negative Regression Guard (W14)

Phase 9 changes MUST NOT regress Phase 7/8 closed requirements. Verify via:

| Phase 7 Closed Req | Original Closure Plan | Phase 9 Touch | Regression Check |
|---|---|---|---|
| ML-01 baseline classifier train | 07-03 | NO (Phase 9 reuses Phase 7 train via D-09-C1) | pytest tests/test_train_classifier.py exit 0 |
| ML-02 walk-forward CV | 07-03 | NO | pytest tests/test_ml_walkforward.py exit 0 |
| ML-03 calibration | 07-04 | NO (Phase 9 reads metadata only) | pytest tests/test_ml_calibration.py exit 0 |
| ML-04 threshold sweep | 07-04 | NO (Phase 9 invokes via run_validation_gate but does NOT modify) | pytest tests/test_ml_threshold.py exit 0 |
| ML-05 inference latency | 07-05 | NO (Phase 9 adds _invalidate_singleton hook only) | pytest tests/test_ml_inference_latency.py exit 0 |
| ML-06 ProposalDraft extension | 07-06 | NO (Phase 9 reads ml_calibrated_prob only) | pytest tests/test_ml_proposal_draft.py exit 0 |
| MCP-04..06 backtest tools | 06-03 | NO | pytest tests/test_mcp_handlers_backtest.py exit 0 |
| MCP-09..12 ml tools | 08-04 | NO (Phase 9 reuses _train_ml_filter_worker via D-09-C1) | pytest tests/test_mcp_handlers_ml.py exit 0 |
| MCP-15..17 position tools | 06-04 | NO (Phase 9 MCP-18 CONSUMES handle_get_position_state read-only) | pytest tests/test_mcp_handlers_position.py exit 0 |

**Cross-phase grep gates:**
- `grep -c "def train_classifier\|def MLFilter\|def get_ml_filter" /workspaces/trading-agent/ml/*.py` MUST return ≥ 3 (Phase 7 surfaces intact)
- `grep -c "def _train_ml_filter_worker" /workspaces/trading-agent/mcp_tools/handlers/ml.py` MUST return exactly 1 (Phase 8 entry point intact)
- `grep -c "def handle_get_position_state\|def handle_modify_position" /workspaces/trading-agent/mcp_tools/handlers/position.py` MUST return ≥ 2 (Phase 6 consumers intact)

Verifica: full suite gate `pytest tests/ --ignore=tests/test_smoke_12month_under_60s.py -q --tb=no` → tutti i test Phase 7/8/6 marked closed still pass (baseline 458 preserved).
```

Resto del documento verbatim (ROADMAP SC, Requirements table, D-09-* table, Pitfalls P1-P15 + W4/W6/W8/W9/W10/W11/W13/W14 row, Cross-Phase Carry-Forward, Project Memory Hard Rule + B3 reference, Full Suite Smoke, Sign-Off).

**Step 2 — Update `.planning/REQUIREMENTS.md`** Traceability table: marca ML-07, ML-08, ML-09, MCP-07, MCP-08, MCP-18, INT-03 ✓ Complete date. Resto invariato.

**Step 3 — Update `.planning/ROADMAP.md`** Phase 9 entry ✓ COMPLETE 7/7 plans. Summary "...via /gsd-execute-phase 9 7 commit atomici. ROADMAP SC#1..4 tutti ✓. ML-07/08/09 + MCP-07/08/18 + INT-03 chiusi. Riusa Phase 7 build_feature_vector + Phase 8 _train_ml_filter_worker (D-09-C1 zero-duplication = memoria training data integrity preserved). HARD-FAIL validation gate (B3 D-09-C2). W4/W6/W8/W9/W10/W11/W13 plan-checker WARNING applicati."

**Step 4 — Update `.planning/STATE.md`:** bump completed_plans/phases, Milestone Status Phase 9 ✓ complete 7/7 100%, Active Work block + notes last_updated bump.
  </action>
  <verify>
    <automated>test -f /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-VERIFICATION.md &amp;&amp; grep -c "ROADMAP Success Criteria Coverage\|Requirements Coverage\|D-09-\* Decision Coverage\|Pitfalls P1-P15 Coverage\|Phase 7/8 Negative Regression Guard" /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-VERIFICATION.md | awk '{exit ($1 >= 5 ? 0 : 1)}' &amp;&amp; grep -c "ML-07.*✓\|ML-08.*✓\|ML-09.*✓\|MCP-07.*✓\|MCP-08.*✓\|MCP-18.*✓\|INT-03.*✓" /workspaces/trading-agent/.planning/REQUIREMENTS.md | awk '{exit ($1 >= 7 ? 0 : 1)}' &amp;&amp; grep -c "Phase 9.*✓ COMPLETE\|Phase 9: Failure Analysis.*✓ COMPLETE" /workspaces/trading-agent/.planning/ROADMAP.md | awk '{exit ($1 >= 1 ? 0 : 1)}' &amp;&amp; pytest tests/ --ignore=tests/test_smoke_12month_under_60s.py -q --tb=no 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-VERIFICATION.md` exits 0
    - `grep -c "## ROADMAP Success Criteria Coverage" /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-VERIFICATION.md` returns at least 1
    - `grep -c "## Requirements Coverage" /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-VERIFICATION.md` returns at least 1
    - `grep -c "## D-09-\* Decision Coverage" /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-VERIFICATION.md` returns at least 1
    - `grep -c "## Pitfalls P1-P15 Coverage" /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-VERIFICATION.md` returns at least 1
    - `grep -c "## Project Memory Hard Rule Honored" /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-VERIFICATION.md` returns at least 1
    - **W14:** `grep -c "## Phase 7/8 Negative Regression Guard" /workspaces/trading-agent/.planning/phases/09-failure-analysis-drift/09-VERIFICATION.md` returns at least 1
    - REQUIREMENTS.md updated: ML-07, ML-08, ML-09, MCP-07, MCP-08, MCP-18, INT-03 all marked complete
    - ROADMAP.md updated: Phase 9 status ✓ COMPLETE
    - STATE.md updated: completed_phases incremented + Phase 9 ✓ complete in Milestone Status table
    - Full suite: `pytest tests/ --ignore=tests/test_smoke_12month_under_60s.py -q --tb=no` exit 0 with passed count ≥ 460 (baseline 458 + Phase 9 D-09-D4 migration tests; skips allowed for Codespace deps)
  </acceptance_criteria>
  <done>VERIFICATION.md scritto con full coverage matrix + W14 Phase 7/8 negative regression guard. REQUIREMENTS + ROADMAP + STATE aggiornati. Full suite zero regression vs Phase 1-8 baseline. Phase 9 ✓ COMPLETE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 9 → Phase 10/11 | VERIFICATION sign-off gate; W14 negative regression guard |
| trades.db trades_log → drift current window | D-09-D4 column post-Wave-0 |
| baseline parquet → drift reference | P14 strict-fail enforced (B6 Option A: Wave 2 in 09-03) |
| `retrain.promotion._invalidate_singleton` monkeypatch target | W13 module-level stable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-07-01 | Tampering | VERIFICATION.md content forged | accept | git-tracked; commit chain audit |
| T-9-07-02 | Repudiation | Phase 9 closure undocumented | mitigate | VERIFICATION.md coverage matrix + ROADMAP marker + STATE.md history |
| T-9-07-03 | DoS | full suite regression introduces hidden break | mitigate | full suite smoke + W14 Phase 7/8 negative regression guard |
| T-9-07-04 | Tampering | Phase 7/8 closed req silently regressed | mitigate | W14 explicit grep + pytest gate per closed Phase 7/8 surfaces |
| T-9-07-05 | Tampering | E2E monkeypatch leak across tests (W13) | mitigate | retrain.promotion._invalidate_singleton module-level patch target — NO direct ml.inference._invalidate_singleton patches |
</threat_model>

<verification>
- All Phase 9 requirements closed: ML-07, ML-08, ML-09, MCP-07, MCP-08, MCP-18, INT-03 (7/7)
- ROADMAP SC#1..4 all verified explicitly
- 16 D-09-* decisions touched
- 15 pitfalls P1-P15 + 8 plan-checker iter-2 WARNINGs (W3/W4/W6/W8/W9/W10/W11/W13) + W14 guarded
- Cross-phase carry-forward honored + W14 negative regression Phase 7/8 guard
- Project memory training data integrity priority preserved (B3 HARD-FAIL)
- Full suite zero regression vs Phase 1-8 baseline
- Wave 2 architectural deviation Rule 3 originale ELIMINATA via B6 Option A (09-03 chiude MCP-08 fully — Plan 09-07 NON re-implementa _load_*)
- VERIFICATION.md sign-off ready con W14 section
- REQUIREMENTS.md + ROADMAP.md + STATE.md updated
</verification>

<success_criteria>
- Phase 9 ✓ COMPLETE 7/7 plans, 100%
- All 7 requirement IDs (ML-07/08/09, MCP-07/08/18, INT-03) closed
- All 16 D-09-* decisions implemented + traceable
- All 15 P1-P15 + plan-checker iter-2 WARNINGs (W3/W4/W6/W8/W9/W10/W11/W13) + W14 guarded
- All 4 ROADMAP SC verified
- Cross-phase: Phase 7 + Phase 8 reuse + Phase 6 conventions honored + W14 negative regression check
- Project memory data integrity priority preserved (B3 HARD-FAIL)
- Full suite green vs Phase 1-8 baseline 458 passed (delta +D-09-D4 + ~70 nuovi Phase 9 = ~528+)
- B6 Option A risolto in Plan 09-03 (Plan 09-07 NON ha Task 1 ridondante)
</success_criteria>

<output>
After completion, create `.planning/phases/09-failure-analysis-drift/09-07-SUMMARY.md` with sections:
- Tasks completed (2: 3 E2E test files + VERIFICATION.md/REQ/ROADMAP/STATE)
- LOC delta per file
- Test delta: ~6 new E2E tests
- W13 monkeypatch stability confirmation (retrain.promotion._invalidate_singleton target)
- W14 Phase 7/8 negative regression guard confirmation
- B6 Option A note: Plan 09-03 closed MCP-08 fully — Plan 09-07 NON re-implementa _load_* (architecture clean)
- Full suite gate result (passed count + skip + xfail count vs baseline)
- 16 D-09-* + 7 req + 15 P + 4 SC closure confirmation
- Cross-phase honored confirmation
- Phase 9 ✓ COMPLETE sign-off
</output>
</content>
