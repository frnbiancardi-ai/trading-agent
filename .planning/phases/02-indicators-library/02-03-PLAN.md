---
phase: 02-indicators-library
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - indicators/momentum.py
  - indicators/__init__.py
  - tests/test_indicators_momentum.py
  - tests/test_indicators_purity.py
autonomous: true
requirements: [INDIC-02, INDIC-03, INDIC-04]
must_haves:
  truths:
    - "ADX/DMI 14 returns ADXResult parity-matched to pandas-ta within 1e-6"
    - "MACD 12/26/9 returns MACDResult parity-matched to pandas-ta within 1e-6"
    - "Stochastic 14/3/3 returns StochasticResult parity-matched to pandas-ta within 1e-6"
    - "All three are leakage-free at 5 spot indices"
    - "pandas_ta NOT imported by indicators/momentum.py at runtime"
  artifacts:
    - path: "indicators/momentum.py"
      provides: "adx, macd, stochastic + ADXResult, MACDResult, StochasticResult"
      contains: "def adx"
    - path: "tests/test_indicators_momentum.py"
      provides: "Parity tests for ADX/MACD/Stoch"
      contains: "test_adx_parity"
  key_links:
    - from: "indicators/momentum.py"
      to: "indicators._helpers._wilder_smooth"
      via: "ADX uses _wilder_smooth"
      pattern: "from indicators._helpers import _wilder_smooth"
---

<objective>
Implement INDIC-02 (ADX/DMI 14, Wilder/RMA smoothing matching pandas-ta `mamode='rma'`), INDIC-03 (MACD 12/26/9, EMA-of-EMA), INDIC-04 (Stochastic 14/3/3) in `indicators/momentum.py`. All three parity-tested vs pandas-ta at 1e-6 tolerance.

Output: ADX/MACD/Stochastic functions + dataclasses + parity + leakage tests.
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-indicators-library/02-CONTEXT.md
@.planning/phases/02-indicators-library/02-RESEARCH.md
@.planning/phases/02-indicators-library/02-PATTERNS.md
@indicators/momentum.py
@indicators/_helpers.py
@indicators/trend.py
@tests/test_indicators_purity.py

<interfaces>
Already available from Wave 0:
- `indicators/_helpers.py: _wilder_smooth(values, period) -> list[float|None]`
- `indicators/trend.py: ema, sma`
- `indicators/momentum.py: rsi, check_rsi_divergence` already present

pandas-ta oracle column names:
- `pta.adx(high, low, close, length=14, mamode='rma')` -> `ADX_14`, `DMP_14`, `DMN_14`
- `pta.macd(close, fast=12, slow=26, signal=9)` -> `MACD_12_26_9`, `MACDh_12_26_9`, `MACDs_12_26_9`
- `pta.stoch(high, low, close, k=14, d=3, smooth_k=3)` -> `STOCHk_14_3_3`, `STOCHd_14_3_3`

Pitfall 2: parity tests start at index `2*period` to dodge seed transient.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement adx + macd + stochastic in indicators/momentum.py</name>
  <files>indicators/momentum.py, indicators/__init__.py</files>
  <read_first>
    indicators/momentum.py,
    indicators/_helpers.py,
    indicators/trend.py,
    .planning/phases/02-indicators-library/02-RESEARCH.md (Code Examples Example 1, Pattern 2, Pitfall 2),
    .planning/phases/02-indicators-library/02-PATTERNS.md (momentum.py section)
  </read_first>
  <behavior>
    - `adx([1.0]*60,[1.0]*60,[1.0]*60,14)` does not raise; returns ADXResult with first 28 indices None.
    - `macd([1.0,2.0]*30)` returns MACDResult with all 3 series length 60.
    - `stochastic` on monotonic-up series eventually yields k near 100.
  </behavior>
  <action>
    EDIT `indicators/momentum.py` — KEEP existing `rsi` and `check_rsi_divergence`. ADD imports + dataclasses + functions exactly as below (formula derivations in RESEARCH §Code Examples Example 1):

    Imports to add at top:
    ```
    from dataclasses import dataclass
    from indicators._helpers import _wilder_smooth
    from indicators.trend import ema, sma
    ```

    Dataclasses:
    ```
    @dataclass
    class ADXResult:
        adx: list[float | None]
        plus_di: list[float | None]
        minus_di: list[float | None]

    @dataclass
    class MACDResult:
        macd: list[float | None]
        signal: list[float | None]
        histogram: list[float | None]

    @dataclass
    class StochasticResult:
        k: list[float | None]
        d: list[float | None]
    ```

    Function `adx(highs, lows, closes, period=14)` per RESEARCH Example 1: build plus_dm/minus_dm/tr arrays, smooth all three via `_wilder_smooth`, compute plus_di = 100 * sm_plus / sm_tr (None when sm_tr None or 0), minus_di likewise, dx = 100 * |+DI - -DI| / (+DI + -DI) (0 when both None), then `_wilder_smooth(dx, period)` for ADX. Mask first `2*period` ADX entries to None. Validate len(highs)==len(lows)==len(closes); raise ValueError "highs, lows, closes devono avere la stessa lunghezza" otherwise. Italian docstring.

    Function `macd(closes, fast=12, slow=26, signal=9)`:
    - macd_line[i] = ema(closes, fast)[i] - ema(closes, slow)[i] when both not None.
    - signal_line: take dense (non-None) prefix of macd_line, run ema(., signal), map back into the full-length list at the same offset.
    - histogram[i] = macd_line[i] - signal_line[i] when both defined.

    Function `stochastic(highs, lows, closes, k_period=14, d_period=3, smooth_k=3)`:
    - For i in range(k_period-1, n): HH = max(highs[i-k_period+1:i+1]), LL = min(lows[...]), raw_k[i] = 100*(closes[i]-LL)/(HH-LL) if HH != LL else None.
    - Smoothed %K = sma(dense_raw_k, smooth_k), reconstituted at offset.
    - %D = sma(dense_smoothed_k, d_period), reconstituted at offset.
    - Validate equal-length inputs.

    Update `indicators/__init__.py` — APPEND import + __all__ entries:
    ```
    from indicators.momentum import (
        adx, macd, stochastic,
        ADXResult, MACDResult, StochasticResult,
    )
    ```

    Italian docstrings everywhere per CLAUDE.md.
  </action>
  <verify>
    <automated>cd C:/trading-agent && python -c "from indicators import adx, macd, stochastic, ADXResult, MACDResult, StochasticResult; r=adx([1.0]*60,[1.0]*60,[1.0]*60,14); m=macd([1.0,2.0]*30); s=stochastic([2.0,1.0]*30,[1.0,2.0]*30,[1.5]*60); assert len(r.adx)==60 and len(m.macd)==60 and len(s.k)==60; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from indicators import adx, macd, stochastic, ADXResult, MACDResult, StochasticResult` succeeds.
    - `grep -c "def adx" indicators/momentum.py` returns 1.
    - `grep -c "def macd" indicators/momentum.py` returns 1.
    - `grep -c "def stochastic" indicators/momentum.py` returns 1.
    - `grep -c "@dataclass" indicators/momentum.py` returns 3.
    - `grep -v "^#" indicators/momentum.py | grep -c "import pandas_ta"` returns 0.
    - Calling adx/macd/stochastic on length-60 inputs returns dataclasses with all series length 60.
    - `python -c "from indicators import rsi, check_rsi_divergence"` still works (Wave 0 functions preserved).
  </acceptance_criteria>
  <done>
    ADX, MACD, Stochastic implemented + re-exported; existing rsi/check_rsi_divergence untouched; runtime free of pandas_ta.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parity + leakage tests for INDIC-02 + INDIC-03 + INDIC-04</name>
  <files>tests/test_indicators_momentum.py, tests/test_indicators_purity.py</files>
  <read_first>
    .planning/phases/02-indicators-library/02-RESEARCH.md (Code Examples Example 4),
    .planning/phases/02-indicators-library/02-PATTERNS.md (parity oracle pattern, skipif gate),
    indicators/momentum.py
  </read_first>
  <action>
    1. CREATE `tests/test_indicators_momentum.py`:
       - Module docstring: "Test indicators.momentum (INDIC-02/03/04) — parity vs pandas-ta a 1e-6."
       - Import `_HAVE_PTA = importlib.util.find_spec("pandas_ta") is not None`; build `_SKIP = pytest.mark.skipif(not _HAVE_PTA, reason="pandas-ta dev-dep non installato")`.
       - `test_adx_parity_with_pandas_ta(eurusd_h1_500)`: build pandas DataFrame from fixture, call our `adx(h,l,c,14)` and `pta.adx(h_series, l_series, c_series, length=14, mamode="rma")`. Loop i from 2*14=28 to len(df); when ours.adx[i] is not None and not pd.isna(expected["ADX_14"].iloc[i]), assert |ours - expected| < 1e-6. Same loop with `plus_di` vs `DMP_14` and `minus_di` vs `DMN_14`.
       - `test_macd_parity_with_pandas_ta(eurusd_h1_500)`: `pta.macd(c_series, fast=12, slow=26, signal=9)`. Loop from i=40. Compare cols MACD_12_26_9 (line), MACDs_12_26_9 (signal), MACDh_12_26_9 (histogram), tolerance 1e-6.
       - `test_stoch_parity_with_pandas_ta(eurusd_h1_500)`: `pta.stoch(h_series, l_series, c_series, k=14, d=3, smooth_k=3)`. Loop from i=20. Compare STOCHk_14_3_3 vs ours.k and STOCHd_14_3_3 vs ours.d, tol 1e-6. Skip indices where either side is NaN/None.
       - `test_adx_constant_no_raise()`: `adx([1.0]*60,[1.0]*60,[1.0]*60,14)` returns ADXResult; first 28 entries of adx are None; no exception raised.
       - `test_macd_signal_lag()`: For monotonic-up `closes = list(range(100))`, MACD line is positive (after warmup) and signal lags it; assert `m.macd[60] > m.signal[60] > 0`.
       - `test_stoch_extreme_high()`: For monotonically increasing series of length 60 (highs == closes == range(60), lows = closes - 1), assert `r.k[59] > 80` (close near top of range).

    2. APPEND to `tests/test_indicators_purity.py` parametrized leakage tests for all three (idx in [50,100,250,400,499]):
       - `test_no_future_leakage_adx`: compare `adx(h,l,c,14)[i]` (full) vs `adx(h[:i+1], l[:i+1], c[:i+1], 14)[i]` for adx, plus_di, minus_di.
       - `test_no_future_leakage_macd`: same pattern for macd line, signal, histogram.
       - `test_no_future_leakage_stochastic`: same pattern for k and d.

    Italian module docstrings; English snake_case test names.
  </action>
  <verify>
    <automated>cd C:/trading-agent && pytest tests/test_indicators_momentum.py tests/test_indicators_purity.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_indicators_momentum.py -x` exits 0.
    - When pandas-ta installed: parity tests for adx/macd/stoch pass at 1e-6.
    - When pandas-ta NOT installed: parity tests skip cleanly.
    - `pytest tests/test_indicators_purity.py::test_no_future_leakage_adx -x` shows 5 parametrized passes.
    - `pytest tests/test_indicators_purity.py::test_no_future_leakage_macd -x` shows 5 passes.
    - `pytest tests/test_indicators_purity.py::test_no_future_leakage_stochastic -x` shows 5 passes.
    - `grep -c "import pandas_ta" tests/test_indicators_momentum.py` >= 1.
  </acceptance_criteria>
  <done>
    INDIC-02/03/04 parity-verified at 1e-6 + leakage-free at 5 indices each.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_indicators_momentum.py tests/test_indicators_purity.py -x` exits 0.
- `! grep -rE "import pandas_ta|from pandas_ta" indicators/`.
</verification>

<success_criteria>
1. INDIC-02 ADX/DMI Wilder/RMA implemented + parity-tested.
2. INDIC-03 MACD 12/26/9 implemented + parity-tested.
3. INDIC-04 Stochastic 14/3/3 implemented + parity-tested.
4. All three leakage-free at 5 spot indices.
5. pandas_ta not imported at runtime.
</success_criteria>

<output>
Create `.planning/phases/02-indicators-library/02-03-SUMMARY.md` with parity tolerances achieved per indicator.
</output>
