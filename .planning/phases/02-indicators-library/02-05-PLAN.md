---
phase: 02-indicators-library
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - indicators/structure.py
  - indicators/__init__.py
  - tests/test_indicators_structure.py
  - tests/test_indicators_purity.py
autonomous: true
requirements: [INDIC-05, INDIC-08, INDIC-09]
must_haves:
  truths:
    - "Donchian 20 returns DonchianResult(upper, lower, middle) length-N"
    - "Fibonacci returns FibonacciResult with levels at 0/0.382/0.5/0.618/1.0 on detected swing leg"
    - "Pivots daily/session/weekly returns PivotResult(p, r1..r3, s1..s3, camarilla) with NY-17 boundary"
    - "Camarilla multipliers verbatim 1.1/12, 1.1/6, 1.1/4, 1.1/2 — verified vs LiteFinance/Babypips/Defcofx"
    - "All three leakage-free at 5 spot indices"
  artifacts:
    - path: "indicators/structure.py"
      provides: "donchian, fibonacci_retracements, pivots + DonchianResult, FibonacciResult, PivotResult (existing find_support_resistance + check_breakout_quality preserved)"
      contains: "def pivots"
    - path: "tests/test_indicators_structure.py"
      provides: "Hand-calc fixture tests for all three"
      contains: "test_pivot_camarilla_formula"
  key_links:
    - from: "indicators/structure.py:fibonacci_retracements"
      to: "indicators/structure.py:find_support_resistance"
      via: "fib leg detection reuses pivot extraction"
      pattern: "find_support_resistance"
    - from: "indicators/structure.py:pivots"
      to: "indicators._helpers._session_id_ny17"
      via: "session bucketing for daily pivots"
      pattern: "_session_id_ny17"
---

<objective>
Implement INDIC-05 (Donchian 20 — rolling max H / min L), INDIC-08 (Fibonacci retracements at 38.2%, 50%, 61.8% on the most recent completed swing leg detected via existing `find_support_resistance` pivots), INDIC-09 (Pivots classic + Camarilla, daily / session / weekly with NY-17 anchor per D-10/D-11).

Camarilla multipliers verbatim: `R/S 1..4 = prev_close ± rng * 1.1 / {12, 6, 4, 2}` (RESEARCH §Don't Hand-Roll). Daily pivot: prior NY-17→NY-17 session H/L/C; weekly pivot: prior Sunday-17 NY → Friday-17 NY week H/L/C.

Output: 3 indicators + dataclasses + hand-calc fixture tests.
</objective>

<execution_context>
@C:/trading-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/trading-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-indicators-library/02-CONTEXT.md
@.planning/phases/02-indicators-library/02-RESEARCH.md
@.planning/phases/02-indicators-library/02-PATTERNS.md
@indicators/structure.py
@indicators/_helpers.py
@tests/conftest.py
@tests/test_indicators_purity.py

<interfaces>
Already available from Wave 0:
- `indicators/structure.py: find_support_resistance(bars, lookback=100, window=2) -> dict` (returns `{"support": float|None, "resistance": float|None}`)
- `indicators/structure.py: check_breakout_quality(...)` — preserved unchanged
- `indicators/_helpers.py: _session_id_ny17(ts_utc) -> date`

Bar dict shape (from fixture): `{"time": int_unix_seconds_utc, "open", "high", "low", "close", "volume", "tick_volume"}`. To use `_session_id_ny17`, convert `bar["time"]` to `datetime.fromtimestamp(bar["time"], tz=timezone.utc)`.

Camarilla formula `[VERIFIED: 3 sources]`:
  rng = prev_h - prev_l
  h1 = prev_c + rng * 1.1/12;  l1 = prev_c - rng * 1.1/12
  h2 = prev_c + rng * 1.1/6;   l2 = prev_c - rng * 1.1/6
  h3 = prev_c + rng * 1.1/4;   l3 = prev_c - rng * 1.1/4
  h4 = prev_c + rng * 1.1/2;   l4 = prev_c - rng * 1.1/2

Classic pivot:
  P = (H+L+C)/3
  R1 = 2P - L; S1 = 2P - H
  R2 = P + (H-L); S2 = P - (H-L)
  R3 = H + 2(P-L); S3 = L - 2(H-P)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement donchian + fibonacci_retracements + pivots in indicators/structure.py</name>
  <files>indicators/structure.py, indicators/__init__.py</files>
  <read_first>
    indicators/structure.py,
    indicators/_helpers.py,
    .planning/phases/02-indicators-library/02-RESEARCH.md (Code Examples Example 2 + Don't Hand-Roll Camarilla row + Pitfall 5 NY-17 + Open-Q5 Fib swing leg + Open-Q8 Camarilla),
    .planning/phases/02-indicators-library/02-PATTERNS.md (structure.py section)
  </read_first>
  <behavior>
    - `donchian([1,2,3,4,5]*5, [0,1,2,3,4]*5, length=5)` returns `DonchianResult` with upper[4]=5, lower[4]=0, middle[4]=2.5; first 4 entries None.
    - `fibonacci_retracements(bars)` on a clean uptrend (low at index 5, high at index 50) returns `FibonacciResult` with `direction="up"`, `leg_low ≈ low_value`, `leg_high ≈ high_value`, levels dict containing keys "0","0.382","0.5","0.618","1.0".
    - `pivots(bars, anchor="daily")` returns `PivotResult` length-N. Verify Camarilla: prev_h=110, prev_l=100, prev_c=105 → h1 = 105 + 10*1.1/12 ≈ 105.91667; h4 = 105 + 10*1.1/2 = 110.5.
  </behavior>
  <action>
    EDIT `indicators/structure.py`. KEEP existing `find_support_resistance` and `check_breakout_quality`. ADD imports + dataclasses + functions:

    Imports at top:
    ```
    from dataclasses import dataclass, field
    from datetime import datetime, timezone, timedelta
    from indicators._helpers import _session_id_ny17
    ```

    Dataclasses:
    ```
    @dataclass
    class DonchianResult:
        upper: list[float | None]
        lower: list[float | None]
        middle: list[float | None]

    @dataclass
    class FibonacciResult:
        levels: dict[str, float | None]
        leg_high: float | None
        leg_low: float | None
        direction: str  # "up" | "down" | "none"

    @dataclass
    class PivotResult:
        p: list[float | None]
        r1: list[float | None]
        r2: list[float | None]
        r3: list[float | None]
        s1: list[float | None]
        s2: list[float | None]
        s3: list[float | None]
        camarilla: dict[str, list[float | None]]  # keys "h1".."h4", "l1".."l4"
    ```

    `donchian(highs, lows, length=20)`:
    - Validate equal lengths; ValueError "highs e lows devono avere la stessa lunghezza".
    - Naive O(n*length): `for i in range(length-1, n): upper[i] = max(highs[i-length+1:i+1]); lower[i] = min(lows[...]); middle[i] = (upper[i]+lower[i])/2`.

    `fibonacci_retracements(bars, lookback=100, window=2)` (per Open-Q5):
    - Reuse `find_support_resistance(bars, lookback, window)` to get `{"support": leg_low, "resistance": leg_high}`. If either is None, return `FibonacciResult(levels={}, leg_high=None, leg_low=None, direction="none")`.
    - Walk back through `bars[-lookback:]` to find which extreme came LAST (the more recent of the two). If swing high is more recent than swing low → `direction="down"` (retracement projecting down from high to low); else `direction="up"`.
    - Levels: project 0%, 38.2%, 50%, 61.8%, 100% between leg_low and leg_high.
      - Up: `{"0": leg_high, "0.382": leg_high - 0.382*(leg_high-leg_low), "0.5": leg_high - 0.5*(...), "0.618": leg_high - 0.618*(...), "1.0": leg_low}`.
      - Down: `{"0": leg_low, "0.382": leg_low + 0.382*(leg_high-leg_low), "0.5": ..., "0.618": ..., "1.0": leg_high}`.

    `pivots(bars, anchor="daily")` per D-10/D-11:
    - `anchor` ∈ {"daily", "weekly"}. Both use NY-17 boundary.
    - Iterate bars: compute session id via `_session_id_ny17(datetime.fromtimestamp(bar["time"], tz=timezone.utc))`.
    - Maintain rolling per-session H/L/C. When session id changes, the prior session's H/L/C becomes the input for pivot levels of every bar in the NEW session.
    - For weekly: same approach but with weekly key = ISO week of session-end date (NY).
    - For each bar i with prior session H/L/C available, compute classic P/R1/R2/R3/S1/S2/S3 + Camarilla h1-h4/l1-l4 verbatim.
    - First session has no prior → all None.

    Update `indicators/__init__.py` — APPEND:
    ```
    from indicators.structure import (
        donchian, fibonacci_retracements, pivots,
        DonchianResult, FibonacciResult, PivotResult,
    )
    ```
    Add to `__all__`.

    Italian docstrings throughout.
  </action>
  <verify>
    <automated>cd C:/trading-agent && python -c "
from indicators import donchian, fibonacci_retracements, pivots, DonchianResult, FibonacciResult, PivotResult
r = donchian([1,2,3,4,5]*5, [0,1,2,3,4]*5, length=5)
assert r.upper[4]==5 and r.lower[4]==0 and r.middle[4]==2.5
# Camarilla hand-calc: prev_h=110, prev_l=100, prev_c=105
import math
rng=10.0; pc=105.0
h1 = pc + rng*1.1/12; h4 = pc + rng*1.1/2
assert abs(h1 - 105.91666666666667) < 1e-9
assert abs(h4 - 110.5) < 1e-9
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `from indicators import donchian, fibonacci_retracements, pivots, DonchianResult, FibonacciResult, PivotResult` succeeds.
    - `grep -c "def donchian" indicators/structure.py` returns 1.
    - `grep -c "def fibonacci_retracements" indicators/structure.py` returns 1.
    - `grep -c "def pivots" indicators/structure.py` returns 1.
    - `grep -c "@dataclass" indicators/structure.py` returns 3.
    - `grep -c "1.1 / 12\|1.1/12" indicators/structure.py` >= 1 (Camarilla literal).
    - `grep -c "1.1 / 2\|1.1/2" indicators/structure.py` >= 1.
    - Existing `find_support_resistance` and `check_breakout_quality` still defined: each `grep -c "^def find_support_resistance" indicators/structure.py` and `grep -c "^def check_breakout_quality" indicators/structure.py` return 1.
    - Verify embedded Python check passes.
  </acceptance_criteria>
  <done>
    Donchian, Fibonacci, Pivots (NY-17, classic+Camarilla) implemented; existing helpers preserved; runtime free of pandas_ta.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand-calculated fixture tests for INDIC-05/08/09 + leakage tests</name>
  <files>tests/test_indicators_structure.py, tests/test_indicators_purity.py</files>
  <read_first>
    .planning/phases/02-indicators-library/02-RESEARCH.md (Camarilla verbatim formula, Pitfall 5 NY-17 DST),
    .planning/phases/02-indicators-library/02-PATTERNS.md (test_indicators_structure.py hand-calc pattern),
    indicators/structure.py
  </read_first>
  <action>
    1. CREATE `tests/test_indicators_structure.py`:

       Module docstring: "Test indicators.structure (INDIC-05/08/09) — hand-calc fixture tier per D-07."

       - `test_donchian_simple_5bar()`: highs=[2,4,3,5,1], lows=[1,2,2,3,0]; donchian length=3 → upper[2]=4, lower[2]=1, middle[2]=2.5; upper[4]=5, lower[4]=0, middle[4]=2.5.

       - `test_donchian_constant_series()`: highs=[5.0]*30, lows=[3.0]*30; length=10 → upper[9]==5.0, lower[9]==3.0, middle[9]==4.0.

       - `test_donchian_first_n_minus_1_none()`: 30-bar input, length=20 → indices 0..18 are None, index 19 is float.

       - `test_pivot_camarilla_formula()`: build 2-day fake bars: day1 covers session NY 2024-01-15 → 2024-01-16 with H=110, L=100, C=105 (use 24 hourly bars where last close is 105); day2 starts at NY-17 2024-01-16 (UTC=2024-01-16T22:00:00Z winter). The first bar of day2 should have pivot.p == (110+100+105)/3 == 105.0; pivot.h1 (Camarilla) ≈ 105 + 10*1.1/12 ≈ 105.91666; pivot.h4 == 110.5; pivot.l4 == 99.5. Use `abs(actual - expected) < 1e-9`.

       - `test_pivot_first_session_all_none()`: bars covering only one session → all P/R/S/Camarilla series have None at every index (no prior session).

       - `test_pivot_ny17_dst_boundary_winter_vs_summer()`: build 2 bars 1 second apart straddling 22:00 UTC on 2024-01-15 (winter, NY-17 == 22:00 UTC) — the second bar is in the NEW session. Then build same test on 2024-07-15 (summer, NY-17 == 21:00 UTC) — boundary at 21:00 UTC. Use `_session_id_ny17` directly to assert both.

       - `test_fib_retracement_up_leg()`: build 30 bars with low=1.0 at index 5 and high=2.0 at index 25 (intervening bars sandwiched between); call `fibonacci_retracements(bars, lookback=30, window=2)`. Assert `direction == "up"`, `leg_low == 1.0`, `leg_high == 2.0`, `levels["0.382"] == 2.0 - 0.382 * 1.0 == 1.618` within 1e-9, `levels["0.5"] == 1.5`, `levels["0.618"] == 1.382`.

       - `test_fib_no_swing_returns_none_direction()`: bars with no detectable swing (constant H=L) → `direction == "none"`, `leg_high is None`.

    2. APPEND to `tests/test_indicators_purity.py`:
       ```
       @pytest.mark.parametrize("idx", [50, 100, 250, 400, 499])
       def test_no_future_leakage_donchian(eurusd_h1_500, idx):
           from indicators.structure import donchian
           bars = eurusd_h1_500
           highs = [b["high"] for b in bars]; lows = [b["low"] for b in bars]
           full = donchian(highs, lows, 20)
           partial = donchian(highs[:idx+1], lows[:idx+1], 20)
           assert partial.upper[idx] == full.upper[idx]
           assert partial.lower[idx] == full.lower[idx]
           assert partial.middle[idx] == full.middle[idx]

       @pytest.mark.parametrize("idx", [100, 200, 300, 400, 499])
       def test_no_future_leakage_pivots(eurusd_h1_500, idx):
           from indicators.structure import pivots
           bars = eurusd_h1_500
           full = pivots(bars, anchor="daily")
           partial = pivots(bars[:idx+1], anchor="daily")
           assert partial.p[idx] == full.p[idx]
           assert partial.camarilla["h1"][idx] == full.camarilla["h1"][idx]
       ```

    Italian module docstring; English test names.
  </action>
  <verify>
    <automated>cd C:/trading-agent && pytest tests/test_indicators_structure.py tests/test_indicators_purity.py::test_no_future_leakage_donchian tests/test_indicators_purity.py::test_no_future_leakage_pivots -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_indicators_structure.py -x -v` exits 0 with all 8 hand-calc tests passing.
    - `pytest tests/test_indicators_purity.py::test_no_future_leakage_donchian -x` shows 5 passes.
    - `pytest tests/test_indicators_purity.py::test_no_future_leakage_pivots -x` shows 5 passes.
    - `grep -c "1.1" tests/test_indicators_structure.py` >= 1 (Camarilla constant referenced in test).
    - `grep -c "_session_id_ny17" tests/test_indicators_structure.py` >= 1 (DST boundary asserted).
  </acceptance_criteria>
  <done>
    INDIC-05/08/09 hand-calc verified; Camarilla formula exact match; NY-17 DST boundary tested both winter and summer; leakage-free.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_indicators_structure.py tests/test_indicators_purity.py -x` exits 0.
- `grep -E "1.1\s*/\s*12|1.1/12" indicators/structure.py` matches (Camarilla literal in code).
</verification>

<success_criteria>
1. INDIC-05 Donchian 20 implemented, hand-calc tested.
2. INDIC-08 Fibonacci on swing leg implemented (reuses find_support_resistance).
3. INDIC-09 Pivots classic+Camarilla daily/weekly with NY-17 anchor implemented + DST verified.
4. All three leakage-free.
5. Camarilla multipliers exactly 1.1/{12,6,4,2}.
</success_criteria>

<output>
Create `.planning/phases/02-indicators-library/02-05-SUMMARY.md` documenting Camarilla parity, NY-17 DST verification (winter/summer transitions), Fibonacci leg detection rationale.
</output>
