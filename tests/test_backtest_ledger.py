"""Tests for backtest.ledger SQLite schema (D-07). Implementations land in plan 05."""
import pytest

pytestmark = pytest.mark.skip(reason="implementation lands in plan 05")


def test_schema_created(): ...
