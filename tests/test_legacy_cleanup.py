"""Tests for legacy code cleanup (D-03, D-04). Implementations land in plan 08."""
import pytest

pytestmark = pytest.mark.skip(reason="implementation lands in plan 08")


def test_backtest_suite_deleted(): ...
def test_ml_feedback_archived(): ...
